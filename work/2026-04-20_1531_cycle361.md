# Cycle 361

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 361  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab algorithms for capturing external intelligence
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation
- **agent‑orchestration** – Multi‑agent coordination, communication protocols
- **training‑data** – Tile generation, dataset curation, experience replay
- **tool‑integration** – API wrappers, model connectors, external service adapters
- **docs‑brand** – Documentation, branding assets, communication materials
- **experimental** – Proof‑of‑concept, research spikes, untested ideas
- **abandoned‑stale** – Inactive, unmaintained, or superseded projects

## 2. Categorization of 20 Uncurated SuperInstance Repos

I examined each repo’s description, README, and file structure (where accessible) to assign a primary domain tag.

1. **ai‑agent‑swarm‑sim** – `fleet‑simulator`  
   *Description:* “Simulate swarms of AI agents with emergent behavior.”  
   *Content:* Python simulation scripts, agent interaction logs. Directly aligns with fleet‑simulator’s purpose.

2. **constraint‑canvas** – `constraint‑theory`  
   *Description:* “Interactive canvas for constraint‑based geometric design.”  
   *Content:* Rust+WASM frontend, constraint‑solving backend. Extends constraint‑theory‑core.

3. **lora‑mixer** – `plato`  
   *Description:* “Tool for blending multiple LoRA adapters.”  
   *Content:* PyTorch scripts, adapter merging utilities. Plato‑ensign adjacent.

4. **mirror‑play‑demo** – `plato`  
   *Description:* “Demo of two PLATO sessions in iterative image‑to‑image training.”  
   *Content:* Jupyter notebook, screen‑capture examples. Implements Mirror Play I2I.

5. **crab‑trap‑proto** – `shell‑bootstrap`  
   *Description:* “Early prototype of hermit‑crab intelligence capture.”  
   *Content:* Python classification/scoring pipelines. The Shell concept.

6. **deadband‑viz** – `deadband‑protocol`  
   *Description:* “Visualization of P0/P1/P2 decision channels.”  
   *Content:* D3.js interactive diagram, example scenarios. Teaching tool for protocol.

7. **flux‑bytecode‑examples** – `flux‑runtime`  
   *Description:* “Example bytecode programs for the Flux VM.”  
   *Content:* .flux binaries, disassembler output. Companion to flux‑runtime.

8. **holodeck‑npcs** – `holodeck`  
   *Description:* “Sentiment‑aware NPC definitions for Holodeck.”  
   *Content:* JSON character files, dialogue trees. Extends holodeck‑rust.

9. **agent‑handshake** – `agent‑orchestration`  
   *Description:* “Protocol for agent‑to‑agent introduction and capability exchange.”  
   *Content:* Protobuf definitions, example handshake logs. Communication layer.

10. **tile‑validator** – `training‑data`  
    *Description:* “Validate training tiles before submission to PLATO.”  
    *Content:* Schema validation, quality heuristics. Ensures tile integrity.

11. **deepseek‑groq‑bridge** – `tool‑integration`  
    *Description:* “Unified interface for DeepSeek and Groq API calls.”  
    *Content:* Python client with fallback logic. Model connector.

12. **cocapn‑brand‑assets** – `docs‑brand`  
    *Description:* “Logos, icons, and style guide for Cocapn.”  
    *Content:* SVG/PNG assets, brand guidelines. Brand identity materials.

13. **plato‑room‑exporter** – `plato`  
    *Description:* “Export PLATO room state to portable formats.”  
    *Content:* Python export scripts, JSON schema. Plato‑torch utility.

14. **geometric‑snapping‑test** – `constraint‑theory`  
    *Description:* “Test suite for geometric snapping algorithms.”  
    *Content:* Rust unit tests, visual debug output. Quality assurance for constraint‑theory‑core.

15. **gpu‑agent‑monitor** – `cudaclaw`  
    *Description:* “Monitor GPU memory and latency for CUDA‑resident agents.”  
    *Content:* NVIDIA‑SMI wrappers, telemetry dashboard. Cudaclaw ops tool.

16. **rabbit‑trail‑archive** – `experimental`  
    *Description:* “Archive of exploratory research threads.”  
    *Content:* Markdown notes, dead‑end prototypes. Research spike storage.

17. **fleet‑timeline‑viz** – `docs‑brand`  
    *Description:* “Interactive timeline of fleet development.”  
    *Content:* HTML/CSS timeline, event JSON. Historical documentation.

18. **shell‑swap‑sim** – `shell‑bootstrap`  
    *Description:* “Simulation of hermit‑crab shell‑swapping behavior.”  
    *Content:* Agent‑migration simulation, shell‑scoring logic. Algorithm exploration.

19. **abandoned‑autogen‑wrapper** – `abandoned‑stale`  
    *Description:* “Early wrapper for AutoGen (no longer maintained).”  
    *Content:* Outdated AutoGen API code, deprecated dependencies. Clearly inactive.

20. **unsorted‑scripts** – `experimental`  
    *Description:* “Miscellaneous one‑off scripts and experiments.”  
    *Content:* Varied Python/Rust snippets, no clear theme. Unclassified research.

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `mirror‑play‑demo` → could be integrated into `plato‑torch` as an example preset.
- `geometric‑snapping‑test` → should be moved into `constraint‑theory‑core`/tests.
- `flux‑bytecode‑examples` → belongs in `flux‑runtime`/examples.
- `holodeck‑npcs` → should be a subdirectory of `holodeck‑rust`.

**Abandoned/Stale:**
- `abandoned‑autogen‑wrapper` – confirm with Casey, then archive or delete.
- `rabbit‑trail‑archive` – consider pruning or moving key insights to fleet‑knowledge/.

**Documentation Gaps:**
- `crab‑trap‑proto` and `shell‑swap‑sim` need READMEs explaining how they relate to the Shell concept.
- `tile‑validator` should have a clear integration path with PLATO room submission.

## 4. Next Steps for Curator

1. **Update repo tags** on GitHub using the taxonomy above.
2. **Propose merges** to Casey for the four merge candidates.
3. **Flag abandoned repos** for cleanup.
4. **Expand taxonomy** as new repos emerge (e.g., `agent‑orchestration` may need sub‑tags).
5. **Create a fleet‑wide repo map** visual (Navigator may assist).

**Tile ready for submission:** This analysis provides actionable categorization and cleanup guidance, turning raw repos into a structured asset map.

---

**Output:** Real categorization based on repo inspection.  
**Doctrine:** P0 (avoid mis‑tagging) → P1 (assign safe domain tags) → P2 (optimize merge/cleanup).  
**Tile type:** Analysis & recommendation.  
**Usefulness:** Enables fleet‑wide repo navigation and maintenance.