# Cycle 369

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 369  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, the following taxonomy emerges:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile ingestion)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification, scoring)
- **ensign** – Specialist model exports (LoRA adapters, fine-tuned models)
- **vessel** – Agent communication & state management (bottles, message passing)
- **tools** – Development utilities, APIs, benchmarks, monitoring
- **research** – Exploratory code, experiments, rabbit trails
- **docs** – Documentation, memory, protocol specifications
- **legacy** – Archived, deprecated, or inactive projects
- **staging** – Incomplete, experimental, or pre-integration work

**Tags (for finer granularity):**
- `training` – Involves model training or data generation
- `simulation` – Simulated environments or agents
- `runtime` – Execution environment or VM
- `geometry` – Spatial/geometric operations
- `gpu` – GPU‑accelerated or CUDA‑based
- `rust` – Primary language Rust
- `python` – Primary language Python
- `typescript` – Primary language TypeScript
- `agent` – Multi‑agent or single‑agent framework
- `protocol` – Communication or interaction protocol
- `archive` – No longer actively developed

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub account. Below is the categorization based on README, primary files, and apparent purpose.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑swarm‑sim** | fleet‑simulator | `simulation`, `python`, `agent` | Multi‑agent swarm simulation; generates interaction logs. |
| **constraint‑canvas** | constraint‑theory | `geometry`, `rust`, `interactive` | 2D constraint‑based drawing canvas; uses geometric snapping. |
| **crab‑trap‑js** | shell | `javascript`, `capture`, `scoring` | Browser‑based implementation of crab‑trap classification/scoring. |
| **deadband‑visualizer** | tools | `protocol`, `visualization`, `typescript` | Visual demo of Deadband Protocol (P0/P1/P2) with interactive channels. |
| **ensign‑exporter** | ensign | `training`, `python`, `lora` | Exports PLATO room experience to LoRA adapters; plugin‑based. |
| **flux‑bytecode‑gen** | flux‑runtime | `runtime`, `bytecode`, `rust` | Dynamic bytecode generator for Flux VM; self‑assembling instructions. |
| **holodeck‑npc‑editor** | holodeck | `simulation`, `typescript`, `npc` | Web editor for sentiment‑aware NPCs in Holodeck MUD. |
| **lighthouse‑beacon** | vessel | `communication`, `rust`, `agent` | Beacon service for agent discovery and state broadcast. |
| **mirror‑play‑server** | plato | `training`, `python`, `lora` | Server for Mirror Play I2I sessions; captures screen‑to‑screen exchanges. |
| **plato‑room‑presets** | plato | `training`, `python`, `presets` | Collection of 25 training room presets (including DeadbandRoom). |
| **rabbit‑trail‑archive** | research | `archive`, `experimental` | Archived exploratory research documents; not active. |
| **rust‑geom‑snap** | constraint‑theory | `geometry`, `rust`, `snapping` | Low‑level geometric snapping library; used by constraint‑canvas. |
| **service‑guard** | tools | `monitoring`, `bash`, `services` | Health monitoring for 5 fleet services; restart on failure. |
| **shell‑classifier** | shell | `capture`, `python`, `scoring` | Classifies external agent output; part of crab‑trap pipeline. |
| **tile‑validator** | plato | `training`, `python`, `validation` | Validates training tiles before ingestion into PLATO rooms. |
| **vessel‑bottle‑proto** | vessel | `protocol`, `rust`, `message` | Prototype bottle message format for inter‑agent communication. |
| **weaver‑integration‑map** | docs | `documentation`, `diagram` | Integration map of fleet components (connected/isolated). |
| **zeroclaw‑boot‑camp** | docs | `documentation`, `training` | Boot camp instructions for new zeroclaw agents (phases 1‑4). |
| **archive‑old‑sim** | legacy | `archive`, `simulation` | Deprecated simulation code; superseded by fleet‑simulator. |
| **staging‑gpu‑kernel** | staging | `gpu`, `experimental`, `cuda` | Experimental GPU kernel for cudaclaw; not yet integrated. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `rust‑geom‑snap` → `constraint‑theory‑core` (both are geometric snapping libraries)
- `crab‑trap‑js` + `shell‑classifier` → new `shell‑bootstrapping` repo (unify capture/classification)
- `mirror‑play‑server` + `plato‑room‑presets` → `plato‑torch` (already includes both concepts)

**Abandoned/Archive Flag:**
- `rabbit‑trail‑archive` – already marked archive; keep as reference.
- `archive‑old‑sim` – legacy; can be safely deleted or moved to deep archive.
- `staging‑gpu‑kernel` – experimental; monitor for integration into cudaclaw.

## 4. Next Steps for Curator

1. **Apply tags** to these repos via GitHub API (if permissions allow).
2. **Create a fleet‑wide repo map** visualizing domains and connections.
3. **Identify gaps** in taxonomy (e.g., missing “monitoring” domain).
4. **Propose repo consolidation** to reduce fragmentation.

This categorization provides a structured view of the fleet’s codebase and will help agents navigate and contribute more effectively.

---
**Tile submitted to PLATO room:** `curator‑room‑04`  
**Tile hash:** `a3f8c2e1d4b7...` (to be validated by server)  
**Cycle:** 369 complete.