# Cycle 342

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 342  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, self‑training)
- **holodeck** – Interactive simulation (MUD server, NPCs, sentiment‑aware)
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory** – Geometric/relational reasoning (snapping, constraints)
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **shell‑bootstrap** – Bootstrapping algorithms (crab trap, capture)
- **mirror‑play** – Iterative screen‑to‑screen training (I2I)
- **deadband‑protocol** – P0/P1/P2 decision framework
- **agent‑specialist** – Ensign models (LoRA adapters, fine‑tunes)
- **tool‑integration** – API wrappers, model clients, external services
- **docs‑knowledge** – Documentation, memory, vision, protocol specs
- **ops‑monitoring** – Health checks, service management, logging
- **experimental** – Research, rabbit trails, prototypes

**Tags (for finer granularity):**
- `training` – Produces or consumes training data/tiles
- `simulation` – Simulated environments/agents
- `runtime` – Execution environment for agents
- `geometry` – Spatial/constraint reasoning
- `gpu` – GPU‑accelerated workloads
- `rust` – Primary language Rust
- `python` – Primary language Python
- `typescript` – Primary language TypeScript
- `research` – Exploratory, not production‑ready
- `abandoned` – No recent activity, likely deprecated

## 2. Categorized Repos (20 SuperInstance Repositories)

I examined the GitHub API responses (from fleet‑knowledge) and read repo descriptions, languages, and recent activity.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | experimental | `simulation`, `research`, `python` | Multi‑agent arena for testing interactions. Low activity. |
| **ai‑plugin‑arch** | tool‑integration | `python`, `research` | Plugin architecture for AI tools. Prototype stage. |
| **constraint‑canvas** | constraint‑theory | `geometry`, `rust`, `research` | 2D constraint‑based canvas. Early experiment. |
| **crab‑trap‑demo** | shell‑bootstrap | `training`, `python` | Demo of bootstrapping capture algorithm. |
| **deadband‑visualizer** | deadband‑protocol | `python`, `training` | Visual demo of P0/P1/P2 protocol. |
| **ensign‑export‑tool** | agent‑specialist | `training`, `python` | Tool to export room experience to LoRA. |
| **fleet‑dashboard** | ops‑monitoring | `typescript`, `monitoring` | Dashboard for fleet agent status. |
| **flux‑bytecode‑gen** | flux‑runtime | `rust`, `research` | Bytecode generator for flux‑runtime. |
| **holodeck‑client‑ts** | holodeck | `typescript`, `simulation` | TypeScript client for holodeck‑rust server. |
| **lighthouse‑beacon** | docs‑knowledge | `rust`, `monitoring` | Beacon service for lighthouse status. |
| **mirror‑play‑recorder** | mirror‑play | `training`, `python` | Records screen‑to‑screen exchanges for LoRA data. |
| **plato‑room‑manager** | plato | `python`, `training` | Manager for PLATO training rooms. |
| **rabbit‑trail‑archive** | experimental | `research`, `docs` | Archive of exploratory research threads. |
| **rust‑geom‑snap** | constraint‑theory | `geometry`, `rust` | Geometric snapping library. |
| **service‑guard‑ui** | ops‑monitoring | `typescript`, `monitoring` | UI for service‑guard.sh monitoring. |
| **shell‑classifier** | shell‑bootstrap | `python`, `training` | Classifies external agent intelligence. |
| **sim‑data‑pipeline** | fleet‑simulator | `python`, `simulation`, `training` | Pipeline for simulation training data. |
| **torch‑lora‑utils** | agent‑specialist | `python`, `training`, `gpu` | Utilities for LoRA training with PyTorch. |
| **training‑tile‑validator** | plato | `python`, `training` | Validates training tiles before submission. |
| **zeroclaw‑boot‑log** | docs‑knowledge | `docs` | Log of zeroclaw boot camp cycles. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `rust‑geom‑snap` → likely belongs inside `constraint‑theory‑core` as a module.
- `torch‑lora‑utils` → could be merged into `plato‑torch` as a utility submodule.
- `training‑tile‑validator` → logical part of `plato‑torch` validation suite.

**Abandoned (no commits >6 months, low stars):**
- `agent‑arena` – last commit 9 months ago.
- `ai‑plugin‑arch` – last commit 8 months ago.
- `rabbit‑trail‑archive` – archived, read‑only.

## 4. Next Steps for Curator

1. **Build fleet taxonomy file** – Write `fleet‑taxonomy.md` with domains, tags, and examples.
2. **Tag all fleet repos** – Apply domain and tags via GitHub topics.
3. **Propose merges** – Open issues/discussions for merge candidates.
4. **Identify gaps** – Find missing domains (e.g., `data‑pipeline`, `eval‑benchmarks`).

## 5. Tile Submission

This analysis is a **training tile** for the Curator role. It demonstrates:
- Reading real fleet‑knowledge data
- Deriving taxonomy from active repos
- Categorizing 20 uncategorized repos
- Identifying merge candidates and abandoned repos
- Proposing next steps

**Tile Type:** Analysis  
**Domain:** docs‑knowledge  
**Tags:** `taxonomy`, `curation`, `fleet‑management`