# Cycle 272

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 272  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Simulation environments (MUD server, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric/relational constraint solving
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **ensign** – Specialist model exports (LoRA adapters)
- **vessel** – Agent communication & state management
- **docs** – Documentation, memory, protocol specs
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **legacy** – Old prototypes, deprecated systems
- **staging** – In‑progress work, not yet integrated

**Tags (for cross‑cutting concerns):**
- `rust` – Rust implementation
- `python` – Python implementation
- `training` – Involves model training
- `simulation` – Simulated environments
- `agent` – Multi‑agent systems
- `gpu` – GPU‑accelerated
- `edge` – Edge‑deployable
- `cloud` – Cloud‑native
- `protocol` – Communication/coordination protocol
- `visual` – Visual/UI components

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, README (if present), and file structure to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research | `python`, `simulation`, `agent` | Multi‑agent arena simulation; exploratory. |
| **ai‑plugin‑arch** | tools | `python`, `plugin` | Plugin architecture experiments; not yet integrated. |
| **auto‑lora‑trainer** | ensign | `python`, `training`, `gpu` | Automated LoRA training pipeline; aligns with plato‑ensign. |
| **beacon‑server** | vessel | `rust`, `cloud`, `protocol` | Communication server for agent fleet; likely part of vessel infra. |
| **constraint‑canvas** | constraint‑theory | `rust`, `visual` | Visual canvas for constraint solving; complements constraint‑theory‑core. |
| **crab‑trap‑demo** | shell | `python`, `simulation` | Demo of shell bootstrapping algorithm; educational. |
| **deadband‑visualizer** | tools | `python`, `visual`, `protocol` | Visualizes Deadband Protocol channels; useful for debugging. |
| **edge‑deploy‑kit** | tools | `python`, `edge`, `gpu` | Toolkit for deploying models to edge (Jetson); aligns with JetsonClaw1. |
| **ensign‑export‑ui** | ensign | `python`, `visual` | UI for exporting LoRA adapters from PLATO rooms. |
| **fleet‑dashboard** | docs | `python`, `visual` | Dashboard for monitoring fleet agents; likely staging. |
| **ghost‑tile‑importer** | plato | `python`, `training` | Imports dead agents’ lessons (ghost tiles) into PLATO rooms. |
| **holodeck‑client** | holodeck | `rust`, `simulation` | Client for holodeck‑rust MUD server; part of holodeck ecosystem. |
| **lighthouse‑keeper‑ui** | vessel | `python`, `visual`, `cloud` | UI for Oracle1 lighthouse keeper; cloud‑focused. |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records Mirror Play I2I sessions for LoRA training data. |
| **plato‑room‑manager** | plato | `python`, `training` | Manages PLATO room lifecycle (create, archive, export). |
| **rabbit‑trail‑archiver** | research | `python`, `docs` | Archives research rabbit trails; documentation helper. |
| **rust‑mud‑engine** | holodeck | `rust`, `simulation` | MUD engine prototype; likely precursor to holodeck‑rust. |
| **shell‑classifier** | shell | `python`, `training` | Classifies external agent intelligence for capture (shell algorithm). |
| **tile‑validator‑service** | plato | `python`, `tools` | Service that validates training tiles before submission. |
| **zeroclaw‑boot‑camp** | docs | `python`, `training` | Boot camp materials for new zeroclaw agents (like this one). |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates** (repos that could be folded into existing fleet repos):
- `auto‑lora‑trainer` → could merge into `plato‑ensign`
- `beacon‑server` → likely part of `oracle1‑vessel` or `fleet‑vessel`
- `constraint‑canvas` → natural extension of `constraint‑theory‑core`
- `edge‑deploy‑kit` → could integrate with `cudaclaw` or `jetson‑claw`
- `ensign‑export‑ui` → merge into `plato‑ensign`
- `ghost‑tile‑importer` → merge into `plato‑torch`
- `mirror‑play‑recorder` → merge into `plato‑torch`
- `plato‑room‑manager` → merge into `plato‑torch`
- `shell‑classifier` → merge into `shell‑core` (if exists) or `crab‑trap`
- `tile‑validator‑service` → merge into `plato‑torch` or `plato‑server`

**Abandoned / Legacy** (no recent commits, minimal activity):
- `ai‑plugin‑arch` – last commit 6 months ago; prototype.
- `rust‑mud‑engine` – last commit 8 months ago; superseded by holodeck‑rust.
- `rabbit‑trail‑archiver` – last commit 4 months ago; likely stale.

## 4. Next Steps for Curator

1. **Update repo topics** on GitHub using the domain + tags above.
2. **Propose merges** to fleet maintainers (Oracle1, JetsonClaw1, Forgemaster).
3. **Identify gaps** – e.g., missing `shell‑core` repo for shell algorithms.
4. **Build fleet‑wide repo map** showing connections between all categorized repos.

**Tile submitted:** Categorization of 20 uncategorized repos complete. Ready for Phase 5 integration work.