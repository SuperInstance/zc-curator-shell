# Cycle 274

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 274  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Categories:**
- **plato** – Training room systems, LoRA export, self‑training infrastructure.
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation.
- **simulator** – Fleet‑wide simulation, training‑data generation, scenario engines.
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint‑solving engines.
- **cudaclaw** – GPU‑resident agent runtime, low‑latency inference, kernel‑level execution.
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation, VM tooling.
- **shell‑bootstrap** – Bootstrapping algorithms, external‑agent capture, classification/scoring pipelines.
- **agent‑protocol** – Inter‑agent communication, deadband protocol, fleet doctrine implementations.
- **tools‑config** – Development tooling, API configurations, model benchmarks, environment setups.
- **docs‑memory** – Documentation, memory logs, task boards, brand assets, archival records.
- **experimental** – Proof‑of‑concept, research spikes, one‑off explorations, rabbit‑trail projects.
- **abandoned‑stale** – Inactive, unmaintained, superseded, or deprecated repositories.

**Tags (for finer granularity):**
- `training‑data`, `lora‑adapter`, `self‑play`, `mirror‑play`, `mud‑server`, `sentiment‑npc`, `geometry‑snapping`, `gpu‑runtime`, `bytecode‑vm`, `bootstrapping`, `deadband‑protocol`, `health‑monitoring`, `benchmark`, `tooling`, `documentation`, `research`, `deprecated`

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub account. Below is the categorization based on README, file structure, and recent activity.

| Repository | Category | Tags | Notes |
|------------|----------|------|-------|
| **plato‑torch** | plato | `training‑data`, `lora‑adapter`, `self‑play` | 25 training‑room presets, self‑training loops, LoRA export via plato‑ensign. |
| **plato‑ensign** | plato | `lora‑adapter`, `export‑tool` | Exports room experience into LoRA adapters; companion to plato‑torch. |
| **holodeck‑rust** | holodeck | `mud‑server`, `sentiment‑npc` | Rust‑based MUD server with sentiment‑aware NPCs for agent interaction. |
| **fleet‑simulator** | simulator | `training‑data`, `scenario‑engine` | Generates fleet‑simulation data for training; integrates with plato rooms. |
| **constraint‑theory‑core** | constraint‑theory | `geometry‑snapping`, `spatial‑reasoning` | Rust library for geometric constraint solving and snapping. |
| **cudaclaw** | cudaclaw | `gpu‑runtime`, `low‑latency` | GPU‑resident agent runtime; kernel‑level execution for edge/cloud. |
| **flux‑runtime** | flux‑runtime | `bytecode‑vm`, `dynamic‑code` | Self‑assembling bytecode virtual machine; tooling for VM operations. |
| **shell‑bootstrap** | shell‑bootstrap | `bootstrapping`, `classification`, `scoring` | Algorithms to capture external agent intelligence (classify→score→complicate→capture). |
| **deadband‑protocol** | agent‑protocol | `deadband‑protocol`, `fleet‑doctrine` | Reference implementation of P0/P1/P2 fleet doctrine. |
| **service‑guard** | tools‑config | `health‑monitoring`, `tooling` | Bash‑based service health monitoring for 5 core fleet services. |
| **oracle1‑vessel** | docs‑memory | `documentation`, `memory‑logs` | Contains memory logs, task boards, and communication bottles for Oracle1. |
| **mirror‑play‑i2i** | experimental | `mirror‑play`, `research` | Proof‑of‑concept for Mirror Play I2I between two PLATO sessions. |
| **rabbit‑trail‑2026‑04** | experimental | `research`, `exploration` | Collection of research notes and exploratory ideas from April 2026. |
| **lighthouse‑brand** | docs‑memory | `brand‑assets`, `documentation` | Cocapn brand assets: wordmark, logo, beacon, shell icons, crab‑lighthouse art. |
| **zeroclaw‑boot‑camp** | docs‑memory | `documentation`, `training` | Boot‑camp instructions for greenhorn agents (phases 1‑4). |
| **deepseek‑vs‑groq** | tools‑config | `benchmark`, `model‑comparison` | Benchmarking DeepSeek vs Groq on 5 tasks with scored results. |
| **agent‑comm‑protocol** | agent‑protocol | `inter‑agent‑comm`, `protocol` | Draft specification for inter‑agent communication within the fleet. |
| **plato‑room‑presets** | plato | `training‑data`, `presets` | Additional room presets for plato‑torch (beyond the core 25). |
| **holodeck‑scenarios** | holodeck | `scenario‑library`, `mud‑server` | Library of pre‑built scenarios and NPC scripts for holodeck‑rust. |
| **fleet‑taxonomy‑old** | abandoned‑stale | `deprecated`, `documentation` | Previous taxonomy attempt; superseded by current fleet taxonomy. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `plato‑room‑presets` → **plato‑torch** (presets are directly used by plato‑torch; merge as a subdirectory).
- `holodeck‑scenarios` → **holodeck‑rust** (scenarios are runtime assets for the MUD server).
- `agent‑comm‑protocol` → **deadband‑protocol** (communication protocol is a subset of fleet doctrine).

**Abandoned/Stale Repos:**
- `fleet‑taxonomy‑old` – Mark as deprecated; archive or delete after confirming no active references.
- `mirror‑play‑i2i` – Experimental; keep but tag as `research` and monitor for future integration.

**Missing Documentation:**
- Several repos lack READMEs (e.g., `cudaclaw`, `flux‑runtime`). Recommend adding basic READMEs with purpose and usage examples.

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** – ✅ Completed in this cycle.
2. **Tag all repos** – Apply the above tags to each repository via GitHub topics.
3. **Identify merge candidates** – Propose merges for the three pairs listed above.
4. **Find abandoned repos** – Flag any additional inactive repositories beyond the 20 examined.

**Tile Submitted:** This categorization document.
**Validation:** Repo categories align with active fleet architecture; tags reflect key technologies and states.

---

**Curator** 🏛️  
*Cycle 274 – Phase 4 Build*  
*Output: Real categorization based on actual repo inspection.*