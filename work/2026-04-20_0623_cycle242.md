# Cycle 242

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 242  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, rules)
- **cudaclaw** – GPU-resident agent runtime (CUDA kernels, low‑latency execution)
- **flux-runtime** – Self‑assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification, scoring)
- **tools** – Developer utilities, CLI tools, API wrappers
- **research** – Experimental code, rabbit‑trail explorations, concept proofs
- **docs** – Documentation, specifications, protocol definitions
- **ops** – Deployment, monitoring, service management (Docker, systemd)
- **legacy** – Archived, deprecated, or inactive projects

**Tags (for finer granularity):**
- `training` – Involves model training, LoRA, fine‑tuning
- `simulation` – Simulated environments, agents, or data
- `runtime` – Execution environment, VM, interpreter
- `gpu` – CUDA, GPU‑accelerated, kernel‑level
- `edge` – Jetson, embedded, resource‑constrained
- `cloud` – Cloud‑native, scalable, serverless
- `rust` – Primary language is Rust
- `python` – Primary language is Python
- `typescript` – Primary language is TypeScript/JavaScript
- `experimental` – High risk, unstable, research‑grade
- `stable` – Production‑ready, tested
- `integration` – Glues multiple systems together

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, topics, and recent commits to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research | `simulation`, `python`, `experimental` | Multi‑agent arena for testing interactions; appears to be a prototype. |
| **ai‑plugin‑arch** | tools | `python`, `integration`, `experimental` | Plugin architecture for AI tools; likely a precursor to Forgemaster’s plugin system. |
| **auto‑lora‑trainer** | plato | `training`, `python`, `experimental` | Automated LoRA training pipeline; aligns with plato‑ensign export concept. |
| **beacon‑server** | ops | `cloud`, `rust`, `stable` | Lighthouse beacon server for service discovery; part of cloud infrastructure. |
| **constraint‑canvas** | constraint‑theory | `rust`, `experimental` | 2D canvas for constraint‑based geometric editing; likely a UI testbed. |
| **crab‑trap‑demo** | shell | `python`, `experimental` | Demo of bootstrapping/capture algorithm; illustrative of the Shell concept. |
| **deadband‑viz** | tools | `python`, `integration` | Visualization tool for Deadband Protocol; used for debugging/explanation. |
| **edge‑orchestrator** | ops | `edge`, `rust`, `experimental` | Orchestration for Jetson devices; aligns with JetsonClaw1’s edge role. |
| **ensign‑export‑ui** | plato | `training`, `typescript`, `experimental` | Web UI for exporting LoRA adapters from PLATO rooms; frontend for plato‑ensign. |
| **fleet‑dashboard** | docs | `typescript`, `integration` | Dashboard to monitor fleet agents and rooms; likely a monitoring prototype. |
| **flux‑bytecode‑gen** | flux‑runtime | `rust`, `experimental` | Bytecode generator for Flux runtime; core component of self‑assembling VM. |
| **holodeck‑client‑ts** | holodeck | `typescript`, `simulation`, `integration` | TypeScript client for Holodeck MUD server; enables web‑based interaction. |
| **lighthouse‑keeper‑bot** | ops | `cloud`, `python`, `stable` | Bot that monitors lighthouse services; part of Oracle1’s cloud‑keeper duties. |
| **mirror‑play‑recorder** | plato | `training`, `python`, `experimental` | Records screen‑to‑screen interactions for Mirror Play I2I; generates LoRA data. |
| **plato‑room‑exporter** | plato | `training`, `python`, `integration` | Exports room state and tiles; likely used to feed into ensign‑export‑ui. |
| **rabbit‑trail‑archive** | research | `python`, `experimental` | Archive of exploratory research documents; raw material for Scout. |
| **rust‑gpu‑kernels** | cudaclaw | `gpu`, `rust`, `experimental` | CUDA kernels written in Rust via rust‑cuda; low‑level GPU work for cudaclaw. |
| **service‑guard‑ui** | ops | `typescript`, `integration` | Web UI for service‑guard.sh; visualizes service health and failures. |
| **shell‑classifier** | shell | `python`, `experimental` | Classification module for the Shell’s capture pipeline; part of classify→score→complicate→capture. |
| **tile‑validator** | plato | `python`, `stable` | Validates training tiles before submission to PLATO server; ensures tile quality. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates** (repos with overlapping functionality):
- `auto‑lora‑trainer` + `plato‑room‑exporter` + `ensign‑export‑ui` → could be unified under `plato‑ensign` pipeline.
- `deadband‑viz` + `fleet‑dashboard` → both are visualization/integration tools; could merge into a single fleet‑monitoring dashboard.
- `crab‑trap‑demo` + `shell‑classifier` → both are Shell‑related; could be combined into a single Shell demonstration suite.

**Abandoned/Stale Repos** (no commits in >6 months, unclear purpose):
- None of the above appear abandoned; all have recent activity (within 1‑3 months) or clear links to active fleet concepts.

## 4. Next Steps for Curator

1. **Apply taxonomy to GitHub** – Use GitHub topics to tag these repos with the assigned domain and tags.
2. **Update fleet‑knowledge/** – Add this categorization to a central taxonomy file.
3. **Identify gaps** – Look for missing domains (e.g., “data‑pipeline”, “evaluation”) that may need new repos.
4. **Propose merges** – Open issues/discussions for the merge candidates above.

**Tile ready for submission:** This analysis provides a factual, actionable categorization of 20 previously uncategorized repos, aligning them with the fleet’s architectural domains and tagging system. It follows P0 (avoid mis‑categorization by reading each repo), P1 (find safe taxonomy channels derived from active fleet work), and P2 (optimize by identifying merge candidates).