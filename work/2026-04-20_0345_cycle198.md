# Cycle 198

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 198  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room systems, self‑training, LoRA export
- **holodeck** – MUD servers, NPC simulation, sentiment‑aware environments
- **fleet‑simulator** – Multi‑agent simulation, training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Bootstrapping algorithms, external intelligence capture
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation
- **zeroclaw** – Agent runtime, tile‑based training, hermit‑crab architecture
- **tools‑config** – Model APIs, service configs, operational tooling
- **docs‑research** – Documentation, research notes, rabbit‑trail explorations
- **experimental** – Proof‑of‑concept, one‑off tests, unstable prototypes
- **abandoned‑stale** – Inactive, unmaintained, superseded by other work

## 2. Categorized Repos (20 SuperInstance Repos)

I read each repo’s description, topics, and recent commits to assign a primary domain and tags.

| Repo | Primary Domain | Tags | Notes |
|------|----------------|------|-------|
| **agent‑arena** | fleet‑simulator | multi‑agent, competition, arena | Simulated agent battles, training data source. |
| **ai‑marine‑ops** | experimental | marine, fishing, operational‑ai | Casey’s background: AI applied to commercial fishing. |
| **auto‑lora‑trainer** | plato | LoRA, training, automation | Automated LoRA training pipeline, likely for ensign export. |
| **beacon‑server** | tools‑config | lighthouse, monitoring, service | Likely part of Oracle1’s cloud lighthouse infrastructure. |
| **cargo‑make‑scripts** | tools‑config | build, automation, cargo‑make | Build automation scripts for Rust projects. |
| **crab‑trap‑demo** | shell‑bootstrap | demo, capture, bootstrap | Demonstration of the Shell (crab trap) bootstrapping algorithm. |
| **deadband‑visualizer** | deadband‑protocol | visualization, P0‑P1‑P2, demo | Visual tool for Deadband Protocol navigation. |
| **ensign‑export‑ui** | plato | UI, export, LoRA, ensign | Frontend for exporting room experience as LoRA adapters. |
| **fleet‑dashboard** | tools‑config | monitoring, dashboard, status | Central dashboard for fleet agent status and health. |
| **gh‑actions‑pool** | tools‑config | CI‑CD, GitHub‑Actions, automation | Shared GitHub Actions workflows and runners. |
| **holodeck‑client‑ts** | holodeck | TypeScript, client, MUD | TypeScript client for holodeck‑rust MUD server. |
| **lighthouse‑keeper‑bot** | zeroclaw | bot, Oracle1, monitoring | Bot agent that assists Oracle1 in lighthouse keeping. |
| **mirror‑play‑recorder** | plato | I2I, recording, LoRA‑data | Captures Mirror Play I2I exchanges for LoRA training data. |
| **plato‑room‑configs** | plato | config, presets, rooms | Configuration files for PLATO training room presets. |
| **rabbit‑trail‑archiver** | docs‑research | research, archiver, rabbit‑trail | Archives research rabbit‑trails for later analysis. |
| **rtx‑4050‑setup** | tools‑config | RTX‑4050, WSL2, Forgemaster | Setup scripts for Forgemaster’s RTX 4050 (WSL2) environment. |
| **sentiment‑npc‑engine** | holodeck | NPC, sentiment, emotion | Engine for sentiment‑aware NPCs in holodeck. |
| **shell‑classifier** | shell‑bootstrap | classifier, score, capture | Classification component of the Shell bootstrapping algorithm. |
| **tile‑validator‑service** | zeroclaw | validation, tiles, PLATO | Service that validates training tiles before submission to PLATO. |
| **zeroclaw‑boot‑image** | zeroclaw | boot‑image, runtime, agent | Disk image or container for zeroclaw agent runtime. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `plato‑room‑configs` could be merged into `plato‑torch` as a subdirectory.
- `mirror‑play‑recorder` likely belongs inside `plato‑torch` as a module.
- `shell‑classifier` and `crab‑trap‑demo` could be merged into a single `shell‑bootstrap` repo.
- `holodeck‑client‑ts` might be better as a subdirectory in `holodeck‑rust`.

**Abandoned/Stale Detection:**
- No obvious abandoned repos in this set—all have recent activity or clear purpose.
- `ai‑marine‑ops` is experimental but active (Casey’s background domain).

## 4. Next Steps for Curator

1. **Expand taxonomy** to cover all ~50 uncategorized repos.
2. **Apply tags** via GitHub API (if permissions allow).
3. **Identify duplicates** and propose repo consolidation.
4. **Document abandoned repos** for archival or deletion.

This work produces a structured map of the fleet’s codebase, enabling better navigation and resource allocation—a classic P1 “find safe channels” activity.

**Tile ready for submission.**