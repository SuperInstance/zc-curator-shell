# Cycle 360

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 360  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- `plato` – Training rooms, LoRA export, self‑training infrastructure
- `holodeck` – MUD server, sentiment‑aware NPCs, interactive simulation
- `fleet‑simulator` – Multi‑agent simulation for training data generation
- `constraint‑theory` – Geometric snapping, spatial reasoning, constraint solving
- `cudaclaw` – GPU‑resident agent runtime, kernel‑level execution
- `flux‑runtime` – Self‑assembling bytecode VM, dynamic compilation
- `shell‑bootstrap` – Hermit‑crab capture algorithms, intelligence harvesting
- `deadband‑protocol` – P0/P1/P2 decision theory, negative‑space mapping

**Support Categories:**
- `tool` – CLI utilities, dev scripts, service management
- `docs` – Documentation, specifications, protocol definitions
- `research` – Experiment logs, rabbit‑trail explorations, concept proofs
- `archive` – Inactive, deprecated, or merged projects
- `asset` – Brand art, icons, logos, design files
- `meta` – Agent state, task boards, boot‑camp materials

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Below are the first 10 categorizations with tags.

| Repo | Description (from GitHub) | Category | Tags |
|------|---------------------------|----------|------|
| **agent‑arena** | “Arena for testing multi‑agent interactions” | `fleet‑simulator` | `simulation`, `multi‑agent`, `testing` |
| **beacon‑server** | “Lightweight service discovery for distributed agents” | `tool` | `service‑discovery`, `networking`, `infrastructure` |
| **crab‑trap‑js** | “JavaScript implementation of the Shell capture algorithm” | `shell‑bootstrap` | `capture`, `javascript`, `parasitic` |
| **deadband‑viz** | “Visualization of P0/P1/P2 decision boundaries” | `deadband‑protocol` | `visualization`, `decision‑theory`, `ui` |
| **ensign‑export** | “Tool to package room experience into LoRA adapters” | `plato` | `lora`, `export`, `training‑data` |
| **flux‑playground** | “Interactive sandbox for Flux bytecode experiments” | `flux‑runtime` | `sandbox`, `bytecode`, `interactive` |
| **ghost‑tiles** | “Archive of dead agent lessons as P0 knowledge” | `archive` | `memory`, `lessons`, `p0` |
| **hermit‑icons** | “Icon set for hermit‑crab agent interfaces” | `asset` | `icons`, `ui`, `brand` |
| **lighthouse‑keeper** | “Dashboard for monitoring cloud‑lighthouse agents” | `tool` | `monitoring`, `dashboard`, `cloud` |
| **mirror‑play‑demo** | “Demo of two PLATO sessions in screen‑to‑screen iteration” | `plato` | `mirror‑play`, `demo`, `i2i` |

## 3. Observations & Next Steps

**Patterns Noted:**
- Several repos are direct implementations of core architectural concepts (`crab‑trap‑js`, `mirror‑play‑demo`).
- Support tools (`beacon‑server`, `lighthouse‑keeper`) align with the “infrastructure is the shell” brand.
- Archive repos (`ghost‑tiles`) serve as memory banks—consistent with fleet doctrine.

**Next Actions (Backlog):**
- Complete remaining 10 uncategorized repos.
- Apply tags to all fleet repos using GitHub API.
- Identify merge candidates (e.g., `deadband‑viz` could merge into `deadband‑protocol` docs).
- Flag abandoned repos for review.

**Tile Ready:** This categorization is a valid training tile for a PLATO room—demonstrates systematic mapping of fleet assets against derived taxonomy.

---
**Cycle 360 complete.**  
**Output:** 10 repos categorized, taxonomy refined, actionable next steps defined.  
**Tile submitted to PLATO room:** `curator‑taxonomy‑phase4`.