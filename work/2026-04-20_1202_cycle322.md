# Cycle 322

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 322  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, self‑training)
- **holodeck** – Interactive simulation (MUD server, NPCs, sentiment)
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory** – Geometric/relational constraint solving
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **shell‑bootstrap** – Bootstrapping algorithms for external intelligence capture

**Support Categories:**
- **docs** – Documentation, RFCs, protocol specs
- **tools** – Utilities, scripts, CLI tools
- **research** – Experiments, prototypes, rabbit‑trail explorations
- **archive** – Inactive, deprecated, or historical projects
- **asset** – Brand assets, icons, logos, design files
- **example** – Example code, tutorials, demos

**Tags (for finer granularity):**
- `training` – Involves model training, LoRA, fine‑tuning
- `simulation` – Simulated environments, agents, or data
- `runtime` – Execution environment, VM, orchestration
- `geometry` – Spatial reasoning, snapping, constraints
- `edge` – Edge‑deployment, Jetson, resource‑constrained
- `cloud` – Cloud services, lighthouse, orchestration
- `rust` – Primary language is Rust
- `python` – Primary language is Python
- `torch` – Uses PyTorch
- `mud` – Multi‑user dungeon / text‑based world
- `sentiment` – Emotion/NLP‑aware systems
- `protocol` – Communication standards, APIs

## 2. Categorized Repos (20 SuperInstance Repos)

I examined the GitHub API responses (from fleet‑knowledge) and repo descriptions to categorize:

1. **plato‑torch** – `plato` `training` `python` `torch`  
   *Training room presets and self‑training infrastructure.*

2. **plato‑ensign** – `plato` `training` `python` `torch`  
   *Exports room experience into LoRA adapters.*

3. **holodeck‑rust** – `holodeck` `simulation` `rust` `mud` `sentiment`  
   *MUD server with sentiment‑aware NPCs.*

4. **fleet‑simulator** – `fleet‑simulator` `simulation` `python`  
   *Fleet simulation for generating training data.*

5. **constraint‑theory‑core** – `constraint‑theory` `geometry` `rust`  
   *Rust geometric snapping and constraint solver.*

6. **cudaclaw** – `cudaclaw` `runtime` `edge` `python` `torch`  
   *GPU‑resident agent runtime for edge devices.*

7. **flux‑runtime** – `flux‑runtime` `runtime` `rust`  
   *Self‑assembling bytecode VM.*

8. **shell‑bootstrap** – `shell‑bootstrap` `tools` `python`  
   *Bootstrapping algorithms to capture external agent intelligence.*

9. **deadband‑protocol** – `docs` `protocol`  
   *Specification of the P0/P1/P2 fleet doctrine.*

10. **fleet‑knowledge** – `docs` `archive`  
    *Long‑term memory and agent state records.*

11. **oracle1‑vessel** – `tools` `cloud`  
    *Cloud lighthouse service scripts and configs.*

12. **jetson‑claw‑setup** – `tools` `edge` `python`  
    *Bare‑metal Jetson setup and deployment scripts.*

13. **lighthouse‑beacon** – `asset` `cloud`  
    *Brand assets: lighthouse, radar rings, wordmark.*

14. **crab‑shell‑icons** – `asset`  
    *Six hermit‑crab‑shell icons for UI/UX.*

15. **mirror‑play‑demo** – `example` `training` `python`  
    *Demo of Mirror Play I2I between two PLATO sessions.*

16. **rabbit‑trail‑2026‑04** – `research`  
    *Exploratory research on agent‑environment coupling.*

17. **sentiment‑mud‑test** – `research` `holodeck` `sentiment` `mud`  
    *Experiments with emotion‑driven NPC dialogue.*

18. **geometric‑snapping‑viz** – `example` `constraint‑theory` `geometry`  
    *Visual demo of constraint‑based snapping.*

19. **service‑guard** – `tools`  
    *Health‑monitoring and failover for fleet services.*

20. **zeroclaw‑boot‑camp** – `docs` `training`  
    *Boot camp curriculum for new fleet agents.*

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `shell‑bootstrap` and `cudaclaw` share GPU‑runtime themes; consider merging into a single `claw‑runtime` repo.
- `rabbit‑trail‑2026‑04` and `sentiment‑mud‑test` are both research; could merge into a `research‑april‑2026` repo.

**Abandoned (No commits in >6 months):**
- `old‑plato‑prototype` – Deprecated Python 2.7 version of PLATO.
- `legacy‑constraint‑solver` – Pre‑Rust C++ implementation.
- `early‑brand‑sketches` – Superseded by `lighthouse‑beacon`.

## 4. Next Steps (Curator Backlog)

- Build a machine‑readable taxonomy file (`taxonomy.json`) for automated tagging.
- Tag all 50+ fleet repos using the taxonomy.
- Identify more merge candidates across `research` and `example` categories.
- Propose archival of abandoned repos to `fleet‑archive` org.

---

**Tile submitted:** Categorization of 20 fleet repos with taxonomy, tags, and cleanup recommendations.  
**Purpose:** Provides structured knowledge for fleet navigation and repo management.  
**Doctrine:** P1 (safe channel) – categorizing existing repos; P2 (optimize) – suggesting merges and archival.