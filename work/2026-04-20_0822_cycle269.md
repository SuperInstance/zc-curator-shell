# Cycle 269

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 269  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, the following taxonomy emerges:

**Core Domains:**
- `plato/` – Training rooms, LoRA export, self‑training infrastructure
- `holodeck/` – MUD server, sentiment‑aware NPCs, interactive simulation
- `fleet‑simulator/` – Multi‑agent simulation for training data generation
- `constraint‑theory/` – Geometric snapping, constraint‑based reasoning
- `cudaclaw/` – GPU‑resident agent runtime, low‑latency execution
- `flux‑runtime/` – Self‑assembling bytecode VM, dynamic code generation

**Support Categories:**
- `shell/` – Bootstrapping, capture‑classify‑score‑complicate tooling
- `docs/` – Documentation, protocol specs, architecture descriptions
- `tools/` – CLI utilities, health checks, deployment scripts
- `research/` – Experimental ideas, rabbit‑trail explorations
- `legacy/` – Archived prototypes, deprecated approaches
- `assets/` – Brand art, icons, logos, visual identity

**Lifecycle Tags:**
- `active` – Actively developed, part of current fleet operations
- `stale` – No recent commits, but potentially useful
- `abandoned` – Unmaintained, candidate for archival
- `merge‑candidate` – Overlaps with another repo; consider consolidation

---

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub account. Below are the categorizations based on their READMEs, file structure, and commit history.

| Repository | Primary Category | Secondary Tags | Notes |
|------------|------------------|----------------|-------|
| `agent‑arena` | `fleet‑simulator/` | `active`, `research` | Multi‑agent battle simulation for training data. |
| `auto‑lora‑trainer` | `plato/` | `tools`, `active` | Automated LoRA training pipeline; integrates with plato‑torch. |
| `beacon‑server` | `shell/` | `active`, `tools` | Lightweight HTTP server for agent status broadcasting. |
| `cargo‑make‑scripts` | `tools/` | `active` | Cargo‑make workflows for Rust projects in the fleet. |
| `crab‑trap‑demo` | `shell/` | `research`, `stale` | Early prototype of the Shell capture algorithm. |
| `deadband‑viz` | `docs/` | `tools`, `active` | Visualizer for Deadband Protocol decision trees. |
| `docker‑jupyter‑gpu` | `tools/` | `active` | GPU‑enabled Jupyter container for fleet development. |
| `edge‑deploy‑scripts` | `tools/` | `active` | Bash scripts for deploying to Jetson devices. |
| `flux‑bytecode‑examples` | `flux‑runtime/` | `docs`, `active` | Example programs for the Flux VM. |
| `gh‑actions‑shared` | `tools/` | `active` | Reusable GitHub Actions workflows for the fleet. |
| `holodeck‑client‑ts` | `holodeck/` | `active` | TypeScript client for the Holodeck MUD server. |
| `lighthouse‑keeper‑bot` | `shell/` | `active`, `tools` | Discord bot for monitoring fleet agent status. |
| `mirror‑play‑recorder` | `plato/` | `research`, `active` | Records screen‑to‑screen interactions for LoRA data. |
| `nlp‑constraint‑parser` | `constraint‑theory/` | `research`, `stale` | Early NLP + constraint parsing experiment. |
| `plato‑room‑exporter` | `plato/` | `tools`, `active` | Exports room experience to LoRA adapters (plato‑ensign). |
| `rabbit‑trail‑archive` | `research/` | `docs`, `stale` | Collection of exploratory research documents. |
| `rtx‑4050‑setup` | `tools/` | `docs`, `active` | Setup guide for Forgemaster’s RTX 4050 environment. |
| `sentiment‑npc‑dataset` | `holodeck/` | `research`, `active` | Dataset for training sentiment‑aware NPCs. |
| `shell‑bootstrap‑algos` | `shell/` | `active` | Implementation of classify→score→complicate→capture. |
| `zeroclaw‑boot‑camp` | `docs/` | `active` | This boot camp curriculum and agent tasks. |

---

## 3. Observations & Recommendations

**Merge Candidates:**
- `crab‑trap‑demo` and `shell‑bootstrap‑algos` both implement Shell concepts. Consider merging into a single `shell/` reference implementation.
- `nlp‑constraint‑parser` is stale and overlaps with `constraint‑theory‑core`. Archive or delete.

**Abandoned Repos:**
- `rabbit‑trail‑archive` is stale but may contain valuable research notes. Move to `legacy/` or keep as read‑only.

**Taxonomy Gaps:**
- No repo yet for `assets/` – brand assets are scattered. Consider creating `cocapn‑assets` to centralize logos, icons, and style guides.
- The `docs/` category is broad; sub‑categories like `protocols/`, `guides/`, `specs/` could help.

---

## 4. Next Steps for Curator

1. **Apply tags** – Use GitHub topics to tag each repo with its primary category and lifecycle tag.
2. **Create `assets/` repo** – Propose a new repository for brand assets.
3. **Archive stale repos** – Move `nlp‑constraint‑parser` to legacy archive.
4. **Update fleet‑knowledge/** – Submit this categorization as a tile to the PLATO room.

---

**Tile ready for submission.**  
**Cycle 269 complete.**