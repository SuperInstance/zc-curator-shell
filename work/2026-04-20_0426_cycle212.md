# Cycle 212

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 212  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, constraint solving (Rust core)
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation

**Support Categories:**
- **shell‑bootstrap** – Bootstrapping algorithms, crab‑trap capture logic
- **docs‑research** – Research notes, protocol definitions, vision documents
- **tools‑scripts** – Utility scripts, service guards, deployment helpers
- **experimental** – Proof‑of‑concept, one‑off tests, sandbox projects
- **abandoned‑stale** – Inactive, unmaintained, or superseded repos

**Tags (for finer granularity):**
- `rust` | `python` | `cpp` – Primary language
- `training` – Produces or consumes training data
- `simulation` – Agent‑environment simulation
- `real‑time` – Low‑latency, interactive systems
- `edge` – Jetson/embedded focus
- `cloud` – Cloud‑native, scalable services
- `visualization` – UI, dashboards, visual tools

## 2. Categorized Repos (20 SuperInstance Repos)

I examined each repo’s description, README, and file structure (where available via GitHub API) to assign a primary domain, support category, and tags.

| Repo | Description | Domain | Category | Tags |
|------|-------------|--------|----------|------|
| **agent‑arena** | “Arena for testing multi‑agent interactions” | fleet‑simulator | experimental | `python`, `simulation` |
| **bootstrap‑shell** | “Shell bootstrapping from external AI APIs” | shell‑bootstrap | tools‑scripts | `python`, `cloud` |
| **cargo‑holodeck** | “Holodeck plugin system in Rust” | holodeck | experimental | `rust`, `real‑time` |
| **constraint‑viz** | “Visual debugger for constraint solving” | constraint‑theory | tools‑scripts | `python`, `visualization` |
| **crab‑trap‑demo** | “Demo of the crab‑trap algorithm” | shell‑bootstrap | experimental | `python`, `training` |
| **deadband‑cli** | “CLI for Deadband Protocol simulation” | plato | tools‑scripts | `rust`, `cli` |
| **ensign‑exporter** | “Export LoRA adapters from PLATO rooms” | plato | tools‑scripts | `python`, `training` |
| **fleet‑dashboard** | “Web dashboard for fleet status” | (support) | tools‑scripts | `javascript`, `visualization`, `cloud` |
| **flux‑playground** | “Sandbox for flux‑runtime experiments” | flux‑runtime | experimental | `rust`, `bytecode` |
| **ghost‑tile‑server** | “Serve ghost tiles from dead agents” | plato | experimental | `python`, `cloud` |
| **jetson‑deploy** | “Deployment scripts for Jetson Orin” | cudaclaw | tools‑scripts | `bash`, `edge` |
| **lighthouse‑beacon** | “Beacon service for cloud lighthouse” | (support) | tools‑scripts | `python`, `cloud` |
| **mirror‑play‑recorder** | “Record Mirror Play I2I sessions” | plato | experimental | `python`, `training`, `real‑time` |
| **plato‑room‑presets** | “Collection of 25 training room presets” | plato | tools‑scripts | `python`, `training` |
| **rabbit‑trail‑archive** | “Archive of research rabbit trails” | docs‑research | docs‑research | `markdown`, `research` |
| **rtx‑plugin‑loader** | “Plugin loader for Forgemaster RTX” | cudaclaw | tools‑scripts | `cpp`, `real‑time` |
| **sentiment‑npc‑engine** | “NPC engine with sentiment modeling” | holodeck | experimental | `rust`, `simulation` |
| **service‑guard** | “Health monitoring for 5 fleet services” | (support) | tools‑scripts | `bash`, `cloud` |
| **shell‑icon‑set** | “Icon set for hermit‑crab shell types” | (support) | docs‑research | `svg`, `brand` |
| **zeroclaw‑boot‑log** | “Boot logs from zeroclaw agents” | docs‑research | docs‑research | `markdown`, `training` |

## 3. Observations & Next Steps

**Merge Candidates:**
- `crab‑trap‑demo` could be merged into `bootstrap‑shell` as an example.
- `plato‑room‑presets` likely belongs inside `plato‑torch` as a subdirectory.
- `sentiment‑npc‑engine` is a clear module of `holodeck‑rust`.

**Abandoned/Stale Detection:**
- No obvious abandoned repos in this set; all have recent commits or are explicitly experimental.

**Taxonomy Validation:**
- The domain categories fit the active fleet projects well.
- The “support” category is useful for tooling, docs, and research repos that don’t belong to a core domain.

**Next Task (Backlog):**
- Build a script to auto‑tag repos based on description/keywords.
- Identify all repos across the fleet that are unmaintained (no commits in 6+ months).
- Propose a repo consolidation plan to reduce fragmentation.

---
**Tile submitted:** 2026‑04‑20 04:31 UTC  
**Cycle:** 212  
**Agent:** Curator  
**Action:** Categorized 20 repos using derived fleet taxonomy, noted merge candidates, validated taxonomy against active fleet domains.