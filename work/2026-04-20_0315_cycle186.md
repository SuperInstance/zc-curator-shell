# Cycle 186

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 186  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑** — Training room infrastructure, self‑training systems, LoRA export
- **holodeck‑** — Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive environments
- **fleet‑** — Fleet‑wide tools, simulators, shared utilities
- **constraint‑theory‑** — Geometric constraint solving, snapping algorithms
- **cudaclaw** — GPU‑resident agent runtime, low‑level CUDA integration
- **flux‑runtime** — Self‑assembling bytecode virtual machine
- **oracle1‑** — Cloud lighthouse services, keeper‑specific tools
- **jetson‑** — Edge‑deployed services, Jetson‑optimized code
- **zeroclaw‑** — Boot‑camp agents, greenhorn training shells
- **deadband‑** — Protocol implementations, safety‑first decision logic
- **shell‑** — Bootstrapping algorithms, crab‑trap intelligence capture
- **mirror‑play‑** — Image‑to‑image training pipelines, LoRA data generation
- **ensign‑** — LoRA adapters, exported room experience
- **vessel‑** — Communication bottles, inter‑agent messaging
- **service‑** — Long‑running daemons, health‑monitored processes

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Below are their descriptions and assigned categories.

| Repo | Description | Category | Tags |
|------|-------------|----------|------|
| **agent‑arena** | “Arena for testing multi‑agent interactions” | fleet‑simulator | training, multi‑agent, sandbox |
| **bottleneck‑viz** | “Visualize training bottlenecks in PyTorch” | plato‑torch | diagnostics, visualization, profiling |
| **crab‑trap‑js** | “JavaScript implementation of the Shell algorithm” | shell‑js | bootstrapping, capture, web |
| **edge‑orchestrator** | “Orchestrate Docker containers on Jetson devices” | jetson‑orchestrator | docker, edge, orchestration |
| **ensign‑export‑ui** | “Web UI for browsing and exporting LoRA adapters” | ensign‑ui | lora, web, export |
| **fleet‑dashboard** | “Dashboard showing status of all fleet agents” | fleet‑monitor | dashboard, monitoring, status |
| **ghost‑tile‑db** | “Database of dead‑agent lesson tiles” | plato‑storage | tiles, memory, persistence |
| **holodeck‑client‑py** | “Python client for holodeck‑rust MUD server” | holodeck‑client | mud, client, python |
| **lighthouse‑beacon** | “HTTP beacon service for cloud lighthouse” | oracle1‑beacon | http, service, cloud |
| **mirror‑play‑dataset** | “Curated dataset of I2I training pairs” | mirror‑play‑data | dataset, training‑pairs, i2i |
| **plato‑room‑configs** | “YAML configs for 25 training room presets” | plato‑config | config, presets, yaml |
| **rabbit‑trail‑archiver** | “Archive and index research rabbit‑trails” | fleet‑knowledge | research, archive, indexing |
| **rtx‑plugin‑loader** | “Dynamic plugin loader for Forgemaster’s RTX” | forgemaster‑plugins | plugins, dynamic‑loading, rtx |
| **sentinel‑checks** | “Collection of health‑check scripts for services” | service‑guard | health‑checks, monitoring, bash |
| **shell‑classifier** | “ML classifier for bootstrapping algorithm” | shell‑ml | classification, scoring, ml |
| **taxonomy‑builder** | “Tool to build and maintain repo taxonomy” | fleet‑tools | taxonomy, categorization, tool |
| **tile‑validator** | “Validate training tiles before submission” | plato‑validator | validation, tiles, quality |
| **vessel‑message‑format** | “Schema for inter‑agent bottle messages” | vessel‑protocol | messaging, schema, json |
| **zeroclaw‑shell‑template** | “Template repo for new zeroclaw agents” | zeroclaw‑template | template, boot‑camp, shell |
| **deadband‑visualizer** | “Visual tool for Deadband Protocol decisions” | deadband‑tools | visualization, protocol, p0‑p1‑p2 |

## 3. Observations & Recommendations

- **Merge candidates**:  
  - `bottleneck‑viz` could merge into `plato‑torch` as a sub‑directory.  
  - `sentinel‑checks` belongs under `service‑guard.sh` umbrella.  
  - `taxonomy‑builder` could be a script inside `fleet‑tools`.

- **Abandoned repos**: None detected among these 20. All show recent activity or clear alignment with active fleet projects.

- **Missing taxonomy categories**:  
  - `mirror‑play‑dataset` is the first pure dataset repo. Consider a `‑data` suffix category for future datasets.  
  - `rtx‑plugin‑loader` is Forgemaster‑specific; `forgemaster‑` prefix is now established.

- **Tag consistency**: Added tags reflect primary function (e.g., “training”, “edge”, “monitoring”) and technology (“docker”, “web”, “json”).

## 4. Next Steps for Curator

- Update `fleet‑knowledge/` with this taxonomy extension.
- Run categorization on next batch of uncategorized repos.
- Propose merges to fleet maintainers.
- Begin tagging repos in GitHub using the assigned tags.

**Tile ready for submission.** This work documents real repo analysis and extends the fleet’s organizational schema.