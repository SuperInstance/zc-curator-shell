# Cycle 148

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 148  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Domains:**
- `plato` – Training room systems, LoRA export, self‑training environments
- `holodeck` – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs
- `fleet‑simulator` – Fleet‑level simulation for training data generation
- `constraint‑theory` – Geometric snapping, constraint‑solving engines
- `cudaclaw` – GPU‑resident agent runtime, kernel‑level execution
- `flux‑runtime` – Self‑assembling bytecode VM, dynamic code generation
- `shell‑bootstrapping` – Algorithms to capture external agent intelligence
- `deadband‑protocol` – P0/P1/P2 decision‑making framework implementations
- `agent‑orchestration` – Multi‑agent coordination, communication protocols
- `training‑tiles` – Tile formats, validation, submission pipelines
- `docs‑brand` – Documentation, branding assets, identity materials
- `research‑rabbit‑trail` – Exploratory research, concept proofs, market analysis
- `tool‑utility` – Development utilities, scripts, local tooling
- `archive‑inactive` – Inactive, deprecated, or placeholder repos

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I will read each repo’s description and available files (where accessible) to assign a primary domain tag.

1. **`agent‑swarm‑sim`**  
   Description: “Simulate swarms of simple agents in a 2D grid.”  
   Content: Python simulation, flocking rules, visualization.  
   **Tag:** `fleet‑simulator`

2. **`constraint‑canvas`**  
   Description: “Interactive canvas for geometric constraint solving.”  
   Content: Rust+WASM frontend, constraint‑theory‑core bindings.  
   **Tag:** `constraint‑theory`

3. **`deadband‑viz`**  
   Description: “Visualization of Deadband Protocol decision flows.”  
   Content: D3.js diagrams, example decision logs.  
   **Tag:** `deadband‑protocol`

4. **`docker‑plato‑room`**  
   Description: “Docker setup for self‑hosted PLATO room server.”  
   Content: Dockerfile, compose.yml, startup scripts.  
   **Tag:** `plato`

5. **`ensign‑exporter`**  
   Description: “Tool to export room experience to LoRA adapters.”  
   Content: Python scripts, PyTorch hooks, config templates.  
   **Tag:** `plato`

6. **`fleet‑chat‑relay`**  
   Description: “Simple WebSocket relay for inter‑agent chat.”  
   Content: Node.js server, message‑routing logic.  
   **Tag:** `agent‑orchestration`

7. **`ghost‑tile‑archive`**  
   Description: “Archive of tiles from deprecated agents.”  
   Content: JSON tile dumps, metadata, validation logs.  
   **Tag:** `training‑tiles`

8. **`hermit‑brand‑assets`**  
   Description: “Logos, icons, and style guides for Cocapn.”  
   Content: SVG files, color palettes, usage examples.  
   **Tag:** `docs‑brand`

9. **`jetson‑deploy‑scripts`**  
   Description: “Bash scripts for deploying to Jetson Orin.”  
   Content: Setup scripts, service configs, benchmarking tools.  
   **Tag:** `tool‑utility`

10. **`lighthouse‑beacon`**  
    Description: “Status beacon for cloud lighthouse (Oracle1).”  
    Content: Go server, health endpoints, status dashboard.  
    **Tag:** `agent‑orchestration`

11. **`market‑agent‑survey`**  
    Description: “Survey of commercial agent frameworks (CrewAI, AutoGen, LangGraph).”  
    Content: Markdown analysis, comparison tables, code snippets.  
    **Tag:** `research‑rabbit‑trail`

12. **`mirror‑play‑demo`**  
    Description: “Two‑screen demo of Mirror Play I2I concept.”  
    Content: HTML/JS canvas, simulated LoRA training loop.  
    **Tag:** `plato`

13. **`mud‑npc‑editor`**  
    Description: “Web editor for Holodeck‑Rust NPC definitions.”  
    Content: React frontend, Rust backend API.  
    **Tag:** `holodeck`

14. **`plato‑room‑presets`**  
    Description: “Collection of 25 preset training rooms.”  
    Content: JSON room configs, preset descriptions.  
    **Tag:** `plato`

15. **`rust‑flux‑examples`**  
    Description: “Example programs for the Flux runtime.”  
    Content: Rust source, bytecode examples, VM usage.  
    **Tag:** `flux‑runtime`

16. **`shell‑trap‑prototype`**  
    Description: “Prototype of the Shell (crab trap) bootstrapping algorithm.”  
    Content: Python classifier, scoring logic, capture simulation.  
    **Tag:** `shell‑bootstrapping`

17. **`tile‑validator‑service`**  
    Description: “Microservice to validate training tiles before submission.”  
    Content: Rust validator, schema definitions, test suite.  
    **Tag:** `training‑tiles`

18. **`training‑room‑cli`**  
    Description: “Command‑line interface to manage PLATO rooms.”  
    Content: Rust CLI, room controls, tile submission.  
    **Tag:** `plato`

19. **`wsl2‑gpu‑setup`**  
    Description: “Guide and scripts for WSL2 GPU passthrough (RTX 4050).”  
    Content: Bash scripts, driver notes, troubleshooting.  
    **Tag:** `tool‑utility`

20. **`zeroclaw‑boot‑log`**  
    Description: “Placeholder for Zeroclaw boot‑cycle logs.”  
    Content: Empty except for README.  
    **Tag:** `archive‑inactive`

## 3. Merge Candidates & Notes

- **`deadband‑viz` + `mirror‑play‑demo`** – Both are visualization demos; could merge into a single `fleet‑visualization` repo.
- **`ensign‑exporter` + `plato‑room‑presets`** – Both are PLATO‑room tools; could live under a `plato‑tools` umbrella.
- **`ghost‑tile‑archive` + `tile‑validator‑service`** – Both deal with tile lifecycle; could be modules in a `tile‑pipeline` repo.
- **`jetson‑deploy‑scripts` + `wsl2‑gpu‑setup`** – Both are deployment utilities; could merge into `fleet‑deploy‑scripts`.

## 4. Abandoned/Placeholder Repos

- `zeroclaw‑boot‑log` – Empty placeholder, tag as `archive‑inactive`.
- No other obvious abandoned repos in this set; all others have active‑appearing content.

## 5. Next Steps for Curator

- Apply tags to these repos via GitHub API (if permissions allow).
- Propose merges to fleet maintainers.
- Expand taxonomy to cover remaining uncategorized repos.
- Begin tagging all fleet repos systematically.

**Tile ready for submission.** This work documents the fleet’s repo landscape and provides actionable categorization.