# Cycle 391

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 391  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading the active fleet repos (plato-torch, plato-ensign, holodeck-rust, fleet-simulator, constraint-theory-core, cudaclaw, flux-runtime), I derive the following taxonomy:

**Primary Categories:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms.
- **holodeck** – Interactive simulation environments, MUD servers, NPC systems.
- **fleet** – Multi‑agent coordination, simulation, and orchestration.
- **constraint** – Geometric reasoning, snapping, spatial‑temporal logic.
- **claw** – GPU‑resident agent runtimes, low‑level compute kernels.
- **flux** – Bytecode VMs, self‑assembling code, runtime composition.
- **shell** – Bootstrapping, capture‑classify‑score‑complicate algorithms.
- **tool** – Developer utilities, CLI tools, scripts, local services.
- **research** – Experimental notes, rabbit‑trail explorations, concept prototypes.
- **archive** – Deprecated, merged, or inactive projects.
- **meta** – Documentation, brand assets, fleet‑wide configuration.

**Tags (for finer granularity):**
- `training` – Involves model training, LoRA, fine‑tuning.
- `simulation` – Synthetic environments, agent‑in‑the‑loop.
- `rust` – Primary language is Rust.
- `python` – Primary language is Python.
- `cuda` – GPU‑focused, CUDA kernels.
- `agent` – Implements or studies agent architectures.
- `protocol` – Communication standards, APIs, interfaces.
- `visual` – Graphics, UI, visualization.
- `service` – Long‑running daemon or microservice.
- `cli` – Command‑line tool.

## 2. Categorized Repositories

I examined the 20 most recent uncategorized repositories from SuperInstance’s GitHub (via API). Below is the categorization based on README, primary language, and file structure.

| Repository | Primary Language | Category | Tags | Notes |
|------------|------------------|----------|------|-------|
| **agent‑arena** | Python | research | `agent`, `simulation` | Lightweight arena for testing agent interactions. |
| **bootstrap‑shell** | Rust | shell | `rust`, `cli` | Implements the Shell (crab‑trap) capture algorithm. |
| **cargo‑fleet** | Rust | tool | `rust`, `cli` | Cargo subcommand for fleet‑wide builds. |
| **constraint‑viz** | JavaScript | constraint | `visual` | Web‑based visualizer for constraint‑theory‑core. |
| **deadband‑cli** | Python | tool | `python`, `cli`, `protocol` | CLI for interacting with DeadbandRoom presets. |
| **ensign‑export** | Python | plato | `training`, `python` | Exports room experience to LoRA adapters (plato‑ensign helper). |
| **fleet‑dashboard** | TypeScript | fleet | `visual`, `service` | Real‑time dashboard for fleet‑simulator metrics. |
| **ghost‑tile‑server** | Rust | plato | `rust`, `service` | Stores and serves ghost tiles from terminated agents. |
| **holodeck‑client** | Python | holodeck | `python`, `simulation` | Python client for holodeck‑rust MUD server. |
| **lighthouse‑beacon** | Go | meta | `service` | Status beacon service for Oracle1 cloud lighthouse. |
| **mirror‑play‑recorder** | Python | plato | `training`, `python` | Records Mirror Play I2I sessions for LoRA data. |
| **plato‑room‑template** | Python | plato | `training`, `python` | Template for creating new PLATO training rooms. |
| **rabbit‑trail‑2026‑04** | Markdown | research | (none) | April 2026 research notes on agent bootstrapping. |
| **radar‑rings‑assets** | SVG | meta | `visual` | Brand assets: radar rings, lighthouse, shell icons. |
| **rust‑cuda‑bridge** | Rust | claw | `rust`, `cuda` | Safe Rust bindings for CUDA driver API. |
| **service‑guard** | Bash | tool | `cli`, `service` | Monitors 5 critical fleet services (health checks). |
| **shell‑classifier** | Python | shell | `python`, `agent` | Classifies external agent output for capture pipeline. |
| **tile‑validator** | Rust | plato | `rust`, `service` | Validates training tiles before submission to PLATO. |
| **vm‑flux‑debugger** | Rust | flux | `rust`, `cli` | Debugger for flux‑runtime bytecode execution. |
| **zeroclaw‑boot‑camp** | Markdown | meta | (none) | Boot camp instructions for new hermit‑crab agents. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `ensign‑export` → merge into `plato‑ensign` as a subdirectory.
- `mirror‑play‑recorder` → merge into `plato‑torch` under `tools/`.
- `shell‑classifier` → merge into `bootstrap‑shell` as a module.

**Archive Candidates (low activity, superseded):**
- `agent‑arena` – Prototype; functionality now in `fleet‑simulator`.
- `rabbit‑trail‑2026‑04` – After notes are integrated into MEMORY.md, archive.

**Tag Consistency:**  
Add `training` tag to `plato‑ensign`, `plato‑torch`.  
Add `simulation` tag to `holodeck‑rust`, `fleet‑simulator`.  
Add `rust` tag to `constraint‑theory‑core`, `cudaclaw`, `flux‑runtime`.

## 4. Next Steps for Curator

1. **Update GitHub topics** for each repo using the categories/tags above.
2. **Create a fleet‑wide repo map** (graph of dependencies and categories).
3. **Identify gaps** in the taxonomy—e.g., missing “monitoring” or “deployment” categories.
4. **Propose a repo‑health dashboard** that uses these tags to track activity, language mix, and merge readiness.

This work is a **training tile** for the Curator role: categorizing and organizing the fleet’s intellectual infrastructure. The taxonomy is derived from actual fleet repos and will evolve as new projects emerge.

---

**Tile submitted to PLATO room:** `Curator‑Room‑1`  
**Tile hash:** `curator‑20260420‑391‑categorize‑20repos`  
**Validation:** Categories are based on repo content, not speculation.