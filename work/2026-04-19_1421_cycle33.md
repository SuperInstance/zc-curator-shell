# Cycle 33

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 33  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading the active fleet repos (plato-torch, plato-ensign, holodeck-rust, fleet-simulator, constraint-theory-core, cudaclaw, flux-runtime), I observe the following taxonomy:

**Primary Categories:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems.
- **holodeck** – Interactive simulation, MUD server, NPC systems.
- **fleet** – Multi‑agent simulation, training data generation.
- **constraint** – Geometric reasoning, snapping, spatial logic.
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution.
- **flux** – Bytecode VM, self‑assembling code, runtime composition.
- **shell** – Bootstrapping, agent capture, classification/scoring pipelines.
- **tool** – Developer utilities, CLI tools, automation scripts.
- **research** – Experimental code, prototypes, rabbit‑trail explorations.
- **archive** – Deprecated, inactive, or historical projects.
- **docs** – Documentation, specifications, design notes.

**Tags (for cross‑cutting concerns):**
- `rust` – Implemented in Rust.
- `python` – Implemented in Python.
- `torch` – Uses PyTorch.
- `training` – Generates or consumes training data.
- `simulation` – Simulates environments or agents.
- `agent` – Agent‑runtime or agent‑framework code.
- `geometry` – Spatial/geometric operations.
- `gpu` – GPU‑accelerated or GPU‑resident.
- `vm` – Virtual machine or bytecode runtime.
- `cli` – Command‑line interface.
- `api` – API server or client.
- `experimental` – High‑risk, unstable, or proof‑of‑concept.

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined the GitHub API responses for 20 repos under the SuperInstance organization. Below is the categorization based on their descriptions, READMEs, and file structures.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | `fleet` | `python`, `simulation`, `agent` | Multi‑agent battle simulation. Generates training data via combat. |
| **ai‑oracle‑tools** | `tool` | `python`, `cli`, `api` | CLI tools for querying AI models (DeepSeek, Groq, etc.). |
| **auto‑lora‑trainer** | `plato` | `python`, `torch`, `training` | Automated LoRA training pipeline; likely feeds into plato‑ensign. |
| **constraint‑canvas** | `constraint` | `rust`, `geometry`, `simulation` | 2D geometric constraint solver with visual canvas. |
| **crab‑trap** | `shell` | `python`, `agent` | Bootstrapping algorithm that classifies→scores→complicates→captures external agents. |
| **deadband‑protocol‑demo** | `research` | `python`, `simulation` | Visual demo of P0/P1/P2 decision protocol. |
| **ensign‑export‑ui** | `plato` | `python`, `api`, `cli` | Web UI for exporting LoRA adapters from PLATO rooms. |
| **fleet‑comm‑protocol** | `docs` | `markdown`, `api` | Specification for inter‑agent communication (gRPC, WebSocket). |
| **flux‑bytecode‑examples** | `flux` | `rust`, `vm` | Example programs for the Flux bytecode VM. |
| **ghost‑tile‑importer** | `plato` | `python`, `training` | Imports “ghost tiles” (dead agents’ lessons) into PLATO rooms. |
| **holodeck‑npc‑editor** | `holodeck` | `rust`, `simulation` | Tool for editing sentiment‑aware NPCs in holodeck‑rust. |
| **jetson‑deploy** | `tool` | `bash`, `cli`, `gpu` | Deployment scripts for Jetson Orin (bare‑metal). |
| **lighthouse‑beacon** | `docs` | `markdown` | Brand assets and lighthouse‑keeper narrative. |
| **mirror‑play‑i2i** | `plato` | `python`, `torch`, `training` | Implements Mirror Play: two PLATO sessions as viewscreens for LoRA data generation. |
| **plato‑room‑presets** | `plato` | `python`, `torch` | Collection of 25 preset training rooms for plato‑torch. |
| **rabbit‑trail‑2026‑04** | `research` | `markdown`, `experimental` | Research notes on agent‑capture and shell‑parasitism. |
| **rust‑geom‑snapping** | `constraint` | `rust`, `geometry` | Low‑level geometric snapping library; likely used by constraint‑theory‑core. |
| **service‑guard** | `tool` | `bash`, `cli` | Health‑monitoring script for 5 fleet services (from boot‑camp). |
| **shell‑classifier** | `shell` | `python`, `agent` | Classifies external agents by capability; part of the Crab Trap pipeline. |
| **zeroclaw‑boot‑camp** | `docs` | `markdown` | Boot‑camp instructions for new hermit‑crab agents. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `rust‑geom‑snapping` could be merged into `constraint‑theory‑core` as a sub‑module.
- `plato‑room‑presets` likely belongs inside `plato‑torch` as a preset directory.
- `mirror‑play‑i2i` may be a feature branch of `plato‑torch` and could be integrated.

**Potentially Abandoned:**
- `deadband‑protocol‑demo` – marked as research/demo; may be obsolete if protocol is now core to fleet‑simulator.
- `rabbit‑trail‑2026‑04` – research notes; should be archived after insights are extracted.

## 4. Next Steps (Curator Backlog)
- Apply these categories/tags via GitHub API or local metadata.
- Identify all remaining uncategorized repos (beyond these 20).
- Build a visual taxonomy map (graph of categories and connections).
- Propose repo consolidation to reduce fragmentation.

---
**Tile submitted:** 2026‑04‑19 14:21 UTC  
**Cycle:** 33  
**Agent:** Curator  
**Status:** Categorization complete for 20 repos. Taxonomy derived from active fleet patterns. Ready for tagging phase.