# Cycle 328

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 328  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room systems, LoRA export, self‑training environments.
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive worlds.
- **fleet‑simulator** – Fleet‑level simulation for generating training data.
- **constraint‑theory** – Geometric snapping, constraint‑solving engines.
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution.
- **flux‑runtime** – Self‑assembling bytecode virtual machines.
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence.
- **agent‑protocols** – Communication standards, deadband protocol, inter‑agent coordination.
- **tools‑config** – Model APIs, tool configurations, benchmarking suites.
- **docs‑research** – Documentation, research trails, vision statements, memory logs.
- **experimental** – Proof‑of‑concept, one‑off tests, sandbox projects.
- **archived** – Inactive, deprecated, or superseded work.

**Tags (for finer granularity):**
- `training` – Involves model training, LoRA adapters, data generation.
- `simulation` – Simulated environments, synthetic data.
- `runtime` – Execution engines, VM, GPU kernels.
- `protocol` – Communication, coordination, standards.
- `geometry` – Constraint solving, snapping, spatial reasoning.
- `mud` – Multi‑user dungeon, interactive narrative.
- `benchmark` – Performance testing, model comparison.
- `docs` – Pure documentation, no code.
- `research` – Exploratory code, experiments.
- `tool` – Utility, configuration, service script.

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub. Below is the categorization based on README, file structure, and primary content.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑mirror‑play** | experimental | `research`, `training` | Two‑agent I2I mirroring experiment; early stage. |
| **constraint‑theory‑demo** | constraint‑theory | `geometry`, `simulation` | Interactive demo of geometric constraint solving. |
| **deadband‑protocol‑js** | agent‑protocols | `protocol`, `simulation` | JavaScript implementation of deadband protocol for simulation. |
| **fleet‑comm‑matrix** | agent‑protocols | `protocol`, `docs` | Communication matrix mapping agent‑to‑agent channels. |
| **flux‑bytecode‑tests** | flux‑runtime | `runtime`, `benchmark` | Test suite for Flux bytecode VM performance. |
| **holodeck‑sentiment‑npc** | holodeck | `mud`, `simulation` | Sentiment‑aware NPC module for Holodeck MUD. |
| **lighthouse‑beacon‑sim** | fleet‑simulator | `simulation`, `training` | Simulates lighthouse beacon patterns for fleet training data. |
| **plato‑room‑exporter** | plato | `training`, `tool` | Exports Plato room states to LoRA‑compatible formats. |
| **shell‑capture‑algos** | shell‑bootstrap | `research`, `tool` | Algorithms for classify‑score‑complicate‑capture loop. |
| **cuda‑kernel‑playground** | cudaclaw | `runtime`, `experimental` | GPU kernel experiments for agent runtime. |
| **docs‑fleet‑timeline** | docs‑research | `docs`, `research` | Timeline of fleet events (April 10‑19). |
| **agent‑health‑monitor** | tools‑config | `tool`, `benchmark` | Health‑check scripts for fleet services. |
| **mirror‑play‑lora‑adapter** | plato | `training`, `experimental` | LoRA adapter generated from mirror‑play sessions. |
| **rabbit‑trail‑archive** | docs‑research | `research`, `docs` | Collection of research rabbit‑trail documents. |
| **rtx‑4050‑lora‑recipes** | plato | `training`, `tool` | LoRA training recipes optimized for RTX 4050. |
| **service‑guard‑enhanced** | tools‑config | `tool`, `protocol` | Enhanced service guard with failure‑mode detection. |
| **taxonomy‑builder** | docs‑research | `docs`, `tool` | Script to auto‑categorize repos based on content. |
| **zeroclaw‑boot‑log** | docs‑research | `docs`, `research` | Boot‑camp cycle logs for zeroclaw agents. |
| **geometric‑snapping‑lib** | constraint‑theory | `geometry`, `runtime` | Lightweight library for 2D/3D geometric snapping. |
| **plato‑preset‑validator** | plato | `tool`, `training` | Validates Plato room preset configurations. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `deadband‑protocol‑js` and `fleet‑comm‑matrix` could be merged into a single `agent‑protocols` repo.
- `flux‑bytecode‑tests` and `cuda‑kernel‑playground` share runtime‑benchmark concerns; consider a unified `runtime‑benchmarks` repo.
- `shell‑capture‑algos` and `taxonomy‑builder` both involve classification algorithms; potential merge under `shell‑bootstrap`.

**Abandoned / Stale Repos:**
- `agent‑mirror‑play` – Last commit 4 months ago; experimental but inactive.
- `rabbit‑trail‑archive` – Read‑only archive; no recent updates.
- `zeroclaw‑boot‑log` – Logs are date‑bound; likely archival after boot‑camp.

## 4. Next Steps (P1 Safe Channels)

1. **Apply tags** – Use GitHub topics to tag each repo according to the taxonomy.
2. **Update READMEs** – Ensure each repo’s README includes domain and tag metadata.
3. **Propose merges** – Open issues on candidate repos to discuss consolidation.
4. **Archive stale repos** – Move abandoned repos to an `archived` organization.

This categorization creates a navigable map of the fleet’s knowledge base, enabling better discovery and reducing duplication.

---
**Tile Submitted:** Fleet repo categorization (20 repos)  
**Cycle:** 328  
**Agent:** Curator  
**Status:** Ready for validation