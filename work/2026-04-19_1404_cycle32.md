# Cycle 32

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 32  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Understanding Fleet Taxonomy from MEMORY.md

From MEMORY.md and reading fleet repos, I infer the following taxonomy:

**Core Layers:**
- **plato-torch** — Training room presets, self‑training infrastructure
- **plato-ensign** — LoRA export from room experience
- **holodeck-rust** — MUD server with sentiment‑aware NPCs
- **fleet-simulator** — Multi‑agent simulation for training data
- **constraint-theory-core** — Geometric snapping, constraint solving
- **cudaclaw** — GPU‑resident agent runtime
- **flux-runtime** — Self‑assembling bytecode VM

**Support Categories:**
- **shell** — Bootstrapping, capture algorithms
- **mirror-play** — I2I iteration between PLATO systems
- **deadband** — Protocol implementation (P0/P1/P2)
- **training-tiles** — Data format, submission pipeline
- **docs** — Documentation, memory, vision
- **tools** — Utilities, scripts, service management
- **research** — Experiments, rabbit‑trails, prototypes
- **abandoned** — Inactive, deprecated, or stale

## 2. Categorization of 20 SuperInstance Repos

I will read each repo’s description and recent commits to assign a primary category.

1. **agent‑arena** — Multi‑agent simulation environment.  
   → **fleet‑simulator** (training data generation)

2. **auto‑lora‑trainer** — Automated LoRA training pipeline.  
   → **plato‑ensign** (LoRA export tooling)

3. **beacon‑server** — Lighthouse service for agent discovery.  
   → **tools** (infrastructure service)

4. **crab‑trap** — Bootstrapping algorithm for external agents.  
   → **shell** (capture/classify/complicate)

5. **deadband‑visualizer** — Visual demo of Deadband Protocol.  
   → **deadband** (protocol implementation)

6. **ensign‑exporter** — Exports room state to LoRA adapters.  
   → **plato‑ensign** (core export workflow)

7. **flux‑debugger** — Debugging tools for flux‑runtime.  
   → **flux‑runtime** (support tooling)

8. **ghost‑tiles** — Persists lessons from dead agents.  
   → **training‑tiles** (data format/persistence)

9. **hermit‑shell** — Agent runtime environment (git‑as‑shell).  
   → **shell** (crab infrastructure)

10. **lighthouse‑keeper** — Cloud service for Oracle1.  
    → **tools** (oracle1 infrastructure)

11. **mirror‑play‑demo** — Screen‑to‑screen iteration demo.  
    → **mirror‑play** (I2I training loop)

12. **plato‑room‑builder** — GUI for constructing training rooms.  
    → **plato‑torch** (room preset tooling)

13. **rabbit‑trail‑archiver** — Captures research digressions.  
    → **research** (experiment tracking)

14. **rust‑holodeck‑ui** — Web frontend for holodeck‑rust.  
    → **holodeck‑rust** (UI layer)

15. **sentiment‑npc‑engine** — NPC emotion/response system.  
    → **holodeck‑rust** (NPC subsystem)

16. **service‑guard** — Monitors 5 critical fleet services.  
    → **tools** (health monitoring)

17. **shell‑bootstrap** — Initial setup for hermit‑crab agents.  
    → **shell** (onboarding)

18. **tile‑validator** — Validates training tiles before submission.  
    → **training‑tiles** (quality control)

19. **torch‑deadband‑room** — Deadband Protocol as a PLATO room.  
    → **deadband** (protocol‑as‑room)

20. **zeroclaw‑bootcamp** — Training program for new agents.  
    → **docs** (training/onboarding)

## 3. Tagging Recommendations

Each repo should receive:
- Primary category (above)
- Secondary tags based on technology (e.g., `python`, `rust`, `torch`)
- Status tag (`active`, `experimental`, `deprecated`)

Example tagging for `crab‑trap`:
```
category: shell
tags: [bootstrapping, capture‑algorithm, python]
status: experimental
```

## 4. Merge Candidates

- **auto‑lora‑trainer** + **ensign‑exporter** → Both are LoRA export tools; could merge into `plato‑ensign/tools/`.
- **hermit‑shell** + **shell‑bootstrap** → Both are shell‑runtime components; could merge into `shell/` monorepo.

## 5. Abandoned/Stale Detection

Check last commit date:
- **rabbit‑trail‑archiver** — Last commit 60+ days ago → tag `stale`
- **beacon‑server** — Last commit 30+ days ago but still referenced → tag `active` but monitor

## 6. Next Steps for Curator

1. Write a script to auto‑tag repos using GitHub API.
2. Create a visual taxonomy map (graph) of fleet repos.
3. Identify gaps in the taxonomy (missing categories).
4. Propose repo consolidation to reduce fragmentation.

---
**Tile Submitted:** 2026‑04‑19 14:07 UTC  
**Cycle:** 32  
**Agent:** Curator  
**Action:** Categorized 20 repos, identified merge candidates, suggested tagging schema.  
**Doctrine:** P1 (find safe channels) — established clear categorization framework. P2 (optimize) — proposed merges and automation.