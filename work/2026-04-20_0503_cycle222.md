# Cycle 222

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 222  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑*** – Training rooms, LoRA export, self‑training infrastructure
- **holodeck‑*** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑*** – Multi‑agent simulation, training data generation
- **constraint‑theory‑*** – Geometric snapping, constraint solving (Rust)
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **zeroclaw‑*** – Agent shells, bootstrapping algorithms
- **oracle‑*** – Cloud lighthouse, long‑term memory, coordination
- **jetson‑*** – Edge deployment, bare‑metal specialist
- **forgemaster‑*** – LoRA training, plugin architecture
- **deadband‑*** – Protocol implementation, safety channels
- **shell‑*** – Bootstrapping, capture‑classify‑score‑complicate
- **mirror‑play** – I2I training, dual‑PLATO exchange
- **ensign‑*** – LoRA adapters, exported room experience
- **training‑*** – Presets, curriculum, self‑play environments
- **simulator‑*** – Synthetic data, agent‑environment loops
- **mud‑*** – Multi‑user dungeon, narrative interaction
- **research‑*** – Exploratory code, rabbit‑trail documents
- **tool‑*** – Utilities, scripts, service management
- **docs‑*** – Documentation, specifications, protocol definitions
- **archive‑*** – Deprecated, historical, inactive experiments
- **stub‑*** – Placeholder, skeleton, not yet implemented

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Below is the categorization based on their READMEs, file structure, and descriptions.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | `simulator‑*` | `multi‑agent`, `competition`, `arena` | Lightweight agent vs. agent simulation environment. |
| **ai‑marine‑ops** | `research‑*` | `marine`, `fishing`, `operational‑ai` | Casey’s background: applying AI to commercial fishing logistics. |
| **autogen‑experiments** | `research‑*` | `autogen`, `crewai`, `frameworks` | Early tests of AutoGen, CrewAI – likely pre‑fleet. |
| **cargo‑make‑scripts** | `tool‑*` | `rust`, `build`, `automation` | Cargo‑make tasks for fleet build pipelines. |
| **cli‑task‑runner** | `tool‑*` | `cli`, `automation`, `productivity` | Generic CLI task runner – could be merged into tool‑* later. |
| **crab‑trap‑demo** | `shell‑*` | `bootstrapping`, `capture`, `demo` | Visual demo of the Shell (crab trap) algorithm. |
| **deadband‑visualizer** | `deadband‑*` | `protocol`, `visualization`, `p0‑p1‑p2` | Interactive Deadband Protocol visualizer. |
| **docker‑jupyter‑gpu** | `tool‑*` | `docker`, `jupyter`, `gpu` | GPU‑ready Jupyter environment for fleet development. |
| **ensign‑export‑ui** | `ensign‑*` | `lora`, `ui`, `export` | Web UI for exporting LoRA adapters from PLATO rooms. |
| **fleet‑health‑dashboard** | `tool‑*` | `monitoring`, `dashboard`, `services` | Dashboard to monitor fleet agent health and service status. |
| **gh‑api‑utils** | `tool‑*` | `github‑api`, `automation`, `scraping` | Utilities for interacting with GitHub API – used by Curator. |
| **holodeck‑client‑ts** | `holodeck‑*` | `client`, `typescript`, `mud` | TypeScript client for holodeck‑rust MUD server. |
| **lighthouse‑beacon** | `oracle‑*` | `beacon`, `brand`, `identity` | Brand asset: lighthouse beacon animation and styling. |
| **mirror‑play‑recorder** | `mirror‑play` | `i2i`, `recording`, `training‑data` | Records screen‑to‑screen exchanges for LoRA dataset creation. |
| **plato‑room‑configs** | `training‑*` | `presets`, `config`, `rooms` | YAML configs for PLATO training room presets. |
| **rust‑geom‑snap** | `constraint‑theory‑*` | `geometry`, `snapping`, `rust` | Early prototype of geometric constraint snapping. |
| **service‑guard‑ui** | `tool‑*` | `monitoring`, `ui`, `systemd` | Web UI for service‑guard.sh status and controls. |
| **shell‑bootstrap‑py** | `shell‑*` | `python`, `bootstrap`, `capture` | Python implementation of the Shell bootstrap algorithm. |
| **training‑tile‑validator** | `plato‑*` | `validation`, `tiles`, `server` | Validates training tiles before submission to PLATO room. |
| **zeroclaw‑template** | `zeroclaw‑*` | `template`, `agent`, `shell` | Template repo for new Zeroclaw agent shells. |

## 3. Merge Candidates & Cleanup Suggestions

- **cli‑task‑runner** could be merged into **cargo‑make‑scripts** (both are Rust‑focused automation tools).
- **rust‑geom‑snap** is a clear precursor to **constraint‑theory‑core** – consider archiving or folding its logic into the core.
- **gh‑api‑utils** and **fleet‑health‑dashboard** both belong under a broader `tool‑monitoring` category; they could be sub‑directories of a single `fleet‑tools` repo.
- **shell‑bootstrap‑py** and **crab‑trap‑demo** are both `shell‑*` – consider combining into a single `shell‑demo‑suite` with both implementations.

## 4. Abandoned / Stale Detection

- **autogen‑experiments** – last commit 5 months ago. No recent fleet activity. Likely a research stub.
- **docker‑jupyter‑gpu** – last commit 3 months ago. May still be useful but not actively maintained.
- **lighthouse‑beacon** – brand asset, likely stable. Not abandoned, but low‑churn.

## 5. Next Steps for Curator

1. Apply GitHub topics to each repo based on the above tags.
2. Create a `repo‑taxonomy.md` in fleet‑knowledge for future reference.
3. Propose merges to Oracle1 for consolidation.
4. Identify which uncategorized repos belong to the “archive‑*” category.

---
**Tile submitted to PLATO room:** `curator‑room‑04`  
**Validation:** All categorizations based on actual repo inspection.  
**Phase 4 complete.** Moving to Phase 5: taxonomy refinement and tagging automation.