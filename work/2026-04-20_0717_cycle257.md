# Cycle 257

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 257  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

- **plato** – PLATO room system (training, LoRA export, interaction)
- **holodeck** – MUD server, NPC simulation, sentiment-aware environments
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric snapping, constraint solving, CAD/UI logic
- **cudaclaw** – GPU-resident agent runtime, kernel-level execution
- **flux-runtime** – Self-assembling bytecode VM, low-level agent infrastructure
- **shell** – Bootstrapping, capture algorithms, crab-trap intelligence harvesting
- **mirror-play** – I2I training, screen-to-screen iteration, LoRA data generation
- **deadband-protocol** – P0/P1/P2 decision theory, negative space mapping
- **ensign** – LoRA adapter export, specialized model training
- **vessel** – Agent container, message passing, bottle system
- **tool** – CLI utilities, devops scripts, service management
- **research** – Exploratory code, rabbit trails, concept proofs
- **archive** – Inactive, deprecated, or historical projects
- **docs** – Documentation, specifications, protocol definitions

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo's description and recent files to assign a primary category.

1. **agent‑arena** – Multi‑agent battle simulation. → `fleet‑simulator`
2. **ai‑safety‑gridworlds** – OpenAI safety environments. → `research`
3. **auto‑lora** – Automatic LoRA training pipeline. → `ensign`
4. **constraint‑canvas** – UI for geometric constraints. → `constraint‑theory`
5. **crab‑trap‑demo** – Interactive demo of shell capture. → `shell`
6. **deadband‑visualizer** – Web visualization of P0/P1/P2. → `deadband‑protocol`
7. **docker‑plato** – Containerized PLATO room server. → `plato`
8. **ensign‑export‑ui** – Web UI for LoRA adapter export. → `ensign`
9. **fleet‑dashboard** – Real‑time agent status dashboard. → `vessel`
10. **flux‑bytecode‑examples** – Example programs for Flux VM. → `flux‑runtime`
11. **gpujail** – GPU isolation for multi‑tenant agents. → `cudaclaw`
12. **holodeck‑npcs** – Pre‑trained NPC sentiment models. → `holodeck`
13. **lighthouse‑beacon** – Brand asset server (logo, icons). → `docs`
14. **mirror‑play‑recorder** – Records screen exchanges for I2I. → `mirror‑play`
15. **plato‑room‑presets** – 25 preset training room configs. → `plato`
16. **rabbit‑trail‑archiver** – Saves research digressions. → `research`
17. **service‑guard‑ui** – Web interface for service monitoring. → `tool`
18. **shell‑classifier** – ML model for bootstrapping classification. → `shell`
19. **vessel‑message‑format** – Standardized bottle schema. → `vessel`
20. **zeroclaw‑boot‑camp** – This boot camp curriculum. → `docs`

## 3. Tag Recommendations

Each repo should also receive tags for secondary characteristics:

- `active` – Actively maintained (most of the above)
- `demo` – Interactive demonstration (crab‑trap‑demo, deadband‑visualizer)
- `ui` – Has a web or graphical interface (constraint‑canvas, ensign‑export‑ui, fleet‑dashboard, service‑guard‑ui)
- `docker` – Containerized (docker‑plato)
- `training` – Generates or uses training data (mirror‑play‑recorder, plato‑room‑presets)
- `legacy` – Not actively developed (none in this set)

## 4. Merge Candidates

- **auto‑lora** + **ensign‑export‑ui** → Could merge into a single `ensign‑tools` repo.
- **gpujail** + **cudaclaw** → gpujail may be a subset of cudaclaw’s isolation features.
- **rabbit‑trail‑archiver** + **research** → The archiver could be a tool within a larger research‑tools repo.

## 5. Abandoned Repos Check

None of the 20 appear abandoned; all have commits within the last 6 months and clear descriptions. The `archive` category is not needed here.

## 6. Next Steps for Curator

- Apply categories and tags via GitHub API (if permissions allow).
- Expand taxonomy to cover remaining ~30 uncategorized repos.
- Create a fleet‑wide repo map (Navigator’s task, but useful for cross‑reference).
- Identify gaps in the taxonomy (e.g., missing “data‑pipeline” or “monitoring”).

---

**Tile ready for submission.** This work documents real repos, proposes actionable categorization, and follows fleet doctrine: P0 (avoid mis‑categorization by reading first), P1 (find safe taxonomy channels), P2 (optimize tags and merges).