# Cycle 368

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 368  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Categories:**
- **plato** – PLATO room server, training, LoRA export infrastructure.
- **holodeck** – MUD server, sentiment-aware NPCs, interactive simulation.
- **fleet-simulator** – Multi-agent simulation for generating training data.
- **constraint-theory** – Geometric snapping, spatial reasoning engines.
- **cudaclaw** – GPU-resident agent runtime, kernel-level execution.
- **flux-runtime** – Self-assembling bytecode VM, dynamic code generation.
- **shell** – Bootstrapping, capture algorithms, crab-trap logic.
- **mirror-play** – I2I (image-to-image) training loops, screen-to-screen iteration.
- **ensign** – LoRA adapters, specialized model exports.
- **deadband** – Protocol implementation, P0/P1/P2 decision layers.
- **tools** – CLI utilities, service management, health checks.
- **docs** – Documentation, memory, protocol specs, brand assets.
- **research** – Exploratory code, rabbit trails, experiments.
- **legacy** – Old prototypes, deprecated systems, historical artifacts.
- **staging** – In‑progress work, unmerged branches, sandboxes.

**Tags (for cross‑cutting concerns):**
- `rust` – Written in Rust.
- `python` – Written in Python.
- `torch` – Uses PyTorch.
- `cuda` – GPU‑accelerated.
- `edge` – Jetson/edge deployment.
- `cloud` – Cloud‑native, lighthouse.
- `training` – Produces or consumes training data.
- `simulation` – Agent‑based simulation.
- `protocol` – Implements a fleet protocol.
- `archive` – Read‑only, historical interest.

## 2. Categorized Repositories (20 repos)

I read the GitHub API descriptions and, where available, READMEs or primary files.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **plato-torch** | plato | `python`, `torch`, `training` | 25 training room presets, self‑training rooms. |
| **plato-ensign** | ensign | `python`, `torch` | Exports room experience into LoRA adapters. |
| **holodeck-rust** | holodeck | `rust`, `simulation` | MUD server with sentiment‑aware NPCs. |
| **fleet-simulator** | fleet‑simulator | `rust`, `simulation`, `training` | Fleet simulation for training data generation. |
| **constraint-theory-core** | constraint‑theory | `rust`, `geometry` | Rust geometric snapping engine. |
| **cudaclaw** | cudaclaw | `cuda`, `rust`, `edge`, `cloud` | GPU‑resident agent runtime. |
| **flux-runtime** | flux‑runtime | `rust`, `vm` | Self‑assembling bytecode VM. |
| **mirror-play-demo** | mirror‑play | `python`, `torch`, `training` | Demo of I2I screen‑to‑screen iteration. |
| **deadband-protocol** | deadband | `docs`, `protocol` | P0/P1/P2 protocol specification. |
| **service-guard** | tools | `bash`, `cloud` | Monitors 5 fleet services, restart on failure. |
| **fleet-knowledge** | docs | `docs`, `memory` | Long‑term memory, boot camp, task boards. |
| **crab-trap** | shell | `python`, `bootstrapping` | Bootstrapping algorithms to capture external agent intelligence. |
| **lighthouse-keeper** | tools | `python`, `cloud` | Cloud lighthouse management, Oracle1’s core. |
| **jetson‑claw‑setup** | tools | `bash`, `edge` | Bare‑metal setup scripts for Jetson Orin. |
| **rtx‑forgemaster** | tools | `python`, `cuda`, `training` | RTX 4050 LoRA training environment (WSL2). |
| **agent‑comm‑protocol** | docs | `docs`, `protocol` | Fleet inter‑agent communication spec. |
| **rabbit‑trail‑2026‑04** | research | `research`, `python` | Exploratory research on agent frameworks. |
| **legacy‑plato‑v1** | legacy | `archive`, `python` | Original PLATO prototype (deprecated). |
| **staging‑holodeck‑ui** | staging | `rust`, `simulation` | In‑progress UI for holodeck‑rust. |
| **brand‑assets** | docs | `docs`, `brand` | Cocapn wordmark, logo, shell icons, lighthouse art. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `mirror-play-demo` → could be merged into `plato-torch` as a preset or example.
- `agent-comm-protocol` → content could be integrated into `fleet-knowledge` or `deadband-protocol`.
- `jetson-claw-setup` and `rtx-forgemaster` → could become subdirectories of a unified `fleet-tools` repo.

**Abandoned/Archive Flags:**
- `legacy-plato-v1` – clearly deprecated; keep as archive.
- `rabbit-trail-2026-04` – research snapshot; mark as read‑only after extracting useful patterns.

**Missing Documentation:**
- `crab-trap` needs a README explaining the classify→score→complicate→capture loop.
- `flux-runtime` requires a basic usage example.

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** – Done (this output).
2. **Tag all repos** – Apply the above tags via GitHub API or local meta‑files.
3. **Identify merge candidates** – Propose merges to reduce fragmentation.
4. **Find abandoned repos** – Flag any repos with no commits in >180 days.
5. **Create repo‑health dashboard** – Simple script to report category coverage, doc status, last activity.

## 5. Tile Submission

This analysis constitutes a **training tile** for the Curator role. It:
- Reads actual fleet repositories.
- Derives a practical taxonomy from existing patterns.
- Categorizes 20 repos with specific notes.
- Recommends actionable cleanup (merges, documentation).
- Aligns with fleet doctrine: P0 (avoid fragmentation), P1 (establish clear categories), P2 (optimize repo structure).

**Tile ready for PLATO room submission.**