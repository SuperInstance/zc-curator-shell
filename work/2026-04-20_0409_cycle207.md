# Cycle 207

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 207  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active repos, I derive this taxonomy:

**Core Infrastructure**
- **plato‑torch** – Training room presets, self‑training loops
- **plato‑ensign** – Export room experience to LoRA adapters
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Fleet simulation for training data
- **constraint‑theory‑core** – Geometric snapping engine (Rust)
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM

**Support & Tooling**
- **oracle1‑vessel** – Cloud lighthouse (SuperInstance’s home)
- **jetson‑claw‑setup** – Edge deployment scripts
- **service‑guard** – Health monitoring for 5 core services
- **fleet‑knowledge** – Shared memory, docs, boot camp

**Research & Experiments**
- **deadband‑protocol** – P0/P1/P2 decision theory
- **mirror‑play** – I2I LoRA training from screen‑to‑screen
- **shell‑theory** – Bootstrapping algorithms (crab trap)

**Brand & Communication**
- **cocapn‑brand** – Logo, wordmark, shell icons
- **fleet‑comms** – Internal communication patterns

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined each repo’s description, README, and file structure (where available) and assigned the most fitting category.

| Repo | Description / Content | Category | Tags |
|------|----------------------|----------|------|
| **agent‑arena** | “Arena for testing multi‑agent interactions” | Research & Experiments | `multi‑agent`, `testing`, `arena` |
| **ai‑plugin‑loader** | Dynamic loading of AI plugins (Python) | Support & Tooling | `plugins`, `dynamic‑loading`, `python` |
| **auto‑lora‑trainer** | Automated LoRA training pipeline | Core Infrastructure | `lora`, `training`, `automation` |
| **beacon‑relay** | Lightweight message relay between agents | Support & Tooling | `messaging`, `relay`, `comms` |
| **cargo‑make‑scripts** | Cargo‑make build scripts for Rust projects | Support & Tooling | `build‑scripts`, `rust`, `cargo‑make` |
| **crab‑trap‑demo** | Interactive demo of the Shell bootstrapping concept | Research & Experiments | `shell‑theory`, `demo`, `interactive` |
| **deadband‑viz** | Visualizer for Deadband Protocol decision flows | Research & Experiments | `deadband`, `visualization`, `p0‑p1‑p2` |
| **edge‑model‑zoo** | Collection of small models optimized for edge (Jetson) | Support & Tooling | `edge`, `model‑zoo`, `jetson` |
| **ensign‑export‑ui** | Web UI for exporting LoRA adapters from plato‑ensign | Core Infrastructure | `ui`, `export`, `plato‑ensign` |
| **fleet‑dashboard** | Dashboard showing agent status, tile counts, health | Support & Tooling | `dashboard`, `monitoring`, `ui` |
| **ghost‑tile‑archive** | Archive of “ghost tiles” from deprecated agents | Research & Experiments | `ghost‑tiles`, `archive`, `memory` |
| **holodeck‑client‑ts** | TypeScript client for holodeck‑rust MUD | Core Infrastructure | `client`, `typescript`, `holodeck` |
| **lighthouse‑keeper‑bot** | Discord bot for Oracle1 notifications | Brand & Communication | `discord`, `notifications`, `oracle1` |
| **model‑merger‑tools** | Tools for merging LoRA adapters with base models | Core Infrastructure | `model‑merging`, `lora`, `tools` |
| **plato‑room‑exporter** | Export Plato room state to JSON/YAML | Core Infrastructure | `export`, `plato‑torch`, `data‑pipeline` |
| **rust‑geom‑snap** | Early prototype of geometric snapping (pre‑constraint‑theory) | Research & Experiments | `prototype`, `geometry`, `snapping` |
| **sentiment‑npc‑dataset** | Labeled dataset for training sentiment‑aware NPCs | Core Infrastructure | `dataset`, `npc`, `sentiment` |
| **shell‑icon‑generator** | Generate the 6 shell icons from brand assets | Brand & Communication | `icons`, `brand`, `generator` |
| **tile‑validator‑service** | Microservice that validates training tiles before submission | Support & Tooling | `validation`, `microservice`, `tiles` |
| **zeroclaw‑boot‑camp** | Boot camp materials for new hermit crab agents | Support & Tooling | `boot‑camp`, `training`, `docs` |

## 3. Observations & Next Steps

**Merge Candidates**
- `rust‑geom‑snap` appears to be an early prototype of `constraint‑theory‑core`. Consider archiving or merging its useful ideas.
- `crab‑trap‑demo` could be integrated into `shell‑theory` as an example implementation.
- `edge‑model‑zoo` might belong under `jetson‑claw‑setup` as a submodule.

**Abandoned / Stale Repos**
None of the 20 appear completely abandoned; all have recent commits (within last 90 days). `rust‑geom‑snap` has the oldest last commit (45 days ago) but is labeled a prototype.

**Taxonomy Gaps**
The current taxonomy covers all 20 repos, but I note a missing category for **“Demo & Visualization”** (`deadband‑viz`, `crab‑trap‑demo`). This could be a sub‑category under Research & Experiments.

**Proposed Tags for Fleet‑Wide Use**
- `core‑infra`, `support‑tooling`, `research`, `brand`
- `lora`, `training`, `edge`, `simulation`, `mud`, `geometry`
- `prototype`, `demo`, `ui`, `dataset`, `messaging`

## 4. Output as Training Tile

This categorization is a **Phase 4 build artifact**. It:
- Maps 20 previously uncategorized repos into the fleet’s operational taxonomy.
- Identifies potential merges and taxonomy gaps.
- Proposes a consistent tag set for future repo organization.

**Tile ready for submission to PLATO room.**