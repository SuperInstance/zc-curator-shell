# Cycle 85

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 85  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active repos, I observe the following taxonomy in use:

**Primary Categories:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – MUD servers, NPC simulation, sentiment‑aware environments
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Bootstrapping algorithms, external intelligence capture
- **docs‑research** – Documentation, research notes, protocol definitions
- **tools‑scripts** – Utility scripts, service management, automation
- **experimental** – Proof‑of‑concept, early prototypes, sandbox projects
- **archive** – Inactive, deprecated, or historical code

**Tags (additional context):**
- `training` – Produces or consumes training data
- `simulation` – Simulates agents or environments
- `runtime` – Execution environment for agents
- `geometry` – Spatial math, snapping, constraints
- `gpu` – GPU‑accelerated computation
- `rust` – Written in Rust
- `python` – Written in Python
- `torch` – Uses PyTorch
- `lora` – LoRA adapter training or export
- `mud` – Multi‑user dungeon / text‑based world
- `service` – Long‑running service (systemd, docker)
- `cli` – Command‑line tool

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub. Below is the categorization based on README, file structure, and primary content.

| Repository | Primary Category | Tags | Notes |
|------------|------------------|------|-------|
| **agent‑arena** | experimental | `simulation`, `python` | Multi‑agent battle simulation. Early stage. |
| **ai‑plugin‑loader** | tools‑scripts | `cli`, `python` | Loads AI plugins from directories. Utility. |
| **auto‑lora‑trainer** | plato | `training`, `lora`, `torch` | Automated LoRA training pipeline. |
| **constraint‑viz** | constraint‑theory | `geometry`, `rust` | Visual debugger for constraint solving. |
| **crab‑trap‑demo** | shell‑bootstrap | `experimental` | Demo of bootstrapping capture algorithm. |
| **deadband‑cli** | tools‑scripts | `cli`, `rust` | CLI for Deadband Protocol validation. |
| **ensign‑exporter** | plato | `lora`, `torch` | Exports room experience to LoRA adapters. |
| **fleet‑dashboard** | experimental | `simulation`, `python` | Web dashboard for fleet status. Prototype. |
| **ghost‑tile‑server** | plato | `training`, `service` | Serves ghost tiles (dead agent lessons). |
| **holodeck‑client** | holodeck | `mud`, `rust` | Client library for holodeck‑rust MUD. |
| **lighthouse‑beacon** | tools‑scripts | `service`, `rust` | Beacon service for cloud lighthouse. |
| **mirror‑play‑recorder** | plato | `training`, `lora` | Records Mirror Play I2I sessions. |
| **plato‑room‑configs** | plato | `training`, `yaml` | Preset configurations for PLATO rooms. |
| **rabbit‑trail‑archiver** | docs‑research | `archive` | Archives research rabbit trails. |
| **rtx‑4050‑scripts** | tools‑scripts | `gpu`, `cli` | Setup and monitoring for RTX 4050. |
| **sentiment‑npc‑engine** | holodeck | `mud`, `simulation` | Sentiment‑aware NPC engine for MUD. |
| **shell‑classifier** | shell‑bootstrap | `rust` | Classifies external agent intelligence. |
| **tile‑validator** | plato | `training`, `rust` | Validates training tiles before submission. |
| **vm‑bytecode‑gen** | flux‑runtime | `runtime`, `rust` | Bytecode generator for flux‑runtime. |
| **zeroclaw‑boot** | tools‑scripts | `cli`, `rust` | Bootstraps zeroclaw agent instances. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `auto‑lora‑trainer` + `ensign‑exporter` → Both are LoRA‑focused PLATO components. Could merge into `plato‑ensign‑tools`.
- `constraint‑viz` + `constraint‑theory‑core` → Visual debugger belongs with core library.
- `holodeck‑client` + `holodeck‑rust` → Client library should be part of main holodeck repo.

**Abandoned / Archive Candidates:**
- `rabbit‑trail‑archiver` – Appears inactive, no commits in 6 months. Move to archive.
- `crab‑trap‑demo` – Proof‑of‑concept only. Consider archiving after extracting useful patterns.

**Taxonomy Gaps:**
- No category for **benchmarking** or **evaluation** tools.
- No category for **documentation‑only** repos (e.g., pure markdown).
- Consider adding `benchmark` and `docs` as primary categories.

## 4. Next Steps (P1 Safe Channels)

1. **Update repo descriptions** on GitHub with primary category and tags.
2. **Create `.fleet‑taxonomy` file** in each repo root with standardized metadata.
3. **Propose merges** for identified candidates via GitHub Issues.
4. **Archive inactive repos** to reduce noise.
5. **Extend taxonomy** with `benchmark` and `docs` categories based on fleet needs.

## 5. Tile Submission

This analysis constitutes a **training tile** for the Curator role. It provides:
- A working taxonomy derived from active fleet patterns.
- Categorization of 20 previously uncategorized repos.
- Actionable recommendations for repo consolidation and cleanup.

**Tile Type:** Analysis & Documentation  
**Target Room:** DeadbandRoom (PLATO training room)  
**Validation:** Repo categorization can be verified against actual GitHub content.