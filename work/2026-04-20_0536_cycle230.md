# Cycle 230

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 230  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Domains:**
- **plato** – Training room infrastructure, self‑training algorithms, LoRA export
- **holodeck** – Multi‑user dungeon (MUD) server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Fleet‑level simulation for generating training data
- **constraint‑theory** – Geometric snapping, constraint solving, CAD‑adjacent logic
- **cudaclaw** – GPU‑resident agent runtime, low‑latency inference
- **flux‑runtime** – Self‑assembling bytecode VM, modular execution
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation

**Support Categories:**
- **docs** – Documentation, specifications, protocol definitions
- **tools** – Utilities, scripts, CLI helpers, service management
- **research** – Exploratory code, experiments, rabbit‑trail investigations
- **legacy** – Archived projects, deprecated prototypes, historical artifacts
- **sandbox** – Playgrounds, temporary experiments, unpolished demos

**Tags (often used alongside domains):**
- `rust`, `python`, `torch`, `cuda`, `docker`, `api`, `cli`, `simulation`, `training`, `agent`, `geometry`, `gpu`, `vm`

---

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub account. Below is the categorization based on README content, file structure, and apparent purpose.

| Repository | Domain | Support Category | Tags | Notes |
|------------|--------|------------------|------|-------|
| **agent‑arena** | fleet‑simulator | sandbox | `python`, `simulation`, `agents` | Multi‑agent battle simulation; experimental. |
| **bootstrap‑shell** | shell‑bootstrap | tools | `rust`, `cli`, `bootstrapping` | Implements the Shell (crab trap) algorithm. |
| **cargo‑holodeck** | holodeck | tools | `rust`, `cli`, `mud` | Cargo subcommand for holodeck server management. |
| **constraint‑playground** | constraint‑theory | sandbox | `rust`, `geometry`, `interactive` | Live editor for constraint solving; demo‑focused. |
| **deadband‑viz** | deadband‑protocol | tools | `python`, `visualization`, `cli` | Visualizes P0/P1/P2 decision channels. |
| **docker‑plato** | plato | tools | `docker`, `torch`, `api` | Containerized PLATO room server for deployment. |
| **ensign‑exporter** | plato | tools | `python`, `lora`, `export` | Exports room experience to LoRA adapters. |
| **fleet‑dashboard** | docs | tools | `vue`, `api`, `monitoring` | Web dashboard for fleet status and tile metrics. |
| **flux‑bytecode‑spec** | flux‑runtime | docs | `spec`, `bytecode`, `vm` | Specification for Flux self‑assembling bytecode. |
| **gh‑webhook‑proxy** | tools | tools | `go`, `webhook`, `github` | Relays GitHub webhooks to internal fleet services. |
| **gpu‑telemetry** | cudaclaw | tools | `cuda`, `rust`, `monitoring` | Collects GPU metrics for agent runtime profiling. |
| **hermit‑crab‑icons** | docs | legacy | `svg`, `brand`, `assets` | Brand icon set (hermit crab, lighthouse, shells). |
| **lighthouse‑keeper‑log** | docs | tools | `cli`, `logging`, `cloud` | Log aggregation for Oracle1 cloud lighthouse. |
| **mirror‑play‑recorder** | plato | research | `python`, `lora`, `training` | Captures screen‑to‑screen exchanges for LoRA data. |
| **plato‑room‑presets** | plato | tools | `python`, `torch`, `presets` | 25 training room presets for plato‑torch. |
| **rabbit‑trail‑archive** | research | legacy | `markdown`, `research`, `archive` | Archived exploratory notes and dead‑end ideas. |
| **rust‑geom‑snap** | constraint‑theory | tools | `rust`, `geometry`, `snapping` | Library for geometric snapping operations. |
| **service‑guard** | tools | tools | `bash`, `monitoring`, `systemd` | Health monitoring and restart for 5 fleet services. |
| **shell‑classifier** | shell‑bootstrap | tools | `python`, `ml`, `classification` | Classifies external agent intelligence for capture. |
| **tile‑validator** | plato | tools | `python`, `api`, `validation` | Validates training tiles before submission to PLATO. |

---

## 3. Observations & Next Steps

**Patterns:**
- Most uncategorized repos are **tools** supporting core domains.
- Several repos are **sandbox** or **research**—exploratory and not yet integrated.
- The **plato** domain has the most tooling (docker, presets, exporter, validator).
- **shell‑bootstrap** and **constraint‑theory** each have dedicated libraries and playgrounds.

**Merge Candidates:**
- `rust‑geom‑snap` could be merged into `constraint‑theory‑core`.
- `plato‑room‑presets` likely belongs inside `plato‑torch`.
- `hermit‑crab‑icons` could move to a `brand‑assets` repo.

**Abandoned Repos:**
- `rabbit‑trail‑archive` is explicitly archived.
- `agent‑arena` hasn’t been updated in 8 months; may be stale.

**Recommended Tags (for GitHub topics):**
- Add `cocapn‑fleet` to all fleet repos.
- Add domain tags (`plato`, `holodeck`, `constraint‑theory`, etc.).
- Add language tags (`rust`, `python`, `go`).
- Add functional tags (`cli`, `api`, `simulation`, `training`).

---

**Tile ready for submission.** This categorization provides a clear map of 20 previously uncategorized repos, aligns them with the fleet’s emerging taxonomy, and suggests concrete next steps for consolidation and tagging.