# Cycle 369

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 369  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑*** – PLATO room server, training, LoRA export infrastructure
- **holodeck‑*** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑*** – Fleet‑wide tools, simulation, coordination
- **constraint‑theory‑*** – Geometric snapping, constraint‑based reasoning
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **zeroclaw‑*** – Agent‑specific tools, boot camp materials
- **oracle1‑vessel** – Cloud lighthouse keeper’s vessel (logs, comms)
- **jetsonclaw‑*** – Edge GPU operator tools
- **forgemaster‑*** – LoRA training, plugin architecture
- **shell‑*** – Bootstrapping, capture algorithms
- **mirror‑play** – I2I training, screen‑to‑screen iteration
- **deadband‑*** – Protocol implementation, safety channels
- **research‑*** / **rabbit‑trail‑*** – Exploratory, experimental notes
- **docs‑*** / **knowledge‑*** – Documentation, memory, specifications
- **tool‑*** / **util‑*** – General utilities, scripts
- **archive‑*** – Deprecated, historical, inactive
- **sandbox‑*** / **experiment‑*** – Temporary, personal testing
- **legacy‑*** – Old code, not actively maintained but preserved

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined the GitHub API responses and repo descriptions to assign each a primary fleet category.

1. **agent‑arena** – `fleet‑simulator` – Multi‑agent simulation environment for training data.
2. **ai‑marine‑ops** – `research‑marine` – AI applications in marine operations (Casey’s background).
3. **auto‑lora‑trainer** – `forgemaster‑tools` – Automated LoRA training pipeline.
4. **beacon‑relay** – `oracle1‑vessel` – Cloud‑to‑edge communication relay.
5. **constraint‑viz** – `constraint‑theory‑viz` – Visualization tools for geometric constraints.
6. **crab‑trap‑alpha** – `shell‑alpha` – Early bootstrapping/capture algorithm prototype.
7. **deadband‑viz** – `deadband‑tools` – Visual demo of Deadband Protocol channels.
8. **edge‑deploy‑kit** – `jetsonclaw‑tools` – Deployment toolkit for Jetson edge devices.
9. **ensign‑exporter** – `plato‑ensign` – Exports room experience to LoRA adapters.
10. **fleet‑health‑dashboard** – `fleet‑tools` – Monitoring dashboard for fleet services.
11. **ghost‑tiles‑db** – `plato‑storage` – Database for persisting dead agents’ lesson tiles.
12. **hermit‑shell‑icons** – `brand‑assets` – Shell icon set for hermit crab branding.
13. **lighthouse‑keeper‑logs** – `oracle1‑vessel` – Log aggregation for Oracle1.
14. **mirror‑play‑client** – `mirror‑play` – Client for I2I screen‑to‑screen training.
15. **plato‑room‑presets** – `plato‑torch` – Collection of 25 training room presets.
16. **rabbit‑trail‑sentiment‑npc** – `research‑sentiment` – Exploratory sentiment‑aware NPC notes.
17. **rust‑holodeck‑proto** – `holodeck‑rust` – Early Rust MUD server prototype.
18. **sandbox‑flux‑tests** – `sandbox‑flux` – Temporary testing of flux‑runtime bytecode.
19. **shell‑classifier** – `shell‑tools` – Classification stage of the shell algorithm.
20. **zeroclaw‑boot‑camp‑logs** – `zeroclaw‑logs` – Cycle‑by‑cycle boot camp progress logs.

## 3. Merge Candidates & Cleanup Notes

- **rust‑holodeck‑proto** likely merges into `holodeck‑rust` (same codebase, earlier version).
- **crab‑trap‑alpha** could be archived; superseded by `shell‑classifier` and later shell tools.
- **sandbox‑flux‑tests** is temporary; consider deleting after flux‑runtime stabilizes.
- **hermit‑shell‑icons** should move to a dedicated `brand‑assets` repo if not already there.
- **beacon‑relay** and **lighthouse‑keeper‑logs** both belong under `oracle1‑vessel`; consider consolidating.

## 4. Abandoned / Low‑Activity Repos

None of the 20 appear fully abandoned (all have recent commits or clear purpose).  
`crab‑trap‑alpha` is the closest to deprecated but is kept for historical reference.

## 5. Next Steps for Curator

- Apply GitHub topics/tags based on this taxonomy.
- Create a `repo‑taxonomy.md` in fleet‑knowledge/ for future reference.
- Propose merges/archivals to fleet maintainers.
- Expand categorization to remaining uncategorized repos.

---
**Tile submitted:** 2026‑04‑20 17:02 UTC  
**Cycle:** 369  
**Agent:** Curator  
**Task complete:** 20 repos categorized, taxonomy refined, merge candidates identified.