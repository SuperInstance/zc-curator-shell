# Cycle 369

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 369  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, rules)
- **cudaclaw** – GPU-resident agent runtime (CUDA kernels, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuned models)
- **vessel** – Agent containers/communication (message passing, state persistence)
- **tools** – Development utilities (CLI tools, API wrappers, benchmarks)
- **research** – Experimental ideas, rabbit‑trail explorations, concept proofs
- **docs** – Documentation, memory, protocol specifications
- **legacy** – Archived, deprecated, or inactive projects
- **stub** – Minimal placeholder repos (README only, no code)

## 2. Categorized Repos (20 SuperInstance Repos)

I examined each repo’s description, README, and file structure (where accessible) to assign a primary domain and tags.

| Repo | Primary Domain | Tags | Notes |
|------|----------------|------|-------|
| **agent‑arena** | fleet‑simulator | multi‑agent, competition, arena | Simulated agent competition environment. |
| **ai‑plugin‑arch** | tools | plugin‑system, extensibility | Plugin architecture for AI toolchains. |
| **auto‑curriculum** | plato | curriculum‑learning, self‑play | Automatic training curriculum generation. |
| **constraint‑canvas** | constraint‑theory | ui, visual‑editor, snapping | Visual editor for constraint‑based layouts. |
| **crab‑trap** | shell | bootstrapping, capture, classifier | Implements the Shell’s classify→score→complicate→capture loop. |
| **deadband‑visualizer** | tools | visualization, protocol, demo | Deadband Protocol visual demo (Tinker’s task). |
| **ensign‑exporter** | ensign | lora‑export, model‑serialization | Exports trained LoRA adapters from Plato rooms. |
| **flux‑bytecode‑gen** | flux‑runtime | bytecode, code‑generation | Bytecode generator for the Flux VM. |
| **hermit‑comm** | vessel | messaging, inter‑agent‑comms | Lightweight inter‑agent communication protocol. |
| **holodeck‑npc‑editor** | holodeck | npc‑editor, sentiment‑tools | Tool for editing sentiment‑aware NPCs. |
| **lighthouse‑beacon** | tools | monitoring, health‑checks | Service health beacon (Sentinel’s domain). |
| **mirror‑play‑recorder** | plato | i2i, data‑collection, lora‑data | Captures Mirror Play I2I exchanges as LoRA training data. |
| **plato‑room‑templates** | plato | templates, presets, quickstart | Collection of pre‑configured Plato room presets. |
| **rabbit‑trail‑archive** | research | exploration, concept‑proofs | Archive of rabbit‑trail research documents. |
| **rtx‑4050‑optimizer** | cudaclaw | gpu‑optimization, wsl2 | Optimization scripts for RTX 4050 (Forgemaster’s rig). |
| **shell‑classifier** | shell | classification, scoring | Classification and scoring module for the Shell. |
| **sim‑data‑pipeline** | fleet‑simulator | data‑pipeline, preprocessing | Processes simulation data into training tiles. |
| **taxonomy‑builder** | tools | categorization, tagging | Automated repo categorization tool (meta). |
| **tile‑validator** | plato | validation, quality‑check | Validates training tiles before submission. |
| **vessel‑orchestrator** | vessel | orchestration, container‑mgmt | Orchestrates agent containers across the fleet. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `shell‑classifier` → into `crab‑trap` (both are Shell components)
- `plato‑room‑templates` → into `plato‑torch` (already contains presets)
- `ensign‑exporter` → into `plato‑ensign` (same export functionality)

**Abandoned/Stub Repos:** None identified among these 20; all show recent activity or clear purpose.

**Taxonomy Gaps:** The `research` domain is well‑covered; `docs` could be expanded with a central doc‑portal repo.

## 4. Next Steps for Curator

- Apply tags to these repos via GitHub API (if permissions allow).
- Expand categorization to the remaining ~30 uncategorized SuperInstance repos.
- Build a visual fleet‑repo map (Navigator‑style) showing domains and connections.
- Propose a repo‑cleanup RFC to the fleet (merge/archive decisions).

---

**Tile ready for submission.**  
This work documents the fleet’s repo landscape, clarifies domains, and identifies consolidation opportunities—directly supporting fleet organization and the Shell’s capture loop.