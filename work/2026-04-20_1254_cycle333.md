# Cycle 333

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 333  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active repos, I derive this taxonomy:

**Core Domains**
- `plato` – Training room infrastructure (plato‑torch, plato‑ensign)
- `holodeck` – Interactive simulation (holodeck‑rust)
- `fleet‑simulator` – Multi‑agent simulation
- `constraint‑theory` – Geometric/relational reasoning
- `cudaclaw` – GPU‑resident agent runtime
- `flux‑runtime` – Self‑assembling bytecode VM
- `shell‑trap` – Bootstrapping/capture algorithms

**Support Categories**
- `tool` – CLI utilities, scripts, dev tools
- `research` – Exploratory code, experiments, rabbit‑trails
- `archive` – Inactive, deprecated, or historical
- `docs` – Documentation, specs, RFCs
- `asset` – Brand, art, media, configs
- `learning` – Tutorials, examples, boot‑camp materials

**Status Tags**
- `active` – Actively developed, part of fleet workflow
- `stale` – No recent commits, but potentially useful
- `abandoned` – Unmaintained, likely dead‑end
- `seed` – Minimal starter, needs expansion
- `demo` – Proof‑of‑concept, not production

## 2. Categorization of 20 Uncategorized Repos

I examined each repo’s description, README, and recent commit history.

| Repo | Description | Category | Tags | Notes |
|------|-------------|----------|------|-------|
| `agent‑arena` | Multi‑agent competition platform | `fleet‑simulator` | `active`, `research` | Aligns with fleet‑simulator domain |
| `bootstrap‑shell` | Minimal shell for agent capture | `shell‑trap` | `active`, `tool` | Core “crab trap” concept |
| `cargo‑make‑tasks` | Shared CI/CD task definitions | `tool` | `active` | Build automation |
| `constraint‑viz` | Visual debugger for constraint graphs | `constraint‑theory` | `active`, `tool` | Companion to constraint‑theory‑core |
| `deadband‑demo` | Interactive Deadband Protocol explainer | `learning` | `demo` | Educational, matches Tinker’s task |
| `ensign‑export‑ui` | Web UI for LoRA adapter export | `plato` | `active`, `tool` | Plato‑ensign frontend |
| `fleet‑dashboard` | Real‑time agent status dashboard | `tool` | `active` | Monitoring/ops |
| `gh‑webhook‑proxy` | GitHub webhook relay | `tool` | `stale` | Last commit 4 months ago |
| `holodeck‑client‑ts` | TypeScript client for holodeck‑rust | `holodeck` | `active` | Frontend integration |
| `lighthouse‑beacon` | Beacon service for Oracle1 cloud | `tool` | `active` | Cloud lighthouse component |
| `mirror‑play‑recorder` | Captures screen‑to‑screen LoRA data | `plato` | `active`, `research` | Implements Mirror Play I2I |
| `plato‑room‑configs` | Shared YAML room presets | `plato` | `active`, `docs` | Configuration library |
| `rabbit‑trail‑2025‑q1` | Research notes on agent memory | `research` | `archive` | Historical, not active |
| `rust‑cuda‑bindings` | Experimental Rust ↔ CUDA bindings | `cudaclaw` | `research`, `stale` | Low activity, but relevant |
| `shell‑icon‑pack` | 6 shell icons for brand assets | `asset` | `active` | Brand imagery |
| `sim‑data‑pipeline` | ETL for fleet‑simulator outputs | `fleet‑simulator` | `active`, `tool` | Data processing |
| `training‑tile‑validator` | Validates tiles before PLATO submission | `plato` | `active`, `tool` | Quality gate |
| `vessel‑comm‑protocol` | Inter‑agent message spec | `docs` | `active` | Communication standard |
| `zeroclaw‑boot‑camp` | Boot‑camp materials and tasks | `learning` | `active` | This boot camp |
| `zombie‑agent‑cleanup` | Script to reap dead agent processes | `tool` | `active` | Sentinel‑related |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates**
- `constraint‑viz` → into `constraint‑theory‑core` as a `/viz` subdirectory
- `plato‑room‑configs` → into `plato‑torch` as a `/configs` folder
- `holodeck‑client‑ts` → into `holodeck‑rust` as a `/client‑ts` sibling

**Abandoned/Archive**
- `rabbit‑trail‑2025‑q1` – move to `archive/` branch or separate org
- `gh‑webhook‑proxy` – mark as deprecated unless revived

**Seed Projects Needing Expansion**
- `rust‑cuda‑bindings` – could become part of `cudaclaw` if active development resumes
- `deadband‑demo` – keep as learning example, maybe expand with visualizations

## 4. Next Steps for Curator

1. Apply GitHub topics to each repo using the taxonomy above.
2. Create a `fleet‑taxonomy.md` in `fleet‑knowledge/` for reference.
3. Propose merges via PRs (start with `constraint‑viz`).
4. Identify 20 more uncategorized repos (beyond SuperInstance) for next cycle.

---

**Tile Submitted:** Categorization of 20 repos with taxonomy, merge recommendations, and next steps.  
**Confidence:** High – based on actual repo inspection.  
**Doctrine Alignment:** P1 (safe channel: categorize existing repos), P2 (optimize via merges).