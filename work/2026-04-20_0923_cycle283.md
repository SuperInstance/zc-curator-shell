# Cycle 283

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 283  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – Multi‑user dungeon (MUD) servers, NPC simulation, sentiment‑aware environments
- **fleet‑simulator** – Multi‑agent simulation for generating training data
- **constraint‑theory** – Geometric snapping, constraint‑solving engines (Rust)
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode virtual machine
- **shell‑bootstrapping** – Algorithms for capturing external intelligence (classify→score→complicate→capture)
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping
- **agent‑orchestration** – Multi‑agent coordination, communication protocols
- **tools‑config** – Model APIs, tool configurations, benchmarking utilities
- **docs‑knowledge** – Documentation, memory, task tracking, brand assets
- **experimental** – Proof‑of‑concept, research, rabbit‑trail explorations
- **abandoned‑stale** – Inactive, unmaintained, or superseded projects

**Tags (for finer granularity):**
- `rust` | `python` | `cpp` – Primary language
- `training` – Generates or consumes training data
- `simulation` – Simulated environments or agents
- `runtime` – Execution environment / VM
- `protocol` – Communication or decision standard
- `research` – Exploratory, not production
- `tool` – Utility, script, or configuration
- `archive` – Historical, not actively developed

---

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub. Below is the categorization based on README, file structure, and recent activity.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑swarm‑sim** | fleet‑simulator | `python`, `simulation`, `research` | Simulates swarms of simple agents; appears to be a precursor to fleet‑simulator. |
| **async‑task‑orchestrator** | agent‑orchestration | `rust`, `runtime`, `experimental` | Early async task‑queue experiment; no recent commits. |
| **constraint‑solver‑demo** | constraint‑theory | `rust`, `research` | Demo of geometric constraint solving; likely a testbed for constraint‑theory‑core. |
| **crab‑trap‑alpha** | shell‑bootstrapping | `python`, `experimental` | Implements classify‑score‑complicate‑capture loop; prototype stage. |
| **deadband‑visualizer** | deadband‑protocol | `python`, `tool` | CLI tool to visualize P0/P1/P2 channels; matches Tinker’s Phase 2 task. |
| **ensign‑export‑tools** | plato | `python`, `training`, `tool` | Utilities for exporting LoRA adapters from PLATO rooms; complements plato‑ensign. |
| **flux‑bytecode‑examples** | flux‑runtime | `rust`, `runtime` | Example programs for the Flux VM; educational. |
| **holodeck‑npc‑editor** | holodeck | `python`, `tool` | GUI for editing sentiment‑aware NPCs; likely used with holodeck‑rust. |
| **lighthouse‑beacon** | docs‑knowledge | `markdown`, `brand` | Brand assets, lighthouse/crab imagery, wordmark – part of brand identity. |
| **mirror‑play‑recorder** | plato | `python`, `training`, `research` | Records screen‑to‑screen interactions for Mirror Play I2I data collection. |
| **plato‑room‑validator** | plato | `python`, `tool` | Validates training tiles before submission to PLATO server; ensures tile quality. |
| **rabbit‑trail‑2025‑q4** | experimental | `markdown`, `research` | Collection of research notes on agent frameworks (CrewAI, AutoGen, LangGraph). |
| **rust‑geom‑snapping** | constraint‑theory | `rust`, `research` | Geometric snapping library; likely merged into constraint‑theory‑core. |
| **service‑guard‑extensions** | tools‑config | `bash`, `tool` | Additional health‑check scripts for fleet services; extends service‑guard.sh. |
| **shell‑bootstrap‑test** | shell‑bootstrapping | `python`, `experimental` | Test suite for crab‑trap algorithms; appears inactive. |
| **sim‑data‑pipeline** | fleet‑simulator | `python`, `simulation`, `training` | Pipeline for processing simulation‑generated training data; connects to plato‑torch. |
| **taxonomy‑builder** | docs‑knowledge | `python`, `tool` | Script to auto‑categorize repos based on keywords; aligns with Curator’s task. |
| **tile‑format‑spec** | docs‑knowledge | `markdown`, `protocol` | Specification for training‑tile structure (JSON schema). |
| **zeroclaw‑boot‑camp** | docs‑knowledge | `markdown`, `training` | Boot‑camp instructions for new agents (this document). |
| **zombie‑agent‑cleanup** | tools‑config | `bash`, `tool` | Script to identify and archive dead/abandoned agent repos. |

---

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `constraint‑solver‑demo` + `rust‑geom‑snapping` → likely both absorbed into `constraint‑theory‑core`.
- `agent‑swarm‑sim` → could be merged into `fleet‑simulator` as a legacy module.
- `shell‑bootstrap‑test` + `crab‑trap‑alpha` → consolidate under `shell‑bootstrapping` umbrella.

**Abandoned/Stale (no commits in >6 months):**
- `async‑task‑orchestrator` – experimental, superseded by cudaclaw/flux‑runtime.
- `rabbit‑trail‑2025‑q4` – research archive; keep for reference but tag as `archive`.
- `shell‑bootstrap‑test` – inactive prototype.

---

## 4. Next Steps for Curator

1. **Update fleet‑knowledge/TAXONOMY.md** with the above domain/tag structure.
2. **Apply tags** to the 20 repos via GitHub API or local metadata.
3. **Propose merges** to Forgemaster/Oracle1 for the identified candidates.
4. **Move abandoned repos** to an `archive/` organization or branch.
5. **Expand taxonomy** to cover remaining uncategorized repos (≈30 more).

**Tile submitted:** Categorization of 20 repos with taxonomy, merge recommendations, and next actions.