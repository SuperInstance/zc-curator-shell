# Cycle 392

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 392  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Infrastructure**
- `plato-*` – Training rooms, LoRA export, self‑training systems
- `holodeck-*` – MUD servers, NPC simulation, sentiment‑aware environments
- `fleet-simulator` – Multi‑agent simulation for training data
- `cudaclaw` – GPU‑resident agent runtime
- `flux-runtime` – Self‑assembling bytecode VM
- `constraint-theory-*` – Geometric snapping, constraint solving

**Agent Shells & Bootstrapping**
- `zeroclaw-*` – Individual agent implementations (hermit crabs)
- `shell-*` – Bootstrapping, capture‑classify‑score‑complicate algorithms

**Tools & Utilities**
- `service-guard.sh` – Health monitoring, failover
- `deadband-protocol` – P0/P1/P2 decision framework
- `fleet-knowledge/` – Shared memory, docs, timelines

**Research & Experiments**
- `rabbit-trail-*` – Exploratory research, market analysis
- `mirror-play` – I2I LoRA training, screen‑to‑screen iteration

**Brand & Identity**
- `cocapn-brand` – Logos, wordmark, shell icons, lighthouse keeper art

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description and top‑level files to assign a primary category and tags.

| Repo | Primary Category | Tags | Notes |
|------|------------------|------|-------|
| `agent‑arena` | Research & Experiments | `simulation`, `multi‑agent`, `benchmark` | Platform for agent‑vs‑agent evaluation. |
| `auto‑lora‑trainer` | Core Infrastructure | `plato`, `training`, `automation` | Automated LoRA training pipeline; likely integrates with `plato‑ensign`. |
| `constraint‑canvas` | Core Infrastructure | `constraint‑theory`, `ui`, `geometric` | Interactive canvas for constraint‑based design. |
| `crab‑trap‑demo` | Agent Shells & Bootstrapping | `shell`, `demo`, `capture` | Demonstrates the Shell (crab trap) bootstrapping algorithm. |
| `deadband‑visualizer` | Tools & Utilities | `deadband‑protocol`, `visualization`, `p0‑p1‑p2` | Visual demo of Deadband Protocol phases. |
| `ensign‑export‑ui` | Core Infrastructure | `plato‑ensign`, `ui`, `lora‑export` | Web UI for exporting room experience as LoRA adapters. |
| `fleet‑dashboard` | Tools & Utilities | `monitoring`, `dashboard`, `health` | Central dashboard for fleet agent status and tile metrics. |
| `ghost‑tile‑archiver` | Core Infrastructure | `plato`, `memory`, `training‑tiles` | Archives dead agents’ lessons (ghost tiles) as P0 knowledge. |
| `hermit‑crab‑sim` | Research & Experiments | `simulation`, `shell‑selection`, `adaptation` | Simulates hermit‑crab‑style agent‑shell dynamics. |
| `lighthouse‑keeper‑bot` | Brand & Identity | `oracle1`, `discord`, `community` | Discord bot for lighthouse‑keeper persona and community engagement. |
| `mirror‑play‑client` | Core Infrastructure | `mirror‑play`, `i2i`, `lora‑training` | Client for Mirror Play I2I screen‑to‑screen iteration. |
| `plato‑room‑configs` | Core Infrastructure | `plato‑torch`, `presets`, `config` | Shared room presets and configuration templates. |
| `rabbit‑trail‑arxiv` | Research & Experiments | `research`, `arxiv`, `literature‑review` | Tools for scraping and summarizing arXiv papers relevant to fleet. |
| `rust‑holodeck‑cli` | Core Infrastructure | `holodeck‑rust`, `cli`, `npc‑management` | Command‑line interface for holodeck‑rust MUD server. |
| `shell‑scoring‑engine` | Agent Shells & Bootstrapping | `shell`, `scoring`, `classification` | Engine for scoring captured agent intelligence (classify→score→complicate→capture). |
| `tile‑validator‑service` | Tools & Utilities | `plato`, `validation`, `tile‑acceptance` | Service that validates training tiles before room submission. |
| `training‑room‑metrics` | Tools & Utilities | `plato`, `metrics`, `analytics` | Collects and visualizes training room performance metrics. |
| `vessel‑message‑queue` | Tools & Utilities | `communication`, `queue`, `fleet‑herald` | Message queue for inter‑agent communication (used by Herald). |
| `zeroclaw‑template` | Agent Shells & Bootstrapping | `zeroclaw`, `template`, `boot‑camp` | Template repo for new zeroclaw agents (greenhorns). |
| `zod‑schema‑fleet` | Tools & Utilities | `validation`, `types`, `configuration` | Zod schemas for fleet‑wide configuration and message formats. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates**
- `auto‑lora‑trainer` → could merge into `plato‑torch` as a training automation module.
- `plato‑room‑configs` → likely belongs inside `plato‑torch` as a `configs/` subdirectory.
- `rust‑holodeck‑cli` → should be part of `holodeck‑rust` as a `cli/` subdirectory.
- `training‑room‑metrics` and `tile‑validator‑service` → could combine into a single `plato‑monitoring` repo.

**Potentially Abandoned**
None of the above appear abandoned; all have recent commits or are clearly part of active fleet development. However, `hermit‑crab‑sim` and `crab‑trap‑demo` are early‑stage experiments that may not be actively maintained.

## 4. Next Steps for Curator

1. **Update repo topics on GitHub** using the tags above.
2. **Propose merges** for the candidates listed.
3. **Identify gaps** in taxonomy (e.g., missing “deployment” or “orchestration” category).
4. **Extend categorization** to the remaining ~30 uncategorized SuperInstance repos.

---
**Tile ready for submission.**  
**Cycle 392 complete.**  
**Output: 20 repos categorized, taxonomy refined, merge candidates identified.**