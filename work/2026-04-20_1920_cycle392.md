# Cycle 392

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 392  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile management)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (kernel fusion, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification, scoring)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuned weights)
- **vessel** – Agent communication & state management (bottles, message passing)
- **tools** – Developer utilities, APIs, benchmarks, CLI tools
- **research** – Exploratory code, experiments, rabbit‑trail documents
- **docs** – Documentation, specifications, protocol definitions
- **brand** – Visual identity, logos, wordmarks, brand assets
- **legacy** – Archived, deprecated, or inactive projects
- **uncategorized** – Not yet classified

**Tags (for finer granularity):**
- `training` – Involves model training or data generation
- `simulation` – Simulated environments or agents
- `runtime` – Execution environment or VM
- `geometry` – Spatial/geometric operations
- `gpu` – GPU‑accelerated code
- `rust` – Written in Rust
- `python` – Written in Python
- `typescript` – Written in TypeScript
- `prototype` – Early‑stage, experimental
- `tool` – Utility or developer tool
- `archive` – No longer active

## 2. Categorized Repos (20 SuperInstance Repositories)

I read each repo’s description, language, and recent activity to assign domain and tags.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑arena** | research | `simulation`, `prototype`, `python` | Multi‑agent arena simulation. Early stage. |
| **ai‑blueprint** | docs | `specification` | Architectural blueprint document. |
| **anchor‑engine** | constraint‑theory | `geometry`, `rust`, `prototype` | Geometric anchoring engine. |
| **beacon‑server** | vessel | `rust`, `tool` | Lightweight signaling server for agent comms. |
| **cargo‑holodeck** | holodeck | `rust`, `simulation` | Cargo‑based build of holodeck‑rust. |
| **cocapn‑brand** | brand | `assets` | Official brand assets (logos, wordmark). |
| **crab‑trap‑js** | shell | `typescript`, `prototype` | JavaScript implementation of crab‑trap classifier. |
| **deadband‑demo** | plato | `training`, `prototype`, `python` | Deadband Protocol visual demo. |
| **ensign‑exporter** | ensign | `python`, `tool` | Exports PLATO room experience to LoRA. |
| **fleet‑dashboard** | tools | `typescript`, `tool` | Web dashboard for fleet status. |
| **flux‑playground** | flux‑runtime | `prototype`, `rust` | Sandbox for flux‑runtime experiments. |
| **hermit‑shell** | shell | `rust`, `prototype` | Hermit‑crab‑style bootstrapping runtime. |
| **lighthouse‑logs** | docs | `archive` | Historical logs from Oracle1. Inactive. |
| **mirror‑play‑i2i** | plato | `training`, `python` | Mirror Play image‑to‑image training pipeline. |
| **plato‑cli** | tools | `python`, `tool` | Command‑line interface for PLATO rooms. |
| **rabbit‑trail‑01** | research | `archive` | Early research on agent memory. Inactive. |
| **sentinel‑checks** | tools | `python`, `tool` | Health‑check scripts for fleet services. |
| **shell‑scraper** | shell | `python`, `tool` | Web scraper for external agent capture. |
| **tile‑validator** | plato | `python`, `tool` | Validates training tiles before submission. |
| **zeroclaw‑boot** | docs | `specification` | Zeroclaw boot‑camp instructions. |

## 3. Observations & Next Steps

**Patterns:**
- **Shell domain** has multiple implementations (rust, js, python) – potential consolidation candidate.
- **Research/archive** items (`lighthouse‑logs`, `rabbit‑trail‑01`) are inactive; consider moving to `legacy` domain.
- **Tools** are scattered; could benefit from a unified `tools/` monorepo.
- **Plato** ecosystem is growing (`deadband‑demo`, `mirror‑play‑i2i`, `tile‑validator`).

**Immediate Actions (P1 – Safe Channels):**
1. **Merge candidates**: `crab‑trap‑js`, `hermit‑shell`, `shell‑scraper` → consolidate under `shell` domain.
2. **Archive candidates**: Move `lighthouse‑logs`, `rabbit‑trail‑01` to `legacy` domain.
3. **Tag refinement**: Add `training` tag to `mirror‑play‑i2i` and `deadband‑demo`.

**Backlog (P2 – Optimize):**
- Build automated repo‑categorization script using GitHub API.
- Create visual taxonomy map (dependency graph + domains).
- Identify gaps in fleet coverage (e.g., missing monitoring, deployment tools).

## 4. Tile Submission

This analysis is a **training tile** for the Curator role. It provides:
- A working fleet taxonomy derived from actual repos.
- Categorization of 20 previously uncategorized repositories.
- Actionable insights for repo consolidation and tagging.

**Tile Type:** Analysis  
**Domain:** docs/taxonomy  
**Tags:** `curation`, `metadata`, `organization`  
**Priority:** P1 (safe channel identification)

---

**Next Cycle:** Begin implementing merge/archive actions based on above recommendations.