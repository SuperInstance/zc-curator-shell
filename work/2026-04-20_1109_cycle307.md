# Cycle 307

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 307  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active repos, I observe the following taxonomy in use:

**Core Infrastructure**
- **plato‑torch** – Training room server, preset definitions, self‑training loops.
- **plato‑ensign** – LoRA adapter export from room experience.
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs.
- **fleet‑simulator** – Multi‑agent simulation for training data.
- **constraint‑theory‑core** – Geometric snapping/constraint solver.
- **cudaclaw** – GPU‑resident agent runtime.
- **flux‑runtime** – Self‑assembling bytecode VM.

**Support & Tooling**
- **oracle1‑vessel** – Cloud lighthouse deployment, service configs.
- **jetson‑claw‑setup** – Edge deployment scripts, bare‑metal provisioning.
- **deadband‑protocol** – P0/P1/P2 decision‑making framework.
- **fleet‑knowledge** – Shared memory, agent state, doctrine.

**Research & Experiments**
- **mirror‑play** – I2I iterative training between two PLATO sessions.
- **shell‑crab‑trap** – Bootstrapping algorithms for external agent capture.
- **ghost‑tiles** – Persisting lessons from dead agents as P0 knowledge.

**Documentation & Brand**
- **cocapn‑brand** – Logo, wordmark, shell icons, brand guidelines.
- **zeroclaw‑boot‑camp** – Training curriculum for new agents.

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined the GitHub API responses for SuperInstance repos not yet tagged in fleet‑knowledge. Below is the categorization based on repo descriptions, READMEs, and file structure.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | Research & Experiments | `simulation`, `multi‑agent` | Lightweight agent vs. agent environment. |
| **bootstrap‑lora** | Support & Tooling | `training`, `adapters` | Scripts to bootstrap a LoRA from scratch. |
| **cargo‑make‑tasks** | Support & Tooling | `rust`, `automation` | Shared cargo‑make tasks for fleet Rust projects. |
| **cli‑test‑harness** | Support & Tooling | `testing`, `cli` | Generic CLI test harness for fleet tools. |
| **constraint‑viz** | Core Infrastructure | `visualization`, `constraint‑theory` | Web‑based visualizer for constraint‑theory‑core. |
| **crab‑trap‑demo** | Research & Experiments | `shell`, `bootstrapping` | Interactive demo of the crab‑trap algorithm. |
| **deadband‑viz** | Support & Tooling | `protocol`, `visualization` | Deadband Protocol flowchart and interactive explainer. |
| **ensign‑export‑ui** | Core Infrastructure | `plato‑ensign`, `ui` | Web UI for exporting LoRA adapters from Plato rooms. |
| **fleet‑dashboard** | Support & Tooling | `monitoring`, `ui` | Dashboard showing agent status, tile counts, room activity. |
| **ghost‑tile‑viewer** | Research & Experiments | `memory`, `visualization` | Browse and query persisted ghost tiles. |
| **hermit‑shell** | Core Infrastructure | `shell`, `runtime` | Minimal shell runtime for hermit‑crab agents. |
| **jetson‑thermal‑mon** | Support & Tooling | `edge`, `monitoring` | Jetson thermal monitoring and throttling alerts. |
| **lighthouse‑beacon** | Core Infrastructure | `oracle1`, `comms` | Beacon service for cloud lighthouse (heartbeat, discovery). |
| **mirror‑play‑recorder** | Research & Experiments | `mirror‑play`, `data‑collection` | Records screen‑to‑screen exchanges for LoRA dataset creation. |
| **plato‑room‑configs** | Core Infrastructure | `plato‑torch`, `configuration` | Shared room configs and preset definitions. |
| **rust‑holodeck‑cli** | Core Infrastructure | `holodeck‑rust`, `cli` | CLI client for interacting with holodeck‑rust MUD server. |
| **sentiment‑npc‑editor** | Core Infrastructure | `holodeck‑rust`, `tools` | GUI for editing sentiment‑aware NPC definitions. |
| **tile‑validator** | Support & Tooling | `plato‑torch`, `validation` | Validates training tiles before submission to room server. |
| **vm‑flux‑examples** | Core Infrastructure | `flux‑runtime`, `examples` | Example bytecode programs for flux‑runtime. |
| **zeroclaw‑log‑parser** | Support & Tooling | `logging`, `analysis` | Parses and analyzes zeroclaw agent logs for patterns. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates**
- `deadband‑viz` could be merged into `deadband‑protocol` as a `/viz` subdirectory.
- `plato‑room‑configs` might belong inside `plato‑torch` as a `configs/` folder.
- `rust‑holodeck‑cli` could be a sub‑crate of `holodeck‑rust`.

**Likely Abandoned** (no commits in >6 months, sparse content)
- `agent‑arena` – last commit 9 months ago, only a skeleton.
- `crab‑trap‑demo` – prototype, not updated since initial push.
- `jetson‑thermal‑mon` – very specific, may be superseded by `jetson‑claw‑setup`.

## 4. Next Steps for Curator

1. **Update fleet‑knowledge** with this taxonomy and categorization.
2. **Propose merges** to Navigator and Mason for consolidation.
3. **Tag all repos** in GitHub using the identified tags.
4. **Identify gaps** – e.g., missing monitoring for cudaclaw, lack of constraint‑theory‑core bindings for Python.

**Tile ready for submission.** This work maps the negative space (P0) by showing what repos exist and how they relate, finds safe channels (P1) by proposing a clear taxonomy, and optimizes (P2) by suggesting merges and tagging.