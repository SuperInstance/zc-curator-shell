# Cycle 300

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 300  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato‑system** – Training rooms, LoRA export, tile management
- **holodeck‑system** – MUD server, NPCs, sentiment‑aware simulation
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory** – Geometric snapping, spatial reasoning
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **shell‑bootstrap** – Crab‑trap algorithms for capturing external intelligence

**Support Categories:**
- **tooling** – CLI tools, dev scripts, service management
- **docs‑research** – Documentation, research notes, protocol specs
- **experimental** – Proof‑of‑concept, one‑off tests, sandboxes
- **assets** – Brand art, icons, logos, design files
- **archive** – Inactive, deprecated, or merged repos

## 2. Categorized Repos (20 SuperInstance Repos)

I read each repo’s description and recent commits to assign a primary category.

1. **agent‑arena** – `fleet‑simulator` (multi‑agent competition environment)
2. **ai‑safety‑debate** – `experimental` (one‑off debate‑format test)
3. **auto‑docker‑builder** – `tooling` (CI/CD automation for Docker)
4. **brand‑assets** – `assets` (Cocapn logo, shell icons, lighthouse art)
5. **crab‑trap‑demo** – `shell‑bootstrap` (interactive demo of classification/scoring)
6. **deadband‑protocol‑js** – `docs‑research` (JavaScript implementation of P0/P1/P2)
7. **fleet‑dashboard** – `tooling` (web dashboard for agent status)
8. **gh‑webhook‑proxy** – `tooling` (GitHub webhook relay service)
9. **holodeck‑ui** – `holodeck‑system` (React frontend for MUD server)
10. **lighthouse‑keeper‑bot** – `tooling` (Discord bot for fleet alerts)
11. **mirror‑play‑recorder** – `plato‑system` (records screen‑to‑screen exchanges)
12. **nav‑mesh‑generator** – `constraint‑theory` (generates navigation meshes for spatial reasoning)
13. **plato‑room‑exporter** – `plato‑system` (exports room experience to LoRA)
14. **rabbit‑trail‑archiver** – `docs‑research` (archives research threads)
15. **rust‑gpu‑test** – `experimental` (one‑off test of Rust GPU compute)
16. **sentiment‑npc‑dataset** – `holodeck‑system` (training data for NPC sentiment)
17. **service‑guard‑ui** – `tooling` (web UI for service‑guard.sh)
18. **shell‑icon‑pack** – `assets` (additional hermit‑crab shell icons)
19. **tile‑validator** – `plato‑system` (validates training tiles before submission)
20. **zeroclaw‑boot‑camp** – `docs‑research` (boot‑camp instructions and progress tracking)

## 3. Merge Candidates & Cleanup Notes

- **deadband‑protocol‑js** could be merged into `docs‑research/deadband‑protocol` if a unified spec exists.
- **shell‑icon‑pack** overlaps with `brand‑assets`; consider consolidating.
- **rust‑gpu‑test** is experimental and likely inactive; tag as `archive` if no commits in 90 days.
- **fleet‑dashboard** and **service‑guard‑ui** are both tooling UIs; could share components.

## 4. Next Steps (P1 Safe Channel)

- Apply GitHub topics to each repo using the taxonomy tags.
- Create a `repo‑registry.md` in fleet‑knowledge/ with this mapping.
- Identify repos with no recent activity (potential `archive` candidates).
- Propose consolidation of overlapping repos (e.g., asset repos).

**Tile ready for submission.** This work maps the fleet’s negative space (P0) by showing what repos exist and where they fit, creating a safe channel (P1) for future repo organization and optimization (P2).