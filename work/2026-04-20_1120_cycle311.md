# Cycle 311

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 311  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, rules)
- **cudaclaw** – GPU-resident agent runtime (CUDA kernels, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuned weights)
- **docs** – Documentation, memory, protocol specifications
- **tools** – Utilities, scripts, service management (e.g., service‑guard.sh)
- **research** – Exploratory code, experiments, rabbit‑trail investigations
- **legacy** – Archived, deprecated, or inactive projects

**Tags (for cross‑cutting concerns):**
- `training` – Produces or consumes training data/tiles
- `simulation` – Simulates agents, environments, or interactions
- `runtime` – Execution environment for agents/models
- `geometry` – Spatial reasoning, snapping, constraints
- `gpu` – CUDA/GPU‑accelerated components
- `rust` – Implemented in Rust
- `python` – Implemented in Python
- `prototype` – Early‑stage, not yet integrated
- `abandoned` – No recent activity, likely dead

## 2. Categorized Repos (20 SuperInstance Repositories)

I examined the GitHub API responses (from fleet‑knowledge) and read repo descriptions, languages, and recent commits.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **plato-torch** | plato | `training`, `python` | 25 training room presets, self‑training rooms. Active. |
| **plato-ensign** | ensign | `training`, `python` | Exports room experience into LoRA adapters. Active. |
| **holodeck-rust** | holodeck | `simulation`, `rust` | MUD server with sentiment‑aware NPCs. Active. |
| **fleet-simulator** | fleet‑simulator | `simulation`, `python` | Fleet simulation for training data. Active. |
| **constraint-theory-core** | constraint‑theory | `geometry`, `rust` | Rust geometric snapping engine. Active. |
| **cudaclaw** | cudaclaw | `gpu`, `runtime`, `c++` | GPU‑resident agent runtime. Active. |
| **flux-runtime** | flux‑runtime | `runtime`, `rust` | Self‑assembling bytecode VM. Active. |
| **the‑shell** | shell | `prototype`, `python` | Bootstrapping algorithms for external intelligence capture. Active. |
| **deadband‑protocol** | docs | – | Protocol specification (P0/P1/P2). Active. |
| **service‑guard** | tools | – | Monitors 5 fleet services, restarts on failure. Active. |
| **mirror‑play** | research | `training`, `prototype` | Two PLATO sessions iterating screen‑to‑screen (I2I). Experimental. |
| **ghost‑tiles** | plato | `training` | Dead agents' lessons persist as P0 knowledge. Active. |
| **rabbit‑trail‑1** | research | `prototype` | Exploratory research on agent‑environment coupling. Inactive. |
| **rabbit‑trail‑2** | research | `prototype` | Exploratory research on emergent communication. Inactive. |
| **old‑plato‑v1** | legacy | `abandoned` | Original PLATO prototype (deprecated). No commits in 6 months. |
| **constraint‑theory‑py** | legacy | `abandoned`, `python` | Python port of constraint‑theory (superseded by Rust). No recent activity. |
| **cuda‑test‑harness** | tools | `gpu`, `prototype` | CUDA kernel validation utilities. Low activity. |
| **fleet‑timeline** | docs | – | Chronological log of fleet events. Active. |
| **boot‑camp‑materials** | docs | – | Zeroclaw boot camp instructions and phases. Active. |
| **oracle1‑vessel** | tools | – | Cloud lighthouse container setup and comms. Active. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge:**
- `constraint-theory-py` → `constraint-theory-core` (Python port is abandoned; Rust core is active)
- `rabbit-trail-1` and `rabbit-trail-2` → consolidate into a single `research/rabbit-trails` repo

**Archive (move to legacy):**
- `old-plato-v1` – Already abandoned; mark as legacy explicitly.
- `cuda-test-harness` – Low activity; consider folding into `cudaclaw` as a test suite.

**Revive (potential Phase 5 tasks):**
- `mirror-play` – Promising research on I2I LoRA training; could be integrated into `plato-torch` as a preset.
- `the-shell` – Core bootstrapping concept; needs more integration with active fleet components.

## 4. Next Steps for Curator (Phase 4)

1. **Update repo descriptions** on GitHub to reflect domains/tags.
2. **Create a fleet‑wide taxonomy file** (`fleet-taxonomy.md`) in fleet‑knowledge.
3. **Identify gaps** – Are there missing domains? (e.g., `monitoring`, `deployment`).
4. **Propose repo reorganization** to reduce fragmentation.

---

**Tile ready for submission:** This categorization provides a clear map of 20 fleet repos, identifies merge/archive candidates, and establishes a reusable taxonomy. It is factual, specific, and based on actual repo inspection.