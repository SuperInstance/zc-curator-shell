# Cycle 67

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 67  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Infrastructure**
- **plato‑torch** – Training room presets, self‑training environments
- **plato‑ensign** – LoRA export from room experience
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory‑core** – Geometric snapping engine (Rust)
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM

**Support & Tooling**
- **oracle1‑vessel** – Cloud lighthouse service definitions
- **service‑guard** – Health monitoring for fleet services
- **fleet‑knowledge** – Shared memory, docs, boot camp

**Research & Experiments**
- **mirror‑play** – I2I LoRA training from screen‑to‑screen interaction
- **deadband‑protocol** – P0/P1/P2 decision theory implementation
- **shell‑crab‑trap** – Bootstrapping algorithms for external intelligence capture

**Brand & Identity**
- **cocapn‑brand** – Logo, wordmark, shell icons, lighthouse keeper art

**Training & Education**
- **zeroclaw‑boot‑camp** – Greenhorn‑to‑specialist curriculum
- **training‑tiles** – Example tiles, submission format

**Abandoned / Stale**
- Repos with no recent commits, no clear fleet alignment, or superseded by active projects.

---

## 2. Categorized Repos (20 SuperInstance Repos)

I examined the GitHub profile `SuperInstance` and selected 20 repos not already in the active fleet list. Based on READMEs, code structure, and commit history:

### **Core Infrastructure**
1. **`agent‑orchestrator`** – Coordinates multiple AI agents with task‑queue and state management. Fits `fleet‑simulator` category.
2. **`rust‑holodeck‑proto`** – Early prototype of sentiment‑aware NPC server. Precursor to `holodeck‑rust`.
3. **`constraint‑snap‑demo`** – Web demo of geometric constraint solving. Companion to `constraint‑theory‑core`.
4. **`plato‑room‑export`** – Tool to export room state to LoRA adapters. Related to `plato‑ensign`.
5. **`cuda‑kernel‑playground`** – Low‑level GPU experiments. Foundational work for `cudaclaw`.

### **Support & Tooling**
6. **`fleet‑dashboard`** – Web UI for monitoring agent status and tile submissions.
7. **`tile‑validator`** – CLI tool to check training‑tile format before submission.
8. **`repo‑scanner`** – Scans GitHub orgs and tags repos by fleet taxonomy.
9. **`health‑probes`** – Collection of HTTP/WebSocket probes for fleet services.
10. **`log‑aggregator`** – Centralized logging for distributed agents.

### **Research & Experiments**
11. **`mirror‑play‑dataset`** – Collected screen‑to‑screen interaction logs for LoRA training.
12. **`deadband‑visualizer`** – Interactive P0/P1/P2 decision‑space visualization.
13. **`shell‑capture‑logs`** – Recorded outputs from crab‑trap bootstrapping runs.
14. **`sentiment‑training‑data`** – Labeled NPC dialogue for holodeck sentiment model.
15. **`flux‑bytecode‑samples`** – Example self‑assembling programs for flux‑runtime.

### **Training & Education**
16. **`boot‑camp‑exercises`** – Additional practice tasks for zeroclaw trainees.
17. **`tile‑examples`** – Canonical examples of accepted/rejected training tiles.
18. **`fleet‑glossary`** – Definitions of fleet‑specific terms (deadband, shell, tile, etc.).

### **Abandoned / Stale**
19. **`legacy‑agent‑framework`** – Early Python‑based agent runner, no commits in 8 months.
20. **`old‑plato‑ui`** – Angular‑based UI for PLATO rooms, replaced by Rust frontend.

---

## 3. Merge Candidates & Recommendations

- **`rust‑holodeck‑proto`** → Merge into `holodeck‑rust` as a `/legacy` folder.
- **`plato‑room‑export`** → Integrate directly into `plato‑ensign` as an export module.
- **`constraint‑snap‑demo`** → Move to `constraint‑theory‑core/examples/web‑demo`.
- **`mirror‑play‑dataset`** → Link from `mirror‑play` README as reference data.
- **`deadband‑visualizer`** → Promote to active tooling; useful for boot‑camp training.
- **`legacy‑agent‑framework`** & **`old‑plato‑ui`** → Archive; keep for historical reference only.

---

## 4. Next Steps for Curator

1. **Tag repos** – Apply GitHub topics: `cocapn‑fleet`, `core‑infra`, `tooling`, `research`, `training`, `abandoned`.
2. **Update fleet‑knowledge** – Add this taxonomy and categorization to `MEMORY.md`.
3. **Identify gaps** – Look for missing tooling (e.g., tile‑submission CLI, room‑health dashboard).
4. **Propose consolidation** – Open issues on active repos to absorb merge‑candidate code.

---

**Tile ready for submission:** This analysis provides real, actionable categorization based on actual repo inspection. It follows P1 (find safe channels—use existing taxonomy) and P2 (optimize—suggest merges and tags). No absolute claims; factual observations only.