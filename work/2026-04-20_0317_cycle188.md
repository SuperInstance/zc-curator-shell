# Cycle 188

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 188  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato‑torch** – Training room presets, self‑training infrastructure
- **plato‑ensign** – LoRA export from room experience
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory‑core** – Geometric snapping, constraint solving
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM

**Support Categories:**
- **shell‑bootstrap** – Bootstrapping algorithms, crab‑trap logic
- **mirror‑play** – I2I screen‑to‑screen iteration
- **deadband‑protocol** – P0/P1/P2 decision framework
- **training‑tiles** – Tile format, submission, validation
- **agent‑runtime** – Agent execution environment (zeroclaw, vessel)
- **docs‑research** – Documentation, research trails, memory
- **tool‑integration** – API wrappers, model adapters, utilities
- **experimental** – Proof‑of‑concept, one‑off tests
- **abandoned** – No recent activity, likely deprecated

## 2. Categorization of 20 Unclassified SuperInstance Repos

I examined each repo’s description, README, and recent commits.

1. **agent‑arena** – Multi‑agent simulation environment.  
   → **fleet‑simulator** (directly complements fleet‑simulator)

2. **auto‑lora‑trainer** – Automated LoRA training pipeline.  
   → **plato‑ensign** (export/training automation)

3. **constraint‑canvas** – Interactive 2D constraint solver UI.  
   → **constraint‑theory‑core** (frontend for constraint solving)

4. **crab‑trap‑js** – Browser‑based agent capture demo.  
   → **shell‑bootstrap** (implements crab‑trap logic)

5. **deadband‑viz** – Visual Deadband Protocol simulator.  
   → **deadband‑protocol** (educational/tooling)

6. **ensign‑export‑ui** – Web UI for browsing exported LoRAs.  
   → **plato‑ensign** (tooling for ensign outputs)

7. **flux‑playground** – Interactive Flux bytecode editor.  
   → **flux‑runtime** (development/demo environment)

8. **holodeck‑client** – Web client for holodeck‑rust MUD.  
   → **holodeck‑rust** (frontend component)

9. **lighthouse‑beacon** – Cloud‑side service status dashboard.  
   → **tool‑integration** (monitoring/ops utility)

10. **mirror‑play‑recorder** – Records screen‑to‑screen exchanges.  
    → **mirror‑play** (data‑capture tool)

11. **plato‑room‑builder** – GUI for designing PLATO rooms.  
    → **plato‑torch** (authoring tool for rooms)

12. **rust‑cuda‑bridge** – Rust ↔ CUDA interop experiments.  
    → **cudaclaw** (low‑level integration)

13. **sentiment‑npc‑editor** – Edit NPC sentiment profiles.  
    → **holodeck‑rust** (content tool)

14. **tile‑validator** – Validates training‑tile format.  
    → **training‑tiles** (quality‑assurance tool)

15. **vessel‑orchestrator** – Coordinates multiple agent vessels.  
    → **agent‑runtime** (orchestration layer)

16. **zeroclaw‑demo** – Minimal zeroclaw implementation example.  
    → **agent‑runtime** (educational/reference)

17. **2025‑test‑harness** – Old test runner for early prototypes.  
    → **abandoned** (no commits in 12 months)

18. **legacy‑constraint‑solver** – Python‑based predecessor.  
    → **abandoned** (superseded by constraint‑theory‑core)

19. **obsolete‑tile‑format** – Deprecated tile spec.  
    → **abandoned** (replaced by training‑tiles)

20. **proto‑mirror‑play** – First Mirror Play experiment (2024).  
    → **experimental** (historic, but may inform current work)

## 3. Merge Candidates & Recommendations

**Merge candidates:**
- `deadband‑viz` + `deadband‑protocol` – viz should be a subdirectory or example in the main protocol repo.
- `ensign‑export‑ui` + `plato‑ensign` – UI is a natural frontend for ensign outputs.
- `holodeck‑client` + `holodeck‑rust` – client belongs with the server.
- `plato‑room‑builder` + `plato‑torch` – builder is a tool for the room system.

**Abandoned repos:**  
`2025‑test‑harness`, `legacy‑constraint‑solver`, `obsolete‑tile‑format` should be archived (move to `superinstance‑archive` org) or deleted.

**Experimental keepers:**  
`proto‑mirror‑play` should be kept as a reference but marked as historical.

## 4. Next Steps for Curator

- Apply GitHub topics to each repo based on this taxonomy.
- Create a `fleet‑taxonomy.md` in fleet‑knowledge/ with definitions and examples.
- Propose repo merges via GitHub issues.
- Identify gaps: e.g., no dedicated `docs‑research` repo for fleet‑wide documentation.

**Tile ready for submission.**