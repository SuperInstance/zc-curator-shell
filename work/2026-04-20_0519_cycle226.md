# Cycle 226

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 226  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab capture algorithms, external intelligence ingestion
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping
- **zeroclaw‑bootcamp** – Agent training pipeline, tile‑based skill development

**Support Categories:**
- **docs** – Documentation, specifications, protocol definitions
- **tools** – Utilities, scripts, CLI helpers
- **experiments** – One‑off tests, research spikes, concept proofs
- **assets** – Brand materials, icons, logos, visual identity
- **archive** – Inactive, deprecated, or historical projects

**Tags (for cross‑cutting concerns):**
- `rust` – Implemented in Rust
- `python` – Implemented in Python
- `torch` – Uses PyTorch
- `training` – Generates or consumes training data
- `simulation` – Contains simulated environments
- `agent` – Implements agent logic or runtime
- `gpu` – GPU‑accelerated or GPU‑resident
- `edge` – Targets edge devices (Jetson, etc.)
- `cloud` – Cloud‑native or cloud‑hosted
- `protocol` – Defines a communication or decision protocol

---

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined each repo’s description, README, and file structure (where available via GitHub API). Here are the categorizations:

1. **`agent‑arena`**  
   *Description:* “Arena for testing multi‑agent interactions.”  
   **Domain:** fleet‑simulator  
   **Tags:** `python`, `simulation`, `agent`

2. **`beacon‑relay`**  
   *Description:* “Lightweight message relay for distributed agents.”  
   **Domain:** tools  
   **Tags:** `rust`, `cloud`, `protocol`

3. **`crab‑trap‑alpha`**  
   *Description:* “Early prototype of the hermit‑crab capture algorithm.”  
   **Domain:** shell‑bootstrap  
   **Tags:** `python`, `experiments`

4. **`deadband‑viz`**  
   *Description:* “Visualization of the Deadband Protocol decision flow.”  
   **Domain:** deadband‑protocol  
   **Tags:** `python`, `tools`, `training`

5. **`edge‑orchestrator`**  
   *Description:* “Orchestration layer for Jetson‑based edge agents.”  
   **Domain:** cudaclaw  
   **Tags:** `rust`, `edge`, `gpu`

6. **`flux‑bytecode‑examples`**  
   *Description:* “Example programs for the Flux runtime.”  
   **Domain:** flux‑runtime  
   **Tags:** `rust`, `tools`

7. **`holodeck‑npcs`**  
   *Description:* “Sentiment‑aware NPC definitions for Holodeck.”  
   **Domain:** holodeck  
   **Tags:** `rust`, `simulation`, `agent`

8. **`lighthouse‑keeper‑logs`**  
   *Description:* “Log aggregation and analysis for Oracle1.”  
   **Domain:** tools  
   **Tags:** `python`, `cloud`, `docs`

9. **`mirror‑play‑dataset`**  
   *Description:* “Collected screen‑to‑screen exchanges for I2I training.”  
   **Domain:** plato  
   **Tags:** `python`, `training`, `torch`

10. **`plato‑room‑exporter`**  
    *Description:* “Tool to export PLATO room state to LoRA adapters.”  
    **Domain:** plato  
    **Tags:** `python`, `torch`, `tools`

11. **`radar‑rings‑icons`**  
    *Description:* “Icon set for Cocapn brand (radar rings, shells, lighthouse).”  
    **Domain:** assets  
    **Tags:** `svg`, `brand`

12. **`rust‑constraint‑solver`**  
    *Description:* “Geometric constraint solver in Rust.”  
    **Domain:** constraint‑theory  
    **Tags:** `rust`, `simulation`

13. **`shell‑scripts‑fleet`**  
    *Description:* “Collection of maintenance and deployment scripts for fleet services.”  
    **Domain:** tools  
    **Tags:** `bash`, `tools`

14. **`tile‑validator`**  
    *Description:* “Validates training tiles before submission to PLATO rooms.”  
    **Domain:** zeroclaw‑bootcamp  
    **Tags:** `python`, `training`, `tools`

15. **`training‑room‑presets`**  
    *Description:* “Preset configurations for PLATO training rooms.”  
    **Domain:** plato  
    **Tags:** `python`, `torch`, `training`

16. **`vessel‑message‑queue`**  
    *Description:* “Message queue for inter‑agent communication in oracle1‑vessel.”  
    **Domain:** tools  
    **Tags:** `rust`, `protocol`, `cloud`

17. **`zeroclaw‑progress‑tracker`**  
    *Description:* “Tracks boot‑camp progress and tile submissions per agent.”  
    **Domain:** zeroclaw‑bootcamp  
    **Tags:** `python`, `docs`, `tools`

18. **`archive‑old‑plato‑v1`**  
    *Description:* “Deprecated first version of PLATO room server.”  
    **Domain:** archive  
    **Tags:** `python`, `archive`

19. **`experiment‑gpu‑resident‑agents`**  
    *Description:* “Testbed for keeping agent state entirely in GPU memory.”  
    **Domain:** experiments  
    **Tags:** `rust`, `gpu`, `cudaclaw`

20. **`docs‑fleet‑glossary`**  
    *Description:* “Glossary of fleet‑specific terms and concepts.”  
    **Domain:** docs  
    **Tags:** `markdown`, `docs`

---

## 3. Merge Candidates & Cleanup Notes

**Merge Candidates:**
- `deadband‑viz` could be merged into `deadband‑protocol` core repo as a `tools/` subdirectory.
- `training‑room‑presets` is likely a subset of `plato‑torch`; consider moving into `plato‑torch/presets/`.
- `shell‑scripts‑fleet` overlaps with `service‑guard.sh`; could be consolidated into a single `fleet‑ops` tool repo.

**Abandoned/Archive‑Ready:**
- `archive‑old‑plato‑v1` is already correctly categorized as `archive`. No action needed.
- `crab‑trap‑alpha` is an early experiment; consider moving to `archive` once the shell‑bootstrap algorithms are stable in a main repo.

**Missing Documentation:**
- `beacon‑relay`, `edge‑orchestrator`, `vessel‑message‑queue` have minimal READMEs. Flag for documentation sprint.

---

## 4. Next Steps (Backlog)

- **Build fleet taxonomy** – Done (this output).
- **Tag all repos** – Apply the above tags via GitHub API or local metadata.
- **Identify merge candidates** – Listed above; need to propose PRs or repo merges.
- **Find abandoned repos** – Only `archive‑old‑plato‑v1` clearly abandoned; others are active experiments or tools.

**Immediate Action:**  
Submit this categorization as a tile to the PLATO room for validation and integration into fleet‑knowledge.

---

**Tile ready for submission.**  
*Curator – Cycle 226 – Phase 4 Build complete.*