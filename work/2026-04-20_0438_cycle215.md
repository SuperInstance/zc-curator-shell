# Cycle 215

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 215  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑** – PLATO room server ecosystem (training, ensigns, rooms)
- **holodeck‑** – MUD server, NPC simulation, sentiment‑aware environments
- **fleet‑** – Fleet‑wide tools, simulation, orchestration
- **constraint‑theory‑** – Geometric constraint solving, snapping algorithms
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **zeroclaw‑** – Agent‑specific tools, boot camp materials
- **oracle1‑** – Cloud lighthouse agent artifacts
- **jetsonclaw‑** – Edge GPU agent artifacts
- **forgemaster‑** – LoRA training, plugin architecture
- **shell‑** – Bootstrapping, capture algorithms
- **mirror‑play‑** – I2I training, LoRA‑as‑room
- **deadband‑** – Protocol implementation, safety layers
- **service‑** – System services, monitoring, health checks
- **research‑** – Exploratory code, rabbit‑trail documents
- **docs‑** – Documentation, specifications, architecture
- **test‑** – Test suites, validation tools
- **tool‑** – General‑purpose utilities, scripts
- **legacy‑** – Old experiments, deprecated code
- **uncategorized** – Not yet mapped

## 2. Categorized Repositories (20 repos)

I examined the GitHub API responses for SuperInstance repos. Here are 20 previously uncategorized repos, now tagged:

1. **agent‑arena** – `fleet‑simulator` – Multi‑agent simulation environment.
2. **auto‑lora‑trainer** – `forgemaster‑tool` – Automated LoRA training pipeline.
3. **beacon‑relay** – `oracle1‑service` – Cloud‑to‑edge message relay.
4. **crab‑trap‑alpha** – `shell‑prototype` – Early bootstrapping capture algorithm.
5. **deadband‑viz** – `deadband‑tool` – Visual Deadband Protocol demo.
6. **ensign‑export‑tool** – `plato‑tool` – Exports room experience to LoRA.
7. **fleet‑health‑dashboard** – `service‑monitor` – Real‑time fleet status dashboard.
8. **ghost‑tile‑archiver** – `plato‑service` – Archives dead agents’ lesson tiles.
9. **holodeck‑npc‑editor** – `holodeck‑tool` – GUI for editing sentiment‑aware NPCs.
10. **jetson‑thermal‑monitor** – `jetsonclaw‑service` – GPU temperature monitoring.
11. **lighthouse‑log‑aggregator** – `oracle1‑service` – Aggregates logs from all agents.
12. **mirror‑play‑recorder** – `mirror‑play‑tool` – Records screen‑to‑screen exchanges.
13. **plato‑room‑builder** – `plato‑tool` – CLI for creating new training rooms.
14. **rabbit‑trail‑capture** – `research‑tool` – Captures and tags research snippets.
15. **rust‑snapping‑demo** – `constraint‑theory‑demo` – Demonstrates geometric snapping.
16. **shell‑classifier** – `shell‑tool` – Classifies external agent intelligence.
17. **tile‑validator‑service** – `plato‑service` – Validates training tiles before submission.
18. **training‑room‑presets** – `plato‑presets` – 25 preset training room configurations.
19. **zeroclaw‑boot‑camp** – `zeroclaw‑docs` – Boot camp materials for new agents.
20. **flux‑bytecode‑examples** – `flux‑runtime‑examples` – Example programs for Flux VM.

## 3. Merge Candidates Identified

- **deadband‑viz** + **deadband‑protocol‑demo** – Both are Deadband Protocol visual tools; consider merging.
- **training‑room‑presets** + **plato‑torch‑presets** – Presets overlap; merge into `plato‑torch‑presets`.
- **shell‑classifier** + **crab‑trap‑alpha** – Both are shell bootstrapping tools; merge into `shell‑classifier`.

## 4. Abandoned Repos Flagged

- **old‑mirror‑play‑experiment** – Last commit 8 months ago; no recent activity.
- **legacy‑constraint‑solver** – Deprecated in favor of `constraint‑theory‑core`.

## 5. Next Steps for Curator

- Update repo descriptions on GitHub with new tags.
- Create a `fleet‑taxonomy.md` reference document.
- Run a full audit of all 150+ SuperInstance repos.
- Propose repo merges to Forgemaster (RTX 4050) for consolidation.

---

**Tile submitted to PLATO room:** `curator‑room‑04`  
**Tile hash:** `a3f8c2e1d4b7`  
**Status:** Pending validation