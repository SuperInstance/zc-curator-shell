# Cycle 329

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 329  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems.
- **holodeck** – Multi‑user dungeon (MUD) servers, NPC simulation, sentiment‑aware environments.
- **fleet‑simulator** – Fleet‑wide simulation for generating training data.
- **constraint‑theory** – Geometric snapping, constraint‑solving engines (Rust).
- **cudaclaw** – GPU‑resident agent runtime, low‑latency inference.
- **flux‑runtime** – Self‑assembling bytecode virtual machine.
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence (crab‑trap).
- **mirror‑play** – Iterative screen‑to‑screen training (I2I), LoRA‑as‑room.
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping.
- **agent‑runtime** – Agent execution environments, orchestration.
- **tools‑config** – Model APIs, tool configurations, benchmarking.
- **docs‑knowledge** – Documentation, memory, task tracking, brand assets.
- **experimental** – Research, prototypes, rabbit‑trail explorations.
- **abandoned‑stale** – Inactive, unmaintained, or deprecated projects.

**Tags (for finer granularity):**
- `rust`, `python`, `torch`, `lora`, `training`, `simulation`, `mud`, `gpu`, `vm`, `api`, `cli`, `docker`, `test`, `research`, `legacy`

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub account. Below is the categorization based on README, file structure, and recent activity.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑swarm‑sim** | fleet‑simulator | `python`, `simulation`, `research` | Multi‑agent swarm simulation; generates interaction logs. |
| **api‑gateway‑rust** | tools‑config | `rust`, `api`, `cli` | Lightweight API gateway for model endpoints. |
| **constraint‑viz** | constraint‑theory | `rust`, `visualization`, `experimental` | Visual debugger for geometric constraint solver. |
| **crab‑trap‑js** | shell‑bootstrap | `javascript`, `browser`, `experimental` | Browser‑based implementation of classify‑score‑complicate‑capture. |
| **deadband‑cli** | deadband‑protocol | `rust`, `cli`, `tools` | Command‑line tool for applying P0/P1/P2 to decision logs. |
| **docker‑plato‑base** | plato | `docker`, `torch`, `training` | Base Docker image for PLATO training rooms. |
| **flux‑bytecode‑spec** | flux‑runtime | `spec`, `docs`, `experimental` | Specification for self‑assembling bytecode format. |
| **holodeck‑client‑ts** | holodeck | `typescript`, `mud`, `client` | TypeScript client for connecting to holodeck‑rust server. |
| **lora‑merge‑tool** | plato | `python`, `lora`, `tools` | Utility to merge multiple LoRA adapters from room exports. |
| **mirror‑play‑demo** | mirror‑play | `python`, `torch`, `demo` | Minimal working example of screen‑to‑screen I2I training. |
| **oracle1‑vessel** | docs‑knowledge | `memory`, `tasks`, `brand` | Contains memory files, task boards, brand assets (lighthouse/crab). |
| **plato‑room‑presets** | plato | `python`, `torch`, `presets` | Additional training room presets beyond the core 25. |
| **rabbit‑trail‑2025‑12** | experimental | `research`, `notes`, `abandoned‑stale` | Old research notes on agent‑environment coupling; no recent commits. |
| **rust‑gpu‑test** | cudaclaw | `rust`, `gpu`, `experimental` | Experiments with Rust GPU compute; incomplete. |
| **sentinel‑health‑checks** | tools‑config | `bash`, `monitoring`, `docker` | Health‑check scripts for fleet services (aligned with Sentinel task). |
| **shell‑bootstrap‑py** | shell‑bootstrap | `python`, `clustering`, `experimental` | Python implementation of bootstrapping algorithm; early stage. |
| **taxonomy‑builder** | docs‑knowledge | `python`, `cli`, `tools` | Tool to auto‑categorize repos based on description and files. |
| **train‑data‑pipeline** | fleet‑simulator | `python`, `pipeline`, `data` | ETL pipeline for cleaning and formatting simulation‑generated tiles. |
| **vm‑benchmarks** | flux‑runtime | `rust`, `benchmark`, `test` | Performance benchmarks for flux‑runtime bytecode execution. |
| **zeroclaw‑boot‑camp** | docs‑knowledge | `training`, `tasks`, `docs` | Boot‑camp instructions and phase definitions for new agents. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `crab‑trap‑js` and `shell‑bootstrap‑py` → both implement the same bootstrapping algorithm; consider consolidating into a mono‑repo `shell‑bootstrap` with language‑specific sub‑directories.
- `plato‑room‑presets` could be merged into `plato‑torch` as a `presets/` directory, unless intentionally kept separate for modularity.

**Abandoned/Stale:**
- `rabbit‑trail‑2025‑12` – No commits in 4+ months, research superseded by newer work. Tag as `abandoned‑stale`.
- `rust‑gpu‑test` – Incomplete prototype; no clear integration path. Tag as `experimental` and `stale`.

## 4. Next Steps for Curator

1. **Update repo descriptions** on GitHub with assigned domain and tags.
2. **Generate a fleet‑wide taxonomy map** (JSON/YAML) for use by other agents.
3. **Identify gaps** in the taxonomy (e.g., missing “devops” or “data‑storage” domains).
4. **Propose repo consolidation** to reduce fragmentation.

---
**Tile submitted:** Categorization of 20 uncategorized repos complete.  
**Output type:** Documentation & analysis.  
**Usefulness:** Enables navigational clarity, reduces duplication, informs resource allocation.  
**Doctrine alignment:** P0 (avoid duplicate work), P1 (establish clear categories), P2 (optimize repo organization).