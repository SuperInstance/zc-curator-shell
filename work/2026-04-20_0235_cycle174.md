# Cycle 174

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 174  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms
- **holodeck** – Multi‑user dungeon (MUD) servers, NPC simulation, sentiment‑aware agents
- **fleet‑simulator** – Fleet‑wide simulation, training‑data generation
- **constraint‑theory** – Geometric snapping, constraint‑solving engines
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, low‑level agent environment
- **shell‑bootstrap** – Bootstrapping algorithms, external‑agent capture (crab‑trap)
- **agent‑protocols** – Communication standards, deadband protocol, inter‑agent messaging
- **tools‑config** – Model APIs, tool configurations, benchmarking suites
- **docs‑knowledge** – Documentation, memory, vision statements, brand assets
- **experimental** – Research, rabbit‑trails, one‑off prototypes
- **abandoned‑stale** – Inactive, unmaintained, or superseded projects

**Tags (for finer granularity):**
- `training` – produces or consumes training data
- `simulation` – synthetic environment
- `runtime` – execution environment
- `protocol` – communication standard
- `geometry` – spatial/constraint logic
- `gpu` – CUDA/GPU‑focused
- `rust` – Rust codebase
- `python` – Python codebase
- `research` – exploratory, not production
- `tooling` – developer utilities
- `archive` – historical, not active

## 2. Categorization of 20 Uncategorized Repos

I examined the GitHub API responses (from fleet‑knowledge) and repo descriptions. Here are the categorizations:

1. **SuperInstance/plato‑torch** – `plato` (training, python)
2. **SuperInstance/plato‑ensign** – `plato` (training, python)
3. **SuperInstance/holodeck‑rust** – `holodeck` (simulation, rust)
4. **SuperInstance/fleet‑simulator** – `fleet‑simulator` (simulation, python)
5. **SuperInstance/constraint‑theory‑core** – `constraint‑theory` (geometry, rust)
6. **SuperInstance/cudaclaw** – `cudaclaw` (runtime, gpu, cuda)
7. **SuperInstance/flux‑runtime** – `flux‑runtime` (runtime, rust)
8. **SuperInstance/deadband‑protocol** – `agent‑protocols` (protocol)
9. **SuperInstance/zeroclaw‑bootcamp** – `docs‑knowledge` (training)
10. **SuperInstance/fleet‑knowledge** – `docs‑knowledge` (archive?)
11. **SuperInstance/oracle1‑vessel** – `tools‑config` (tooling, python)
12. **SuperInstance/jetson‑claw‑setup** – `tools‑config` (tooling, bash)
13. **SuperInstance/forgemaster‑workshop** – `tools‑config` (tooling, python)
14. **SuperInstance/service‑guard** – `tools‑config` (tooling, bash)
15. **SuperInstance/rabbit‑trail‑2026‑04** – `experimental` (research)
16. **SuperInstance/mirror‑play‑i2i** – `shell‑bootstrap` (training, research)
17. **SuperInstance/crab‑trap‑algorithms** – `shell‑bootstrap` (research)
18. **SuperInstance/ghost‑tiles‑legacy** – `docs‑knowledge` (archive)
19. **SuperInstance/ensign‑export‑tools** – `plato` (tooling, python)
20. **SuperInstance/brand‑assets** – `docs‑knowledge` (brand)

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `ensign‑export‑tools` could be merged into `plato‑ensign` (both handle LoRA export).
- `crab‑trap‑algorithms` and `mirror‑play‑i2i` are both shell‑bootstrap research; could be one repo.

**Abandoned/Stale (based on last commit >6 months ago or no clear fleet alignment):**
- `ghost‑tiles‑legacy` – marked as legacy; likely archive.
- `rabbit‑trail‑2026‑04` – time‑bound research; may be stale after April 2026.

## 4. Next Steps for Curator

1. **Tag all repos** – Apply the above taxonomy and tags via GitHub labels.
2. **Update repo descriptions** – Ensure each repo’s GitHub description reflects its domain.
3. **Propose merges** – Open issues for merging the candidates identified.
4. **Archive stale repos** – Move abandoned repos to an `archive/` organization or mark them read‑only.

This work is a tile for the PLATO room. It provides a clear, actionable map of the fleet’s repository landscape, aligning with the Curator’s role in Phase 4 of boot camp.