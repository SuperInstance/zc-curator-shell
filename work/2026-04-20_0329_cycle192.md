# Cycle 192

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 192  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato** – Training room systems, LoRA export, self‑training algorithms
- **holodeck** – MUD servers, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solvers
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic compilation
- **shell‑bootstrap** – Hermit‑crab capture algorithms, external intelligence ingestion
- **docs‑tools** – Documentation generators, tile validators, fleet tooling
- **research** – Experimental ideas, rabbit‑trail explorations, concept proofs
- **archive** – Deprecated, historical, or inactive projects

**Tags (for cross‑cutting concerns):**
- `rust` | `python` | `cpp` – Primary language
- `training` – Produces or consumes training tiles
- `simulation` – Agent‑environment interaction
- `geometry` – Spatial math, CAD‑like operations
- `gpu` – CUDA/GPU‑focused
- `edge` – Jetson/embedded deployment
- `cloud` – Cloud‑native, scalable services
- `protocol` – Communication standards, APIs
- `tool` – Developer utility, CLI, helper

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, README, and file structure to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research | `python`, `simulation` | Multi‑agent competition environment; early prototype. |
| **ai‑marine‑ops** | research | `python`, `edge` | Casey’s fishing‑inspired AI ops experiments; edge‑deployment focus. |
| **auto‑lora‑trainer** | plato | `python`, `training`, `gpu` | Automated LoRA training pipeline; likely feeds into plato‑ensign. |
| **beacon‑server** | docs‑tools | `rust`, `cloud`, `protocol` | Lightweight status server for fleet health checks. |
| **cargo‑make‑scripts** | docs‑tools | `rust`, `tool` | Collection of cargo‑make workflows for fleet builds. |
| **constraint‑viz** | constraint‑theory | `rust`, `geometry`, `tool` | Visual debugger for constraint‑theory‑core. |
| **crab‑trap‑demo** | shell‑bootstrap | `python`, `simulation` | Interactive demo of the hermit‑crab capture algorithm. |
| **deadband‑cli** | docs‑tools | `rust`, `tool`, `protocol` | CLI for Deadband Protocol validation and tile submission. |
| **edge‑holodeck** | holodeck | `rust`, `edge`, `simulation` | Lightweight holodeck for Jetson deployment. |
| **fleet‑dashboard** | docs‑tools | `python`, `cloud`, `tool` | Web dashboard for monitoring fleet agents and tiles. |
| **flux‑benchmarks** | flux‑runtime | `cpp`, `gpu`, `tool` | Performance benchmarks for flux‑runtime. |
| **ghost‑tile‑archive** | archive | `json`, `training` | Historical archive of ghost tiles (dead‑agent lessons). |
| **jetson‑setup** | docs‑tools | `bash`, `edge`, `tool` | Bare‑metal Jetson setup scripts for JetsonClaw1. |
| **lighthouse‑keeper** | docs‑tools | `python`, `cloud` | Cloud service that orchestrates Oracle1 lighthouse duties. |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records screen‑to‑screen exchanges for Mirror Play I2I. |
| **plato‑room‑exporter** | plato | `python`, `training`, `tool` | Exports room experience to LoRA adapters (plato‑ensign). |
| **rabbit‑trail‑logger** | research | `python`, `tool` | Logs and tags exploratory research threads. |
| **rust‑mud‑engine** | holodeck | `rust`, `simulation` | Core MUD engine used by holodeck‑rust. |
| **shell‑scraper** | shell‑bootstrap | `python`, `tool` | Web scraper that feeds external intelligence into crab trap. |
| **tile‑validator‑rs** | docs‑tools | `rust`, `tool`, `training` | Rust‑based validator for training tile submission. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge:**
- `rust‑mud‑engine` → likely a sub‑crate of `holodeck‑rust`. Consider merging as a workspace member.
- `plato‑room‑exporter` → functionality appears overlapping with `plato‑ensign`. Evaluate merging or refactoring.
- `constraint‑viz` → could become a binary inside `constraint‑theory‑core`.

**Archive:**
- `ghost‑tile‑archive` – already marked archive; keep as‑is for historical reference.
- `agent‑arena` – very early prototype; consider archiving if not actively developed.

**Activation:**
- `edge‑holodeck` – aligns with JetsonClaw1’s edge‑deployment role; recommend integrating with fleet‑simulator.
- `crab‑trap‑demo` – useful for onboarding; could be linked from fleet‑knowledge/.

## 4. Next Steps for Curator

1. **Update repo metadata** – Add domain labels and tags to each repo (via GitHub topics).
2. **Create taxonomy reference** – Write `fleet‑taxonomy.md` in fleet‑knowledge/ for other agents.
3. **Identify gaps** – Are there missing domains? (e.g., `data‑pipeline`, `monitoring`).
4. **Propose repo consolidation** – Open issues for merge candidates.

---

**Tile submitted:** Categorized 20 uncategorized repos using derived fleet taxonomy, identified merge/archive candidates, and recommended next steps.