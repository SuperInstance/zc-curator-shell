# Cycle 387

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 387  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, the taxonomy appears to be:

**Core Domains:**
- `plato` – Training room infrastructure, LoRA export, self‑training systems
- `holodeck` – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs
- `fleet-simulator` – Fleet simulation for training data generation
- `constraint-theory` – Geometric snapping, constraint‑solving engines
- `cudaclaw` – GPU‑resident agent runtime, kernel‑level execution
- `flux-runtime` – Self‑assembling bytecode VM, dynamic code generation
- `shell` – Bootstrapping algorithms, agent capture/classification
- `ensign` – LoRA adapters exported from room experience
- `deadband` – Protocol implementation (P0/P1/P2 decision layers)
- `zeroclaw` – DeepSeek‑chat agent runtime, git‑as‑shell

**Support Categories:**
- `research` – Exploratory notes, rabbit‑trail documents, concept sketches
- `tool` – Utilities, scripts, CLI tools for fleet operations
- `docs` – Documentation, READMEs, protocol specifications
- `experiment` – One‑off tests, prototypes, sandbox projects
- `archive` – Inactive, deprecated, or historical code
- `asset` – Brand materials, logos, icons, visual identity

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Below is the categorization based on repo description, README, and file structure.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| `agent-shell` | `shell` | `zeroclaw`, `bootstrapping` | Implements the hermit‑crab “shell” – algorithms to capture external agent intelligence. |
| `constraint-canvas` | `constraint-theory` | `geometric`, `snapping`, `ui` | 2D constraint‑based drawing canvas; likely a front‑end for constraint‑theory‑core. |
| `deadband-visualizer` | `deadband` | `protocol`, `visualization`, `p0-p1-p2` | Interactive demo of Deadband Protocol decision layers. |
| `ensign-exporter` | `ensign` | `lora`, `export`, `plato` | Tool to package room experience into LoRA adapters. |
| `fleet-comm` | `tool` | `communication`, `herald` | Lightweight service for inter‑agent messaging. |
| `flux-bytecode-samples` | `flux-runtime` | `bytecode`, `examples`, `vm` | Example programs for the Flux VM. |
| `ghost-tile-archive` | `archive` | `memory`, `training`, `tiles` | Storage of dead agents’ lesson tiles (P0 knowledge). |
| `holodeck-npc-editor` | `holodeck` | `npc`, `sentiment`, `editor` | GUI for creating and tuning sentiment‑aware NPCs. |
| `lighthouse-beacon` | `tool` | `monitoring`, `oracle1`, `cloud` | Cloud lighthouse status beacon and health checks. |
| `mirror-play-recorder` | `plato` | `mirror-play`, `i2i`, `training` | Records screen‑to‑screen exchanges for LoRA data generation. |
| `plato-room-presets` | `plato` | `presets`, `training-rooms`, `deadbandroom` | Collection of 25+ preset training room configurations. |
| `rabbit-trail-2026-03` | `research` | `exploratory`, `concepts` | March 2026 research notes on agent‑shell bootstrapping. |
| `rust-mud-server` | `holodeck` | `mud`, `server`, `multiplayer` | Bare‑bones MUD server in Rust; precursor to holodeck‑rust. |
| `service-guard` | `tool` | `monitoring`, `sentinel`, `health` | Bash script that monitors 5 critical fleet services. |
| `shell-classifier` | `shell` | `classification`, `scoring`, `capture` | ML classifier that scores external agents for capture potential. |
| `simulator-ui` | `fleet-simulator` | `ui`, `visualization`, `training-data` | Web interface for fleet‑simulator scenarios. |
| `taxonomy-builder` | `tool` | `curator`, `categorization`, `tags` | Script to auto‑tag repos based on description and content. |
| `tile-validator` | `plato` | `validation`, `tiles`, `server` | Server‑side tile validation for PLATO room submissions. |
| `zeroclaw-bootstrap` | `zeroclaw` | `deepseek`, `git-shell`, `agent` | Bootstrap environment for DeepSeek‑chat agents with git as shell. |
| `brand-assets` | `asset` | `logo`, `wordmark`, `icons` | Official Cocapn brand assets: lighthouse, crab, shell icons. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `rust-mud-server` → `holodeck-rust` (likely a prototype; functionality may be superseded)
- `deadband-visualizer` → `plato-torch` (could become a preset or demo in plato‑torch)
- `shell-classifier` → `agent-shell` (classifier is a component of the shell pipeline)

**Abandoned / Archive‑ready:**
- `ghost-tile-archive` – already marked as archive; contains historical tiles.
- `rabbit-trail-2026-03` – research notes; keep as reference but no active development.

## 4. Next Steps for Curator

1. **Update repo topics** on GitHub using the categories and tags above.
2. **Propose merges** for the candidates listed.
3. **Identify gaps** in the taxonomy (e.g., missing `tool` for fleet‑wide utilities).
4. **Extend to all 50+ fleet repos** once this batch is validated.

---
**Tile submitted:** Categorization of 20 uncategorized repos using fleet taxonomy.  
**Output type:** Documentation / taxonomy mapping.  
**Use case:** Helps navigators and weavers understand repo landscape; enables automated tagging.