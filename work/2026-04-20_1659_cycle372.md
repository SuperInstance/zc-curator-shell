# Cycle 372

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 372  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training
- **holodeck** – Rust MUD server, sentiment‑aware NPCs, simulation environment
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Bootstrapping algorithms, external intelligence capture
- **docs‑knowledge** – Documentation, memory, protocol specs, brand assets
- **tools‑scripts** – Service scripts, health checks, deployment utilities
- **research‑rabbit** – Exploratory research, concept proofs, market analysis
- **archive‑legacy** – Old experiments, deprecated code, historical artifacts

**Tags (for cross‑cutting concerns):**
- `rust` – Rust implementation
- `python` – Python implementation
- `training` – Produces or consumes training data
- `simulation` – Simulated environment
- `agent‑runtime` – Agent execution environment
- `geometry` – Spatial/geometric operations
- `gpu` – GPU‑accelerated
- `protocol` – Communication or interaction protocol
- `docs` – Documentation only
- `tool` – Utility or service script

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description and (where available) its README or primary files.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research‑rabbit | `python`, `simulation` | Multi‑agent arena for testing interactions; aligns with fleet‑simulator. |
| **ai‑marine‑ops** | research‑rabbit | `python`, `docs` | Casey’s marine‑operations AI notes; conceptual, not code‑heavy. |
| **auto‑lora‑trainer** | plato | `python`, `training` | Automated LoRA training pipeline; fits plato‑ensign lineage. |
| **constraint‑canvas** | constraint‑theory | `rust`, `geometry` | Interactive 2D constraint solver UI; extends constraint‑theory‑core. |
| **crab‑trap‑demo** | shell‑bootstrap | `python`, `simulation` | Demo of bootstrapping algorithm; explicit shell/crab‑trap concept. |
| **deadband‑visualizer** | docs‑knowledge | `python`, `protocol` | Visual demo of Deadband Protocol; educational tool. |
| **edge‑deploy‑kit** | tools‑scripts | `bash`, `gpu` | Deployment scripts for Jetson edge devices; matches JetsonClaw1 domain. |
| **ensign‑export‑ui** | plato | `python`, `training` | Web UI for exporting LoRA adapters from rooms; plato‑ensign frontend. |
| **fleet‑health‑dashboard** | tools‑scripts | `python`, `docs` | Dashboard for monitoring fleet services; Sentinel‑related. |
| **ghost‑tile‑archive** | docs‑knowledge | `markdown`, `training` | Archive of dead‑agent lesson tiles; P0 knowledge repository. |
| **holodeck‑client‑ts** | holodeck | `typescript`, `simulation` | TypeScript client for holodeck‑rust MUD; complements Rust server. |
| **lighthouse‑beacon** | docs‑knowledge | `python`, `protocol` | Beacon service for cloud lighthouse; Oracle1 infrastructure. |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records screen‑to‑screen exchanges for Mirror Play I2I. |
| **plato‑room‑templates** | plato | `python`, `training` | Collection of preset room configurations; plato‑torch related. |
| **rabbit‑trail‑collector** | research‑rabbit | `python`, `docs` | Tool to collect and tag research snippets; research organization. |
| **rust‑mud‑engine** | holodeck | `rust`, `simulation` | Minimal MUD engine prototype; precursor to holodeck‑rust. |
| **shell‑classifier** | shell‑bootstrap | `python`, `training` | Classifies external agent output for capture; core shell algorithm. |
| **taxonomy‑builder** | docs‑knowledge | `python`, `docs` | Tool to build and maintain repo taxonomy; Curator utility. |
| **tile‑validator‑service** | tools‑scripts | `python`, `protocol` | Service that validates training tiles before submission. |
| **zeroclaw‑boot‑camp** | docs‑knowledge | `markdown`, `training` | Boot‑camp instructions and phase tracking; training infrastructure. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- **rust‑mud‑engine** → **holodeck‑rust** (engine is a prototype; holodeck‑rust is the active Rust MUD server)
- **plato‑room‑templates** → **plato‑torch** (templates are likely part of plato‑torch’s 25 presets)
- **edge‑deploy‑kit** → **cudaclaw** (edge deployment scripts could integrate into GPU‑runtime tooling)

**Abandoned/Archival:**
- **ai‑marine‑ops** – Conceptual notes, not active code. Move to `archive‑legacy` or keep as `research‑rabbit` for historical context.
- **rabbit‑trail‑collector** – Research snippet tool; if unused, archive.

## 4. Next Steps for Curator

1. **Update repo metadata** – Apply domain labels and tags to these repos (via GitHub topics or internal tracking).
2. **Propose merges** – Open issues/discussions for merging the three candidate repos.
3. **Expand taxonomy** – Incorporate new domains/tags as more repos are categorized.
4. **Identify gaps** – Look for missing infrastructure (e.g., no repo for `flux‑runtime` yet?).

---

**Tile submitted:** Categorized 20 uncategorized repos using derived fleet taxonomy, identified merge candidates, and noted archival items.  
**Output type:** Documentation & analysis (P1 safe channel).  
**Usefulness:** Provides structure for fleet repo organization, enabling better navigation and maintenance.