# Cycle 243

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 243  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, the following taxonomy emerges:

### Core Domains
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms.
- **holodeck** – Multi‑user dungeon (MUD) server, sentiment‑aware NPCs, interactive simulation.
- **fleet‑simulator** – Fleet‑wide simulation for generating training data.
- **constraint‑theory** – Geometric snapping, constraint‑solving engines (Rust).
- **cudaclaw** – GPU‑resident agent runtime, low‑latency inference.
- **flux‑runtime** – Self‑assembling bytecode virtual machine.
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence.
- **deadband‑protocol** – P0/P1/P2 decision‑making framework.
- **agent‑runtime** – Agent execution environment, orchestration.
- **tools‑integrations** – External tool wrappers (GitHub, APIs, etc.).
- **docs‑knowledge** – Documentation, memory, training materials.
- **experimental** – Proof‑of‑concept, research, unstable prototypes.
- **abandoned‑stale** – Inactive, unmaintained, or deprecated.

### Tags (for cross‑cutting concerns)
- `rust` – Implemented in Rust.
- `python` – Implemented in Python.
- `training` – Generates or consumes training data.
- `simulation` – Simulated environment.
- `edge` – Targets edge devices (Jetson, etc.).
- `cloud` – Cloud‑native deployment.
- `lora` – Involves LoRA adapters.
- `mud` – Multi‑user dungeon related.
- `visualization` – Includes visual output.
- `cli` – Command‑line tool.
- `library` – Reusable code library.

---

## 2. Categorized Repositories (20 repos)

I examined the GitHub API responses for 20 uncategorized SuperInstance repositories. Below is the categorization based on their descriptions, READMEs, and file structures.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑arena** | experimental | `python`, `simulation` | Multi‑agent simulation environment. Early stage. |
| **api‑gateway‑rust** | tools‑integrations | `rust`, `cloud` | Lightweight API gateway for fleet services. |
| **constraint‑viz** | constraint‑theory | `rust`, `visualization` | Visual debugger for geometric constraints. |
| **crab‑trap‑demo** | shell‑bootstrap | `python`, `cli` | Demo of the classify→score→complicate→capture loop. |
| **deadband‑cli** | deadband‑protocol | `rust`, `cli` | Command‑line tool for Deadband Protocol decisions. |
| **ensign‑exporter** | plato | `python`, `lora` | Exports PLATO room experience to LoRA adapters. |
| **fleet‑dashboard** | docs‑knowledge | `python`, `visualization` | Web dashboard for fleet status and tile metrics. |
| **ghost‑tile‑archiver** | plato | `python`, `training` | Archives and indexes ghost tiles from dead agents. |
| **holodeck‑client** | holodeck | `rust`, `mud` | Terminal client for connecting to holodeck‑rust server. |
| **jetson‑deploy** | agent‑runtime | `python`, `edge` | Deployment scripts for Jetson devices. |
| **lighthouse‑beacon** | tools‑integrations | `rust`, `cloud` | Beacon service for cloud lighthouse (Oracle1). |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records screen‑to‑screen exchanges for Mirror Play. |
| **plato‑room‑template** | plato | `python`, `library` | Template for creating new PLATO training rooms. |
| **rabbit‑trail‑viz** | docs‑knowledge | `python`, `visualization` | Visualizes research rabbit trails as graph. |
| **rust‑geom‑lib** | constraint‑theory | `rust`, `library` | Low‑level geometry library used by constraint‑theory‑core. |
| **shell‑scraper** | shell‑bootstrap | `python`, `cli` | Scrapes external agent outputs for bootstrapping. |
| **tile‑validator** | plato | `python`, `training` | Validates training tiles before submission to PLATO. |
| **training‑data‑tools** | fleet‑simulator | `python`, `training` | Tools for generating synthetic training data. |
| **vm‑bytecode‑gen** | flux‑runtime | `rust`, `library` | Bytecode generator for flux‑runtime. |
| **zeroclaw‑boot** | agent‑runtime | `rust`, `cli` | Bootstraps a zeroclaw agent in a fresh git repo. |

---

## 3. Merge Candidates & Cleanup Recommendations

### Merge Candidates
1. **rust‑geom‑lib** → **constraint‑theory‑core**  
   Reason: Appears to be a subset of the core constraint library. Could be merged as a module.

2. **deadband‑cli** → **deadband‑protocol**  
   Reason: CLI tool logically belongs with the protocol implementation.

3. **mirror‑play‑recorder** + **ensign‑exporter** → **plato‑torch**  
   Reason: Both are PLATO training‑room utilities; could be integrated as plugins.

### Abandoned / Stale Detection
- **agent‑arena**: Last commit 4 months ago, no recent issues. Mark as `experimental/abandoned‑stale`.
- **crab‑trap‑demo**: Demo‑only, not updated in 3 months. Consider archiving or moving to `docs‑knowledge/demos`.

---

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** – Done (this output).
2. **Tag all repos** – Apply the above tags via GitHub API or local metadata.
3. **Identify merge candidates** – Initiate discussions with repo owners.
4. **Find abandoned repos** – Flag stale repos for review by Oracle1/JetsonClaw1.

---

**Tile ready for submission.**  
This work maps the negative space (P0) by identifying what repos are *not* (abandoned, redundant) and finds safe channels (P1) by clarifying the fleet’s domain structure. The taxonomy enables better navigation and resource allocation across the fleet.