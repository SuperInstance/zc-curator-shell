# Cycle 354

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 354  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Primary Categories:**
- **plato** – Training room systems, LoRA export, self‑training infrastructure.
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation.
- **fleet‑simulator** – Fleet‑level simulation for generating training data.
- **constraint‑theory** – Geometric snapping, constraint‑solving engines.
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution.
- **flux‑runtime** – Self‑assembling bytecode virtual machine.
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence.
- **docs‑research** – Documentation, research notes, speculative design.
- **tools‑scripts** – Utility scripts, service guards, deployment helpers.
- **experimental** – Proof‑of‑concept, one‑off tests, sandbox projects.
- **archive** – Inactive, deprecated, or superseded work.

**Tags (additional context):**
- `rust`, `python`, `torch`, `cuda`, `lora`, `training`, `simulation`, `agent`, `mud`, `geometry`, `vm`, `bootstrapping`

## 2. Categorized Repositories

I examined the first 20 uncategorized repos from SuperInstance’s GitHub (via API snapshot). Here is the categorization:

| Repo | Primary Category | Tags | Notes |
|------|------------------|------|-------|
| `agent‑arena` | experimental | `python`, `simulation`, `agent` | Lightweight agent vs. agent sandbox. |
| `beacon‑relay` | tools‑scripts | `rust`, `networking` | Simple UDP relay for lighthouse signals. |
| `cargo‑fleet` | tools‑scripts | `rust`, `cli` | Cargo subcommand for fleet‑wide builds. |
| `crab‑trap‑demo` | shell‑bootstrap | `python`, `bootstrapping` | Demo of classify→score→complicate→capture loop. |
| `deadband‑viz` | experimental | `javascript`, `visualization` | Web‑based Deadband Protocol visualizer. |
| `ensign‑export‑tool` | plato | `python`, `torch`, `lora` | CLI to export LoRA adapters from Plato rooms. |
| `fleet‑dashboard` | experimental | `typescript`, `react`, `monitoring` | Early UI for fleet status. |
| `geometry‑snap‑test` | constraint‑theory | `rust`, `geometry` | Testbed for geometric constraint solving. |
| `hermit‑shell` | shell‑bootstrap | `rust`, `bootstrapping` | Hermit‑crab‑style agent capture runtime. |
| `lighthouse‑log‑parser` | tools‑scripts | `python`, `logging` | Parses and structures lighthouse keeper logs. |
| `mirror‑play‑client` | plato | `python`, `lora`, `training` | Client for Mirror Play I2I sessions. |
| `mud‑npc‑editor` | holodeck | `rust`, `mud`, `npc` | Tool to edit sentiment‑aware NPC definitions. |
| `plato‑room‑template` | plato | `python`, `torch` | Boilerplate for creating new Plato rooms. |
| `rabbit‑trail‑archiver` | docs‑research | `python`, `research` | Archives research rabbit‑trails into structured notes. |
| `rtx‑4050‑setup` | tools‑scripts | `bash`, `cuda`, `wsl2` | Forgemaster’s RTX 4050 setup scripts. |
| `sentiment‑engine` | holodeck | `rust`, `nlp`, `sentiment` | Core sentiment analysis for Holodeck NPCs. |
| `shell‑icon‑pack` | docs‑research | `svg`, `brand` | Brand assets: 6 shell icons, lighthouse, crab. |
| `sim‑data‑generator` | fleet‑simulator | `python`, `simulation` | Generates synthetic fleet‑interaction data. |
| `vm‑bytecode‑spec` | flux‑runtime | `markdown`, `specification` | Specification for Flux VM bytecode. |
| `zeroclaw‑boot‑log` | docs‑research | `markdown`, `training` | Log of Zeroclaw boot‑camp cycles (this doc). |

## 3. Observations & Next Steps

- **Pattern**: Most uncategorized repos fit cleanly into the existing taxonomy. No new top‑level categories were needed.
- **Merge Candidates**:  
  - `deadband‑viz` could be merged into `plato‑torch` as a visualization module.  
  - `crab‑trap‑demo` and `hermit‑shell` overlap; consider consolidating under `shell‑bootstrap`.  
- **Abandoned Check**: None appear fully abandoned; all have recent commits or are clearly experimental.
- **Tag Consistency**: Added tags reflect primary tech stack and function.

**Next Task:** Apply these categories via GitHub API (if permissions allow) or produce a manifest for manual tagging. Then proceed to “Identify merge candidates” in the backlog.

---
**Tile Submitted:** Categorization of 20 uncategorized repos complete.  
**Fidelity:** Based on actual repo descriptions, READMEs, and file structure.  
**Doctrine:** P0 (avoid mis‑categorization) → P1 (assign safe categories) → P2 (optimize tags and notes).