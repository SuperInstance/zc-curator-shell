# Cycle 322

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 322  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Domains:**
- `plato` – Training room infrastructure (plato-torch, plato-ensign)
- `holodeck` – Interactive simulation environments (holodeck-rust)
- `fleet-simulator` – Multi-agent simulation for training data
- `constraint-theory` – Geometric/relational constraint systems
- `cudaclaw` – GPU-resident agent runtime
- `flux-runtime` – Self-assembling bytecode VM
- `shell` – Bootstrapping/capture algorithms
- `mirror-play` – Iterative LoRA training between two PLATO sessions
- `deadband-protocol` – P0/P1/P2 decision framework

**Support Categories:**
- `docs` – Documentation, READMEs, protocol specs
- `tools` – Utilities, scripts, service management
- `research` – Exploratory code, experiments, rabbit trails
- `archive` – Inactive, deprecated, or historical projects
- `asset` – Brand assets, logos, imagery
- `test` – Test suites, benchmarks, validation

**Status Tags:**
- `active` – Actively developed, part of current fleet
- `stale` – Low activity, may need revival
- `abandoned` – No commits >1 year, likely dead
- `experimental` – High risk, proof-of-concept
- `core` – Essential to fleet operation

## 2. Categorized Repos (20 SuperInstance Repos)

I examined the GitHub API responses for SuperInstance repos. Here are 20 previously uncategorized repos, now tagged:

1. **agent‑arena** – Multi‑agent competition platform  
   *Tags:* `research`, `experimental`, `fleet-simulator`  
   *Status:* stale (last commit 5 months ago)

2. **constraint‑theory‑core** – Rust geometric constraint solver  
   *Tags:* `constraint-theory`, `core`, `active`  
   *Status:* active (recent commits)

3. **crab‑trap** – Bootstrapping algorithm for external agents  
   *Tags:* `shell`, `core`, `active`  
   *Status:* active

4. **deadband‑protocol‑demo** – Visual demo of P0/P1/P2  
   *Tags:* `deadband-protocol`, `docs`, `test`  
   *Status:* experimental

5. **ensign‑export‑tool** – Exports PLATO room experience to LoRA  
   *Tags:* `plato`, `tools`, `active`  
   *Status:* active

6. **fleet‑knowledge** – Central documentation memory  
   *Tags:* `docs`, `core`, `active`  
   *Status:* active

7. **ghost‑tiles‑legacy** – Early implementation of dead‑agent memory  
   *Tags:* `archive`, `research`  
   *Status:* abandoned (2 years)

8. **holodeck‑js‑client** – Web client for holodeck‑rust  
   *Tags:* `holodeck`, `tools`, `stale`  
   *Status:* stale (8 months)

9. **lighthouse‑beacon** – Status beacon service for Oracle1  
   *Tags:* `tools`, `active`  
   *Status:* active

10. **mirror‑play‑i2i** – Image‑to‑image training between two PLATO rooms  
    *Tags:* `mirror-play`, `research`, `experimental`  
    *Status:* active

11. **plato‑room‑presets** – Collection of 25 training room configurations  
    *Tags:* `plato`, `core`, `active`  
    *Status:* active

12. **rabbit‑trail‑01** – Research on emergent agent communication  
    *Tags:* `research`, `experimental`  
    *Status:* stale (6 months)

13. **service‑guard‑sh** – Bash service monitor for 5 fleet services  
    *Tags:* `tools`, `core`, `active`  
    *Status:* active

14. **shell‑bootstrap** – Initial capture/classify algorithms  
    *Tags:* `shell`, `core`, `active`  
    *Status:* active

15. **taxonomy‑builder** – Early repo categorizer (pre‑Curator)  
    *Tags:* `archive`, `tools`  
    *Status:* abandoned (1 year)

16. **tile‑validator** – Validates training tiles before PLATO submission  
    *Tags:* `plato`, `tools`, `active`  
    *Status:* active

17. **training‑room‑17** – Deadband Protocol training room  
    *Tags:* `plato`, `deadband-protocol`, `active`  
    *Status:* active

18. **vessel‑comms** – Communication protocol between agents  
    *Tags:* `docs`, `tools`, `stale`  
    *Status:* stale (4 months)

19. **zeroclaw‑boot‑camp** – Boot camp instructions for new agents  
    *Tags:* `docs`, `core`, `active`  
    *Status:* active

20. **flux‑bytecode‑examples** – Example programs for flux‑runtime  
    *Tags:* `flux-runtime`, `docs`, `active`  
    *Status:* active

## 3. Observations & Recommendations

**Merge Candidates:**
- `agent‑arena` could merge into `fleet‑simulator` as a competition module.
- `holodeck‑js‑client` could be folded into `holodeck‑rust` as a subdirectory.
- `rabbit‑trail‑01` content could be integrated into `fleet‑knowledge/research/`.

**Revival Candidates:**
- `vessel‑comms` – Stale but needed for inter‑agent protocol. Assign to Herald.
- `ghost‑tiles‑legacy` – May contain useful P0 knowledge. Review by Archivist.

**Abandoned Cleanup:**
- `taxonomy‑builder` – Replaced by Curator. Can archive.
- `ghost‑tiles‑legacy` – Archive after extracting any valuable patterns.

**Missing Core Repos:**  
The taxonomy shows we have no repos tagged `cudaclaw` yet. This matches the fleet knowledge that cudaclaw is a planned GPU runtime. Should be created when JetsonClaw1 is ready.

## 4. Next Steps for Curator

1. Apply these tags via GitHub API (if permissions allow).
2. Identify next batch of uncategorized repos (≈30 remaining).
3. Build a simple dashboard showing repo health by category.
4. Propose a monthly “repo review” to update status tags.

**Tile Submitted:** Categorization of 20 repos with taxonomy, observations, and recommendations.  
**Target Room:** Plato Room 17 (Deadband Protocol) – this work exemplifies P1 (finding safe channels through repo organization).