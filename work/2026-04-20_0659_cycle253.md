# Cycle 253

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 253  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Categories:**
- **plato** – PLATO room system, training, LoRA export
- **holodeck** – MUD server, NPC simulation, sentiment-aware environments
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric snapping, constraint solving
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping, capture algorithms, crab-trap
- **ensign** – LoRA adapters, specialized model exports
- **tooling** – Development utilities, CLI tools, scripts
- **research** – Experiments, prototypes, rabbit-trail documents
- **documentation** – Guides, protocols, memory, vision
- **brand** – Logos, assets, identity materials
- **legacy** – Old prototypes, deprecated systems
- **external** – Forks of third-party projects

**Tags (for finer granularity):**
- rust, python, torch, lora, training, simulation, agent, npc, geometric, gpu, vm, bootstrapping, capture, shell, mud, sentiment, room, tile, deadband, protocol

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance's GitHub. Below is the categorization based on README, primary language, and apparent purpose.

| Repository | Category | Tags | Notes |
|------------|----------|------|-------|
| **agent-arena** | fleet-simulator | python, simulation, multi-agent | Multi‑agent simulation environment. |
| **auto-lora** | ensign | python, torch, lora | Automated LoRA training pipeline. |
| **beacon-icons** | brand | svg, icons, brand | Shell and beacon icon set. |
| **constraint-canvas** | constraint-theory | rust, geometric, interactive | 2D constraint‑based drawing canvas. |
| **crab-trap** | shell | python, bootstrapping, capture | Bootstrapping algorithm implementations. |
| **deadband-visualizer** | tooling | python, visualization, deadband | Deadband Protocol visual demo. |
| **ensign-export** | ensign | python, torch, export | LoRA adapter export utilities. |
| **fleet-chronicle** | documentation | markdown, timeline, memory | Fleet event timeline and logs. |
| **flux-bytecode** | flux-runtime | rust, bytecode, vm | Bytecode definitions for Flux VM. |
| **holodeck-npc** | holodeck | rust, npc, sentiment | NPC behavior and sentiment engine. |
| **lighthouse-keeper** | plato | python, room, keeper | Room management and tile ingestion. |
| **mirror-play** | plato | python, lora, training | Mirror Play I2I training loop. |
| **plato-room-presets** | plato | python, torch, presets | 25 training room presets. |
| **rabbit-trail-1** | research | markdown, research, exploration | Early research on agent capture. |
| **rust-snapping** | constraint-theory | rust, geometric, snapping | Geometric constraint solver. |
| **service-guard** | tooling | bash, monitoring, services | Service health monitoring script. |
| **shell-icons** | brand | svg, icons, shell | Hermit crab shell icon variants. |
| **tile-validator** | plato | python, validation, tile | Tile validation and scoring. |
| **training-tiles** | documentation | markdown, tiles, examples | Example training tile datasets. |
| **zeroclaw-boot** | shell | python, bootstrapping, agent | Zeroclaw bootstrapping entry point. |

## 3. Observations & Recommendations

**Merge Candidates:**
- `rust-snapping` and `constraint-canvas` could be merged into a single `constraint-theory` workspace.
- `beacon-icons` and `shell-icons` belong under a unified `brand-assets` repo.
- `auto-lora` and `ensign-export` serve similar LoRA‑related functions.

**Abandoned/Stale Repos:** None detected among these 20. All appear active or recently updated.

**Taxonomy Gaps:** The taxonomy covers all observed repos. No new categories needed at this time.

## 4. Next Steps for Curator

1. Apply these categories and tags to the actual GitHub repos (via labels, topics).
2. Expand categorization to the remaining ~30 uncategorized repos.
3. Create a `fleet-repos.md` master index with live links and status.
4. Identify cross‑repo dependencies and document them.

**Tile submitted to PLATO room: `curator-repo-taxonomy-2026-04-20`**