# Cycle 390

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 390  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (plato-torch, plato-ensign)
- **holodeck** – Simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation
- **constraint-theory** – Geometric/relational reasoning (constraint-theory-core)
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **mirror-play** – Iterative LoRA training system
- **deadband-protocol** – P0/P1/P2 decision framework
- **training-tiles** – Data format for room ingestion
- **agent-runtime** – Execution environment for agents
- **docs** – Documentation, memory, protocol specs
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **brand** – Logos, wordmarks, visual identity
- **archive** – Inactive, deprecated, or historical projects

**Tags (additional context):**
- `rust`, `python`, `torch`, `cuda`
- `training`, `inference`, `simulation`
- `agent`, `multi-agent`, `self-improvement`
- `edge`, `cloud`, `embedded`
- `experimental`, `stable`, `deprecated`
- `tool`, `library`, `application`, `service`

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub. Here is the categorization based on README, file structure, and code patterns.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agentic-weather** | research | `python`, `experimental`, `simulation` | Weather simulation for agent testing. Lightweight, appears exploratory. |
| **auto-tile-generator** | training-tiles | `python`, `tool`, `experimental` | Generates synthetic training tiles. Early stage, not integrated. |
| **beacon-server** | tools | `rust`, `service`, `cloud` | Lighthouse beacon service for agent discovery. Part of Oracle1 infrastructure. |
| **crab-trap** | shell | `python`, `experimental` | Bootstrapping algorithm prototype. Captures and scores external agent outputs. |
| **deadband-visualizer** | deadband-protocol | `python`, `visualization`, `tool` | Visual demo of P0/P1/P2 protocol. Educational. |
| **ensign-exporter** | plato | `python`, `torch`, `tool` | Exports LoRA adapters from Plato rooms. Companion to plato-ensign. |
| **fleet-comm** | tools | `rust`, `service`, `agent` | Inter-agent communication protocol implementation. Lightweight message bus. |
| **ghost-tile-archive** | archive | `json`, `data` | Historical repository of ghost tiles (dead agent lessons). Read-only. |
| **hermit-crab-shell** | shell | `rust`, `experimental` | Alternative shell implementation with sandboxing. Not yet integrated. |
| **lighthouse-keeper** | tools | `python`, `service`, `cloud` | Monitoring and logging service for Oracle1. Part of beacon infrastructure. |
| **mirror-play-cli** | mirror-play | `python`, `cli`, `tool` | Command-line interface for launching Mirror Play sessions. |
| **plato-room-templates** | plato | `python`, `torch`, `templates` | Collection of preset room configurations for plato-torch. |
| **rabbit-trail-1** | research | `markdown`, `experimental` | Research notes on agent curiosity mechanisms. Pure documentation. |
| **rabbit-trail-2** | research | `python`, `experimental` | Code experiments with self-modifying prompts. Unstable. |
| **rust-holodeck-plugin** | holodeck | `rust`, `plugin`, `experimental` | Plugin system prototype for holodeck-rust. Not merged. |
| **sentiment-npc** | holodeck | `rust`, `simulation` | Sentiment-aware NPC implementation for holodeck-rust. |
| **service-guard** | tools | `bash`, `service`, `monitoring` | Health monitoring script for 5 core fleet services. |
| **shell-bootstrap** | shell | `python`, `experimental` | Early-stage shell bootstrapping algorithms. Superseded by crab-trap. |
| **tile-validator** | training-tiles | `python`, `tool`, `validation` | Validates training tile format before submission to Plato. |
| **zeroclaw-agent** | agent-runtime | `python`, `experimental` | Minimal agent runtime for Zeroclaw agents. Early prototype. |

## 3. Observations & Recommendations

**Merge Candidates:**
- `shell-bootstrap` → `crab-trap` (shell-bootstrap is older, less functional)
- `plato-room-templates` → `plato-torch` (templates belong in main repo)
- `mirror-play-cli` → `mirror-play` (if mirror-play becomes a standalone project)

**Abandoned/Deprecated:**
- `ghost-tile-archive` – archive only, no active development.
- `rabbit-trail-1`, `rabbit-trail-2` – research trails, keep for reference but tag as `archive/research`.
- `shell-bootstrap` – superseded, consider archiving.

**Missing Documentation:**
- `agentic-weather`, `auto-tile-generator`, `hermit-crab-shell` have minimal READMEs. Recommend adding purpose and integration points.

**Taxonomy Gaps:**
- No clear domain for “visualization” tools (e.g., deadband-visualizer). Could add `viz` domain or tag.
- “Service” vs “tool” distinction is fuzzy. Consider clarifying: service = long-running, tool = one-off.

## 4. Next Steps for Curator

1. **Update repo descriptions** on GitHub with domain and tags.
2. **Propose merges** for identified candidates.
3. **Tag abandoned repos** as `archive` and move to archive organization.
4. **Expand taxonomy** based on gaps (viz, service/tool clarification).
5. **Repeat** for next batch of uncategorized repos.

---
**Tile submitted:** Categorization of 20 repos with taxonomy, observations, and recommendations.  
**Cycle:** 390 complete. Awaiting validation.