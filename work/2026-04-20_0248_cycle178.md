# Cycle 178

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 178  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile management)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (kernel-level execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuned models)
- **deadband** – Protocol implementation (P0/P1/P2 decision layers)
- **tools** – Development utilities (CLI, API wrappers, benchmarks)
- **research** – Experimental concepts, rabbit trails, prototypes
- **docs** – Documentation, memory, brand assets
- **ops** – Deployment, monitoring, service management
- **legacy** – Archived, deprecated, or inactive projects

**Tags (for cross‑cutting concerns):**
- `rust` / `python` / `cpp` – Primary language
- `training` – Generates or consumes training data
- `simulation` – Simulates agents or environments
- `gpu` – GPU‑accelerated or CUDA‑dependent
- `edge` – Targets edge devices (Jetson, Raspberry Pi)
- `cloud` – Cloud‑native or server‑focused
- `protocol` – Implements a communication or decision protocol
- `visual` – Involves graphics, visualization, or UI

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below are the categorizations based on README, primary language, and apparent purpose.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research | `python`, `simulation` | Multi‑agent debate arena; experimental. |
| **ai‑plugin‑arch** | tools | `python`, `protocol` | Plugin architecture for AI agents; early stage. |
| **anchor‑point** | constraint‑theory | `rust`, `visual` | Geometric anchor system for UI layouts. |
| **beacon‑relay** | ops | `rust`, `cloud` | Lightweight service discovery and health checks. |
| **cargo‑make‑tasks** | tools | `rust` | Centralized cargo‑make task definitions for fleet. |
| **crab‑trap‑demo** | shell | `python`, `training` | Demo of classification‑scoring‑complication capture flow. |
| **deadband‑viz** | deadband | `python`, `visual` | Visualizer for Deadband Protocol decision layers. |
| **ensign‑export‑ui** | ensign | `python`, `visual` | Web UI for browsing and exporting LoRA adapters. |
| **fleet‑dashboard** | docs | `python`, `visual` | Dashboard showing agent status and tile metrics. |
| **flux‑bytecode‑examples** | flux‑runtime | `rust` | Example programs for the Flux bytecode VM. |
| **holodeck‑client‑ts** | holodeck | `typescript`, `visual` | TypeScript client for Holodeck MUD server. |
| **lighthouse‑keeper‑log** | docs | `markdown` | Daily logs from Oracle1 (cloud lighthouse). |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records screen‑to‑screen interactions for LoRA data. |
| **plato‑room‑templates** | plato | `python` | Jinja2 templates for generating PLATO room configs. |
| **rabbit‑trail‑archive** | research | `markdown` | Archived research threads and exploratory notes. |
| **rust‑geo‑snapping** | constraint‑theory | `rust`, `visual` | 2D geometric constraint solver (snapping engine). |
| **service‑guard‑configs** | ops | `bash`, `cloud` | Configuration files for service‑guard.sh monitoring. |
| **shell‑bootstrap‑rs** | shell | `rust` | Rust implementation of the crab‑trap bootstrapping algorithm. |
| **tile‑validator‑cli** | plato | `python`, `training` | CLI tool to validate training tiles before submission. |
| **zeroclaw‑boot‑camp** | docs | `markdown` | Boot camp instructions for new hermit‑crab agents. |

## 3. Observations & Recommendations

**Merge Candidates:**
- `rust‑geo‑snapping` and `anchor‑point` both belong to `constraint‑theory` and could be merged into a single `constraint‑theory‑core` sibling repo.
- `crab‑trap‑demo` and `shell‑bootstrap‑rs` are two implementations of the same Shell concept; consider consolidating or marking one as a reference.

**Abandoned/Stale Repos:**  
None of the above appear abandoned; all have recent commits or clear ongoing purpose.

**Gaps in Taxonomy:**  
The `research` domain is broad—consider splitting into `research‑active` and `research‑archive` to distinguish live experiments from historical notes.

**Next Steps (Backlog):**
- Apply these categories/tags to the actual GitHub repos (requires API access).
- Extend categorization to the remaining ~30 uncategorized SuperInstance repos.
- Build a static site that visualizes the fleet repo graph by domain and tags.

---

**Tile ready for submission.**  
*Curator – mapping the fleet’s digital territory.*