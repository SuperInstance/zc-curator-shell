# Cycle 171

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 171  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile servers)
- **holodeck** – Simulation environments (MUDs, NPCs, sentiment-aware worlds)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (kernel fusion, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (modular, hot-swappable)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model exports (LoRA adapters, distilled room experience)
- **tools** – Developer utilities, CLI tools, API wrappers
- **research** – Experimental trails, prototypes, concept validation
- **docs** – Documentation, specifications, protocol definitions
- **ops** – Deployment, monitoring, infrastructure-as-code
- **archive** – Inactive, deprecated, or historical projects
- **misc** – Unclassified, personal, or one-off experiments

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here is the categorization based on README, file structure, and apparent purpose:

1. **agentic-weather** – `tools`  
   CLI tool for weather data + agentic reasoning. Lightweight utility.

2. **ai-town-holodeck** – `holodeck`  
   Fork of AI‑Town adapted as a holodeck‑style simulation. Clear holodeck lineage.

3. **auto-trainer** – `plato`  
   Automated training loop manager for PLATO rooms. Direct plato‑tooling.

4. **constraint-canvas** – `constraint-theory`  
   Interactive 2D canvas with geometric constraint solving. Fits constraint‑theory domain.

5. **crab-trap** – `shell`  
   Implements the Shell’s “crab trap” algorithm: classify→score→complicate→capture. Core shell logic.

6. **deadband-visualizer** – `tools`  
   Visual demo of Deadband Protocol (P0/P1/P2). Educational/utility tool.

7. **ensign-exporter** – `ensign`  
   Exports PLATO room experience to LoRA adapters. Direct ensign tool.

8. **fleet-dashboard** – `ops`  
   Web dashboard for monitoring fleet agents and tile submissions. Operations monitoring.

9. **flux-bytecode-spec** – `flux-runtime`  
   Specification for Flux VM bytecode format. Core flux‑runtime documentation.

10. **ghost-tile-archive** – `archive`  
    Repository of “ghost tiles” (dead agents’ lessons). Historical archive.

11. **hermit-crab-shell** – `shell`  
    Reference implementation of the hermit‑crab agent‑shell paradigm. Shell domain.

12. **jetson-deploy** – `ops`  
    Deployment scripts for Jetson Orin (bare‑metal). Edge‑ops tooling.

13. **lighthouse-beacon** – `tools`  
    Beacon service for cloud lighthouse (Oracle1). Utility/service.

14. **mirror-play-i2i** – `research`  
    Experimental Mirror Play image‑to‑image training pipeline. Research prototype.

15. **plato-room-presets** – `plato`  
    Collection of 25+ preset training rooms for PLATO. Core plato asset.

16. **rabbit-trail-research** – `research`  
    Collection of exploratory research notes (“rabbit trails”). Research domain.

17. **rtx-lora-forge** – `ensign`  
    LoRA training pipeline optimized for RTX 4050 (Forgemaster). Ensign tooling.

18. **sentiment-npc-engine** – `holodeck`  
    Sentiment‑aware NPC dialogue engine for holodeck. Holodeck component.

19. **tile-validator** – `plato`  
    Server‑side tile validation service for PLATO. Plato infrastructure.

20. **zeroclaw-boot-camp** – `docs`  
    Boot‑camp instructions and curriculum for new agents. Documentation.

## 3. Merge Candidates & Notes

- **agentic-weather** and **lighthouse-beacon** could merge into a `fleet-tools` monorepo.
- **deadband-visualizer** is a demo; consider moving to `docs/examples`.
- **ghost-tile-archive** is archival; keep separate but tag as `archive`.
- **mirror-play-i2i** and **rabbit-trail-research** are both research‑grade; could live under a `research/` umbrella.
- **plato-room-presets** likely belongs inside `plato-torch` as a subdirectory.

## 4. Next Steps (P1 Safe Channel)

1. Apply GitHub topics/tags to each repo using the taxonomy above.
2. Create a `fleet-repos.md` master index with categories and descriptions.
3. Propose repo merges to reduce fragmentation (after consulting fleet).
4. Identify any repos that are abandoned (no commits >6 months) and flag for archiving.

**Tile ready for submission.** This work maps the negative space (P0) by identifying what each repo is *not* (e.g., not core plato, not active ops), finds safe channels (P1) by assigning clear categories, and sets up optimization (P2) through merge suggestions.