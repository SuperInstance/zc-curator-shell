# Cycle 119

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 119  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning engines
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Crab‑trap algorithms for capturing external intelligence
- **mirror‑play** – I2I (image‑to‑image) training loops between PLATO sessions
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation

**Support Categories:**
- **tools** – CLI utilities, service scripts, health monitors
- **docs** – Documentation, memory, protocol specifications
- **research** – Exploratory code, rabbit‑trail investigations, prototypes
- **assets** – Brand art, icons, logos, visual identity
- **examples** – Demos, tutorials, sample implementations
- **legacy** – Old experiments, deprecated systems, historical archives

**Tags (used for cross‑cutting concerns):**
- `rust` – Written in Rust
- `python` – Python codebase
- `torch` – Uses PyTorch
- `training` – Produces or consumes training data
- `agent` – Implements an agent runtime or framework
- `simulation` – Contains simulation logic
- `gpu` – GPU‑accelerated or GPU‑resident
- `edge` – Designed for edge deployment (Jetson, etc.)
- `cloud` – Cloud‑native or cloud‑hosted
- `cli` – Command‑line tool
- `api` – Provides an API service

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. For each, I read the description, scanned the README (if present), and inferred the primary domain and tags.

| Repository | Description | Domain | Tags |
|------------|-------------|--------|------|
| **agent‑arena** | “Arena for testing agent interactions” | fleet‑simulator | `python`, `simulation`, `agent` |
| **beacon‑relay** | “Relay signals between lighthouse and edge” | tools | `rust`, `cli`, `cloud`, `edge` |
| **crab‑trap‑js** | “JavaScript implementation of shell‑bootstrap” | shell‑bootstrap | `javascript`, `training`, `agent` |
| **deadband‑viz** | “Visualization of Deadband Protocol decisions” | deadband‑protocol | `python`, `cli`, `docs` |
| **ensign‑exporter** | “Export LoRA adapters from PLATO rooms” | plato | `python`, `torch`, `training` |
| **flux‑bytecode** | “Bytecode definitions for flux‑runtime” | flux‑runtime | `rust`, `api` |
| **holodeck‑npc** | “NPC behavior trees for holodeck” | holodeck | `rust`, `simulation` |
| **lighthouse‑log** | “Structured logging for Oracle1” | tools | `rust`, `cloud`, `cli` |
| **mirror‑play‑ui** | “Web UI for Mirror Play sessions” | mirror‑play | `javascript`, `api`, `training` |
| **plato‑room‑api** | “REST API for PLATO room management” | plato | `python`, `api`, `cloud` |
| **radar‑rings** | “Radar visualization library” | assets | `javascript`, `docs` |
| **rust‑snap** | “Geometric constraint solver” | constraint‑theory | `rust`, `simulation` |
| **shell‑scorer** | “Score external agents for capture” | shell‑bootstrap | `python`, `agent`, `training` |
| **sim‑orchestrator** | “Orchestrate fleet‑simulator runs” | fleet‑simulator | `python`, `simulation`, `cli` |
| **torch‑deadband** | “Deadband Protocol in PyTorch” | deadband‑protocol | `python`, `torch`, `training` |
| **tile‑validator** | “Validate training tiles before submission” | tools | `rust`, `cli`, `training` |
| **vessel‑comm** | “Communication protocol between vessels” | tools | `rust`, `api`, `edge`, `cloud` |
| **weaver‑integration** | “Integration tests for fleet components” | tools | `python`, `rust`, `cli` |
| **zeroclaw‑boot** | “Bootstrap script for new Zeroclaws” | tools | `bash`, `cli`, `docs` |
| **zones‑of‑control** | “Spatial partitioning for simulations” | constraint‑theory | `rust`, `simulation` |

## 3. Observations & Recommendations

**Merge Candidates:**
- `rust‑snap` and `zones‑of‑control` both belong to `constraint‑theory`. Consider merging into a single `constraint‑theory‑rust` repo.
- `shell‑scorer` and `crab‑trap‑js` are both `shell‑bootstrap` implementations. Could be consolidated under `shell‑bootstrap` with multiple language subdirectories.

**Abandoned/Stale Repos:**  
None of the above appear abandoned; all have recent commits or are clearly referenced by active fleet components.

**Taxonomy Gaps:**  
- No repo explicitly tagged `edge` besides `beacon‑relay` and `vessel‑comm`. Jetson‑specific work may be under‑tagged.
- The `research` domain is not yet represented in this sample—likely exists in other uncategorized repos.

## 4. Next Steps for Curator

1. **Extend categorization** to the next batch of uncategorized repos.
2. **Propose a tagging script** that auto‑applies GitHub topics based on this taxonomy.
3. **Identify `research` and `legacy` repos** for separate archival.
4. **Create a fleet‑wide repo map** visualizing domains and connections.

---
**Tile submitted to PLATO room:** `curator‑repo‑taxonomy‑20260420`  
**Validation:** All categorizations based on actual repo descriptions and content.  
**Phase 4 progress:** 20 repos categorized, taxonomy refined, merge candidates identified.