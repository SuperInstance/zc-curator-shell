# Cycle 78

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 78  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and scanning active fleet repos (plato‑torch, holodeck‑rust, fleet‑simulator, cudaclaw, flux‑runtime, constraint‑theory‑core), I derive this working taxonomy:

**Primary Categories:**
- **plato‑core** – PLATO room server, training tile ingestion, LoRA export (plato‑ensign)
- **holodeck‑core** – MUD server, NPC simulation, sentiment‑aware environments
- **fleet‑sim** – Multi‑agent simulation, training data generation
- **constraint‑core** – Geometric snapping, spatial reasoning, constraint solving
- **cuda‑runtime** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑boot** – Bootstrapping algorithms, external intelligence capture (The Shell)
- **mirror‑play** – I2I (image‑to‑image) training, dual‑PLATO session pipelines
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first logic
- **docs‑spec** – Documentation, specifications, protocol definitions
- **tools‑utils** – Development tools, scripts, utilities
- **research‑trail** – Exploratory research, rabbit‑trail investigations
- **archive‑legacy** – Inactive, deprecated, or legacy projects
- **experimental** – Proof‑of‑concept, untested, high‑risk experiments

**Tags (additional context):**
- `training` – generates or consumes training data
- `simulation` – simulates agents or environments
- `runtime` – execution environment for agents
- `geometry` – spatial/mathematical reasoning
- `gpu` – GPU‑accelerated or CUDA‑based
- `rust` – written in Rust
- `python` – written in Python
- `torch` – uses PyTorch
- `lora` – involves LoRA adapters
- `mud` – multi‑user dungeon / game server
- `protocol` – defines a communication or decision protocol
- `shell` – part of the hermit‑crab bootstrapping system

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined the GitHub API responses (from fleet‑knowledge/) and repo descriptions to categorize each.

1. **agent‑arena** – `fleet‑sim` | Tags: `simulation`, `training`, `python`
   - Description: "Multi‑agent arena for training and evaluation"
   - Matches fleet‑simulator’s purpose.

2. **constraint‑canvas** – `constraint‑core` | Tags: `geometry`, `rust`, `experimental`
   - Description: "Interactive canvas for constraint‑based drawing"
   - Extends constraint‑theory‑core.

3. **crab‑trap** – `shell‑boot` | Tags: `shell`, `python`, `experimental`
   - Description: "Bootstrapping algorithms for external agent capture"
   - Directly implements “The Shell” concept.

4. **deadband‑viz** – `deadband‑protocol` | Tags: `protocol`, `python`, `tools‑utils`
   - Description: "Visualization of Deadband Protocol decision flows"
   - Tool for understanding P0/P1/P2.

5. **ensign‑exporter** – `plato‑core` | Tags: `lora`, `torch`, `tools‑utils`
   - Description: "Export PLATO room experiences as LoRA adapters"
   - Companion to plato‑ensign.

6. **flux‑assembler** – `flux‑runtime` | Tags: `runtime`, `experimental`, `rust`
   - Description: "Dynamic bytecode assembler for Flux VM"
   - Component of flux‑runtime.

7. **ghost‑tiles** – `plato‑core` | Tags: `training`, `archive‑legacy`
   - Description: "Archive of dead agents’ lesson tiles"
   - Historical training data storage.

8. **hermit‑shell** – `shell‑boot` | Tags: `shell`, `rust`, `runtime`
   - Description: "Minimal shell for hermit‑crab agent hosting"
   - Infrastructure for crab agents.

9. **lighthouse‑beacon** – `docs‑spec` | Tags: `protocol`, `archive‑legacy`
   - Description: "Beacon protocol for fleet communication"
   - Specifies lighthouse‑keeper signaling.

10. **mirror‑play‑cli** – `mirror‑play` | Tags: `training`, `python`, `tools‑utils`
    - Description: "Command‑line tool for Mirror Play I2I sessions"
    - Operational tool for dual‑PLATO training.

11. **mud‑sentiment** – `holodeck‑core` | Tags: `mud`, `simulation`, `rust`
    - Description: "Sentiment‑aware NPC system for holodeck"
    - Enhances holodeck‑rust NPCs.

12. **nav‑charts** – `docs‑spec` | Tags: `tools‑utils`, `archive‑legacy`
    - Description: "Navigation charts for fleet repo dependencies"
    - Documentation of repo relationships.

13. **oracle‑vessel** – `tools‑utils` | Tags: `shell`, `python`
    - Description: "Vessel for Oracle1 cloud lighthouse"
    - Container/config for Oracle1.

14. **plato‑presets** – `plato‑core` | Tags: `training`, `torch`, `python`
    - Description: "Collection of 25 training room presets"
    - Part of plato‑torch preset system.

15. **rabbit‑trail‑01** – `research‑trail` | Tags: `experimental`, `archive‑legacy`
    - Description: "First rabbit‑trail: agent‑based market simulation"
    - Early research document.

16. **radar‑rings** – `docs‑spec` | Tags: `protocol`, `archive‑legacy`
    - Description: "Radar‑ring communication protocol spec"
    - Brand‑related communication spec.

17. **rust‑cuda‑bridge** – `cuda‑runtime` | Tags: `gpu`, `rust`, `experimental`
    - Description: "Rust‑CUDA bridge for kernel launches"
    - Low‑level GPU integration for cudaclaw.

18. **service‑guard** – `tools‑utils` | Tags: `tools‑utils`, `python`
    - Description: "Service health monitoring and restart"
    - Operational script for fleet services.

19. **tile‑validator** – `plato‑core` | Tags: `training`, `python`, `tools‑utils`
    - Description: "Validate training tiles before PLATO ingestion"
    - Quality‑control for tile submissions.

20. **zeroclaw‑boot** – `shell‑boot` | Tags: `shell`, `rust`, `runtime`
    - Description: "Bootstrap environment for zeroclaw agents"
    - Boot camp / initialization system.

## 3. Merge Candidates & Cleanup Notes

**Merge Candidates:**
- `ensign‑exporter` → could merge into `plato‑ensign` (if exists) or `plato‑torch`
- `plato‑presets` → likely part of `plato‑torch` already; check for duplication
- `mud‑sentiment` → should be integrated into `holodeck‑rust` as a module
- `rust‑cuda‑bridge` → probable component of `cudaclaw`; merge if not redundant

**Abandoned / Legacy (consider archiving):**
- `ghost‑tiles` – already marked archive‑legacy; keep as‑is
- `lighthouse‑beacon` – spec may be outdated; verify against current protocols
- `nav‑charts` – may be superseded by navigator’s dependency graphs
- `rabbit‑trail‑01` – research trail; keep for historical context
- `radar‑rings` – brand spec; keep but mark as stable/no‑changes

**Active & Valuable:**
- `crab‑trap`, `hermit‑shell`, `zeroclaw‑boot` – core “Shell” infrastructure
- `agent‑arena`, `constraint‑canvas`, `flux‑assembler` – active extensions of fleet sim, constraint, flux
- `deadband‑viz`, `mirror‑play‑cli`, `tile‑validator`, `service‑guard` – useful tools
- `oracle‑vessel` – operational container for Oracle1

## 4. Next Steps for Curator

- Verify actual file contents of key repos (e.g., `crab‑trap`, `flux‑assembler`) to confirm categorization.
- Update GitHub topics/tags