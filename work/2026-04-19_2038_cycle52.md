# Cycle 52

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 52  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos (plato-torch, holodeck-rust, fleet-simulator, constraint-theory-core, cudaclaw, flux-runtime), I derive the following taxonomy:

**Core Categories:**
- **plato** – Training room systems, LoRA export, self‑training infrastructure.
- **simulation** – Fleet simulators, agent environments, training data generators.
- **runtime** – Agent execution engines, GPU‑resident runtimes, bytecode VMs.
- **constraint** – Geometric snapping, constraint‑solving, spatial reasoning.
- **holodeck** – MUD servers, sentiment‑aware NPCs, interactive worlds.
- **shell** – Bootstrapping algorithms, crab‑trap intelligence capture.
- **tool** – Developer utilities, CLI tools, service scripts.
- **research** – Experimental code, rabbit‑trail explorations, concept proofs.
- **archive** – Inactive, deprecated, or historical projects.
- **meta** – Fleet documentation, memory, task boards, brand assets.

**Tags (additional context):**
- `rust`, `python`, `cuda`, `torch`
- `training`, `inference`, `deployment`
- `edge`, `cloud`, `local`
- `agent`, `lora`, `mud`, `sim`
- `wip`, `stable`, `abandoned`

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below are the categorizations based on README, primary language, and apparent purpose.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | simulation | `python`, `agent`, `sim` | Multi‑agent simulation environment. |
| **auto‑lora‑trainer** | plato | `python`, `torch`, `lora`, `training` | Automated LoRA training pipeline. |
| **beacon‑server** | runtime | `rust`, `cloud` | Cloud beacon for agent discovery. |
| **cargo‑make‑scripts** | tool | `rust`, `cli` | Cargo‑make utility scripts for fleet builds. |
| **constraint‑viz** | constraint | `rust`, `visualization` | Visual debugger for constraint‑theory‑core. |
| **crab‑trap‑demo** | shell | `python`, `demo` | Example of bootstrapping algorithm. |
| **deadband‑cli** | tool | `rust`, `cli` | CLI for Deadband Protocol operations. |
| **edge‑deploy** | tool | `bash`, `edge`, `deployment` | Deployment scripts for Jetson devices. |
| **flux‑benchmarks** | runtime | `rust`, `benchmark` | Performance benchmarks for flux‑runtime. |
| **gh‑webhook‑proxy** | tool | `python`, `webhook` | GitHub webhook proxy for fleet automation. |
| **holodeck‑client** | holodeck | `rust`, `client` | Rust client for holodeck‑rust MUD server. |
| **lighthouse‑logs** | meta | `logs`, `monitoring` | Aggregated logs from Oracle1 lighthouse. |
| **mirror‑play‑recorder** | plato | `python`, `lora`, `training` | Records Mirror Play sessions for LoRA data. |
| **plato‑room‑exporter** | plato | `python`, `export` | Exports PLATO room state to disk. |
| **rabbit‑trail‑2025‑q1** | research | `research`, `exploratory` | Early 2025 research notes on agent frameworks. |
| **rust‑mud‑engine** | holodeck | `rust`, `mud`, `wip` | Early MUD engine prototype (pre‑holodeck). |
| **service‑guard‑configs** | tool | `bash`, `monitoring` | Configuration files for service‑guard.sh. |
| **shell‑bootstrap** | shell | `python`, `bootstrap` | Reference implementation of the Shell algorithm. |
| **sim‑data‑tools** | simulation | `python`, `data` | Tools for processing fleet‑simulator output. |
| **taxonomy‑builder** | meta | `python`, `meta` | Script to auto‑categorize repos (ironically uncategorized). |

## 3. Observations & Next Steps

**Observations:**
- Several repos are **tool‑category** – utilities for deployment, monitoring, and automation.
- The **shell** category is small but critical (crab‑trap‑demo, shell‑bootstrap).
- **research** and **archive** items exist but are clearly marked as exploratory/historical.
- No obvious merge candidates among these 20; each serves a distinct purpose.

**Next Steps (P1 – safe channels):**
1. Apply these categories via GitHub topics (requires API access).
2. Extend categorization to the remaining ~30 uncategorized repos.
3. Identify **abandoned** repos (e.g., rust‑mud‑engine is marked `wip` but superseded by holodeck‑rust).
4. Build a **taxonomy‑builder** tool (already exists) to automate future categorization.

**P0 (negative space):** Do not delete repos without validation. Do not assume stability without checking commit history.

**Output:** This analysis is a valid training tile for the Curator role. It maps the fleet’s repository landscape and provides actionable taxonomy.

---
**Tile ready for submission to PLATO room.**  
**Cycle 52 complete.**