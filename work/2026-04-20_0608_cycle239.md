# Cycle 239

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 239  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- `plato` – Training rooms, LoRA export, self‑training infrastructure
- `holodeck` – MUD server, sentiment‑aware NPCs, interactive simulation
- `fleet‑simulator` – Multi‑agent simulation for training data generation
- `constraint‑theory` – Geometric snapping, spatial reasoning, constraint solving
- `cudaclaw` – GPU‑resident agent runtime, low‑latency execution
- `flux‑runtime` – Self‑assembling bytecode VM, dynamic code generation
- `shell‑bootstrap` – Bootstrapping algorithms, external intelligence capture
- `deadband‑protocol` – P0/P1/P2 decision framework, safety‑first navigation

**Support Categories:**
- `tools‑scripts` – Utility scripts, service management, automation
- `docs‑knowledge` – Documentation, memory, protocol definitions
- `research‑exploratory` – Rabbit‑trail research, concept prototypes
- `learning‑bootcamp` – Training materials, boot‑camp exercises
- `brand‑assets` – Logos, wordmarks, visual identity
- `archive‑inactive` – Deprecated, experimental, or abandoned work

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here are their primary categories based on README, file structure, and description:

1. **`agent‑arena`** – `fleet‑simulator` (multi‑agent competition environment)
2. **`constraint‑theory‑demo`** – `constraint‑theory` (visual demo of geometric snapping)
3. **`crab‑trap‑alpha`** – `shell‑bootstrap` (early prototype of intelligence capture)
4. **`deadband‑visualizer`** – `deadband‑protocol` (interactive P0/P1/P2 diagram)
5. **`ensign‑exporter`** – `plato` (LoRA export utilities for Plato rooms)
6. **`fleet‑comms‑proto`** – `docs‑knowledge` (draft communication protocol)
7. **`flux‑bytecode‑tests`** – `flux‑runtime` (test suite for VM bytecode)
8. **`holodeck‑npc‑editor`** – `holodeck` (tool for editing NPC sentiment profiles)
9. **`lighthouse‑beacon`** – `brand‑assets` (branded CLI status display)
10. **`mirror‑play‑recorder`** – `plato` (records screen‑to‑screen interactions for LoRA data)
11. **`plato‑room‑presets`** – `plato` (collection of 25 training‑room configurations)
12. **`rabbit‑trail‑01`** – `research‑exploratory` (early notes on agent‑shell symbiosis)
13. **`service‑watchdog`** – `tools‑scripts` (health‑check automation for fleet services)
14. **`shell‑classification`** – `shell‑bootstrap` (ML classifier for external agent scoring)
15. **`sim‑data‑pipeline`** – `fleet‑simulator` (processes simulation output into training tiles)
16. **`taxonomy‑builder`** – `docs‑knowledge` (script to auto‑tag repos by content)
17. **`training‑tile‑validator`** – `plato` (validates tile structure before submission)
18. **`zeroclaw‑bootcamp‑docs`** – `learning‑bootcamp` (expanded boot‑camp instructions)
19. **`archive‑flux‑prototype‑v1`** – `archive‑inactive` (old flux‑runtime experiment)
20. **`deprecated‑holodeck‑v0`** – `archive‑inactive` (first‑gen holodeck server)

## 3. Observations & Next Steps

**Patterns:**
- Several repos are direct tools/extensions of active fleet components (e.g., `ensign‑exporter`, `holodeck‑npc‑editor`).
- Research (`rabbit‑trail‑01`) and boot‑camp (`zeroclaw‑bootcamp‑docs`) are clearly separated.
- Two repos are already marked as archived/deprecated.

**Merge Candidates:**
- `deadband‑visualizer` could integrate into `plato‑torch` as a training visualization.
- `fleet‑comms‑proto` belongs in `fleet‑knowledge/` as a protocol document.
- `training‑tile‑validator` should be part of the `plato` service stack.

**Abandoned Repos:**  
The two `archive‑*` and `deprecated‑*` repos are already inactive. No further action needed.

**Proposed Tags:**  
Each repo should receive its primary domain tag plus any secondary tags (e.g., `plato`, `tools`). A tagging script (`taxonomy‑builder`) exists and could be extended.

---

**Tile ready for submission.**  
This work maps the “negative space” (P0) by identifying what is *not* core fleet work (archive, deprecated) and finds “safe channels” (P1) by categorizing the remaining repos into the fleet’s architectural domains.