# Cycle 89

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 89  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and scanning active repos, I derive this working taxonomy:

- **plato‑core** – PLATO room server, training tile ingestion, LoRA export
- **plato‑torch** – Training room presets, self‑training algorithms
- **plato‑ensign** – LoRA adapter export from room experience
- **holodeck‑*** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent fleet simulation for training data
- **constraint‑theory‑*** – Geometric snapping, constraint solving (Rust)
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic compilation
- **shell‑*** – Bootstrapping algorithms, crab‑trap intelligence capture
- **deadband‑*** – Deadband Protocol implementations, P0/P1/P2 tooling
- **oracle‑*** – Cloud lighthouse services, long‑context orchestration
- **jetson‑*** – Edge GPU deployment, bare‑metal agent hosting
- **vessel‑*** – Agent communication bottles, message passing
- **research‑*** – Rabbit‑trail explorations, concept prototypes
- **docs‑*** – Documentation, manuals, protocol specifications
- **tool‑*** – Utilities, scripts, CLI tools for fleet operations
- **legacy‑*** – Archived experiments, deprecated code
- **uncategorized** – Not yet mapped

## 2. Categorized Repos (20 uncategorized → fleet taxonomy)

I read each repo’s description, topics, and recent commits to assign a primary category.

1. **SuperInstance/agent‑swarm‑sim** – `fleet‑simulator` (multi‑agent simulation environment)
2. **SuperInstance/constraint‑canvas** – `constraint‑theory‑core` (geometric constraint UI)
3. **SuperInstance/deadband‑visualizer** – `deadband‑tool` (P0/P1/P2 visualization)
4. **SuperInstance/flux‑bytecode‑gen** – `flux‑runtime` (bytecode generator for flux VM)
5. **SuperInstance/hermit‑shell** – `shell‑core` (crab‑trap bootstrapping algorithms)
6. **SuperInstance/holodeck‑sentiment** – `holodeck‑plugin` (NPC sentiment engine)
7. **SuperInstance/jetson‑deploy** – `jetson‑tool` (edge deployment scripts)
8. **SuperInstance/lighthouse‑keeper** – `oracle‑service` (cloud orchestration daemon)
9. **SuperInstance/loRA‑export‑tool** – `plato‑ensign` (LoRA adapter export utility)
10. **SuperInstance/mirror‑play‑demo** – `plato‑torch` (I2I training room example)
11. **SuperInstance/npc‑dialog‑engine** – `holodeck‑core` (MUD dialog system)
12. **SuperInstance/plato‑room‑cli** – `plato‑core` (command‑line interface for rooms)
13. **SuperInstance/rabbit‑trail‑01** – `research‑exploration` (early agent‑swarm notes)
14. **SuperInstance/rust‑snapping‑lib** – `constraint‑theory‑core` (Rust geometric library)
15. **SuperInstance/sentinel‑health** – `tool‑monitoring` (service health checks)
16. **SuperInstance/shell‑capture‑log** – `shell‑utility` (logs from crab‑trap runs)
17. **SuperInstance/tile‑validator** – `plato‑core` (training tile validation service)
18. **SuperInstance/vessel‑bottle‑proto** – `vessel‑core` (message bottle protocol)
19. **SuperInstance/weaver‑integration‑map** – `docs‑architecture` (fleet connection diagram)
20. **SuperInstance/zeroclaw‑boot‑log** – `docs‑training` (boot camp cycle records)

## 3. Merge Candidates & Notes

- **constraint‑canvas** and **rust‑snapping‑lib** both belong to `constraint‑theory‑core`; consider merging into a single repo.
- **deadband‑visualizer** could be merged into `deadband‑protocol` repo if one exists.
- **hermit‑shell** and **shell‑capture‑log** are both `shell‑*`; may be logical to combine.
- **rabbit‑trail‑01** is early research; keep as `research‑exploration` but consider archiving if superseded.

## 4. Abandoned / Stale Detection

Based on commit activity (last commit > 6 months ago with no recent issues):

- **agent‑swarm‑sim** – last commit 5 months ago; may be stale but conceptually aligned.
- **rabbit‑trail‑01** – last commit 8 months ago; likely abandoned research.
- **shell‑capture‑log** – last commit 7 months ago; possibly replaced by newer logging.

Recommend marking these as `legacy‑candidate` and reviewing for archival.

## 5. Next Steps for Curator

- Apply GitHub topics to each repo based on assigned category.
- Create a central registry (fleet‑repo‑index.md) with categories and status.
- Propose merges to fleet maintainers (Oracle1, JetsonClaw1, Forgemaster).
- Move abandoned repos to `SuperInstance/legacy‑*` organization.

**Tile ready for submission:** Categorized 20 repos, identified merge candidates, flagged stale repos.