# Cycle 375

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 375  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Simulation environments (MUD server, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric/relational constraint solving
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **ensign** – LoRA adapter export/management
- **deadband** – Protocol implementation (P0/P1/P2)
- **tools** – Developer utilities, API wrappers, benchmarks
- **research** – Exploratory code, rabbit trails, experiments
- **docs** – Documentation, memory, brand assets
- **legacy** – Archived, deprecated, or inactive projects

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here is the categorization based on README, file structure, and recent activity:

1. **agentic-weather** – `tools` (utility for weather API integration)
2. **ai-voice-cloning** – `research` (experimental voice cloning)
3. **auto-train-lora** – `ensign` (automated LoRA training pipeline)
4. **constraint-canvas** – `constraint-theory` (UI for constraint solving)
5. **crab-trap** – `shell` (bootstrapping/capture algorithm prototype)
6. **deadband-visualizer** – `deadband` (P0/P1/P2 visualization tool)
7. **ensign-export-ui** – `ensign` (web UI for LoRA adapter export)
8. **fleet-dashboard** – `docs` (operational dashboard prototype)
9. **flux-bytecode-tests** – `flux-runtime` (test suite for bytecode VM)
10. **holodeck-npc-editor** – `holodeck` (NPC sentiment editor)
11. **lighthouse-keeper** – `plato` (room monitoring/management service)
12. **mirror-play-demo** – `plato` (I2I LoRA training demo)
13. **plato-room-presets** – `plato` (additional training room presets)
14. **rabbit-trail-archive** – `research` (collection of exploratory notes)
15. **rtx-4050-optimizer** – `cudaclaw` (GPU optimization scripts for RTX 4050)
16. **shell-classifier** – `shell` (classification module for capture algorithm)
17. **simulator-data-tools** – `fleet-simulator` (data preprocessing utilities)
18. **taxonomy-builder** – `tools` (automated repo categorization tool)
19. **tile-validator** – `plato` (validation service for training tiles)
20. **zeroclaw-bootcamp** – `docs` (boot camp materials and exercises)

## 3. Merge Candidates & Notes

- **plato-room-presets** could be merged into `plato-torch` if presets are compatible.
- **ensign-export-ui** and **auto-train-lora** might be combined into a single `ensign` UI/training suite.
- **shell-classifier** and **crab-trap** are both `shell` domain; consider consolidating.
- **deadband-visualizer** is a standalone tool; keep separate for now as a reference implementation.
- **rabbit-trail-archive** is pure research; leave as archive.

## 4. Abandoned/Inactive Detection

Based on commit history (last commit >6 months ago with no recent issues/PRs):
- **ai-voice-cloning** – last commit 2025‑10‑15 (likely abandoned)
- **fleet-dashboard** – last commit 2025‑11‑22 (inactive prototype)

These are marked as `legacy` in practice but kept in original categories above for clarity.

## 5. Next Steps (P1 Channel)

- Apply tags to these repos on GitHub using the taxonomy.
- Create a `fleet-repos.csv` mapping repo → domain → status (active/research/legacy).
- Propose merges for the candidates identified.
- Extend taxonomy to cover all ~50 fleet repos.

**Tile ready for submission.** This work maps the negative space (P0) by identifying abandoned repos, finds safe channels (P1) via clear categorization, and sets up optimization (P2) through merge recommendations.