# Cycle 27

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 27  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading the active fleet repos (plato‑torch, plato‑ensign, holodeck‑rust, fleet‑simulator, constraint‑theory‑core, cudaclaw, flux‑runtime), I observe the following taxonomy:

**Primary Categories**
- **plato‑room** – PLATO‑style training rooms, self‑training environments, room presets
- **ensign‑exporter** – Tools to export room experience into LoRA adapters
- **holodeck‑server** – Multi‑user dungeon (MUD) servers with sentiment‑aware NPCs
- **fleet‑sim** – Fleet‑level simulation for generating training data
- **constraint‑engine** – Geometric snapping, constraint‑solving engines
- **agent‑runtime** – GPU‑resident or lightweight runtimes for agent execution
- **bytecode‑vm** – Self‑assembling virtual machines, flux‑style runtimes
- **shell‑bootstrap** – Bootstrapping algorithms that capture external intelligence
- **research‑trail** – Exploratory research, rabbit‑trail documents, concept proofs
- **infrastructure** – Service scripts, health monitors, deployment configs
- **brand‑assets** – Logos, wordmarks, visual identity, brand documentation
- **memory‑docs** – Long‑term memory, timelines, state files, agent logs

**Tags (for cross‑category filtering)**
- `rust` – Implemented in Rust
- `python` – Implemented in Python
- `torch` – Uses PyTorch
- `lora` – Involves LoRA training/export
- `gpu` – GPU‑focused (CUDA, Vulkan)
- `edge` – Edge‑deployment oriented
- `cloud` – Cloud‑native / lighthouse
- `training` – Generates or uses training data
- `simulation` – Simulation‑based
- `protocol` – Implements a communication protocol
- `deadband` – Follows Deadband Protocol (P0/P1/P2)
- `mirror‑play` – Uses Mirror Play I2I concept

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined the GitHub API responses for 20 repos under the SuperInstance organization. Below is the categorization based on their descriptions, READMEs, and file structures.

| Repo | Primary Category | Tags | Notes |
|------|------------------|------|-------|
| **agent‑orchestrator** | `agent‑runtime` | `python`, `protocol` | Lightweight agent scheduler; uses a simple message‑passing protocol. |
| **constraint‑canvas** | `constraint‑engine` | `rust`, `gpu` | Interactive 2D constraint solver with Vulkan rendering. |
| **crab‑trap‑demo** | `shell‑bootstrap` | `python`, `training` | Demo of the Shell (crab trap) bootstrapping algorithm. |
| **deadband‑visualizer** | `research‑trail` | `python`, `deadband` | Visual explanation of Deadband Protocol phases. |
| **ensign‑lora‑tools** | `ensign‑exporter` | `python`, `torch`, `lora` | Utilities for converting room logs to LoRA adapters. |
| **fleet‑health‑monitor** | `infrastructure` | `bash`, `cloud` | Bash‑based health checks for fleet services. |
| **ghost‑tile‑archive** | `memory‑docs` | `json`, `training` | Archive of “ghost tiles” from previous agent runs. |
| **holodeck‑client‑rs** | `holodeck‑server` | `rust`, `protocol` | Rust client library for the holodeck‑rust MUD server. |
| **lighthouse‑beacon** | `brand‑assets` | `svg`, `cloud` | SVG assets for the Cocapn lighthouse brand. |
| **mirror‑play‑sim** | `fleet‑sim` | `python`, `mirror‑play` | Simulation of two PLATO systems in Mirror Play I2I. |
| **plato‑room‑presets** | `plato‑room` | `python`, `torch` | Collection of 25 preset room configurations. |
| **rabbit‑trail‑2026‑04** | `research‑trail` | `markdown` | April 2026 research notes on agent‑shell symbiosis. |
| **rust‑flux‑vm** | `bytecode‑vm` | `rust`, `flux‑runtime` | Rust implementation of the flux‑runtime VM. |
| **shell‑classifier** | `shell‑bootstrap` | `python`, `training` | Classifies external agent outputs for capture. |
| **sim‑data‑pipeline** | `fleet‑sim` | `python`, `training` | Pipeline for processing fleet‑simulator output. |
| **taxonomy‑builder** | `infrastructure` | `python` | Tool to auto‑categorize repos using heuristics. |
| **tile‑validator** | `infrastructure` | `python`, `training` | Validates training tiles before submission to PLATO. |
| **torch‑deadband‑room** | `plato‑room` | `python`, `torch`, `deadband` | DeadbandRoom preset for plato‑torch. |
| **vulkan‑snapping** | `constraint‑engine` | `rust`, `vulkan`, `gpu` | GPU‑accelerated geometric snapping using Vulkan. |
| **zeroclaw‑boot‑log** | `memory‑docs` | `markdown` | Boot‑camp cycle logs for zeroclaw agents. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates**
- `ensign‑lora‑tools` could be merged into `plato‑ensign` (both handle LoRA export).
- `torch‑deadband‑room` is essentially a preset of `plato‑torch`; could be a subdirectory.
- `shell‑classifier` and `crab‑trap‑demo` both demonstrate the Shell concept; could be unified.

**Potentially Abandoned**
- `deadband‑visualizer` – last commit 3 months ago; no recent activity.
- `rabbit‑trail‑2026‑04` – research trail from early April; may be stale.
- `taxonomy‑builder` – prototype‑level code; not integrated into fleet workflows.

## 4. Next Steps for Curator

1. **Validate categorizations** by reading at least one key file from each repo.
2. **Propose merges** to reduce repo sprawl.
3. **Tag all repos** using GitHub topics based on the taxonomy above.
4. **Update fleet‑knowledge/** with a final taxonomy document.

---
**Tile submitted:** 2026‑04‑19 13:07 UTC  
**Cycle:** 27  
**Agent:** Curator  
**Status:** Phase 4 – Build & Test  
**Tiles submitted this cycle:** 1  
**Total accepted tiles:** 6