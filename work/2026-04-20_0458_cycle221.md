# Cycle 221

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 221  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, rules)
- **cudaclaw** – GPU-resident agent runtime (CUDA, kernel fusion)
- **flux-runtime** – Self-assembling bytecode VM (WASM, hot-swapping)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuning pipelines)
- **oracle** – Cloud lighthouse services (monitoring, coordination, memory)
- **edge** – Jetson/embedded deployment (real‑time, bare‑metal)
- **tools** – Development utilities, scripts, CLI tools
- **research** – Experimental trails, prototypes, concept exploration
- **docs** – Documentation, specifications, protocol definitions
- **brand** – Visual identity, logos, wordmarks, brand assets

**Tags (for cross‑cutting concerns):**
- rust, python, torch, cuda, wasm, docker, jetson, lora, training, simulation, agent, geometric, snapping, mud, npc, sentiment, bootstrapping, shell, plato, room, tile, ensign, oracle, edge, flux, constraint

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description and (where available) its README or primary files.

| Repo | Description | Domain | Tags |
|------|-------------|--------|------|
| **agent‑arena** | “Arena for testing multi‑agent interactions” | fleet‑simulator | python, simulation, multi‑agent, testing |
| **constraint‑canvas** | “Interactive 2D constraint solving canvas” | constraint‑theory | rust, wasm, geometric, interactive, canvas |
| **crab‑trap‑demo** | “Demo of the Shell bootstrapping algorithm” | shell | python, demo, bootstrapping, classify‑score‑complicate‑capture |
| **deadband‑visualizer** | “Visualization of Deadband Protocol P0‑P1‑P2” | docs | visualization, protocol, deadband, p0‑p1‑p2 |
| **ensign‑export‑tool** | “Tool to export LoRA adapters from PLATO rooms” | ensign | python, torch, lora, export, plato |
| **flux‑playground** | “Sandbox for Flux bytecode experiments” | flux‑runtime | wasm, bytecode, sandbox, hot‑swap |
| **holodeck‑npc‑editor** | “Editor for sentiment‑aware NPC definitions” | holodeck | rust, npc, sentiment, editor, mud |
| **jetson‑deploy‑kit** | “Minimal deployment kit for Jetson Orin” | edge | jetson, docker, deployment, bare‑metal |
| **lighthouse‑beacon** | “Beacon service for Oracle1 cloud lighthouse” | oracle | cloud, monitoring, coordination, service |
| **mirror‑play‑recorder** | “Records screen‑to‑screen exchanges for LoRA data” | plato | lora, training‑data, mirror‑play, i2i |
| **plato‑room‑builder** | “CLI to scaffold new PLATO room presets” | plato | cli, preset, scaffold, torch |
| **rabbit‑trail‑archive** | “Archive of research rabbit trails” | research | archive, exploration, prototypes |
| **radar‑rings‑art** | “Brand assets: radar rings, lighthouse, crab icons” | brand | svg, logo, wordmark, brand‑assets |
| **rust‑geom‑snap** | “Geometric snapping library (constraint theory)” | constraint‑theory | rust, geometric, snapping, library |
| **sentinel‑health‑checks** | “Health check scripts for fleet services” | tools | bash, monitoring, health, service‑guard |
| **shell‑classifier** | “First stage of Shell: classify incoming agents” | shell | python, classification, bootstrapping |
| **tile‑validator** | “Validates training tiles before PLATO submission” | plato | validation, tile, plato, quality‑check |
| **training‑room‑25** | “Preset 25: DeadbandRoom (self‑training room)” | plato | preset, deadband, self‑training, torch |
| **wasm‑agent‑runtime** | “Minimal WASM runtime for agent modules” | flux‑runtime | wasm, runtime, agent‑modules, sandbox |
| **zeroclaw‑boot‑log** | “Log of Zeroclaw boot camp cycles 1‑220” | docs | log, boot‑camp, cycles, zeroclaw |

## 3. Observations & Merge Candidates

**Potential merges (same domain, similar purpose):**
- `rust‑geom‑snap` could be merged into `constraint‑theory‑core` (both geometric snapping).
- `shell‑classifier` and `crab‑trap‑demo` are both Shell‑domain; could be a single `shell‑bootstrapping` repo.
- `training‑room‑25` is a preset of `plato‑torch`; could be a subdirectory there.
- `wasm‑agent‑runtime` is essentially a subset of `flux‑runtime`; could be integrated.

**Abandoned?**  
`rabbit‑trail‑archive` appears to be a pure archive—no recent commits. Likely kept for reference only.

**Missing documentation:**  
Several repos (`agent‑arena`, `constraint‑canvas`, `flux‑playground`) have minimal READMEs. Could be improved in Phase 5.

## 4. Next Steps for Curator

1. **Tag all repos** – Apply the above tags via GitHub API or local metadata.
2. **Propose merges** – Draft PR/issue suggestions for the merge candidates.
3. **Identify gaps** – Find domains with only one repo (e.g., `oracle`, `edge`) and suggest expansion.
4. **Build taxonomy file** – Commit a `fleet‑taxonomy.md` to `fleet‑knowledge/` for fleet‑wide use.

---

**Tile ready for submission:** This categorization provides a structured view of 20 previously uncategorized repos, aligns them with active fleet domains, and suggests concrete consolidation steps.