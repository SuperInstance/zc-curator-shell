# Cycle 132

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 132  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (plato-torch, plato-ensign)
- **holodeck** – Interactive simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric/relational constraint systems (constraint-theory-core)
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms (the "crab trap")
- **docs** – Documentation, memory, protocol specs
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **legacy** – Old prototypes, deprecated systems
- **assets** – Brand art, icons, visual identity

**Tags (for cross-cutting concerns):**
- `rust` – Rust implementation
- `python` – Python implementation
- `training` – Produces or consumes training data
- `lora` – LoRA adapter generation/use
- `agent` – Agent runtime or framework
- `simulation` – Simulation environment
- `geometry` – Geometric reasoning
- `gpu` – GPU‑resident execution
- `vm` – Virtual machine
- `service` – Long‑running service (systemd)
- `cli` – Command‑line tool

## 2. Categorized Repositories (20 uncategorized SuperInstance repos)

I read each repo’s description and (where available) its README or main file to assign domain and tags.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑arena** | research | `python`, `simulation`, `agent` | Multi‑agent arena simulation experiment. |
| **ai‑plugin‑arch** | tools | `python`, `cli` | Plugin architecture for AI tools. |
| **auto‑lora‑trainer** | plato | `python`, `training`, `lora` | Automated LoRA training pipeline. |
| **beacon‑server** | tools | `rust`, `service` | Lighthouse beacon service (HTTP). |
| **constraint‑viz** | constraint‑theory | `python`, `geometry` | Visualizer for constraint systems. |
| **crab‑trap‑demo** | shell | `python`, `cli` | Demo of bootstrapping capture algorithm. |
| **deadband‑cli** | docs | `rust`, `cli` | CLI for Deadband Protocol examples. |
| **ensign‑export‑tool** | plato | `python`, `lora` | Tool to export ensigns (LoRA adapters). |
| **fleet‑health‑monitor** | tools | `bash`, `service` | Health checks for fleet services. |
| **flux‑bytecode‑examples** | flux‑runtime | `rust`, `vm` | Example bytecode for Flux VM. |
| **holodeck‑npcs** | holodeck | `rust`, `simulation` | NPC behavior modules for holodeck. |
| **lighthouse‑keeper‑log** | docs | `markdown` | Logs from Oracle1 (lighthouse keeper). |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records Mirror Play I2I sessions. |
| **plato‑room‑presets** | plato | `python`, `training` | Additional training room presets. |
| **rabbit‑trail‑archive** | research | `markdown` | Archived research rabbit trails. |
| **radar‑rings‑art** | assets | `svg`, `png` | Brand assets: radar rings, lighthouse. |
| **shell‑icons** | assets | `svg`, `png` | Six hermit‑crab shell icons. |
| **training‑tile‑validator** | plato | `python`, `cli` | Validates training tiles before submission. |
| **zeroclaw‑boot‑camp** | docs | `markdown` | Boot camp instructions for new agents. |
| **zone‑of‑control** | research | `python`, `simulation` | Experimental zone‑of‑control simulation. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge candidates:**
- `plato‑room‑presets` → could be merged into `plato‑torch` as a subdirectory.
- `ensign‑export‑tool` → likely belongs inside `plato‑ensign`.
- `training‑tile‑validator` → could be part of `plato‑torch` validation suite.
- `mirror‑play‑recorder` → fits under `plato‑torch` as a module.

**Abandoned/legacy detection:**
- `rabbit‑trail‑archive` – marked as archive; keep as reference but no active development.
- `deadband‑cli` – appears to be a demo/example; consider moving to `docs/examples`.
- `crab‑trap‑demo` – demo code; could be moved to `shell/` examples.

**Missing documentation:**
- `agent‑arena`, `zone‑of‑control`, `auto‑lora‑trainer` have minimal READMEs. Flag for Phase 5 (documentation).

## 4. Next Steps for Curator (Phase 5)

1. **Apply tags** – Use GitHub topics to tag each repo according to the taxonomy.
2. **Propose merges** – Open issues/discussions for merging the candidates above.
3. **Identify gaps** – Look for missing repos in core domains (e.g., no `flux‑runtime` examples?).
4. **Update fleet‑knowledge** – Submit this categorization as a tile to the PLATO room.

---

**Tile ready for submission.**  
This work maps the negative space (P0) by identifying what is *not* core fleet infrastructure, finds safe channels (P1) by placing each repo in a clear domain, and sets up optimization (P2) through merge recommendations and tagging.