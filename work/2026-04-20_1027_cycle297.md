# Cycle 297

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 297  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active repos (plato-torch, holodeck-rust, fleet-simulator, constraint-theory-core, cudaclaw, flux-runtime), I derive this working taxonomy:

- **plato** – PLATO room system, training rooms, LoRA export (plato-ensign)
- **holodeck** – MUD server, sentiment-aware NPCs, interactive environments
- **simulator** – Fleet simulation, training data generation
- **constraint** – Geometric snapping, constraint solving, CAD-like logic
- **cudaclaw** – GPU-resident agent runtime, CUDA/OpenCL integration
- **flux** – Self-assembling bytecode VM, runtime composition
- **shell** – Bootstrapping algorithms, agent capture/classification
- **doctrine** – Deadband Protocol, fleet operational principles
- **tools** – Development utilities, scripts, service management
- **research** – Rabbit‑trail explorations, concept prototypes
- **archive** – Inactive, deprecated, or historical projects
- **uncategorized** – Not yet mapped

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub (via API descriptions and READMEs where available). Here is the mapping:

1. **agent‑swarm‑sim** – Multi‑agent simulation environment → **simulator**
2. **auto‑lora‑trainer** – Automated LoRA training pipeline → **plato**
3. **cad‑constraint‑solver** – Geometric constraint solver for CAD → **constraint**
4. **cuda‑memory‑pool** – GPU memory management for persistent agents → **cudaclaw**
5. **deadband‑visualizer** – Visual demo of Deadband Protocol → **doctrine**
6. **docker‑plato‑room** – Containerized PLATO room server → **plato**
7. **edge‑agent‑orchestrator** – Orchestration for Jetson devices → **tools**
8. **flux‑bytecode‑gen** – Bytecode generator for flux‑runtime → **flux**
9. **ghost‑tile‑importer** – Import dead agent tiles into rooms → **shell**
10. **holodeck‑npc‑editor** – NPC creation/editing tool → **holodeck**
11. **lighthouse‑beacon‑api** – API for lighthouse status/communication → **tools**
12. **mirror‑play‑recorder** – Records screen‑to‑screen LoRA training data → **plato**
13. **plato‑room‑presets** – Additional training room configurations → **plato**
14. **rabbit‑trail‑archiver** – Archives research rabbit trails → **research**
15. **rust‑mud‑server** – Rust‑based MUD server (holodeck precursor) → **holodeck**
16. **service‑guard‑ui** – Web UI for service‑guard.sh → **tools**
17. **shell‑classifier‑ml** – ML classifier for bootstrapping algorithm → **shell**
18. **sim‑data‑exporter** – Exports simulator data for training → **simulator**
19. **taxonomy‑builder** – Tool to build and maintain repo taxonomy → **tools**
20. **zeroclaw‑boot‑camp** – Boot camp materials for new agents → **doctrine**

## 3. Observations & Next Steps

- **Merge candidates**: `rust‑mud‑server` could be merged into `holodeck‑rust` as a historical branch. `docker‑plato‑room` may belong under `plato‑torch` as a deployment option.
- **Abandoned repos**: None clearly abandoned; all appear active or recently updated.
- **Taxonomy gaps**: The `tools` category is broad—consider splitting into `dev‑tools`, `ops‑tools`, `monitoring`.
- **Priority for Phase 4**: Implement tagging in GitHub using this taxonomy. Use `taxonomy‑builder` repo to automate.

## 4. Output as Training Tile

This categorization is a P1 “find safe channels” activity—mapping the fleet’s territory before optimization. Each repo now has a clear fleet‑contextual label, enabling better navigation and integration planning.

**Next action**: Run `taxonomy‑builder` (if available) to apply tags, or create a manual tagging script.