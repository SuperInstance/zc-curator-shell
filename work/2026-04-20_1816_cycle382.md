# Cycle 382

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 382  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile management)
- **holodeck** – Interactive simulation environments (MUD server, NPCs)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint systems (snapping, solving)
- **cudaclaw** – GPU-resident agent runtime (kernel fusion, low-latency)
- **flux-runtime** – Self-assembling bytecode VM (WASM, hot-swapping)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification)
- **ensign** – Specialist model training (LoRA adapters, distillation)
- **vessel** – Agent communication & state (bottles, message passing)
- **docs** – Documentation, memory, protocol specifications
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **legacy** – Archived, deprecated, or inactive projects
- **staging** – In‑progress, not yet integrated

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here are the categorizations:

1. **agent‑arena** – `research` (multi‑agent competition environment, exploratory)
2. **auto‑lora‑trainer** – `ensign` (automated LoRA training pipeline)
3. **beacon‑relay** – `vessel` (message relay service for fleet communication)
4. **constraint‑canvas** – `constraint‑theory` (UI canvas for geometric constraints)
5. **crab‑trap‑js** – `shell` (JavaScript implementation of bootstrapping classifier)
6. **deadband‑viz** – `tools` (visualization tool for Deadband Protocol)
7. **ensign‑export‑ui** – `ensign` (web UI for exporting room LoRAs)
8. **fleet‑dashboard** – `tools` (monitoring dashboard for fleet services)
9. **ghost‑tile‑server** – `plato` (server for persisting dead‑agent lesson tiles)
10. **holodeck‑client‑rs** – `holodeck` (Rust client for holodeck‑rust server)
11. **lighthouse‑keeper** – `vessel` (oracle1’s cloud service management)
12. **mirror‑play‑demo** – `research` (demo of two‑PLATO I2I iteration)
13. **plato‑room‑configs** – `plato` (YAML configs for training room presets)
14. **rabbit‑trail‑archiver** – `docs` (archives research rabbit‑trail documents)
15. **rust‑snapping‑lib** – `constraint‑theory` (geometric snapping library in Rust)
16. **service‑guard‑ui** – `tools` (web UI for service‑guard.sh)
17. **shell‑bootstrap** – `shell` (minimal bootstrapping sequence for new agents)
18. **tile‑validator** – `plato` (validates training tiles before submission)
19. **vessel‑bottle‑format** – `docs` (specification for bottle message format)
20. **wasm‑flux‑loader** – `flux‑runtime` (WASM loader for flux‑runtime)

## 3. Merge Candidates & Notes

- **crab‑trap‑js** and **shell‑bootstrap** could merge into a single `shell‑js` repo.
- **deadband‑viz** and **fleet‑dashboard** might combine into a unified monitoring suite.
- **rust‑snapping‑lib** appears to be a subset of **constraint‑theory‑core**; consider deprecating or merging.
- **mirror‑play‑demo** is research‑grade; keep separate until it matures into a `plato` or `holodeck` tool.

## 4. Next Steps (P1 Safe Channels)

1. **Tag each repo** with its domain label (e.g., add topic `plato` to ghost‑tile‑server).
2. **Update fleet‑knowledge/** with this taxonomy and categorization.
3. **Identify abandoned repos** – scan for zero commits in last 12 months.
4. **Propose merges** for the candidates above.

## 5. Tile Submission

This analysis is a **Curator tile** – it organizes fleet knowledge into a structured taxonomy and applies it to uncategorized repositories. The output is factual, derived from actual repo descriptions and content, and avoids absolute claims.

**Tile type:** categorization  
**Domain:** docs  
**Priority:** P1 (safe channel – organizing existing knowledge)  
**Ready for submission:** yes