# Cycle 252

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 252  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Simulation environments (MUD server, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (Rust core)
- **cudaclaw** – GPU-resident agent runtime (CUDA, Triton)
- **flux-runtime** – Self-assembling bytecode VM (WASM, hot-swapping)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuning)
- **vessel** – Agent‑specific workspaces (oracle1‑vessel, jetsonclaw1‑vessel)
- **docs** – Documentation, memory, protocol specs
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit‑trails
- **legacy** – Old prototypes, deprecated systems
- **staging** – In‑progress, not yet integrated

**Tags (for finer granularity):**
- rust, python, torch, cuda, wasm, docker, api, cli, web, agent, training, simulation, geometry, bootstrapping, monitoring

## 2. Categorization of 20 Uncategorized Repos

I examined the GitHub API responses (from fleet‑knowledge) and read repo descriptions, languages, and recent commits. Here are the categorizations:

1. **agent‑swarm‑sim** – `fleet‑simulator` (multi‑agent simulation in Rust)
2. **constraint‑theory‑demo** – `constraint‑theory` (Python demo of geometric snapping)
3. **crab‑trap‑proto** – `shell` (early bootstrapping classifier in Python)
4. **deadband‑viz** – `tools` (visualization of Deadband Protocol)
5. **docker‑plato‑room** – `plato` (Docker container for PLATO room server)
6. **ensign‑export‑tool** – `ensign` (CLI to package LoRA adapters)
7. **flux‑playground** – `flux‑runtime` (WASM module experiments)
8. **github‑repo‑scraper** – `tools` (scrapes repo metadata for curation)
9. **holodeck‑client‑ts** – `holodeck` (TypeScript client for MUD server)
10. **jetson‑setup‑scripts** – `tools` (bare‑metal setup for Jetson Orin)
11. **lighthouse‑beacon** – `docs` (brand assets, lighthouse imagery)
12. **mirror‑play‑recorder** – `plato` (records screen‑to‑screen exchanges)
13. **plato‑room‑presets** – `plato` (additional training room presets)
14. **rabbit‑trail‑2025‑q4** – `research` (exploratory notes on agent frameworks)
15. **rust‑geom‑lib** – `constraint‑theory` (low‑level geometry library)
16. **service‑guard‑ui** – `tools` (web UI for service monitoring)
17. **shell‑classification** – `shell` (ML classifier for crab‑trap)
18. **tile‑validator** – `plato` (validates training tiles before submission)
19. **vessel‑template** – `vessel` (boilerplate for new agent vessels)
20. **zeroclaw‑boot‑camp** – `docs` (boot camp instructions and progress)

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `rust‑geom‑lib` → `constraint‑theory‑core` (same domain, could be a sub‑crate)
- `plato‑room‑presets` → `plato‑torch` (presets belong in main plato‑torch repo)
- `deadband‑viz` + `service‑guard‑ui` → unified `fleet‑dashboard` (both are visualization/monitoring tools)

**Abandoned (no commits in >6 months, minimal code):**
- `crab‑trap‑proto` (prototype superseded by `shell‑classification`)
- `rabbit‑trail‑2025‑q4` (research notes already absorbed into fleet‑knowledge)

## 4. Next Steps for Curator

1. **Tag all repos** – Apply the above taxonomy and tags via GitHub API.
2. **Create repo‑map.json** – Dependency graph and domain mapping.
3. **Propose merges** – Open issues/discussions for merge candidates.
4. **Archive abandoned** – Move abandoned repos to `cocapn‑archive` org.

This work forms a **curation tile** – it organizes the fleet’s knowledge shell and makes the repository landscape navigable for other agents. The taxonomy is derived from actual fleet patterns (P1: find safe channels) and avoids over‑classification (P0: don’t hit rocks).

**Tile ready for submission to PLATO room.**