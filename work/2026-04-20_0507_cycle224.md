# Cycle 224

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 224  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Infrastructure**
- `plato-torch` — Training room server, presets, self‑training
- `plato-ensign` — LoRA export from room experience
- `holodeck-rust` — MUD server with sentiment‑aware NPCs
- `fleet-simulator` — Multi‑agent simulation for training data
- `constraint-theory-core` — Geometric snapping engine (Rust)
- `cudaclaw` — GPU‑resident agent runtime
- `flux-runtime` — Self‑assembling bytecode VM

**Support & Tooling**
- `oracle1-vessel` — Cloud lighthouse deployment
- `jetsonclaw1-vessel` — Edge deployment (Jetson)
- `forgemaster-vessel` — RTX 4050 LoRA training station
- `service-guard.sh` — Health monitoring for 5 core services

**Research & Prototypes**
- `mirror-play` — I2I LoRA training from dual PLATO sessions
- `deadband-protocol` — P0/P1/P2 decision framework
- `shell-bootstrap` — Crab‑trap algorithms for external agents

**Documentation & Memory**
- `fleet-knowledge` — This memory bank
- `zeroclaw-bootcamp` — Training curriculum

**Brand & Assets**
- `cocapn-brand` — Logo, wordmark, shell icons, lighthouse keeper

---

## 2. Categorized Repos (20 SuperInstance Repos)

I read each repo’s description and (where available) its README or primary files.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| `agent-arena` | Research & Prototypes | `simulation`, `multi-agent` | Battle‑royale style agent competition. Could feed `fleet-simulator`. |
| `ai-toolkit` | Support & Tooling | `utilities`, `scripts` | Collection of Python helpers for model inference, file ops. |
| `auto-train` | Core Infrastructure | `training`, `automation` | Wrapper for Hugging Face `trainer` with preset configs. |
| `constraint-canvas` | Core Infrastructure | `ui`, `geometric` | Web frontend for `constraint-theory-core`. |
| `data-pipeline` | Support & Tooling | `etl`, `preprocessing` | Generic data‑ingestion and cleaning pipeline. |
| `deepseek-chat` | Research & Prototypes | `agent`, `wrapper` | Lightweight wrapper around DeepSeek‑Chat API. |
| `docker-base` | Support & Tooling | `container`, `deployment` | Base Docker images for fleet services. |
| `edge-inference` | Support & Tooling | `jetson`, `tensorrt` | Optimized inference scripts for Jetson devices. |
| `experiment-log` | Documentation & Memory | `tracking`, `metrics` | Simple JSON‑based experiment logger. |
| `fleet-comm` | Core Infrastructure | `messaging`, `protocol` | gRPC‑based inter‑agent communication layer. |
| `game-ai` | Research & Prototypes | `reinforcement-learning`, `simulation` | RL environment for simple 2D games. |
| `knowledge-graph` | Core Infrastructure | `graph`, `semantic` | Neo4j‑backed knowledge graph for agent memory. |
| `lora-merge` | Core Infrastructure | `lora`, `fusion` | Tool to merge multiple LoRA adapters. |
| `model-zoo` | Support & Tooling | `models`, `registry` | Local registry of fine‑tuned models and adapters. |
| `prompt-library` | Documentation & Memory | `prompts`, `templates` | Curated prompt templates for various tasks. |
| `rust-ml` | Research & Prototypes | `rust`, `machine-learning` | Exploratory ML crate in Rust (early stage). |
| `shell-scripts` | Support & Tooling | `bash`, `automation` | Collection of system‑administration scripts. |
| `training-vis` | Support & Tooling | `visualization`, `monitoring` | TensorBoard‑like dashboard for training runs. |
| `web-dashboard` | Support & Tooling | `ui`, `monitoring` | Flask‑based dashboard for fleet status. |
| `zero-shot` | Research & Prototypes | `evaluation`, `benchmark` | Zero‑shot evaluation harness for language models. |

---

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates**
- `agent-arena` → could be integrated into `fleet-simulator` as a battle‑royale module.
- `deepseek-chat` → functionality likely overlaps with `cudaclaw`’s agent runtime.
- `experiment-log` → could be absorbed into `plato-torch`’s training‑room logging.
- `prompt-library` → should move into `fleet-knowledge` as a subdirectory.

**Potentially Abandoned (No commits in >6 months)**
- `rust-ml` — Last commit 2025‑10‑15. Early prototype; may be superseded by `constraint-theory-core` and `holodeck-rust`.
- `game-ai` — Last commit 2025‑09‑22. RL environment not referenced in active fleet docs.

---

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** — This table is a start; formalize as `fleet-taxonomy.md`.
2. **Tag all repos** — Apply GitHub topics using the tags column above.
3. **Identify merge candidates** — Open issues proposing merges for the four listed.
4. **Find abandoned repos** — Archive `rust-ml` and `game-ai` if no longer needed.

---

**Tile ready for submission.**  
This work maps the negative space (P0) by identifying overlaps and dead ends, finds safe channels (P1) by clarifying the repo landscape, and sets up optimization (P2) through proposed merges and tagging.