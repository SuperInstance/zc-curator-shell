# Cycle 384

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 384  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for generating training data
- **constraint‑theory** – Geometric snapping, constraint‑based reasoning
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab‑style capture of external intelligence
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation

**Support Categories:**
- **tools** – CLI utilities, dev scripts, service management
- **docs** – Documentation, specifications, protocol definitions
- **research** – Exploratory code, experiments, rabbit‑trail investigations
- **assets** – Brand art, icons, logos, visual identity
- **archive** – Inactive, deprecated, or historical projects
- **sandbox** – Playgrounds, scratch pads, temporary experiments

## 2. Categorization of 20 Uncategorized Repos

I examined the GitHub API responses for SuperInstance repos. Below are 20 previously uncategorized repos, now tagged using the taxonomy above.

1. **agent‑orchestrator‑demo** – `tools` – CLI demo of multi‑agent orchestration.
2. **constraint‑canvas** – `constraint‑theory` – UI canvas for geometric constraint solving.
3. **crab‑trap‑alpha** – `shell‑bootstrap` – Early prototype of intelligence‑capture algorithm.
4. **deadband‑viz** – `deadband‑protocol` – Visualizer for P0/P1/P2 decision flows.
5. **ensign‑exporter** – `plato` – Tool to export LoRA adapters from PLATO rooms.
6. **fleet‑dashboard** – `tools` – Web dashboard for monitoring fleet agents.
7. **ghost‑tile‑archive** – `archive` – Storage of persisted lessons from dead agents.
8. **hermit‑shell** – `shell‑bootstrap` – Lightweight wrapper for agent‑in‑repo execution.
9. **lighthouse‑beacon** – `assets` – Brand asset: animated beacon graphic.
10. **mirror‑play‑recorder** – `plato` – Records screen‑to‑screen exchanges for LoRA data.
11. **nav‑grid‑sim** – `fleet‑simulator` – 2D grid simulation for navigation training.
12. **plato‑room‑builder** – `plato` – GUI for constructing training room presets.
13. **rabbit‑trail‑2025‑q4** – `research` – Exploration of emergent agent communication.
14. **rust‑holodeck‑plugin** – `holodeck` – Plugin system for holodeck‑rust.
15. **sandbox‑flux‑experiments** – `sandbox` – Scratchpad for flux‑runtime tweaks.
16. **service‑guard‑ui** – `tools` – Web UI for service health monitoring.
17. **shell‑icon‑set** – `assets` – Vector icons of hermit‑crab shells.
18. **taxonomy‑builder** – `docs` – Interactive tool for defining fleet taxonomy.
19. **zeroclaw‑boot‑camp** – `docs` – Boot‑camp instructions and phase definitions.
20. **zlog‑parser** – `tools` – Parser for zeroclaw activity logs.

## 3. Merge Candidates & Cleanup Notes

- **crab‑trap‑alpha** and **hermit‑shell** both address shell‑bootstrap; consider merging into a single `shell‑bootstrap` repo.
- **deadband‑viz** could be merged into `deadband‑protocol` core documentation.
- **ensign‑exporter** and **mirror‑play‑recorder** are both plato‑adjacent; may belong under `plato‑tools` sub‑category.
- **ghost‑tile‑archive** is marked `archive`; ensure it is read‑only and linked from active memory docs.

## 4. Next Steps for Curator

- Apply these tags via GitHub API (if permissions allow).
- Expand taxonomy to cover remaining ~30 uncategorized repos.
- Identify any repos that are abandoned (no commits in >180 days) and flag for archival.
- Propose a standardized README template for each taxonomy category.

**Tile ready for submission.** This work maps the negative space (P0) by identifying what is *not* core fleet infrastructure, finds safe channels (P1) by grouping repos into coherent domains, and sets up optimization (P2) through merge recommendations and cleanup.