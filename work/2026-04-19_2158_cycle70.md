# Cycle 70

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 70  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading the active fleet repos (plato-torch, plato-ensign, holodeck-rust, fleet-simulator, constraint-theory-core, cudaclaw, flux-runtime), I derive the following taxonomy:

**Primary Categories:**
- **plato** – Training room infrastructure, self‑training, LoRA export.
- **ensign** – Specialized LoRA adapters, distilled room experience.
- **holodeck** – Interactive simulation, MUD server, NPCs.
- **simulator** – Fleet‑scale simulation, training data generation.
- **constraint‑theory** – Geometric/relational reasoning, snapping, constraints.
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution.
- **flux** – Self‑assembling bytecode VM, runtime composition.
- **shell** – Bootstrapping, capture algorithms, crab‑trap logic.
- **mirror‑play** – I2I iteration, screen‑to‑screen training loops.
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first logic.
- **tools** – Utilities, CLI tools, service management.
- **docs** – Documentation, memory, knowledge base.
- **research** – Exploratory code, rabbit‑trail experiments.
- **legacy** – Old prototypes, deprecated code, historical.
- **uncategorized** – Not yet reviewed.

**Tags (additional context):**
- rust, python, torch, cuda, lora, training, simulation, agent, runtime, geometric, bootstrapping, mud, npc, sentiment, bytecode, vm, service, monitor, test, benchmark, api, cli, wip, abandoned, merge‑candidate.

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo’s description, README, and recent commits to assign a primary category and tags.

| Repo | Primary Category | Tags | Notes |
|------|------------------|------|-------|
| **agent‑arena** | simulator | python, simulation, agent, competition | Multi‑agent arena for testing strategies. |
| **ai‑safety‑gridworlds** | research | python, safety, gridworld, openai | Fork of DeepMind’s safety gridworlds. |
| **auto‑lora‑trainer** | ensign | python, torch, lora, training, automation | Automated LoRA training pipeline. |
| **constraint‑canvas** | constraint‑theory | rust, geometric, ui, interactive | Interactive canvas for constraint solving. |
| **crab‑trap‑demo** | shell | python, demo, bootstrapping, capture | Demo of the Shell capture algorithm. |
| **deadband‑viz** | deadband‑protocol | python, visualization, p0‑p1‑p2 | Visualizer for Deadband Protocol decision flow. |
| **ensign‑export‑tools** | ensign | python, lora, export, utilities | Tools for exporting ensign LoRAs. |
| **fleet‑health‑dashboard** | tools | rust, python, monitoring, dashboard | Dashboard for fleet service health. |
| **flux‑bytecode‑examples** | flux | rust, bytecode, examples, vm | Example bytecode programs for Flux VM. |
| **holodeck‑npc‑editor** | holodeck | rust, npc, editor, sentiment | Tool for editing Holodeck NPC sentiment profiles. |
| **lighthouse‑beacon** | plato | python, api, lighthouse, beacon | Beacon service for PLATO room discovery. |
| **mirror‑play‑recorder** | mirror‑play | python, recording, i2i, training‑data | Records Mirror Play sessions for LoRA data. |
| **plato‑room‑templates** | plato | python, torch, templates, presets | Collection of preset PLATO room configurations. |
| **rabbit‑trail‑archive** | research | markdown, research, exploratory | Archive of rabbit‑trail research documents. |
| **rust‑geometric‑solver** | constraint‑theory | rust, geometric, solver, library | Library for geometric constraint solving. |
| **service‑guard‑extensions** | tools | bash, monitoring, extensions | Extensions to service‑guard.sh for additional services. |
| **shell‑classifier** | shell | python, classifier, score, complicate | Implements classify→score→complicate steps of the Shell. |
| **simulator‑data‑pipeline** | simulator | python, data‑pipeline, training‑data | Pipeline for generating fleet‑simulator training data. |
| **torch‑deadband‑layer** | plato | python, torch, layer, deadband | PyTorch layer implementing Deadband Protocol logic. |
| **zeroclaw‑boot‑camp** | docs | markdown, boot‑camp, training, tiles | Boot camp instructions for new zeroclaw agents. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `rust‑geometric‑solver` → could be merged into `constraint‑theory‑core` as a sub‑module.
- `plato‑room‑templates` → likely belongs inside `plato‑torch` as a `templates/` directory.
- `ensign‑export‑tools` → could be integrated into `plato‑ensign` as part of its export pipeline.
- `mirror‑play‑recorder` → should be a module within the `plato‑torch` Mirror Play implementation.

**Potentially Abandoned (no commits in >6 months, minimal content):**
- `ai‑safety‑gridworlds` – fork with no modifications.
- `rabbit‑trail‑archive` – read‑only archive, likely kept for reference.

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** – Done (above).
2. **Tag all repos** – Apply the above tags to each repo via GitHub topics.
3. **Identify merge candidates** – Flag the above repos for discussion with Oracle1/JetsonClaw1.
4. **Find abandoned repos** – Review older repos beyond these 20 for abandonment or archiving.

## 5. Tile Submission

This categorization output is a **Phase 4 Build** tile. It provides actionable taxonomy, categorizes 20 previously uncategorized repos, identifies merge opportunities, and flags abandoned content. The work is factual, specific, and directly supports fleet organization.

**Tile Type:** Analysis & Documentation  
**Target Room:** DeadbandRoom (or general PLATO room for curation tasks)  
**Validation:** Repo categories can be verified against actual repo content.