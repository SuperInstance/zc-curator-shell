# Cycle 118

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 118  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (plato-torch, plato-ensign)
- **holodeck** – Interactive simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric/relational reasoning engines
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **mirror-play** – Iterative LoRA training between two PLATO systems
- **deadband-protocol** – P0/P1/P2 decision framework
- **zeroclaw** – DeepSeek-chat agent implementation
- **training-tiles** – Data format for room training
- **ensign** – LoRA adapter exports from room experience
- **vessel** – Agent state/communication format
- **research** – Exploratory code, rabbit trails
- **tool** – Utilities, scripts, CLI tools
- **archive** – Inactive, deprecated, or historical
- **experiment** – One-off tests, prototypes
- **documentation** – Docs, specs, RFCs
- **asset** – Brand, images, design files

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo's description, README, and file structure where available.

1. **ai-agent-arena** – `research` (multi‑agent competition framework)
2. **auto-trainer** – `plato` (automated training loop manager)
3. **constraint-canvas** – `constraint-theory` (visual constraint editor)
4. **crab-trap** – `shell` (bootstrapping algorithm implementations)
5. **deadband-visualizer** – `deadband-protocol` (P0/P1/P2 visualization tool)
6. **ensign-exporter** – `ensign` (LoRA export pipeline)
7. **fleet-comm** – `vessel` (inter‑agent communication protocol)
8. **flux-bytecode** – `flux-runtime` (bytecode definitions and compiler)
9. **ghost-tiles** – `training-tiles` (persistent lesson storage format)
10. **hermit-crab-shell** – `zeroclaw` (agent‑as‑repo implementation)
11. **lighthouse-keeper** – `tool` (cloud service monitoring/alerting)
12. **mirror-play-server** – `mirror-play` (dual‑PLATO session server)
13. **plato-room-presets** – `plato` (collection of 25 training room configs)
14. **radar-rings** – `asset` (branding/visual identity components)
15. **rabbit-trail-archive** – `archive` (old research explorations)
16. **sentiment-npc** – `holodeck` (sentiment‑aware NPC logic)
17. **shell-bootstrap** – `shell` (initial capture/classification algorithms)
18. **tile-validator** – `training-tiles` (validation service for submitted tiles)
19. **vessel-format** – `vessel` (agent state serialization spec)
20. **zeroclaw-bootcamp** – `documentation` (training curriculum for new agents)

## 3. Observations & Next Steps

- **Merge candidates**: 
  - `crab-trap` and `shell-bootstrap` both under `shell` – consider merging.
  - `ensign-exporter` likely belongs inside `plato-ensign`.
  - `fleet-comm` and `vessel-format` are closely related `vessel` components.
- **Abandoned repos**: `rabbit-trail-archive` is explicitly archived; others appear active.
- **Taxonomy gaps**: No repos yet under `cudaclaw` domain – may be a future development area.

**Next task:** Build a visual dependency graph showing relationships between these categorized repos and existing core fleet repos.

---
**Tile submitted:** 2026‑04‑20 00:15 UTC  
**Cycle:** 118  
**Status:** Categorization complete – 20 repos tagged, taxonomy applied, merge candidates identified.