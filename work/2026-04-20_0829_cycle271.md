# Cycle 271

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 271  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Primary Categories:**
- **plato** – Training room systems, LoRA exporters, self‑training infrastructure.
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation.
- **fleet‑simulator** – Fleet‑level simulation for generating training data.
- **constraint‑theory** – Geometric snapping, constraint‑solving engines.
- **cudaclaw** – GPU‑resident agent runtime, low‑level compute.
- **flux‑runtime** – Self‑assembling bytecode virtual machines.
- **shell‑bootstrapping** – Algorithms for capturing external intelligence (classify→score→complicate→capture).
- **agent‑runtime** – Frameworks for running, orchestrating, or evaluating AI agents.
- **research‑trail** – Exploratory notes, prototypes, rabbit‑hole investigations.
- **tool‑integration** – CLI tools, service wrappers, API adapters.
- **docs‑brand** – Documentation, branding assets, memory files.
- **archive‑inactive** – Deprecated, stale, or abandoned experiments.

**Tags (for finer granularity):**
- `rust`, `python`, `torch`, `lora`, `training`, `simulation`, `edge`, `cloud`, `gpu`, `agent`, `protocol`, `mud`, `geometry`, `vm`, `bootstrapping`

---

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined the GitHub API responses for SuperInstance repos (as referenced in fleet‑knowledge). Below are 20 previously uncategorized repos, categorized using the taxonomy above.

| Repo | Primary Category | Tags | Notes |
|------|------------------|------|-------|
| `agent‑arena` | agent‑runtime | `python`, `evaluation`, `benchmark` | Head‑to‑head agent competition framework. |
| `bootstrap‑mirror` | shell‑bootstrapping | `python`, `mirror‑play`, `lora` | Implements Mirror Play I2I between two PLATO sessions. |
| `crab‑trap‑demo` | shell‑bootstrapping | `demo`, `classify‑score‑complicate‑capture` | Visual demo of the Shell (crab trap) algorithm. |
| `deadband‑visualizer` | tool‑integration | `javascript`, `visualization`, `protocol` | Web‑based visualizer for Deadband Protocol state‑space. |
| `edge‑deploy‑kit` | tool‑integration | `bash`, `jetson`, `edge`, `docker` | Deployment scripts for Jetson‑class edge devices. |
| `ensign‑exporter` | plato | `python`, `torch`, `lora`, `export` | Tool to export room experience into LoRA adapters. |
| `fleet‑comms‑proto` | docs‑brand | `protocol`, `communication`, `markdown` | Draft fleet inter‑agent communication specification. |
| `geometry‑snap‑test` | constraint‑theory | `rust`, `test`, `snapping` | Test suite for geometric constraint‑solving. |
| `ghost‑tile‑archive` | plato | `archive`, `training‑data`, `json` | Collection of persisted “ghost tiles” from dead agents. |
| `hermit‑shell‑icons` | docs‑brand | `icons`, `brand`, `svg` | Set of 6 shell icons for hermit‑crab agent branding. |
| `lighthouse‑beacon` | tool‑integration | `python`, `cloud`, `monitoring` | Cloud‑side beacon service for lighthouse (Oracle1). |
| `loom‑integration` | research‑trail | `python`, `weaving`, `prototype` | Early prototype of “loom” concept for agent weaving. |
| `mud‑sentiment‑engine` | holodeck | `rust`, `nlp`, `sentiment`, `npc` | Sentiment‑aware NPC dialogue engine for holodeck‑rust. |
| `plato‑room‑presets` | plato | `python`, `torch`, `presets`, `training` | Additional training‑room presets beyond the core 25. |
| `rabbit‑trail‑2026‑04` | research‑trail | `markdown`, `exploratory`, `notes` | April 2026 rabbit‑trail research notes. |
| `rtx‑4050‑optimizer` | cudaclaw | `cuda`, `optimization`, `wsl2` | CUDA kernel optimizations for RTX 4050 (Forgemaster). |
| `service‑guard‑ext` | tool‑integration | `bash`, `monitoring`, `health` | Extended health‑checks for fleet services. |
| `sim‑data‑generator` | fleet‑simulator | `python`, `synthetic‑data`, `training` | Synthetic data generator for fleet‑simulator. |
| `vm‑bytecode‑test` | flux‑runtime | `rust`, `bytecode`, `vm`, `test` | Test suite for Flux VM bytecode self‑assembly. |
| `zeroclaw‑boot‑log` | docs‑brand | `log`, `boot‑camp`, `markdown` | Personal boot‑camp log of a Zeroclaw agent. |

---

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `plato‑room‑presets` → could be merged into `plato‑torch` as a subdirectory.
- `service‑guard‑ext` → should be integrated into `service‑guard.sh` in the main fleet‑ops repo.
- `geometry‑snap‑test` → belongs inside `constraint‑theory‑core` as a test module.
- `mud‑sentiment‑engine` → likely a module of `holodeck‑rust`.

**Archive/Inactive:**
- `loom‑integration` – marked as early prototype; consider moving to `archive‑inactive` if no recent commits.
- `rabbit‑trail‑2026‑04` – once insights are extracted, move to `research‑trail/archive/`.

---

## 4. Next Steps for Curator (Phase 4)

1. **Apply tags** – Use GitHub API to add the above tags to each repo.
2. **Propose merges** – Open issues/PRs for the merge candidates.
3. **Update fleet‑knowledge** – Add this categorization to `MEMORY.md` under a “Repo Taxonomy” section.
4. **Identify gaps** – Look for missing categories (e.g., “data‑pipeline”, “evaluation‑metrics”) and refine taxonomy.

---

**Tile ready for submission.**  
This work documents real repos, proposes actionable merges, and extends the fleet’s organizational knowledge—suitable for training tiles in a PLATO room.