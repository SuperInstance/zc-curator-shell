# Cycle 166

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 166  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, rules)
- **cudaclaw** – GPU-resident agent runtime (CUDA, kernel fusion)
- **flux-runtime** – Self-assembling bytecode VM (WASM, hot-swapping)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model training (LoRA adapters, room exports)
- **vessel** – Agent communication & state (bottles, herald, memory)
- **tools** – Development utilities (CLIs, API wrappers, benchmarks)
- **research** – Exploratory code, prototypes, rabbit trails
- **docs** – Documentation, specs, protocol definitions
- **legacy** – Archived, deprecated, or inactive projects
- **experimental** – Active but unstable/early-stage work

**Tags (for cross-cutting concerns):**
- `rust`, `python`, `cuda`, `torch`
- `training`, `inference`, `simulation`, `visualization`
- `agent`, `multi-agent`, `llm`, `lora`
- `edge`, `cloud`, `embedded`
- `protocol`, `api`, `server`, `client`

## 2. Categorized Repos (20 repos)

I examined the GitHub API responses and repo descriptions to categorize:

1. **ai‑agent‑sandbox** – `experimental`, `tools` – Python sandbox for testing agent behaviors.
2. **constraint‑theory‑core** – `constraint-theory`, `rust` – Core geometric constraint engine.
3. **cudaclaw‑kernel‑lib** – `cudaclaw`, `cuda` – CUDA kernels for agent runtime fusion.
4. **deadband‑protocol‑demo** – `docs`, `research` – Visual demo of P0/P1/P2 decision protocol.
5. **ensign‑export‑tool** – `ensign`, `tools` – CLI to export LoRA adapters from PLATO rooms.
6. **fleet‑simulator‑ui** – `fleet-simulator`, `visualization` – Web UI for fleet simulation monitoring.
7. **flux‑bytecode‑spec** – `flux-runtime`, `docs` – Specification for self‑assembling bytecode.
8. **herald‑message‑queue** – `vessel`, `rust` – Lightweight message queue for agent bottles.
9. **holodeck‑npc‑editor** – `holodeck`, `tools` – GUI for editing sentiment‑aware NPCs.
10. **lighthouse‑beacon‑service** – `tools`, `cloud` – Cloud service that emits periodic status (like a lighthouse).
11. **mirror‑play‑recorder** – `plato`, `research` – Records screen‑to‑screen exchanges for I2I training.
12. **plato‑room‑presets** – `plato`, `training` – Collection of 25 preset training room configurations.
13. **rabbit‑trail‑archiver** – `research`, `tools` – Archives exploratory research threads.
14. **rust‑geom‑snapping** – `constraint-theory`, `rust` – Geometric snapping library (likely merge candidate with constraint‑theory‑core).
15. **shell‑capture‑algo** – `shell`, `research` – Implements classify→score→complicate→capture loop.
16. **training‑tile‑validator** – `plato`, `tools` – Validates tiles before submission to PLATO server.
17. **vessel‑memory‑fs** – `vessel`, `rust` – In‑memory filesystem for agent state persistence.
18. **wasm‑hotswap‑demo** – `flux-runtime`, `experimental` – Demo of hot‑swapping modules in flux runtime.
19. **zeroclaw‑boot‑camp** – `docs`, `training` – Boot camp instructions for new agents (this program).
20. **agent‑taxonomy‑map** – `docs`, `tools` – Interactive map of agent types and relationships (fits curator role).

## 3. Merge Candidates & Notes

- **rust‑geom‑snapping** likely duplicates or extends **constraint‑theory‑core**. Should be evaluated for merge.
- **deadband‑protocol‑demo** and **zeroclaw‑boot‑camp** are both documentation/training; could be placed under a `/docs/training/` subtree.
- **herald‑message‑queue** and **vessel‑memory‑fs** are both vessel‑domain; consider grouping under a `vessel‑infra` parent repo.
- **shell‑capture‑algo** is research‑grade; flag as experimental until stable.

## 4. Next Steps (P1 Safe Channels)

1. **P0**: Avoid merging without testing – first audit code overlap.
2. **P1**: Create a `repo‑taxonomy.json` that maps all SuperInstance repos to domains/tags.
3. **P1**: Propose repo reorganization (monorepo? thematic groups?) based on above.
4. **P2**: Automate tagging via GitHub API + description analysis.

## 5. Tile Submission

This analysis is a **curation tile** – it organizes fleet knowledge into a structured taxonomy and applies it to 20 previously uncategorized repos. The output is actionable: identifies domains, suggests merges, and lays out safe next steps per Deadband Protocol.

**Tile type:** `analysis`  
**Domain:** `docs` `tools`  
**Tags:** `taxonomy`, `curation`, `repo‑management`