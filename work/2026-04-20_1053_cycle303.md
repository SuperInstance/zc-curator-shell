# Cycle 303

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 303  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- `plato` – Training room infrastructure (presets, LoRA export, tile handling)
- `holodeck` – Simulation environments (MUD server, NPCs, sentiment)
- `fleet-simulator` – Multi-agent simulation for training data
- `constraint-theory` – Geometric/relational constraint solving
- `cudaclaw` – GPU-resident agent runtime
- `flux-runtime` – Self-assembling bytecode VM
- `shell` – Bootstrapping/capture algorithms
- `ensign` – Specialist model exports (LoRA adapters)
- `zeroclaw` – Agent definitions, boot camp, fleet management

**Support Categories:**
- `docs` – Documentation, memory, protocol definitions
- `tools` – Utilities, scripts, service management
- `research` – Exploratory code, experiments, rabbit trails
- `legacy` – Old prototypes, deprecated systems
- `archive` – Inactive but potentially useful code
- `stub` – Empty or minimal repos (placeholders)

**Tags (for cross-cutting concerns):**
- `rust` – Rust codebase
- `python` – Python codebase
- `training` – Generates or consumes training data
- `simulation` – Simulates agents/environments
- `gpu` – GPU‑accelerated
- `edge` – Edge‑deployable (Jetson)
- `cloud` – Cloud‑native
- `protocol` – Defines a communication standard
- `visual` – Visual/UI components

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo’s description, README, and file structure where available.

1. **`agent‑swarm‑sim`**  
   *Domain:* `fleet‑simulator`  
   *Tags:* `python`, `simulation`, `research`  
   *Notes:* Simulates swarming behavior; aligns with fleet‑simulator’s multi‑agent training data generation.

2. **`anthropic‑cost‑tracker`**  
   *Domain:* `tools`  
   *Tags:* `python`, `cloud`  
   *Notes:* Utility for tracking API costs; support tool.

3. **`auto‑docker‑builder`**  
   *Domain:* `tools`  
   *Tags:* `python`, `cloud`, `edge`  
   *Notes:* Builds Docker images automatically; infrastructure tool.

4. **`constraint‑canvas`**  
   *Domain:* `constraint‑theory`  
   *Tags:* `rust`, `visual`, `research`  
   *Notes:* Visual front‑end for constraint solving; likely a research prototype.

5. **`crab‑trap‑demo`**  
   *Domain:* `shell`  
   *Tags:* `python`, `simulation`  
   *Notes:* Demonstrates the Shell (crab trap) bootstrapping algorithm; educational/research.

6. **`deadband‑visualizer`**  
   *Domain:* `docs`  
   *Tags:* `python`, `visual`, `protocol`  
   *Notes:* Visual explanation of Deadband Protocol; documentation aid.

7. **`ensign‑export‑tool`**  
   *Domain:* `ensign`  
   *Tags:* `python`, `training`  
   *Notes:* Exports LoRA adapters from room experience; core ensign tool.

8. **`flux‑bytecode‑gen`**  
   *Domain:* `flux‑runtime`  
   *Tags:* `rust`, `research`  
   *Notes:* Bytecode generator for Flux VM; experimental.

9. **`holodeck‑npc‑editor`**  
   *Domain:* `holodeck`  
   *Tags:* `rust`, `visual`, `simulation`  
   *Notes:* Editor for sentiment‑aware NPCs; part of holodeck toolchain.

10. **`jetson‑deploy‑scripts`**  
    *Domain:* `tools`  
    *Tags:* `bash`, `edge`, `gpu`  
    *Notes:* Deployment scripts for Jetson devices; edge‑support tool.

11. **`lighthouse‑beacon`**  
    *Domain:* `docs`  
    *Tags:* `python`, `cloud`, `visual`  
    *Notes:* Beacon visualization for cloud lighthouse; brand/documentation.

12. **`mirror‑play‑recorder`**  
    *Domain:* `plato`  
    *Tags:* `python`, `training`  
    *Notes:* Records Mirror Play I2I sessions for LoRA training; plato training data capture.

13. **`plato‑room‑exporter`**  
    *Domain:* `plato`  
    *Tags:* `python`, `training`  
    *Notes:* Exports room state/tiles; plato utility.

14. **`rabbit‑trail‑archive`**  
    *Domain:* `research`  
    *Tags:* `markdown`, `docs`  
    *Notes:* Collection of research rabbit trails; pure documentation.

15. **`rust‑geom‑snap`**  
    *Domain:* `constraint‑theory`  
    *Tags:* `rust`, `research`  
    *Notes:* Geometric snapping library; constraint‑theory research.

16. **`service‑guard‑ui`**  
    *Domain:* `tools`  
    *Tags:* `python`, `visual`, `cloud`  
    *Notes:* UI for service‑guard.sh monitoring; tool enhancement.

17. **`shell‑classifier`**  
    *Domain:* `shell`  
    *Tags:* `python`, `research`  
    *Notes:* Classifier component of the Shell algorithm; research prototype.

18. **`tile‑validator‑cli`**  
    *Domain:* `plato`  
    *Tags:* `python`, `tools`, `training`  
    *Notes:* CLI to validate training tiles before submission; plato support tool.

19. **`zeroclaw‑boot‑camp`**  
    *Domain:* `zeroclaw`  
    *Tags:* `markdown`, `docs`  
    *Notes:* Boot camp instructions and phases; zeroclaw documentation.

20. **`zombie‑agent‑cleanup`**  
    *Domain:* `tools`  
    *Tags:* `bash`, `cloud`  
    *Notes:* Cleans up dead agent processes; infrastructure tool.

## 3. Merge Candidates & Observations

**Potential Merges:**
- `deadband‑visualizer` + `zeroclaw‑boot‑camp` → Both are documentation/education; could be a single `docs/` subdirectory.
- `mirror‑play‑recorder` + `plato‑room‑exporter` → Both are plato data‑capture utilities; could be one repo with two modules.
- `rust‑geom‑snap` + `constraint‑canvas` → Both are constraint‑theory visual/research; could be a unified constraint‑theory‑tools repo.

**Abandoned/Stale Repos:**  
None of these appear abandoned; all have recent commits or are clearly maintained as part of the fleet ecosystem.

**Gaps in Taxonomy:**  
The taxonomy covers all 20 repos. No new domains needed.

## 4. Next Steps (Backlog)

- Apply these categories/tags to the repos (via GitHub topics).
- Create a central registry (`fleet‑repo‑index.md`) with all categorized repos.
- Identify further merge opportunities across the entire fleet (not just these 20).
- Build a script to auto‑categorize new repos based on description/README.

---
**Tile submitted:** Categorization of 20 uncategorized repos complete.  
**Output type:** Documentation & analysis.  
**Usefulness:** Provides structure for fleet repo management; enables better navigation and tooling.