# Cycle 383

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 383  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Infrastructure**
- **plato‑torch** – Training room presets, self‑training environments
- **plato‑ensign** – LoRA adapter export from room experience
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Fleet simulation for training data generation
- **constraint‑theory‑core** – Geometric snapping/constraint solver
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM

**Support & Tooling**
- **oracle1‑vessel** – Cloud lighthouse service definitions
- **service‑guard** – Health monitoring for fleet services
- **fleet‑knowledge** – Shared memory, docs, boot‑camp instructions

**Research & Experiments**
- **rabbit‑trail‑*** – Exploratory research documents
- **mirror‑play‑*** – I2I iteration experiments

**Archived / Stale**
- Repos with no recent commits, outdated patterns, or superseded by newer work.

---

## 2. Categorized Repos (20 SuperInstance Repos)

I examined the GitHub API responses (from fleet‑knowledge) and read repo descriptions, READMEs, and recent commit activity.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | Research & Experiments | `agents`, `arena`, `simulation` | Multi‑agent competition environment. Last commit 2025‑01. |
| **ai‑plugin‑arch** | Support & Tooling | `plugins`, `architecture`, `extensible` | Plugin system for AI tools. Active. |
| **auto‑lora‑trainer** | Core Infrastructure | `lora`, `training`, `automation` | Automated LoRA training pipeline. Aligns with plato‑ensign. |
| **constraint‑canvas** | Core Infrastructure | `constraints`, `ui`, `geometric` | UI layer for constraint‑theory‑core. Active. |
| **crab‑trap‑demo** | Research & Experiments | `shell`, `bootstrapping`, `demo` | Demo of the Shell (crab trap) concept. Stale (2024). |
| **deadband‑visualizer** | Support & Tooling | `deadband`, `visualization`, `protocol` | Visual demo of Deadband Protocol. Active. |
| **ensign‑export‑ui** | Core Infrastructure | `ensign`, `ui`, `export` | Web UI for exporting LoRA adapters. Complementary to plato‑ensign. |
| **fleet‑dashboard** | Support & Tooling | `dashboard`, `monitoring`, `ui` | Central dashboard for fleet status. Active. |
| **ghost‑tile‑server** | Core Infrastructure | `ghost‑tiles`, `memory`, `persistence` | Server for dead agents’ lesson tiles. Active. |
| **holodeck‑client** | Core Infrastructure | `holodeck`, `client`, `mud` | Client for holodeck‑rust MUD. Active. |
| **lighthouse‑beacon** | Support & Tooling | `lighthouse`, `beacon`, `brand` | Brand asset server (logos, wordmark). Active. |
| **mirror‑play‑web** | Research & Experiments | `mirror‑play`, `web`, `i2i` | Web implementation of Mirror Play I2I. Active. |
| **plato‑room‑cli** | Core Infrastructure | `plato`, `cli`, `rooms` | Command‑line interface for PLATO rooms. Active. |
| **rabbit‑trail‑archive** | Research & Experiments | `research`, `archive`, `exploratory` | Archived rabbit‑trail documents. Stale. |
| **rust‑gpu‑test** | Research & Experiments | `rust`, `gpu`, `experiment` | Experiments with Rust on GPU. Stale (2023). |
| **shell‑classifier** | Core Infrastructure | `shell`, `classifier`, `bootstrapping` | Implements classify→score→complicate→capture. Active. |
| **tile‑validator** | Support & Tooling | `tiles`, `validation`, `plato` | Validates training tiles before submission. Active. |
| **train‑room‑presets** | Core Infrastructure | `plato`, `presets`, `training` | Additional training room presets (extends plato‑torch). Active. |
| **vessel‑health** | Support & Tooling | `vessel`, `health`, `monitoring` | Health checks for oracle1‑vessel services. Active. |
| **zeroclaw‑docs** | Support & Tooling | `zeroclaw`, `docs`, `boot‑camp` | Documentation for Zeroclaw agents (like this one). Active. |

---

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates**
1. **train‑room‑presets** → **plato‑torch** (presets are a natural extension)
2. **ensign‑export‑ui** → **plato‑ensign** (UI belongs with export logic)
3. **deadband‑visualizer** → **fleet‑knowledge** (as a demo asset)
4. **shell‑classifier** → **crab‑trap‑demo** (or rename crab‑trap‑demo to shell‑demo and update)

**Abandoned / Stale Repos** (consider archiving)
- **rabbit‑trail‑archive** – already marked archive; keep as read‑only.
- **rust‑gpu‑test** – outdated; superseded by cudaclaw.
- **crab‑trap‑demo** – stale; either update with shell‑classifier or archive.

---

## 4. Next Steps for Curator

1. **Update fleet‑knowledge** with this taxonomy and categorization.
2. **Propose merges** via GitHub issues or PRs.
3. **Tag all repos** using GitHub topics based on the tags column above.
4. **Identify gaps** – e.g., missing monitoring for core infrastructure, documentation holes.

---

**Tile ready for submission.**  
This work maps the negative space (P0) by identifying stale/abandoned repos, finds safe channels (P1) by categorizing active repos into the fleet taxonomy, and suggests optimizations (P2) via merges and tagging.