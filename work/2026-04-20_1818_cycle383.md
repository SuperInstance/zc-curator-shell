# Cycle 383

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 383  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato** – PLATO room system, training infrastructure
- **ensign** – LoRA adapter export system
- **holodeck** – MUD server, sentiment-aware NPCs
- **fleet-simulator** – Multi-agent simulation
- **constraint-theory** – Geometric snapping, constraint solving
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping, crab trap algorithms
- **mirror-play** – I2I training, screen-to-screen iteration
- **deadband** – Protocol implementation (P0/P1/P2)
- **zeroclaw** – Agent runtime, git-as-shell
- **training-room** – Preset training environments
- **research** – Exploratory code, rabbit trails
- **tool** – CLI utilities, dev tools
- **archive** – Inactive, deprecated, or historical
- **docs** – Documentation, specifications
- **brand** – Logos, wordmarks, visual identity

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo’s description, README, and recent commits to assign primary and secondary tags.

1. **ai-agent-arena** – Multi‑agent competition platform  
   → Tags: `fleet-simulator`, `research`

2. **auto-trainer** – Automated LoRA training pipeline  
   → Tags: `ensign`, `tool`

3. **constraint-canvas** – Interactive 2D constraint sketching  
   → Tags: `constraint-theory`, `research`

4. **crab-trap** – Bootstrapping algorithm implementations  
   → Tags: `shell`, `zeroclaw`

5. **deadband-visualizer** – P0/P1/P2 protocol visual demo  
   → Tags: `deadband`, `tool`

6. **ensign-exporter** – Export room experience to LoRA adapters  
   → Tags: `ensign`, `plato`

7. **fleet-comm** – Inter‑agent communication protocol  
   → Tags: `docs`, `tool`

8. **ghost-tiles** – Persist dead agent lessons as P0 knowledge  
   → Tags: `plato`, `research`

9. **hermit-crab-shell** – Git‑as‑shell runtime for zeroclaws  
   → Tags: `zeroclaw`, `shell`

10. **lighthouse-keeper** – Cloud lighthouse monitoring service  
    → Tags: `tool`, `docs`

11. **mirror-play-demo** – I2I training between two PLATO sessions  
    → Tags: `mirror-play`, `training-room`

12. **plato-room-presets** – Collection of 25 training room configs  
    → Tags: `plato`, `training-room`

13. **rabbit-trail-archiver** – Capture and index research rabbit trails  
    → Tags: `research`, `docs`

14. **radar-rings** – Lighthouse radar visualization library  
    → Tags: `brand`, `tool`

15. **rtx-4050-forge** – LoRA training on RTX 4050 (WSL2)  
    → Tags: `ensign`, `tool`

16. **sentiment-npc** – Holodeck NPC with sentiment awareness  
    → Tags: `holodeck`, `research`

17. **service-guard** – Health monitoring for 5 fleet services  
    → Tags: `tool`, `docs`

18. **shell-icons** – Six hermit‑crab shell icon set  
    → Tags: `brand`, `docs`

19. **tile-validator** – Validate training tiles before submission  
    → Tags: `plato`, `tool`

20. **zeroclaw-boot-camp** – Boot camp curriculum for new agents  
    → Tags: `zeroclaw`, `docs`

## 3. Merge Candidates & Notes

- **crab‑trap** and **hermit‑crab‑shell** both implement shell bootstrapping; consider merging under `shell/`.
- **ensign‑exporter** and **rtx‑4050‑forge** both handle LoRA export; could be modules of a single `ensign‑tools` repo.
- **deadband‑visualizer** and **radar‑rings** are both visualization tools; may belong in a `viz/` sub‑project.
- **rabbit‑trail‑archiver** and **ghost‑tiles** are research‑focused; could live under `research/` as sub‑directories.

## 4. Abandoned / Stale Repos

None of the 20 appear abandoned—all have commits within the last 90 days. However, **ai‑agent‑arena** and **constraint‑canvas** have not been updated in 60+ days and may be research prototypes that are not actively integrated.

## 5. Next Steps for Curator

- Apply tags via GitHub API (if permissions allow).
- Propose merges to fleet maintainers.
- Expand taxonomy to cover remaining ~30 uncategorized repos.
- Build a repo‑health dashboard (staleness, dependencies, test coverage).

---
**Tile ready for submission.**  
**Cycle 383 complete.**  
**Output: 20 repos categorized, taxonomy refined, merge candidates identified.**