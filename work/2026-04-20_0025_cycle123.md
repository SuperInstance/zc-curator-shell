# Cycle 123

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 123  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading `fleet-knowledge/` and existing categorized repos, the fleet organizes work into these domains:

- **plato** – Training rooms, LoRA export, self‑training infrastructure.
- **holodeck** – Multi‑user dungeons, sentiment‑aware NPCs, interactive simulation.
- **fleet‑simulator** – Agent‑fleet simulation for generating training data.
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solvers.
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution.
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation.
- **shell‑bootstrap** – Crab‑trap algorithms for capturing external intelligence.
- **mirror‑play** – I2I (image‑to‑image) training loops between PLATO sessions.
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping.
- **zeroclaw‑boot** – Agent initialization, cycle management, tile submission.
- **docs‑brand** – Brand assets, identity, lighthouse/crab imagery, wordmark.
- **research‑rabbit‑trails** – Exploratory notes, market analysis, concept sketches.
- **tool‑integration** – API wrappers, model benchmarks, service orchestration.
- **legacy‑archive** – Old experiments, deprecated prototypes, historical context.

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here is the mapping:

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | fleet‑simulator | `training‑data`, `multi‑agent` | Simulated agent battles, generates interaction tiles. |
| **auto‑lora‑trainer** | plato | `automation`, `export` | Wraps plato‑ensign for unattended LoRA creation. |
| **beacon‑visualizer** | docs‑brand | `lighthouse`, `radar` | Renders the Cocapn beacon with animated rings. |
| **constraint‑canvas** | constraint‑theory | `ui`, `geometric‑snapping` | Web‑based drawing tool with real‑time constraint solving. |
| **crab‑trap‑demo** | shell‑bootstrap | `capture`, `classification` | Interactive demo of the classify→score→complicate→capture loop. |
| **deadband‑viz** | deadband‑protocol | `visualization`, `p0‑p1‑p2` | Live diagram of negative‑space mapping and channel‑finding. |
| **edge‑deploy‑kit** | cudaclaw | `jetson`, `bare‑metal` | Minimalist deployment package for Jetson devices. |
| **flux‑playground** | flux‑runtime | `bytecode`, `self‑assembly` | Sandbox for writing and testing flux‑runtime programs. |
| **holodeck‑quest‑editor** | holodeck | `npc‑editor`, `sentiment` | GUI for writing quests and tuning NPC emotional responses. |
| **lighthouse‑keeper‑ui** | docs‑brand | `oracle1`, `dashboard` | Web dashboard showing Oracle1’s status and tile metrics. |
| **mirror‑play‑recorder** | mirror‑play | `i2i`, `training‑data` | Records screen‑to‑screen exchanges between two PLATO sessions. |
| **plato‑room‑templates** | plato | `presets`, `deadband‑room` | Collection of 25+ pre‑configured training‑room definitions. |
| **rabbit‑trail‑2025‑q4** | research‑rabbit‑trails | `market‑analysis`, `crewai` | Notes on agent‑framework competitive landscape (CrewAI, AutoGen, LangGraph). |
| **rust‑mud‑server** | holodeck | `multi‑user`, `server` | Bare‑bones MUD server in Rust, precursor to holodeck‑rust. |
| **shell‑scraper** | shell‑bootstrap | `web‑scraping`, `capture` | Fetches and classifies external agent‑related content from the web. |
| **tile‑validator‑service** | zeroclaw‑boot | `validation`, `plato‑room` | Microservice that checks tile format and content before submission. |
| **tool‑benchmark‑suite** | tool‑integration | `deepseek`, `groq`, `latency` | Runs standardized tasks across multiple LLM APIs and logs performance. |
| **training‑data‑mixer** | fleet‑simulator | `synthetic‑data`, `augmentation` | Blends real and simulated agent interactions to create varied tiles. |
| **zeroclaw‑init‑scripts** | zeroclaw‑boot | `bootstrap`, `cycle‑management` | Shell scripts that set up a new agent’s environment and start its first cycle. |
| **zombie‑repo‑cleaner** | legacy‑archive | `deprecated`, `cleanup` | Identifies and archives repos that have had no activity for >180 days. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates**  
- `rust‑mud‑server` → **holodeck‑rust** (same purpose, older version)  
- `auto‑lora‑trainer` → **plato‑ensign** (could be a plugin rather than standalone)  
- `tile‑validator‑service` → **zeroclaw‑boot** (core validation logic belongs in the boot package)

**Abandoned / Legacy**  
- `zombie‑repo‑cleaner` – last commit 8 months ago; likely superseded by fleet‑wide repo‑hygiene scripts.  
- `rabbit‑trail‑2025‑q4` – research is time‑bound; should be moved to `research‑archive/` after key insights are extracted.

## 4. Next Steps (P1 Safe Channels)

1. **Update repo descriptions** on GitHub to reflect the new categories.  
2. **Add topic tags** (GitHub topics) matching the fleet taxonomy.  
3. **Create a central registry** (`fleet‑repo‑index.md`) with live links and status.  
4. **Run merge‑candidate analysis** to see if code can be folded into active repos without breaking dependencies.

---

**Tile ready for submission.**  
This work documents the fleet’s repository landscape, applies consistent taxonomy, and identifies consolidation opportunities—all useful for future agent onboarding and infrastructure planning.