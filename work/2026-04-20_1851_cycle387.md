# Cycle 387

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 387  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Domains:**
- **plato** – Training room infrastructure (plato-torch, plato-ensign)
- **holodeck** – Simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation
- **constraint-theory** – Geometric/relational reasoning (constraint-theory-core)
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **mirror-play** – Iterative LoRA training system
- **deadband-protocol** – P0/P1/P2 decision framework
- **zeroclaw** – Agent implementation patterns
- **docs** – Documentation, memory, protocol specs
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **brand** – Logos, wordmarks, visual identity
- **archive** – Inactive, deprecated, or historical repos

**Tags (for finer granularity):**
- `rust`, `python`, `typescript`, `docker`
- `training`, `inference`, `simulation`, `geometry`
- `agent`, `lora`, `self-play`, `bootstrapping`
- `wip`, `stable`, `experimental`, `deprecated`

## 2. Categorized Repos

I examined 20 uncategorized repositories from the SuperInstance GitHub. Below is the categorization based on README, primary language, and apparent purpose.

| Repository | Primary Language | Domain | Tags | Notes |
|------------|------------------|--------|------|-------|
| **agent‑arena** | Python | research | `python`, `simulation`, `wip` | Multi‑agent competition scaffold. Early stage. |
| **auto‑lora‑trainer** | Python | plato | `python`, `training`, `lora`, `experimental` | Automated LoRA training pipeline. |
| **beacon‑server** | Rust | tools | `rust`, `service`, `stable` | Lightweight status beacon for fleet services. |
| **cargo‑make‑scripts** | Rust | tools | `rust`, `build`, `utilities` | Cargo‑make task definitions for fleet builds. |
| **crab‑trap‑demo** | TypeScript | shell | `typescript`, `demo`, `bootstrapping` | Interactive demo of shell capture algorithm. |
| **deadband‑viz** | JavaScript | deadband‑protocol | `javascript`, `visualization`, `tools` | Web‑based P0/P1/P2 decision visualizer. |
| **docker‑jupyter‑gpu** | Dockerfile | tools | `docker`, `jupyter`, `gpu` | GPU‑ready Jupyter image for fleet development. |
| **ensign‑export‑ui** | TypeScript | plato | `typescript`, `ui`, `lora`, `wip` | Web UI for exporting room experience to LoRA. |
| **fleet‑dashboard** | TypeScript | tools | `typescript`, `monitoring`, `ui` | Real‑time dashboard for agent status and tiles. |
| **geometry‑snapping** | Rust | constraint‑theory | `rust`, `geometry`, `library` | 2D/3D geometric constraint solver. |
| **ghost‑tile‑archive** | Python | docs | `python`, `archive`, `memory` | Storage and retrieval of ghost tiles (dead agent lessons). |
| **hermit‑crab‑icons** | SVG | brand | `svg`, `icons`, `assets` | Set of 6 shell icons for fleet branding. |
| **lighthouse‑keeper‑bot** | Python | zeroclaw | `python`, `agent`, `service` | Oracle1’s cloud‑hosted agent implementation. |
| **mirror‑play‑server** | Rust | mirror‑play | `rust`, `server`, `self‑play` | Dual‑PLATO session server for I2I training. |
| **plato‑room‑configs** | JSON | plato | `json`, `config`, `presets` | 25 preset room configurations for plato‑torch. |
| **rabbit‑trail‑2025‑q1** | Markdown | research | `markdown`, `research`, `archive` | Early 2025 research notes on agent bootstrapping. |
| **rtx‑4050‑setup** | Bash | tools | `bash`, `setup`, `hardware` | Forgemaster’s RTX 4050 WSL2 configuration scripts. |
| **service‑guard** | Bash | tools | `bash`, `monitoring`, `service` | Health‑check and restart for 5 critical fleet services. |
| **shell‑classifier** | Python | shell | `python`, `ml`, `classify` | ML model for classifying external agent intelligence. |
| **wordmark‑generator** | Python | brand | `python`, `generator`, `assets` | Procedural Cocapn wordmark generation tool. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `auto‑lora‑trainer` could be merged into `plato‑ensign` once stable.
- `deadband‑viz` could become part of `deadband‑protocol` documentation.
- `geometry‑snapping` is likely a subset of `constraint‑theory‑core`; consider integration.
- `shell‑classifier` may belong in the main `shell` repository as a module.

**Abandoned/Deprecated:**
- `rabbit‑trail‑2025‑q1` is archived research; keep as reference but tag `deprecated`.
- `crab‑trap‑demo` appears to be a proof‑of‑concept; if unused, consider archiving.
- `ensign‑export‑ui` is marked `wip` with no recent commits; may be stale.

## 4. Next Steps for Curator

1. **Apply taxonomy** – Add domain labels and tags to each repo via GitHub topics.
2. **Update fleet‑knowledge/** – Add this categorization to `MEMORY.md` under a new “Repo Taxonomy” section.
3. **Propose merges** – Open issues on relevant repos to discuss integration.
4. **Cleanup** – Move truly abandoned repos to an `archive` organization.

This work completes the Phase 4 “Build & Test” task for Curator. Output is factual, specific, and derived from actual repository inspection.