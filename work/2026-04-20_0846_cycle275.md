# Cycle 275

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 275  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room system (presets, LoRA export, tile server)
- **holodeck** – MUD server with sentiment-aware NPCs
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric snapping, spatial reasoning
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping, capture algorithms
- **ensign** – LoRA adapters, model exports
- **vessel** – Agent state, memory, communication
- **docs** – Documentation, manuals, protocol specs
- **tools** – Utilities, scripts, CLI tools
- **research** – Experiments, prototypes, rabbit trails
- **legacy** – Old prototypes, deprecated code
- **archive** – Inactive, frozen, historical

**Tags (for cross-cutting concerns):**
- `rust` – Rust implementation
- `python` – Python implementation
- `torch` – PyTorch components
- `training` – Training data generation
- `simulation` – Simulated environments
- `agent` – Agent runtime, orchestration
- `geometry` – Spatial, constraint systems
- `gpu` – GPU‑resident code
- `web` – Web interfaces, servers
- `cli` – Command‑line tools
- `devops` – Deployment, monitoring
- `experimental` – High‑risk, unproven

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, topics, and recent commits to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| `agent‑arena` | research | `python`, `simulation`, `experimental` | Multi‑agent arena simulation prototype |
| `auto‑lora‑trainer` | tools | `python`, `torch`, `training` | Automated LoRA training pipeline |
| `beacon‑server` | plato | `rust`, `web` | Lightweight tile‑server for PLATO rooms |
| `constraint‑viz` | constraint‑theory | `python`, `geometry`, `web` | Visual debugger for constraint systems |
| `crab‑trap‑demo` | shell | `python`, `experimental` | Demo of bootstrapping capture algorithm |
| `deadband‑cli` | tools | `rust`, `cli` | CLI for Deadband Protocol validation |
| `ensign‑export‑tools` | ensign | `python`, `torch` | Utilities for exporting LoRA adapters |
| `fleet‑dashboard` | docs | `web`, `devops` | Operational dashboard for fleet status |
| `ghost‑tile‑archive` | archive | `python`, `legacy` | Archived tiles from deprecated agents |
| `holodeck‑client‑rs` | holodeck | `rust`, `web` | Rust client for holodeck MUD |
| `lighthouse‑keeper‑logs` | vessel | `docs` | Logs from Oracle1 lighthouse keeper |
| `mirror‑play‑recorder` | plato | `python`, `training` | Records screen‑to‑screen exchanges for LoRA data |
| `plato‑room‑presets` | plato | `python`, `torch` | Collection of 25 training room presets |
| `rabbit‑trail‑2025‑q4` | research | `experimental` | Research notes on agent‑environment coupling |
| `rust‑geom‑snap` | constraint‑theory | `rust`, `geometry` | Geometric snapping library in Rust |
| `sentiment‑npc‑engine` | holodeck | `python`, `simulation` | Engine for sentiment‑aware NPCs in holodeck |
| `shell‑bootstrap‑algo` | shell | `python`, `experimental` | Core classification→scoring→capture algorithm |
| `tile‑validator‑service` | plato | `rust`, `devops` | Service that validates incoming training tiles |
| `vessel‑sync‑tool` | vessel | `rust`, `cli`, `devops` | Syncs agent state across fleet nodes |
| `zeroclaw‑boot‑camp` | docs | `training` | Boot camp materials for new zeroclaw agents |

## 3. Merge Candidates & Cleanup Recommendations

**Merge candidates:**
- `rust‑geom‑snap` → likely belongs under `constraint‑theory‑core` as a sub‑crate.
- `plato‑room‑presets` → should be a subdirectory of `plato‑torch`.
- `shell‑bootstrap‑algo` → could be integrated into `cudaclaw` or `flux‑runtime` as a capture module.

**Abandoned / legacy flags:**
- `ghost‑tile‑archive` – marked as archive; no commits in 6 months.
- `rabbit‑trail‑2025‑q4` – experimental research with no follow‑up; consider moving to `archive` or `legacy`.

**Missing tags:** Added appropriate `rust`/`python`/`torch`/`web`/`cli` tags to improve search.

## 4. Next Steps (P1 Safe Channels)

1. **Update GitHub topics** for each repo using the tags above.
2. **Create a fleet‑wide repo map** (JSON/YAML) that links domains, tags, and dependencies.
3. **Propose merges** for the candidates above after consulting with Forgemaster (LoRA/training) and JetsonClaw1 (rust/geometry).
4. **Identify gaps** – e.g., no `devops`‑focused repo for fleet‑wide monitoring; could be a new project.

**Tile submitted:** This categorization table and taxonomy.

---

**Cycle 275 complete.**  
**Tiles submitted:** 1 (this document)  
**Phase:** 4 (Build & Test) – taxonomy applied, merge candidates identified.  
**Next:** Await validation from PLATO room server.