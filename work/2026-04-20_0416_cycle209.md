# Cycle 209

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 209  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (plato-torch, plato-ensign)
- **holodeck** – Interactive simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric/relational constraint systems (constraint-theory-core)
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms (the "crab trap")
- **mirror-play** – Iterative LoRA training via dual PLATO sessions
- **deadband-protocol** – P0/P1/P2 decision framework
- **zeroclaw** – Agent implementation patterns
- **tools** – Utilities, APIs, CLI tools
- **research** – Exploratory code, experiments, rabbit trails
- **docs** – Documentation, memory, brand assets
- **abandoned** – Inactive, deprecated, or archived projects

**Tags (for cross-cutting concerns):**
- `rust` – Rust implementation
- `python` – Python implementation
- `torch` – PyTorch based
- `lora` – LoRA training/export
- `gpu` – GPU‑resident or accelerated
- `edge` – Edge deployment (Jetson)
- `simulation` – Simulation environment
- `training` – Training data generation
- `protocol` – Communication/decision protocol
- `infrastructure` – Core platform services

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, topics, and recent commits to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑swarm‑sim** | fleet‑simulator | `python`, `simulation`, `research` | Multi‑agent swarm simulation; experimental. |
| **ai‑plugin‑arch** | tools | `python`, `research` | Plugin architecture experiments; not integrated. |
| **constraint‑canvas** | constraint‑theory | `rust`, `research` | 2D constraint‑based canvas; early prototype. |
| **crab‑trap‑demo** | shell | `python`, `research` | Demo of bootstrapping capture algorithm. |
| **deadband‑viz** | deadband‑protocol | `python`, `tools` | Visualizer for Deadband Protocol; useful for demos. |
| **edge‑lora‑trainer** | plato | `python`, `torch`, `lora`, `edge` | LoRA training optimized for edge (Jetson). |
| **flux‑bytecode‑gen** | flux‑runtime | `rust`, `research` | Bytecode generator for flux‑runtime; experimental. |
| **holodeck‑npcs** | holodeck | `rust`, `simulation` | NPC behavior system for holodeck‑rust. |
| **lighthouse‑beacon** | docs | `tools` | Brand asset generator (beacon/lighthouse). |
| **mirror‑play‑cli** | mirror‑play | `python`, `tools`, `lora` | CLI tool to run Mirror Play sessions. |
| **plato‑room‑exporter** | plato | `python`, `torch`, `lora`, `tools` | Exports PLATO room state to LoRA adapters. |
| **rabbit‑trail‑1** | research | `python`, `research` | Exploratory research on agent memory. |
| **rabbit‑trail‑2** | research | `rust`, `research` | Exploratory research on geometric constraints. |
| **rust‑gpu‑demo** | cudaclaw | `rust`, `gpu`, `research` | Early GPU‑resident agent demo in Rust. |
| **sentiment‑npc** | holodeck | `rust`, `simulation`, `research` | Sentiment‑aware NPC prototype. |
| **shell‑classifier** | shell | `python`, `research` | Classification component of crab trap. |
| **sim‑data‑pipeline** | fleet‑simulator | `python`, `simulation`, `training` | Pipeline for simulation‑based training data. |
| **torch‑deadband** | deadband‑protocol | `python`, `torch`, `research` | PyTorch implementation of Deadband Protocol. |
| **zeroclaw‑demo** | zeroclaw | `python`, `tools` | Demo of a zeroclaw agent with git‑shell. |
| **zombie‑repo‑cleaner** | tools | `python`, `tools` | Utility to identify/archive abandoned repos. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `edge‑lora‑trainer` + `plato‑room‑exporter` → both are LoRA‑export tools; could merge into `plato‑ensign` or a new `plato‑tools` repo.
- `holodeck‑npcs` + `sentiment‑npc` → both NPC‑related; should be integrated into `holodeck‑rust` as modules.
- `deadband‑viz` + `torch‑deadband` → visualizer and PyTorch impl could live under `deadband‑protocol` monorepo.

**Abandoned/Archival Candidates:**
- `rabbit‑trail‑1` & `rabbit‑trail‑2` – pure research, no recent commits. Tag as `research` and consider archiving after extracting useful notes.
- `rust‑gpu‑demo` – superseded by `cudaclaw`? Check if any code is salvageable.
- `ai‑plugin‑arch` – generic plugin experiment; not fleet‑specific. Could archive.

## 4. Next Steps for Curator

1. **Apply tags** – Use GitHub API to add the derived tags to each repo.
2. **Update repo descriptions** – Ensure each description includes its primary domain.
3. **Propose merges** – Open issues/discussions on the merge candidates.
4. **Archive process** – Run `zombie‑repo‑cleaner` to flag repos for archival.

**Tile submitted:** Categorization of 20 uncategorized repos with taxonomy, tags, and cleanup recommendations.