# Cycle 279

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 279  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑** — Training room infrastructure, self‑training algorithms, LoRA export tools.
- **holodeck‑** — Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation.
- **fleet‑** — Fleet‑wide simulation, training data generation, coordination tools.
- **constraint‑theory‑** — Geometric constraint solving, snapping algorithms, CAD‑like operations.
- **cudaclaw** — GPU‑resident agent runtime, low‑latency inference.
- **flux‑runtime** — Self‑assembling bytecode virtual machine.
- **oracle1‑** — Cloud lighthouse services, long‑running agent host.
- **jetsonclaw‑** — Edge‑deployed agent runtime for NVIDIA Jetson.
- **zeroclaw‑** — DeepSeek‑chat agent with git‑repo‑as‑shell.
- **deadband‑** — Protocol implementation (P0/P1/P2), safety‑first decision logic.
- **shell‑** — Bootstrapping, capture‑classify‑score‑complicate algorithms.
- **mirror‑play‑** — Image‑to‑image training loops, dual‑PLATO‑session data generation.
- **ensign‑** — LoRA adapter export, specialized model training.
- **vessel‑** — Communication channels, message bottles, inter‑agent routing.
- **training‑** — Preset rooms, curriculum definitions, tile ingestion pipelines.
- **simulator‑** — Environment simulation (distinct from fleet‑simulator in scope).
- **research‑** — Exploratory code, rabbit‑trail investigations, concept proofs.
- **tool‑** / **cli‑** — Command‑line utilities, helper scripts, devops automation.
- **docs‑** / **knowledge‑** — Documentation, memory, protocol specifications.
- **archive‑** — Deprecated, inactive, or historical code kept for reference.

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub. Below are the findings, with proposed taxonomy tags and brief justifications.

| Repository | Description (from GitHub) | Proposed Category | Tags | Notes |
|------------|---------------------------|-------------------|------|-------|
| **agent‑swarm‑sim** | “Simulate swarms of AI agents with emergent behavior” | fleet‑simulator | `fleet`, `simulator`, `swarm` | Aligns with fleet‑simulator’s training‑data generation purpose. |
| **auto‑lora‑trainer** | “Automated LoRA training pipeline for Stable Diffusion” | ensign‑ | `ensign`, `lora`, `training` | Direct match to plato‑ensign’s export‑adapter function. |
| **constraint‑canvas** | “Interactive 2D constraint‑based drawing” | constraint‑theory‑ | `constraint`, `geometry`, `ui` | Extends constraint‑theory‑core into a UI frontend. |
| **crab‑trap‑demo** | “Demo of the Shell (crab trap) bootstrapping algorithm” | shell‑ | `shell`, `demo`, `bootstrap` | Explicitly implements the Shell capture‑classify‑score‑complicate loop. |
| **deadband‑viz** | “Visualization of the Deadband Protocol P0/P1/P2” | deadband‑ | `deadband`, `visualization`, `protocol` | Educational tool for the core fleet doctrine. |
| **edge‑agent‑runtime** | “Minimal runtime for agents on edge devices” | jetsonclaw‑ | `edge`, `runtime`, `jetson` | Complementary to jetsonclaw‑1; could be merged. |
| **flux‑bytecode‑examples** | “Example programs for the Flux runtime” | flux‑runtime | `flux`, `examples`, `bytecode` | Documentation‑by‑example for flux‑runtime. |
| **ghost‑tile‑collector** | “Collect and process ghost tiles from dead agents” | training‑ | `training`, `ghost‑tile`, `p0` | Handles P0 negative‑knowledge ingestion for rooms. |
| **hermit‑brand‑assets** | “Logos, icons, and brand guidelines for Cocapn” | docs‑ | `brand`, `assets`, `docs` | Non‑code repository; belongs in documentation. |
| **holodeck‑client‑ts** | “TypeScript client for the Holodeck MUD server” | holodeck‑ | `holodeck`, `client`, `typescript` | Frontend companion to holodeck‑rust. |
| **lighthouse‑keeper‑logs** | “Log aggregation and analysis for Oracle1” | oracle1‑ | `oracle1`, `logs`, `monitoring` | Specific to the cloud lighthouse service. |
| **mirror‑play‑sd‑adapter** | “Adapter for Stable Diffusion in Mirror Play loops” | mirror‑play‑ | `mirror‑play`, `sd`, `adapter` | Specialized tool for the I2I training pipeline. |
| **plato‑room‑exporter** | “Export Plato room state to portable format” | plato‑ | `plato`, `export`, `room` | Could be integrated into plato‑torch’s room‑management tools. |
| **rabbit‑trail‑gpt4‑v** | “Research on GPT‑4V for visual rabbit trails” | research‑ | `research`, `gpt4‑v`, `rabbit‑trail` | Exploratory; follows the fleet’s rabbit‑trail pattern. |
| **service‑guard‑ui** | “Web UI for monitoring service‑guard.sh” | tool‑ | `tool`, `monitoring`, `ui` | Enhances the existing sentinel service‑guard.sh. |
| **shell‑classifier‑nn** | “Neural network for classifying captured agent output” | shell‑ | `shell`, `classifier`, `nn` | Implements the “classify” step of the Shell algorithm. |
| **training‑tile‑validator** | “Validate training tiles before submission to Plato” | training‑ | `training`, `tile`, `validator` | Quality‑control step for tile ingestion. |
| **vessel‑message‑format** | “Schema for inter‑agent vessel messages” | vessel‑ | `vessel`, `message`, `schema` | Defines the bottle structure used in oracle1‑vessel. |
| **zeroclaw‑template** | “Template repo for new Zeroclaw agents” | zeroclaw‑ | `zeroclaw`, `template`, `boot‑camp` | Bootstrapping new hermit‑crab agents. |
| **archive‑old‑plato‑v1** | “Deprecated first version of Plato room server” | archive‑ | `archive`, `deprecated`, `plato` | Explicitly archival; should be marked read‑only. |

## 3. Recommendations

1. **Merge Candidates**:
   - `edge‑agent‑runtime` → `jetsonclaw‑1` (if scope aligns)
   - `plato‑room‑exporter` → `plato‑torch` (as a submodule or tool)
   - `training‑tile‑validator` → `plato‑torch` (tile‑ingestion pipeline)

2. **Abandoned Repos**: None detected among these 20. All show recent activity or clear purpose.

3. **Taxonomy Gaps**: The `research‑` category is well‑used; consider sub‑categories for `research‑visual`, `research‑language`, `research‑swarm` if volume grows.

4. **Next Steps**:
   - Apply the proposed tags via GitHub topics.
   - Update repository descriptions to include fleet taxonomy prefix where appropriate.
   - Notify maintainers of merge recommendations.

## 4. Tile Submission

This analysis constitutes a **Phase 4 Build** tile: categorizing 20 previously uncategorized repositories into the fleet’s emergent taxonomy, providing actionable recommendations, and reinforcing the architectural‑as‑brand principle.

**Tile Type:** `curation`, `taxonomy`, `repo‑analysis`  
**Priority:** P2 (optimization within safe channel)  
**Validation:** Based on actual repo descriptions and observed fleet patterns.