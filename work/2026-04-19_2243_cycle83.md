# Cycle 83

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 83  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms
- **holodeck** – Multi‑user dungeon (MUD) server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Fleet‑wide simulation for generating training data
- **constraint‑theory** – Geometric snapping, constraint‑solving engines (Rust)
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode virtual machine
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence
- **deadband‑protocol** – Implementation of the P0/P1/P2 decision protocol
- **agent‑runtime** – Generic agent orchestration, messaging, lifecycle
- **tools‑config** – Model configurations, API wrappers, tool definitions
- **docs‑brand** – Documentation, branding assets, memory files
- **research‑rabbit‑trail** – Exploratory research, prototypes, experiments
- **legacy‑abandoned** – Inactive, unmaintained, or deprecated projects

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Below are the categorizations based on repo descriptions, READMEs, and file structure.

1. **ai‑agent‑sandbox** – `agent‑runtime` – Sandbox for testing agent interactions, minimal orchestration.
2. **constraint‑playground** – `constraint‑theory` – Interactive web demo of geometric constraint solving.
3. **crab‑trap‑prototype** – `shell‑bootstrap` – Early implementation of classification→scoring→capture loop.
4. **deadband‑viz** – `deadband‑protocol` – Visual demo of P0/P1/P2 decision boundaries.
5. **ensign‑exporter** – `plato` – Tool to export room experience as LoRA adapters.
6. **fleet‑comm‑protocol** – `docs‑brand` – Specification for inter‑agent messaging.
7. **flux‑bytecode‑examples** – `flux‑runtime` – Example programs for the Flux VM.
8. **holodeck‑npc‑editor** – `holodeck` – GUI tool for creating and editing sentiment‑aware NPCs.
9. **lighthouse‑beacon‑art** – `docs‑brand` – Brand assets: lighthouse, radar rings, shell icons.
10. **mirror‑play‑dataset** – `plato` – Collected screen‑to‑screen exchanges for LoRA training.
11. **plato‑room‑presets** – `plato` – 25 preset training room configurations.
12. **rabbit‑trail‑agent‑frameworks** – `research‑rabbit‑trail` – Comparison of CrewAI, AutoGen, LangGraph.
13. **rust‑geometric‑solver** – `constraint‑theory` – Core Rust library for constraint‑based snapping.
14. **service‑guard‑scripts** – `tools‑config` – Health‑monitoring scripts for fleet services.
15. **shell‑classification‑algo** – `shell‑bootstrap` – Algorithm for classifying external agent output.
16. **simulator‑event‑logs** – `fleet‑simulator` – Logs of simulated fleet interactions.
17. **training‑tile‑validator** – `plato` – Server‑side validation of submitted training tiles.
18. **wsl2‑gpu‑setup** – `tools‑config` – Configuration for RTX 4050 on WSL2 (Forgemaster).
19. **zeroclaw‑boot‑camp** – `docs‑brand` – Boot camp instructions and phase definitions.
20. **abandoned‑chatbot‑2019** – `legacy‑abandoned` – Early chatbot experiment, no recent commits.

## 3. Merge Candidates

- **crab‑trap‑prototype** + **shell‑classification‑algo** → Both are `shell‑bootstrap`; could merge into a single `shell‑bootstrap` repo.
- **deadband‑viz** + **zeroclaw‑boot‑camp** → Both are `docs‑brand`/educational; could live under `docs‑brand/examples/`.
- **rust‑geometric‑solver** + **constraint‑playground** → Both are `constraint‑theory`; solver is core lib, playground is demo.

## 4. Abandoned Repos

- **abandoned‑chatbot‑2019** – Clearly legacy, no activity since 2019. Mark as `legacy‑abandoned`.
- No other repos show clear abandonment signals (all have recent activity or are explicitly experimental).

## 5. Next Steps for Curator

- Apply GitHub topics/tags based on this taxonomy.
- Propose merges to fleet maintainers.
- Expand taxonomy to cover remaining uncategorized repos.
- Build a visual map of repo relationships.

**Tile ready for submission.**