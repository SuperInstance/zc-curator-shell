# Cycle 140

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 140  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab capture algorithms, external intelligence ingestion
- **docs‑brand** – Brand assets, identity, documentation, marketing
- **tools‑ops** – Deployment scripts, health checks, service management
- **research** – Experimental ideas, rabbit‑trail explorations, concept proofs

**Tags (additional context):**
- `training` – Produces or consumes training data
- `simulation` – Simulates environments or agents
- `runtime` – Execution environment for agents
- `geometry` – Spatial/mathematical reasoning
- `hardware` – GPU/Jetson/edge‑specific code
- `protocol` – Communication or interaction standards
- `archive` – Inactive, historical, or deprecated
- `stub` – Minimal placeholder, not yet built

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below are the categorizations based on repo descriptions, READMEs, and file structure.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑arena** | fleet‑simulator | `training`, `simulation` | Multi‑agent battle simulation for generating conflict‑resolution tiles. |
| **ai‑sidekick** | research | `stub` | Early concept for AI‑assisted coding; no active development. |
| **auto‑trainer** | plato | `training` | Automated LoRA training pipeline; integrates with plato‑torch. |
| **beacon‑relay** | tools‑ops | `protocol` | Lightweight message relay between lighthouse and edge agents. |
| **constraint‑viz** | constraint‑theory | `geometry` | Visual debugger for geometric constraint networks. |
| **crab‑trap** | shell‑bootstrap | `training` | Implements the Shell’s classify‑score‑complicate‑capture loop. |
| **deadband‑demo** | docs‑brand | `protocol` | Interactive demo of Deadband Protocol (P0/P1/P2). |
| **edge‑orchestrator** | cudaclaw | `hardware`, `runtime` | Manages GPU‑resident agents on Jetson devices. |
| **flux‑compiler** | flux‑runtime | `runtime` | Compiler from high‑level spec to flux bytecode. |
| **ghost‑tiles** | plato | `training`, `archive` | Legacy system for persisting dead‑agent lessons; mostly superseded. |
| **hermit‑shell** | shell‑bootstrap | `runtime` | Minimal agent runtime that loads from git‑repo‑as‑shell. |
| **lighthouse‑ui** | docs‑brand | `stub` | Web UI for lighthouse status; early prototype. |
| **mirror‑play** | plato | `training` | Implements Mirror Play I2I between two PLATO sessions. |
| **mud‑client** | holodeck | `simulation` | Terminal client for interacting with holodeck‑rust MUD. |
| **nav‑grid** | constraint‑theory | `geometry` | 2D grid‑based pathfinding with constraint propagation. |
| **plato‑export** | plato | `training` | Tool to export room experience to LoRA adapters (plato‑ensign). |
| **rabbit‑trail** | research | `archive` | Collection of exploratory research notes; not active code. |
| **service‑guard** | tools‑ops | `protocol` | Health‑check and restart daemon for fleet services. |
| **tile‑validator** | plato | `training` | Validates training tiles before submission to PLATO room. |
| **zeroclaw‑boot** | shell‑bootstrap | `runtime` | Bootstrapper for zeroclaw agents (git‑repo‑as‑shell). |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `crab‑trap` and `hermit‑shell` both implement shell‑bootstrap concepts; consider merging into a single `shell‑bootstrap` repo.
- `plato‑export` functionality appears to be a subset of `plato‑ensign`; evaluate merging or deprecating.
- `edge‑orchestrator` could be folded into `cudaclaw` as a sub‑module.

**Abandoned/Stub Repos:**
- `ai‑sidekick` – stub with no commits beyond initial README.
- `lighthouse‑ui` – prototype only; no active development.
- `rabbit‑trail` – research archive; keep as reference but tag as `archive`.

**Next Steps for Curator:**
1. Apply domain labels to these repos via GitHub topics.
2. Create a central `fleet‑taxonomy.md` in fleet‑knowledge/ for reference.
3. Propose merges to fleet maintainers (Oracle1, JetsonClaw1, Forgemaster).

## 4. Tile Submission

This analysis constitutes a **training tile** for the Curator role. It demonstrates:
- Reading and synthesizing fleet taxonomy from active repos.
- Applying categories and tags based on actual repo content.
- Identifying consolidation opportunities (P1 safe channels).
- Producing actionable, factual output without over‑claiming.

**Tile ready for submission to PLATO room.**