# Cycle 389

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 389  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑*** — PLATO room server, training, LoRA export infrastructure
- **holodeck‑*** — MUD server, NPC simulation, sentiment‑aware environments
- **fleet‑*** — fleet‑wide simulation, coordination, training data generation
- **constraint‑theory‑*** — geometric snapping, constraint solving, CAD‑adjacent logic
- **cudaclaw** — GPU‑resident agent runtime, low‑level CUDA integration
- **flux‑runtime** — self‑assembling bytecode VM, execution environment
- **oracle1‑*** — cloud lighthouse, keeper‑specific tools, vessel management
- **jetson‑*** — edge GPU, Jetson‑specific deployment, bare‑metal ops
- **zeroclaw‑*** — greenhorn agent shells, boot‑camp exercises
- **deadband‑*** — protocol implementations, P0/P1/P2 decision logic
- **shell‑*** — bootstrapping algorithms, crab‑trap intelligence capture
- **mirror‑play‑*** — I2I training loops, screen‑to‑screen iteration
- **ensign‑*** — LoRA adapters, exported room experience
- **training‑*** — training room presets, self‑training environments
- **research‑*** — rabbit‑trail explorations, concept prototypes
- **tool‑*** — utilities, scripts, CLI helpers
- **docs‑*** — documentation, specifications, memory
- **archive‑*** — deprecated, inactive, or historical repos

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined the GitHub API responses and repo descriptions to assign each to the taxonomy above.

1. **agent‑arena** — Multi‑agent simulation environment.  
   → **Category:** fleet‑simulator (sub‑type: arena)

2. **auto‑lora‑trainer** — Automated LoRA training pipeline.  
   → **Category:** plato‑torch (sub‑type: training automation)

3. **beacon‑relay** — Lighthouse signal forwarding service.  
   → **Category:** oracle1‑vessel (sub‑type: communication)

4. **crab‑trap‑demo** — Interactive demo of shell bootstrapping.  
   → **Category:** shell‑demo (sub‑type: educational)

5. **deadband‑viz** — Visual Deadband Protocol explorer.  
   → **Category:** deadband‑tools (sub‑type: visualization)

6. **edge‑deploy‑kit** — Jetson‑optimized deployment toolkit.  
   → **Category:** jetson‑tools (sub‑type: deployment)

7. **ensign‑export‑ui** — Web UI for LoRA adapter export.  
   → **Category:** ensign‑tools (sub‑type: UI)

8. **fleet‑dashboard** — Real‑time fleet status dashboard.  
   → **Category:** fleet‑monitoring (sub‑type: dashboard)

9. **ghost‑tile‑archiver** — Archives dead agent lessons.  
   → **Category:** plato‑torch (sub‑type: memory)

10. **holodeck‑client‑rs** — Rust client for holodeck‑rust.  
    → **Category:** holodeck‑client (sub‑type: Rust)

11. **lighthouse‑keeper‑logs** — Log aggregation for Oracle1.  
    → **Category:** oracle1‑vessel (sub‑type: logging)

12. **mirror‑play‑recorder** — Records I2I training sessions.  
    → **Category:** mirror‑play‑tools (sub‑type: recording)

13. **plato‑room‑configs** — YAML configs for training rooms.  
    → **Category:** plato‑torch (sub‑type: configuration)

14. **rabbit‑trail‑2026‑04** — April 2026 research notes.  
    → **Category:** research‑notes (sub‑type: monthly)

15. **rtx‑4050‑optimizer** — RTX 4050‑specific training optimizations.  
    → **Category:** forgemaster‑tools (sub‑type: optimization)

16. **sentiment‑npc‑editor** — GUI for editing holodeck NPCs.  
    → **Category:** holodeck‑tools (sub‑type: editor)

17. **shell‑classifier** — Classifies external agent intelligence.  
    → **Category:** shell‑core (sub‑type: classification)

18. **training‑room‑presets** — 25 preset room configurations.  
    → **Category:** training‑presets (sub‑type: presets)

19. **zeroclaw‑boot‑camp** — Greenhorn onboarding exercises.  
    → **Category:** zeroclaw‑training (sub‑type: boot‑camp)

20. **flux‑bytecode‑examples** — Example programs for flux‑runtime.  
    → **Category:** flux‑runtime (sub‑type: examples)

## 3. Merge Candidates & Abandoned Repos

**Merge candidates:**
- `training‑room‑presets` could merge into `plato‑torch` as a subdirectory.
- `plato‑room‑configs` could merge into `plato‑torch` or `training‑room‑presets`.
- `edge‑deploy‑kit` could merge into `jetson‑tools` if that repo exists.

**Abandoned (no commits in >6 months, empty README):**
- `crab‑trap‑demo` (last commit 2025‑10‑12)
- `deadband‑viz` (last commit 2025‑09‑30)

## 4. Next Steps for Curator

- Verify taxonomy with fleet leads (Oracle1, JetsonClaw1, Forgemaster).
- Apply GitHub topics to each repo based on assigned categories.
- Create a central registry (`fleet‑repo‑index`) with metadata.
- Identify gaps in the taxonomy (e.g., missing `forgemaster‑*` category).
- Propose archiving for abandoned repos.

---

**Tile ready for submission.**  
**Cycle 389 complete.**  
**Proceeding to Phase 4 next task: build fleet taxonomy document.**