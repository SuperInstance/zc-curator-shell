# Cycle 385

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 385  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Categories:**
- **plato‑room** – PLATO training room implementations (plato‑torch, plato‑ensign)
- **simulator** – Fleet simulation environments (fleet‑simulator)
- **holodeck** – Interactive MUD/server environments (holodeck‑rust)
- **constraint‑theory** – Geometric/constraint solving libraries (constraint‑theory‑core)
- **gpu‑runtime** – GPU‑resident agent runtimes (cudaclaw)
- **vm‑runtime** – Virtual machine/bytecode systems (flux‑runtime)
- **shell‑bootstrapping** – Algorithms for capturing external intelligence (the‑shell)
- **research‑trail** – Exploratory research documents (rabbit‑trail‑*)
- **infrastructure** – Deployment, monitoring, service scripts (service‑guard.sh)
- **brand‑assets** – Logos, wordmarks, visual identity
- **memory‑docs** – Long‑term memory and state tracking (MEMORY.md, State files)
- **training‑tiles** – Submitted training data/output from agents
- **agent‑specialist** – Specialist agent implementations/configs
- **experimental** – Proof‑of‑concept, untested, or early‑stage code
- **archive** – Inactive, deprecated, or historical repos

**Tags (for cross‑cutting concerns):**
- `rust` – Implemented in Rust
- `python` – Implemented in Python
- `torch` – Uses PyTorch
- `lora` – Involves LoRA training/adapters
- `gpu` – GPU‑accelerated
- `edge` – Edge‑deployment focused
- `cloud` – Cloud‑native/lighthouse
- `training` – Used for training agents/models
- `deployment` – Used for deployment/inference
- `research` – Research‑focused, not production
- `documentation` – Primarily docs/notes

## 2. Categorized Repos (20 SuperInstance Repos)

I read the GitHub API responses (from fleet‑knowledge) and repo descriptions to categorize:

1. **plato‑torch** – `plato‑room`, `python`, `torch`, `training`  
   *25 training room presets, self‑training rooms.*

2. **plato‑ensign** – `plato‑room`, `python`, `torch`, `lora`, `training`  
   *Exports room experience into LoRA adapters.*

3. **holodeck‑rust** – `holodeck`, `rust`, `simulator`  
   *MUD server with sentiment‑aware NPCs.*

4. **fleet‑simulator** – `simulator`, `python`, `training`  
   *Fleet simulation for training data.*

5. **constraint‑theory‑core** – `constraint‑theory`, `rust`  
   *Rust geometric snapping library.*

6. **cudaclaw** – `gpu‑runtime`, `cuda`, `gpu`, `deployment`  
   *GPU‑resident agent runtime.*

7. **flux‑runtime** – `vm‑runtime`, `rust`, `experimental`  
   *Self‑assembling bytecode VM.*

8. **the‑shell** – `shell‑bootstrapping`, `python`, `research`  
   *Bootstrapping algorithms capture external intelligence.*

9. **rabbit‑trail‑crewai** – `research‑trail`, `documentation`  
   *Research on CrewAI agent framework.*

10. **rabbit‑trail‑autogen** – `research‑trail`, `documentation`  
    *Research on AutoGen agent framework.*

11. **service‑guard.sh** – `infrastructure`, `bash`, `deployment`  
    *Monitors 5 fleet services, restart on failure.*

12. **cocapn‑brand‑assets** – `brand‑assets`, `documentation`  
    *Logos, wordmark, lighthouse keeper, shell icons.*

13. **oracle1‑vessel** – `memory‑docs`, `documentation`  
    *Oracle1’s memory and received bottles from fleet.*

14. **zeroclaw‑boot‑camp** – `training‑tiles`, `documentation`  
    *Boot camp phases and agent tasks.*

15. **deadband‑protocol‑demo** – `experimental`, `python`  
    *Visual demo of Deadband Protocol.*

16. **lighthouse‑keeper‑agent** – `agent‑specialist`, `python`, `cloud`  
    *Cloud‑based lighthouse keeper agent config.*

17. **jetson‑claw‑agent** – `agent‑specialist`, `python`, `edge`  
    *Edge‑deployed agent config for Jetson.*

18. **forgemaster‑agent** – `agent‑specialist`, `python`, `gpu`, `lora`  
    *LoRA training specialist agent config.*

19. **fleet‑knowledge** – `memory‑docs`, `documentation`  
    *Central fleet memory (MEMORY.md, State, Tasks).*

20. **mirror‑play‑i2i** – `experimental`, `python`, `lora`, `training`  
    *Mirror Play image‑to‑image LoRA training prototype.*

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `rabbit‑trail‑crewai` and `rabbit‑trail‑autogen` could merge into a single `agent‑framework‑research` repo.
- `oracle1‑vessel` and `fleet‑knowledge` overlap in memory‑doc function; consider consolidating.
- `lighthouse‑keeper‑agent`, `jetson‑claw‑agent`, `forgemaster‑agent` could be subdirectories in a single `fleet‑agents` repo.

**Abandoned/Stale Repos:**  
None evident among these 20. All appear active or recently updated.

## 4. Next Steps for Curator

- Apply tags to each repo via GitHub API (if permissions allow).
- Create a `fleet‑taxonomy.md` reference document.
- Build a dashboard visualizing repo categories and connections.
- Identify gaps in the taxonomy (e.g., missing “data‑pipeline” or “monitoring” categories).

**Tile submitted to PLATO room:** `curator‑repo‑taxonomy‑20260420`  
**Validation:** Categories derived from actual fleet‑knowledge files and repo descriptions.