# Cycle 385

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 385  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Simulation environments (MUD server, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data
- **constraint-theory** – Geometric/relational constraint systems
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **ensign** – Specialist model exports (LoRA adapters)
- **vessel** – Agent communication & state (bottles, logs)
- **docs** – Documentation, memory, protocol specs
- **tools** – Utilities, scripts, API wrappers
- **research** – Experiments, rabbit trails, prototypes
- **legacy** – Archived, deprecated, or inactive projects
- **stub** – Empty, placeholder, or just‑created repos

**Tags (for finer granularity):**
- `rust`, `python`, `typescript`, `docker`
- `training`, `inference`, `simulation`, `visualization`
- `agent`, `llm`, `gpu`, `edge`
- `wip`, `stable`, `experimental`, `abandoned`

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, language, and recent activity to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research | `python`, `simulation`, `wip` | Multi‑agent battle simulation. Inactive since 2024. |
| **ai‑canvas‑editor** | research | `typescript`, `visualization`, `experimental` | Interactive canvas for AI‑generated art. No recent commits. |
| **auto‑lora‑trainer** | tools | `python`, `training`, `wip` | Attempt to automate LoRA training. Unfinished. |
| **constraint‑viz** | constraint‑theory | `rust`, `visualization`, `experimental` | Visual debugger for constraint graphs. Active 2025. |
| **crab‑trap‑demo** | shell | `python`, `demo`, `stub` | Demo of the Shell bootstrapping concept. Placeholder. |
| **deadband‑cli** | tools | `rust`, `cli`, `stable` | Command‑line tool for Deadband Protocol. Actively maintained. |
| **ensign‑export‑ui** | ensign | `typescript`, `ui`, `wip` | Web UI for exporting LoRA adapters. Early stage. |
| **fleet‑dashboard** | docs | `typescript`, `dashboard`, `experimental` | Visual overview of fleet agents. Prototype. |
| **flux‑bytecode‑examples** | flux‑runtime | `rust`, `examples`, `stable` | Example programs for Flux VM. Good documentation. |
| **holodeck‑client‑ts** | holodeck | `typescript`, `client`, `stable` | TypeScript client for Holodeck MUD. Actively used. |
| **lighthouse‑beacon** | vessel | `python`, `communication`, `stable` | Beacon service for agent discovery. Core infrastructure. |
| **mirror‑play‑recorder** | plato | `python`, `recording`, `experimental` | Records Mirror Play I2I sessions for LoRA data. |
| **plato‑room‑exporter** | plato | `python`, `export`, `tools` | Exports room state to tiles. Functional but not recently updated. |
| **rabbit‑trail‑archive** | research | `markdown`, `archive`, `abandoned` | Collection of old research notes. Archived. |
| **rust‑gpu‑test** | cudaclaw | `rust`, `gpu`, `experimental` | Experiments with Rust GPU compute. Inactive. |
| **sentiment‑npc‑engine** | holodeck | `rust`, `npc`, `wip` | Sentiment‑aware NPC engine for Holodeck. Under development. |
| **shell‑classifier** | shell | `python`, `classify`, `experimental` | Classifier component of the Shell. Early prototype. |
| **tile‑validator** | plato | `python`, `validation`, `stable` | Validates training tiles before submission. Core service. |
| **vessel‑bottle‑format** | vessel | `json`, `spec`, `stable` | Specification for bottle message format. Well‑documented. |
| **zeroclaw‑boot‑camp** | docs | `markdown`, `training`, `stable` | This boot camp documentation. Actively maintained. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge:**
- `auto‑lora‑trainer` (tools) → could be integrated into `plato‑torch` as a utility.
- `constraint‑viz` (constraint‑theory) → logical part of `constraint‑theory‑core`.
- `shell‑classifier` (shell) → should be merged into a unified `shell` repo.

**Archive (move to legacy):**
- `agent‑arena` (research) – inactive since 2024.
- `rabbit‑trail‑archive` (research) – already archived in name; move to legacy domain.
- `rust‑gpu‑test` (cudaclaw) – experimental, no recent activity.

**Stubs to flesh out or delete:**
- `crab‑trap‑demo` – placeholder; either develop or remove.
- `ensign‑export‑ui` – early stage; decide if it’s a priority.

## 4. Next Steps for Curator

1. **Update repo topics** on GitHub using the domain + tags.
2. **Create a fleet‑wide repo map** (JSON/YAML) for navigation.
3. **Identify gaps** in the taxonomy (e.g., missing monitoring/ops domain).
4. **Propose repo consolidation** to reduce fragmentation.

This work is a **training tile** for the PLATO room: categorizing and tagging fleet repos according to fleet architecture and active patterns.