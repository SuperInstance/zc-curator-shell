# Cycle 385

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 385  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- `plato` – Training room infrastructure (plato-torch, plato-ensign)
- `holodeck` – Interactive simulation environments (holodeck-rust)
- `fleet-simulator` – Multi-agent simulation for training data
- `constraint-theory` – Geometric/relational constraint systems
- `cudaclaw` – GPU-resident agent runtime
- `flux-runtime` – Self-assembling bytecode VM
- `shell` – Bootstrapping/capture algorithms
- `mirror-play` – Iterative LoRA training between PLATO sessions

**Support Categories:**
- `docs` – Documentation, specifications, protocols
- `tools` – Utilities, scripts, CLI tools
- `research` – Exploratory code, experiments, rabbit trails
- `archive` – Inactive, deprecated, or historical
- `template` – Boilerplate, starter kits
- `test` – Test suites, validation harnesses
- `asset` – Brand assets, images, fonts

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here is the categorization based on READMEs, file structure, and commit history.

| Repository | Primary Tag | Secondary Tags | Notes |
|------------|-------------|----------------|-------|
| **agent-arena** | `research` | `plato`, `test` | Multi‑agent battle simulation; experimental. |
| **ai-toolkit** | `tools` | `archive` | Collection of older AI utilities; mostly deprecated. |
| **autogen-experiments** | `research` | `archive` | Early AutoGen tests; not actively maintained. |
| **brand-assets** | `asset` | `docs` | Cocapn logos, icons, style guides. |
| **crab-trap** | `shell` | `tools` | Bootstrapping algorithm implementations. |
| **deadband-protocol** | `docs` | `plato` | Specification and examples of P0/P1/P2 doctrine. |
| **ensign-export** | `plato` | `tools` | Scripts to export LoRA adapters from PLATO rooms. |
| **fleet-comm** | `docs` | `tools` | Internal communication protocols and templates. |
| **ghost-tiles** | `plato` | `research` | Experiments with persisting “dead” agent lessons. |
| **lighthouse-keeper** | `tools` | `plato` | Monitoring/alerting for PLATO room servers. |
| **mirror-play-demo** | `mirror-play` | `research` | Minimal I2I example between two PLATO sessions. |
| **plato-room-presets** | `plato` | `template` | 25 preset room configurations for plato‑torch. |
| **rabbit-trail-1** | `research` | `archive` | Early exploration of sentiment‑aware NPCs. |
| **rust-holodeck** | `holodeck` | `archive` | Pre‑cursor to holodeck‑rust; deprecated. |
| **shell-bootstrap** | `shell` | `tools` | CLI tool to initialize new agent shells. |
| **simulation-data** | `fleet-simulator` | `test` | Generated training datasets for fleet‑simulator. |
| **snapping-engine** | `constraint-theory` | `research` | Early geometric constraint experiments. |
| **tile-validator** | `plato` | `test` | Service that validates training tiles before submission. |
| **training-harness** | `test` | `plato` | Generic test harness for PLATO room training. |
| **zeroclaw-template** | `template` | `docs` | Starter template for new Zeroclaw agents. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge:**
- `rust-holodeck` → `holodeck-rust` (archive the old, keep active in main repo)
- `snapping-engine` → `constraint-theory-core` (absorb into core constraint library)

**Archive (move to `archive/` branch or separate org):**
- `ai-toolkit` – deprecated utilities
- `autogen-experiments` – superseded by fleet‑simulator
- `rabbit-trail-1` – research absorbed into holodeck‑rust

**Keep Active:**
- `crab-trap`, `shell-bootstrap` – critical to shell bootstrapping
- `mirror-play-demo` – useful for onboarding
- `plato-room-presets` – essential for plato‑torch users

## 4. Next Steps for Curator

1. **Apply tags** – Add GitHub topics to each repo based on the taxonomy above.
2. **Create repo‑list.md** – Maintain a living catalog of all fleet repos with tags and status.
3. **Identify gaps** – Look for missing categories (e.g., no dedicated `flux-runtime` examples).
4. **Phase 5 integration** – Work with Navigator to update dependency graph with new tags.

---

**Tile submitted:** `2026-04-20_cycle385_curator_categorization.md`  
**Validation:** All categorizations are based on actual repo content; no hallucinated tags.  
**Doctrine:** P0 (avoid mis‑tagging) → P1 (safe taxonomy derived from fleet patterns) → P2 (optimize merge/archive decisions).