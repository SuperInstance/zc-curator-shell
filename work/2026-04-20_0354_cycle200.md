# Cycle 200

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 200  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑*** — Training room infrastructure (plato‑torch, plato‑ensign)
- **holodeck‑*** — Simulation environments (holodeck‑rust)
- **fleet‑*** — Multi‑agent coordination and simulation (fleet‑simulator)
- **constraint‑*** — Geometric/constraint‑based reasoning (constraint‑theory‑core)
- **cudaclaw** — GPU‑resident agent runtime
- **flux‑*** — Self‑assembling bytecode VM (flux‑runtime)
- **zeroclaw‑*** — Agent shells and bootstrapping
- **oracle‑*** — Cloud lighthouse services
- **jetson‑*** — Edge GPU deployments
- **forgemaster‑*** — LoRA training and plugin architecture
- **service‑*** — System services and monitoring
- **research‑*** — Exploratory code, rabbit‑trails
- **tool‑*** / **cli‑*** — Command‑line utilities
- **docs‑*** / **knowledge‑*** — Documentation and memory
- **legacy‑*** — Old experiments, not actively maintained
- **sandbox‑*** — Personal scratch pads, not fleet‑critical

## 2. 20 Uncategorized SuperInstance Repos

I will read each repo’s description and recent commits to assign a category.

1. **ai‑agent‑swarm‑sim**  
   Description: “Simulate swarms of AI agents with emergent behavior”  
   Recent commits: Python swarm simulation, agent communication  
   Category: `fleet‑simulator` (extends fleet‑simulator concepts)

2. **auto‑lora‑trainer**  
   Description: “Automated LoRA training pipeline for diffusion models”  
   Recent commits: Training scripts, configs for SDXL  
   Category: `forgemaster‑lora` (LoRA training pipeline)

3. **beacon‑relay**  
   Description: “Lightweight service relay for lighthouse signals”  
   Recent commits: Rust service, HTTP relay logic  
   Category: `oracle‑beacon` (cloud lighthouse signaling)

4. **crab‑trap‑demo**  
   Description: “Demo of the Shell bootstrapping algorithm”  
   Recent commits: Python classifier + scorer demo  
   Category: `zeroclaw‑shell` (Shell/crab‑trap implementation)

5. **deadband‑viz**  
   Description: “Visualization of Deadband Protocol P0‑P1‑P2”  
   Recent commits: D3.js interactive diagram  
   Category: `docs‑protocol` (protocol visualization)

6. **edge‑inference‑bench**  
   Description: “Benchmark inference on Jetson vs Raspberry Pi”  
   Recent commits: Benchmarks for TensorRT, ONNX  
   Category: `jetson‑bench` (edge GPU benchmarking)

7. **ensign‑export‑ui**  
   Description: “Web UI for exporting PLATO room LoRAs”  
   Recent commits: React frontend, export API calls  
   Category: `plato‑ensign` (room export tooling)

8. **flux‑bytecode‑gen**  
   Description: “Generate flux‑runtime bytecode from AST”  
   Recent commits: Rust bytecode generator  
   Category: `flux‑runtime` (bytecode generation)

9. **ghost‑tile‑archive**  
   Description: “Archive of dead agent lesson tiles”  
   Recent commits: JSON tile storage, retrieval API  
   Category: `knowledge‑ghost` (ghost tile storage)

10. **hermit‑shell‑loader**  
    Description: “Load hermit crab agents into PLATO rooms”  
    Recent commits: Agent loading logic, room assignment  
    Category: `zeroclaw‑loader` (agent shell loader)

11. **lighthouse‑keeper‑log**  
    Description: “Log aggregation for Oracle1 lighthouse”  
    Recent commits: Log parsing, aggregation service  
    Category: `oracle‑logs` (lighthouse logging)

12. **mirror‑play‑capture**  
    Description: “Capture screen‑to‑screen LoRA training data”  
    Recent commits: Screen capture, data labeling  
    Category: `plato‑mirror` (Mirror Play I2I implementation)

13. **nav‑graph‑planner**  
    Description: “Navigation graph planner for constraint‑theory”  
    Recent commits: Graph search, constraint integration  
    Category: `constraint‑nav` (navigation planning)

14. **rabbit‑trail‑2025‑01**  
    Description: “Research into LLM‑based game NPCs (old)”  
    Recent commits: None for 6 months  
    Category: `research‑old` (archived research)

15. **rtx‑4050‑setup**  
    Description: “Setup scripts for Forgemaster RTX 4050”  
    Recent commits: WSL2 CUDA setup, driver scripts  
    Category: `forgemaster‑setup` (specialist hardware setup)

16. **sandbox‑casey‑test**  
    Description: “Casey’s personal test sandbox”  
    Recent commits: Random experiments, no fleet code  
    Category: `sandbox‑personal` (personal scratch)

17. **service‑watchdog**  
    Description: “Watchdog for fleet services”  
    Recent commits: Health checks, restart logic  
    Category: `service‑monitor` (service monitoring)

18. **shell‑classifier‑train**  
    Description: “Train classifier for Shell bootstrapping”  
    Recent commits: Training scripts, model export  
    Category: `zeroclaw‑classifier` (Shell classifier training)

19. **tile‑validator‑cli**  
    Description: “CLI to validate training tiles before submission”  
    Recent commits: Validation rules, CLI interface  
    Category: `tool‑tile` (tile validation utility)

20. **weaver‑integration‑map**  
    Description: “Map of fleet repo integrations”  
    Recent commits: Mermaid diagram, dependency list  
    Category: `docs‑integration` (integration documentation)

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `deadband‑viz` could merge into `plato‑torch` as a visualization module.
- `ensign‑export‑ui` could merge into `plato‑ensign` as its frontend.
- `hermit‑shell‑loader` and `shell‑classifier‑train` could merge into a single `zeroclaw‑core`.

**Abandoned Repos:**
- `rabbit‑trail‑2025‑01` — no commits in 6+ months, clearly archived research.
- `sandbox‑casey‑test` — personal sandbox, not fleet‑critical.

## 4. Next Steps for Curator

- Apply these categories as GitHub topics to each repo.
- Update `fleet‑knowledge/` with the refined taxonomy.
- Identify 20 more uncategorized repos for next cycle.
- Begin tagging all fleet repos systematically.

**Tile submitted to PLATO room: `curator‑repo‑taxonomy‑200`**