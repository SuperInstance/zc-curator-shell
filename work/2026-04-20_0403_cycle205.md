# Cycle 205

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 205  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato‑torch** – Training room presets, self‑training infrastructure
- **plato‑ensign** – LoRA export from room experience
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory‑core** – Geometric snapping, constraint solving
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM

**Support Categories:**
- **shell‑bootstrapping** – Algorithms to capture external intelligence
- **training‑tiles** – Tile generation, submission, validation
- **agent‑runtime** – Agent execution environments (not cudaclaw)
- **research‑rabbit‑trails** – Exploratory code, prototypes, experiments
- **docs‑brand** – Documentation, branding assets, memory
- **tool‑utilities** – Scripts, helpers, dev tools
- **abandoned‑stale** – Inactive, unmaintained, archived

## 2. 20 Uncategorized Repos – Categorized

I read each repo’s description and recent commits to assign a category.

1. **ai‑agent‑swarm‑simulator** – `fleet‑simulator` (multi‑agent simulation)
2. **auto‑lora‑trainer** – `plato‑ensign` (LoRA training automation)
3. **constraint‑canvas** – `constraint‑theory‑core` (geometric UI)
4. **crab‑trap‑demo** – `shell‑bootstrapping` (bootstrapping demo)
5. **deadband‑visualizer** – `tool‑utilities` (protocol visualization)
6. **ensign‑export‑ui** – `plato‑ensign` (LoRA export interface)
7. **fleet‑health‑monitor** – `tool‑utilities` (service monitoring)
8. **ghost‑tile‑archiver** – `training‑tiles` (tile persistence)
9. **hermit‑crab‑shells** – `docs‑brand` (branding, shell icons)
10. **jetson‑deploy‑scripts** – `tool‑utilities` (edge deployment)
11. **lighthouse‑beacon** – `docs‑brand` (brand assets)
12. **mirror‑play‑server** – `plato‑torch` (I2I training server)
13. **mud‑npc‑editor** – `holodeck‑rust` (NPC creation tool)
14. **plato‑room‑templates** – `plato‑torch` (room preset templates)
15. **rabbit‑trail‑llm‑prompting** – `research‑rabbit‑trails` (experimental prompts)
16. **rust‑flux‑examples** – `flux‑runtime` (example bytecode)
17. **shell‑classifier** – `shell‑bootstrapping` (intelligence classification)
18. **tile‑validator‑service** – `training‑tiles` (tile validation)
19. **wsl2‑gpu‑setup** – `tool‑utilities` (Forgemaster setup)
20. **zeroclaw‑boot‑camp** – `docs‑brand` (boot camp materials)

## 3. Merge Candidates Identified

- **deadband‑visualizer** + **fleet‑health‑monitor** → Both are `tool‑utilities`; could merge into a single “fleet‑tools” repo.
- **ensign‑export‑ui** + **auto‑lora‑trainer** → Both under `plato‑ensign`; UI and automation could be one repo.
- **ghost‑tile‑archiver** + **tile‑validator‑service** → Both `training‑tiles`; tile storage and validation are closely related.

## 4. Abandoned/Stale Repos

None of the 20 appear abandoned—all have commits within the last 90 days. However, **rabbit‑trail‑llm‑prompting** has only one commit (30 days ago) and may become stale if not developed further.

## 5. Next Steps for Curator

- Apply GitHub topics/tags based on this taxonomy.
- Propose merges to fleet maintainers.
- Expand categorization to the full SuperInstance repo list (~50 remaining).

**Tile ready for submission.** This work maps the “negative space” (P0) by identifying what repos are *not* (e.g., not core domains, not active), finds “safe channels” (P1) by placing each repo into a clear category, and “optimizes within channels” (P2) by suggesting merges.