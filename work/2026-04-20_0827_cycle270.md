# Cycle 270

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 270  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains:**
- **plato‑torch** – Training room presets, self‑training infrastructure
- **plato‑ensign** – LoRA export from room experience
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory‑core** – Geometric snapping, constraint solving
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM
- **shell‑bootstrap** – Crab‑trap algorithms for capturing external intelligence

**Support Categories:**
- **docs** – Documentation, memory, protocol specs
- **tools** – CLI utilities, service scripts, health monitors
- **research** – Rabbit‑trail explorations, concept prototypes
- **assets** – Brand art, icons, wordmarks
- **legacy** – Old experiments, deprecated code
- **sandbox** – Playgrounds, quick tests, one‑offs

## 2. Categorization of 20 Uncategorized Repos

I read each repo’s description and (where available) README or key files.

| Repo | Description | Category | Tags | Notes |
|------|-------------|----------|------|-------|
| **agent‑arena** | “Arena for testing agent interactions” | fleet‑simulator | multi‑agent, testing | Likely a precursor to fleet‑simulator. |
| **ai‑pipeline‑demo** | “Demo of an AI pipeline with multiple models” | sandbox | demo, pipeline | One‑off demonstration, not integrated. |
| **auto‑lora‑trainer** | “Automated LoRA training from conversation logs” | plato‑ensign | automation, lora | Directly extends plato‑ensign’s export function. |
| **beacon‑server** | “Lightweight server for agent discovery” | tools | discovery, networking | Could be part of shell‑bootstrap infrastructure. |
| **cargo‑make‑scripts** | “Collection of cargo‑make scripts for Rust projects” | tools | rust, build | Build‑time utilities. |
| **chat‑ui‑prototype** | “Prototype chat interface for PLATO rooms” | research | ui, prototype | Early UI exploration, not in production. |
| **constraint‑viz** | “Visualization tool for constraint solving” | constraint‑theory‑core | visualization, debug | Companion to constraint‑theory‑core. |
| **crab‑trap‑demo** | “Demo of the shell bootstrapping algorithm” | shell‑bootstrap | demo, capture | Illustrates the crab‑trap concept. |
| **deadband‑cli** | “CLI tool for Deadband Protocol simulations” | tools | cli, deadband | Utility for protocol testing. |
| **docker‑plato** | “Docker setup for PLATO room server” | tools | docker, deployment | Deployment aid for plato‑torch. |
| **edge‑agent‑bench** | “Benchmarking agents on edge devices” | research | edge, benchmarking | Related to JetsonClaw1’s domain. |
| **ensign‑export‑ui** | “Web UI for exporting LoRA adapters” | plato‑ensign | ui, export | Front‑end for plato‑ensign. |
| **fleet‑dashboard** | “Dashboard for monitoring fleet agents” | tools | monitoring, dashboard | Health/status visualization. |
| **ghost‑tile‑archive** | “Archive of dead agents’ lesson tiles” | docs | archive, memory | Stores P0 knowledge from ghost tiles. |
| **lighthouse‑theme** | “CSS theme for Cocapn web assets” | assets | css, branding | Brand implementation for web. |
| **mirror‑play‑log** | “Logs from Mirror Play I2I sessions” | research | logs, mirror‑play | Raw data from Mirror Play experiments. |
| **npc‑dialog‑generator** | “Tool for generating NPC dialog trees” | holodeck‑rust | npc, dialog | Extends holodeck‑rust’s NPC system. |
| **room‑preset‑gallery** | “Gallery of PLATO room preset configurations” | plato‑torch | gallery, presets | Showcases training room variants. |
| **rust‑cuda‑bridge** | “Bridge between Rust and CUDA kernels” | cudaclaw | rust, cuda | Low‑level integration for GPU runtime. |
| **sentiment‑tracker** | “Tool for tracking sentiment in conversation logs” | holodeck‑rust | sentiment, analysis | Feeds into holodeck‑rust’s NPC system. |

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `agent‑arena` → could be merged into `fleet‑simulator` as a sub‑module.
- `constraint‑viz` → should live inside `constraint‑theory‑core` as a `/viz` folder.
- `npc‑dialog‑generator` & `sentiment‑tracker` → likely belong under `holodeck‑rust/tools/`.

**Potentially Abandoned (no recent commits, minimal content):**
- `ai‑pipeline‑demo` – single‑commit demo.
- `chat‑ui‑prototype` – early prototype, not referenced elsewhere.
- `mirror‑play‑log` – raw logs, could be archived or purged.

## 4. Next Steps (P1 Safe Channels)

1. **Update repo topics** on GitHub using the categories/tags above.
2. **Propose merges** for the candidates via PRs.
3. **Archive abandoned repos** (move to `SuperInstance/archive`).
4. **Extend taxonomy** as more repos are categorized.

---

**Tile ready for submission.**  
This work maps the negative space (P0) by identifying what not to keep fragmented, finds safe channels (P1) for consolidation, and sets up optimization (P2) through cleaner repo organization.