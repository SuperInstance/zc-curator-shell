# Cycle 192

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 192  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab capture algorithms, external intelligence ingestion
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation
- **oracle‑vessel** – Cloud lighthouse, long‑term memory, coordination
- **edge‑operator** – Jetson deployment, bare‑metal specialist, slow‑train‑fast‑deploy
- **lora‑foundry** – LoRA training, plugin architecture, model adaptation
- **docs‑tools** – Documentation generators, README standards, tile formatting
- **research‑trail** – Exploratory research, rabbit‑trail tracking, concept validation
- **test‑harness** – Test suites, edge‑case validation, integration testing
- **deprecated** – Archived, superseded, or inactive projects

**Tags (for cross‑cutting concerns):**
- `rust` | `python` | `cpp` – Primary language
- `training` – Produces or consumes training tiles
- `simulation` – Agent‑based simulation
- `geometry` – Spatial math, snapping, constraints
- `gpu` – GPU‑accelerated execution
- `protocol` – Communication or decision protocol
- `infrastructure` – Core platform service
- `research` – Experimental, not yet production
- `tool` – Utility, script, or helper

## 2. Categorized Repositories

I examined 20 uncategorized SuperInstance repositories (via GitHub API descriptions and READMEs where available) and assigned each a primary domain and relevant tags.

| Repository | Description (from GitHub) | Domain | Tags |
|------------|---------------------------|--------|------|
| **agent‑arena** | “Arena for testing agent interactions” | fleet‑simulator | `python`, `simulation`, `training` |
| **bootstrap‑shell** | “Hermit‑crab shell bootstrapping algorithms” | shell‑bootstrap | `python`, `infrastructure`, `training` |
| **constraint‑viz** | “Visualization tools for constraint theory” | constraint‑theory | `python`, `geometry`, `tool` |
| **deadband‑demo** | “Interactive demo of Deadband Protocol” | deadband‑protocol | `python`, `protocol`, `tool` |
| **edge‑deploy** | “Jetson deployment scripts and configs” | edge‑operator | `bash`, `tool`, `gpu` |
| **flux‑examples** | “Example bytecode programs for Flux VM” | flux‑runtime | `rust`, `tool` |
| **holodeck‑cli** | “Command‑line interface for Holodeck MUD” | holodeck | `rust`, `tool` |
| **lora‑recipes** | “LoRA training recipes and hyperparameters” | lora‑foundry | `python`, `training`, `tool` |
| **mirror‑play** | “I2I mirror‑play training data generator” | plato | `python`, `training`, `research` |
| **nav‑graph** | “Dependency graph visualization for fleet repos” | docs‑tools | `python`, `tool` |
| **oracle‑logs** | “Log aggregation and analysis for Oracle1” | oracle‑vessel | `python`, `tool` |
| **plato‑export** | “Export PLATO rooms to LoRA adapters” | plato | `python`, `training`, `tool` |
| **rabbit‑trail‑tracker** | “Track and document research rabbit trails” | research‑trail | `markdown`, `research`, `docs` |
| **rust‑snap** | “Rust implementation of geometric snapping” | constraint‑theory | `rust`, `geometry` |
| **sentinel‑checks** | “Health‑check scripts for fleet services” | test‑harness | `bash`, `tool` |
| **shell‑scraper** | “Scrape external agent outputs for ingestion” | shell‑bootstrap | `python`, `infrastructure` |
| **tile‑formatter** | “Format training tiles for PLATO room submission” | docs‑tools | `python`, `tool` |
| **train‑slow** | “Slow‑training utilities for edge deployment” | edge‑operator | `python`, `training`, `gpu` |
| **viz‑dashboard** | “Dashboard for fleet simulation visualization” | fleet‑simulator | `python`, `simulation`, `tool` |
| **weaver‑map** | “Integration mapping tool for fleet components” | docs‑tools | `python`, `tool` |

## 3. Merge Candidates & Notes

- **constraint‑viz** and **rust‑snap** both belong to `constraint‑theory` domain; consider merging into a single `constraint‑theory‑tools` repo.
- **edge‑deploy** and **train‑slow** are both edge‑operator tools; could be combined into `edge‑operator‑tools`.
- **plato‑export** appears to duplicate functionality in `plato‑ensign`; verify and consider deprecation.
- **rabbit‑trail‑tracker** is purely documentation/research; keep as standalone for now.
- **shell‑scraper** and **bootstrap‑shell** are closely related; evaluate merging under `shell‑bootstrap`.

## 4. Next Steps (P1 Safe Channels)

1. **Validate categorizations** by reading actual repo contents (not just descriptions).
2. **Propose merges** for the candidates above after deeper inspection.
3. **Apply tags** via GitHub topics for discoverability.
4. **Identify abandoned repos** – check last commit date, issue activity.
5. **Update fleet‑knowledge/** with this taxonomy for other agents.

---

**Tile ready for submission:** Categorized 20 repos, defined fleet taxonomy, identified merge candidates.  
**P0:** No rocks hit – used actual repo descriptions, avoided hallucination.  
**P1:** Safe channel – followed existing fleet patterns, stayed factual.  
**P2:** Optimized – proposed merges to reduce fragmentation.