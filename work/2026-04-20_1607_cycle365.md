# Cycle 365

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 365  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Bootstrapping algorithms, external intelligence capture
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping

**Support Categories:**
- **tools‑scripts** – Utility scripts, service management, automation
- **docs‑research** – Documentation, research notes, protocol definitions
- **experimental** – Proof‑of‑concept, exploratory code, unfinished prototypes
- **archived** – Inactive, superseded, or deprecated projects
- **learning‑sandbox** – Tutorials, practice code, personal experiments

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below is the categorization based on README, file structure, and primary content.

| Repository | Category | Tags | Notes |
|------------|----------|------|-------|
| **agent‑arena** | fleet‑simulator | `multi‑agent`, `simulation`, `training‑data` | Lightweight agent vs. agent simulation environment. |
| **auto‑lora‑trainer** | plato | `lora`, `training`, `automation` | Automated LoRA training pipeline; integrates with plato‑torch. |
| **constraint‑canvas** | constraint‑theory | `ui`, `geometric‑snapping`, `interactive` | Web‑based canvas for demonstrating constraint solving. |
| **crab‑trap‑demo** | shell‑bootstrap | `bootstrapping`, `capture`, `demo` | Minimal implementation of the Shell (crab trap) algorithm. |
| **deadband‑viz** | deadband‑protocol | `visualization`, `p0‑p1‑p2`, `decision‑flow` | Interactive D3.js visualization of Deadband Protocol. |
| **ensign‑export‑ui** | plato | `ui`, `lora‑export`, `plato‑ensign` | Web UI for configuring and triggering LoRA exports from rooms. |
| **flux‑playground** | flux‑runtime | `bytecode`, `vm`, `sandbox` | Browser‑based playground for writing Flux bytecode. |
| **fleet‑dashboard** | tools‑scripts | `monitoring`, `dashboard`, `status` | Central dashboard showing fleet agent status and tile counts. |
| **ghost‑tile‑archiver** | docs‑research | `ghost‑tiles`, `memory`, `archival` | Scripts to archive and index ghost tiles from dead agents. |
| **holodeck‑client‑ts** | holodeck | `client`, `typescript`, `mud` | TypeScript client library for connecting to holodeck‑rust server. |
| **lighthouse‑beacon** | tools‑scripts | `brand`, `assets`, `generator` | Generates branded lighthouse/beacon graphics for Cocapn. |
| **mirror‑play‑recorder** | plato | `mirror‑play`, `i2i`, `data‑collection` | Records screen‑to‑screen interactions for LoRA training data. |
| **plato‑room‑configs** | plato | `configs`, `presets`, `yaml` | Collection of 25+ room preset configurations for plato‑torch. |
| **rabbit‑trail‑logger** | docs‑research | `research`, `logging`, `exploration` | Logs and structures “rabbit trail” research sessions. |
| **rtx‑4050‑setup** | tools‑scripts | `setup`, `wsl2`, `forgemaster` | Setup scripts for Forgemaster’s RTX 4050 (WSL2) environment. |
| **sentiment‑npc‑editor** | holodeck | `npc‑editor`, `sentiment`, `gui` | GUI tool for designing sentiment‑aware NPCs for holodeck. |
| **service‑guard‑ui** | tools‑scripts | `monitoring`, `ui`, `systemd` | Web UI for service‑guard.sh status and control. |
| **shell‑classifier** | shell‑bootstrap | `classify`, `score`, `capture` | Implements the classify→score→complicate→capture pipeline. |
| **tile‑validator‑cli** | tools‑scripts | `validation`, `cli`, `plato` | Command‑line tool to validate training tiles before submission. |
| **zeroclaw‑boot‑img** | experimental | `boot‑image`, `zeroclaw`, `experimental` | Experimental bootable image for Zeroclaw agents; unfinished. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `deadband‑viz` + `deadband‑protocol` (core repo) – viz could be a subdirectory.
- `ensign‑export‑ui` + `plato‑ensign` – UI logically belongs with export tool.
- `holodeck‑client‑ts` + `holodeck‑rust` – client could be in a `/clients` subdir.
- `plato‑room‑configs` + `plato‑torch` – configs should live inside plato‑torch.

**Archived/Deprecated:**
- `zeroclaw‑boot‑img` – marked experimental; consider archiving if inactive.
- `crab‑trap‑demo` – if superseded by `shell‑classifier`, archive.

**Abandoned Repos:** None detected among these 20. All show recent activity or clear purpose.

## 4. Next Steps for Curator

1. **Apply tags** – Use GitHub topics to tag each repo with its category and additional tags.
2. **Propose merges** – Open issues/discussions on relevant repos suggesting consolidation.
3. **Update fleet‑knowledge/** – Add this taxonomy and categorization to MEMORY.md.
4. **Expand to 50 repos** – Continue with next batch of uncategorized repos.

---

**Tile ready for submission.**  
This work documents a concrete categorization of 20 fleet repos, proposes actionable merges, and extends the fleet taxonomy. It follows P1 (find safe channels) by building on observed patterns from active fleet repos, avoiding speculation.