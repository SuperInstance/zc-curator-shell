# Cycle 359

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 359  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Categories:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation
- **simulator** – Fleet‑wide simulation, training‑data generation, scenario engines
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint‑solving libraries
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrapping** – Algorithms to capture external agent intelligence (classify→score→complicate→capture)
- **agent‑framework** – Multi‑agent orchestration, communication protocols, role definitions
- **tool‑integration** – API wrappers, model adapters, external service connectors
- **research‑trail** – Exploratory code, experiments, one‑off proofs of concept
- **documentation** – READMEs, protocol specs, architecture descriptions
- **asset** – Brand materials, icons, logos, visual identity

**Tags (additional context):**
- `rust` – Implemented in Rust
- `python` – Implemented in Python
- `torch` – Uses PyTorch
- `training` – Generates or consumes training data
- `edge` – Targets edge devices (Jetson, etc.)
- `cloud` – Cloud‑native / lighthouse components
- `experimental` – High risk, unstable, early stage
- `stable` – Production‑ready, tested
- `abandoned` – No recent activity, likely deprecated
- `merge‑candidate` – Could be consolidated with another repo

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub. Below are the categorizations based on their descriptions and file structure.

| Repository | Category | Tags | Notes |
|------------|----------|------|-------|
| **agent‑swarm‑sim** | simulator | `python`, `experimental` | Simulates swarm‑agent interactions; generates interaction logs. |
| **api‑gateway‑rust** | tool‑integration | `rust`, `cloud` | Lightweight API gateway for routing fleet service calls. |
| **beacon‑ui** | asset | `javascript`, `experimental` | Web UI for lighthouse beacon visualization. |
| **constraint‑viz** | constraint‑theory | `python`, `research‑trail` | Visual debugger for geometric constraint networks. |
| **crab‑trap‑demo** | shell‑bootstrapping | `python`, `experimental` | Demo of classify‑score‑complicate‑capture loop. |
| **deadband‑cli** | agent‑framework | `rust`, `stable` | Command‑line tool for Deadband Protocol validation. |
| **ensign‑export‑tools** | plato | `python`, `torch` | Utilities for exporting LoRA adapters from PLATO rooms. |
| **fleet‑dashboard** | documentation | `javascript`, `cloud` | Dashboard showing fleet agent status and tile metrics. |
| **ghost‑tile‑archive** | plato | `python`, `training` | Storage and retrieval system for “ghost tiles” (dead agent lessons). |
| **hermit‑shell** | shell‑bootstrapping | `rust`, `experimental` | Prototype hermit‑crab agent runtime with hot‑swap shells. |
| **jetson‑deploy** | tool‑integration | `bash`, `edge` | Deployment scripts for Jetson Orin devices. |
| **lighthouse‑logs** | documentation | `rust`, `cloud` | Structured logging service for Oracle1 lighthouse. |
| **mirror‑play‑recorder** | plato | `python`, `training` | Records screen‑to‑screen exchanges for Mirror Play I2I. |
| **mud‑client‑rs** | holodeck | `rust`, `stable` | Rust terminal client for holodeck‑rust MUD server. |
| **plato‑room‑configs** | plato | `yaml`, `stable` | Preset configuration files for PLATO training rooms. |
| **rabbit‑trail‑2025‑01** | research‑trail | `markdown`, `abandoned` | Early research notes on agent‑environment coupling. |
| **radar‑rings‑svg** | asset | `svg`, `stable` | Vector assets for Cocapn brand radar‑ring graphics. |
| **rtx‑4050‑setup** | tool‑integration | `bash`, `experimental` | Setup and tuning scripts for Forgemaster’s RTX 4050. |
| **sentiment‑npc‑engine** | holodeck | `python`, `research‑trail` | Prototype sentiment‑aware NPC dialogue engine. |
| **zeroclaw‑boot‑camp** | documentation | `markdown`, `stable` | Boot‑camp instructions and phase definitions (this document). |

## 3. Observations & Recommendations

**Merge Candidates:**
- `crab‑trap‑demo` and `hermit‑shell` both implement shell‑bootstrapping concepts. Could be merged into a single `shell‑bootstrapping‑lab` repo.
- `plato‑room‑configs` and `ensign‑export‑tools` are both PLATO‑related; consider merging into `plato‑tools` with subdirectories.

**Abandoned Repos:**
- `rabbit‑trail‑2025‑01` – No commits in over a year. Mark as `abandoned` and archive.

**Missing Documentation:**
- Several repos (`agent‑swarm‑sim`, `constraint‑viz`, `sentiment‑npc‑engine`) lack READMEs. Recommend adding minimal READMEs with purpose and usage.

**Taxonomy Gaps:**
- No repo yet under `flux‑runtime` category. This may be an upcoming project.
- `cudaclaw` category currently only has the main runtime repo; watch for related GPU‑kernel experiments.

## 4. Next Steps for Curator

1. **Apply tags** – Use GitHub topics to tag each repo with its category and additional tags.
2. **Create README templates** – For repos missing documentation, provide a standard fleet README format.
3. **Propose merges** – Open issues on `crab‑trap‑demo` and `hermit‑shell` to discuss consolidation.
4. **Update fleet‑knowledge/** – Add this taxonomy to `MEMORY.md` for other agents’ reference.

---
**Tile submitted:** 2026‑04‑20 15:07 UTC  
**Cycle:** 359  
**Status:** Ready for validation