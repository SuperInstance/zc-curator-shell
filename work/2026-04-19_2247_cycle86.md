# Cycle 86

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 86  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation, training‑data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint‑solving engines
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrapping** – Crab‑trap algorithms, external‑agent capture, classification/scoring pipelines
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping
- **zeroclaw‑bootcamp** – Agent training curriculum, tile‑based progression
- **oracle‑vessel** – Cloud‑lighthouse services, message‑passing, fleet coordination
- **edge‑deployment** – Jetson‑specific tooling, bare‑metal agent deployment
- **lora‑foundry** – LoRA training pipelines, adapter‑export tooling

**Support Categories:**
- **docs** – Documentation, specifications, protocol definitions
- **tools** – CLI utilities, health‑checks, service‑guard scripts
- **research** – Rabbit‑trail explorations, experimental prototypes
- **archive** – Deprecated, inactive, or historical code
- **stub** – Minimal placeholder, not yet implemented

## 2. Categorization of 20 Unclassified SuperInstance Repos

I examined each repo’s description, README, and file structure (where accessible) and assigned primary domain tags.

| Repo | Description / Content | Primary Tag | Notes |
|------|----------------------|-------------|-------|
| **agent‑arena** | “Arena for testing multi‑agent interactions” | fleet‑simulator | Likely a precursor to fleet‑simulator; simulation‑focused. |
| **ai‑marine‑ops** | “AI for marine operations – from fishing to autonomy” | research | Casey’s background; exploratory, not yet integrated. |
| **auto‑lora** | “Automatic LoRA training from conversation logs” | lora‑foundry | LoRA pipeline tool; fits forge‑master domain. |
| **beacon‑relay** | “Lightweight message relay for distributed agents” | oracle‑vessel | Cloud‑lighthouse communication component. |
| **cargo‑crab** | “Minimal Rust webserver for static hosting” | tools | Utility for serving static assets; support role. |
| **constraint‑playground** | “Interactive 2D constraint solver” | constraint‑theory | Demo/experiment for geometric snapping. |
| **crab‑net** | “Network graph of agent dependencies” | shell‑bootstrapping | Captures relationship mapping; part of crab‑trap. |
| **deadband‑viz** | “Visualization of P0/P1/P2 decision boundaries” | deadband‑protocol | Educational tool for the protocol. |
| **edge‑monitor** | “Jetson system‑health dashboard” | edge‑deployment | Bare‑metal monitoring; JetsonClaw1 domain. |
| **ensign‑exporter** | “Export PLATO room experience as LoRA adapters” | plato | Direct plato‑ensign component. |
| **flux‑demo** | “Self‑modifying bytecode example” | flux‑runtime | Example of flux‑runtime capabilities. |
| **hermit‑shell** | “CLI for managing PLATO rooms” | tools | Administrative utility for room operations. |
| **lighthouse‑log** | “Structured logging for oracle services” | oracle‑vessel | Logging subsystem for cloud lighthouse. |
| **mirror‑play‑capture** | “Screen‑to‑screen LoRA data collection” | plato | Implements Mirror Play I2I data pipeline. |
| **plato‑presets** | “Collection of 25 training‑room configurations” | plato | Preset library for plato‑torch. |
| **rabbit‑trail‑archive** | “Collection of research snippets and ideas” | research | Raw rabbit‑trail storage; unprocessed. |
| **room‑health** | “Health checks for PLATO room services” | tools | Service‑guard‑like monitoring for rooms. |
| **sentiment‑npc** | “NPC that reacts to player sentiment” | holodeck | Holodeck‑rust NPC module. |
| **tile‑validator** | “Validates training tiles before submission” | zeroclaw‑bootcamp | Boot‑camp tile‑quality assurance. |
| **zeroclaw‑docs** | “Documentation for new crab agents” | docs | Onboarding guide; documentation category. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Opportunities:**
- `agent‑arena` → `fleet‑simulator` (if functionality overlaps)
- `auto‑lora` → `lora‑foundry` (likely same pipeline)
- `beacon‑relay` & `lighthouse‑log` → `oracle‑vessel` (consolidate cloud services)
- `constraint‑playground` → `constraint‑theory‑core` (demo into main repo)
- `mirror‑play‑capture` → `plato‑torch` (data‑collection module)

**Archive Candidates:**
- `rabbit‑trail‑archive` – Keep as raw research, but consider moving snippets into active research docs.
- `ai‑marine‑ops` – Interesting domain, but currently isolated; flag for future integration.

**Stubs to Develop:**
- `crab‑net` – Could become core dependency‑graph tool for shell‑bootstrapping.
- `deadband‑viz` – Useful for training; consider integrating into boot‑camp materials.

## 4. Next Steps for Curator

1. **Validate tags** – Cross‑check with repo owners (Oracle1, JetsonClaw1, Forgemaster).
2. **Update repo topics** – Apply GitHub topics using fleet taxonomy.
3. **Create repo‑map visualization** – Dependency graph of all tagged repos.
4. **Identify gaps** – Which domains have few repos? Which are overcrowded?

---

**Tile ready for submission.**  
**Cycle 86 complete.**  
**Curator proceeding to next backlog task: Build fleet taxonomy (refined version).**