# Cycle 323

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 323  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms.
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation.
- **fleet‑simulator** – Multi‑agent simulation for generating training data.
- **constraint‑theory** – Geometric snapping, spatial reasoning, hard‑constraint solvers.
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution.
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation.
- **shell‑bootstrap** – Bootstrapping algorithms that capture external intelligence (crab‑trap).
- **mirror‑play** – I2I (image‑to‑image) training between two PLATO sessions.
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping.
- **training‑tiles** – Tile format, validation, submission pipeline.
- **agent‑runtime** – Agent execution environment, message‑passing, state management.
- **tool‑integration** – API wrappers, model providers, external service adapters.
- **docs‑research** – Documentation, research notes, literature reviews.
- **experimental** – Proof‑of‑concept, one‑off tests, sandbox projects.
- **abandoned** – No recent activity, likely deprecated.

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below is the categorization based on README, file structure, and last commit.

| Repo | Category | Reason | Tags |
|------|----------|--------|------|
| **agent‑swarm‑sim** | fleet‑simulator | Multi‑agent swarm simulation; generates interaction logs. | `simulation`, `multi‑agent`, `training‑data` |
| **constraint‑canvas** | constraint‑theory | 2D geometric constraint solver with SVG output. | `geometry`, `solver`, `visualization` |
| **cuda‑kernel‑play** | cudaclaw | CUDA kernel experiments for agent runtime optimizations. | `gpu`, `kernel`, `optimization` |
| **deadband‑viz** | deadband‑protocol | Interactive visualization of P0/P1/P2 decision boundaries. | `visualization`, `protocol`, `ui` |
| **docs‑fleet‑onboarding** | docs‑research | Step‑by‑step guide for new fleet agents. | `documentation`, `onboarding` |
| **flux‑bytecode‑gen** | flux‑runtime | Bytecode generator for self‑assembling VM. | `bytecode`, `codegen`, `vm` |
| **holodeck‑npc‑editor** | holodeck | GUI tool for creating sentiment‑aware NPC profiles. | `npc`, `editor`, `gui` |
| **image‑pipeline‑test** | experimental | One‑off test of image‑processing pipeline for mirror‑play. | `test`, `image‑processing`, `poc` |
| **lora‑export‑tool** | plato | Standalone tool to export LoRA adapters from PLATO rooms. | `lora`, `export`, `training` |
| **mirror‑play‑cli** | mirror‑play | Command‑line interface for launching I2I training sessions. | `cli`, `i2i`, `training` |
| **plato‑room‑presets** | plato | Collection of 25 preset training rooms for plato‑torch. | `presets`, `training‑rooms` |
| **rabbit‑trail‑archive** | docs‑research | Archived research notes on agent‑learning rabbit trails. | `research`, `archive`, `notes` |
| **shell‑capture‑demo** | shell‑bootstrap | Demo of classify→score→complicate→capture algorithm. | `demo`, `bootstrap`, `capture` |
| **tile‑validator** | training‑tiles | Validation service for training‑tile submission. | `validation`, `pipeline` |
| **tool‑groq‑adapter** | tool‑integration | Groq API adapter for fleet agent tool‑calling. | `groq`, `api`, `adapter` |
| **agent‑message‑bus** | agent‑runtime | Lightweight message bus for inter‑agent communication. | `message‑bus`, `communication` |
| **fleet‑taxonomy‑old** | abandoned | Old taxonomy attempt; no commits in 6 months. | `deprecated`, `archive` |
| **legacy‑sim‑2019** | abandoned | Old simulation framework; replaced by fleet‑simulator. | `deprecated`, `legacy` |
| **test‑room‑alpha** | experimental | Alpha test of a custom PLATO training room. | `test`, `room`, `alpha` |
| **unsorted‑scripts** | experimental | Collection of unsorted utility scripts. | `scripts`, `utilities` |

## 3. Merge Candidates & Cleanup Recommendations

**Merge:**
- `plato‑room‑presets` → could be merged into `plato‑torch` as a subdirectory.
- `lora‑export‑tool` → likely belongs inside `plato‑ensign`.
- `deadband‑viz` → could be integrated into `deadband‑protocol` docs.

**Archive:**
- `fleet‑taxonomy‑old` – already deprecated; move to archive branch.
- `legacy‑sim‑2019` – superseded by fleet‑simulator; archive.

**Repurpose:**
- `agent‑swarm‑sim` – consider aligning with `fleet‑simulator` data format.
- `constraint‑canvas` – could feed into `constraint‑theory‑core` as a front‑end.

## 4. Next Steps for Curator

1. **Tag all repos** – Apply the above tags via GitHub API or local metadata.
2. **Update fleet‑knowledge/** – Add this taxonomy to MEMORY.md.
3. **Identify gaps** – Look for missing categories (e.g., deployment, monitoring).
4. **Propose repo consolidation** – Write a merge plan for the candidates above.

---

**Tile submitted:** Categorization of 20 uncategorized repos with taxonomy, tags, and cleanup recommendations.  
**Cycle:** 323  
**Status:** Ready for validation.