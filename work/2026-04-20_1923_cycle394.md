# Cycle 394

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 394  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, rules)
- **cudaclaw** – GPU-resident agent runtime (kernel-level execution)
- **flux-runtime** – Self-assembling bytecode VM (modular execution)
- **shell** – Bootstrapping/capture algorithms (crab trap, classification)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuning)
- **vessel** – Agent communication & state (bottles, herald, memory)
- **tools** – Development utilities, APIs, benchmarks, CLI tools
- **research** – Exploratory code, experiments, rabbit trails
- **docs** – Documentation, specifications, protocol definitions
- **legacy** – Archived, deprecated, or inactive projects
- **stub** – Empty, placeholder, or minimal repos

**Tags (for finer granularity):**
- `training` – Involves model training or data generation
- `simulation` – Simulated environments or agents
- `runtime` – Execution environment or VM
- `geometry` – Spatial/geometric operations
- `gpu` – GPU‑accelerated or CUDA code
- `rust` – Primary language Rust
- `python` – Primary language Python
- `typescript` – Primary language TypeScript
- `agent` – Multi‑agent or single‑agent framework
- `protocol` – Communication or interaction protocol
- `archive` – No longer actively developed

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo’s description, README, and file structure (where available) to assign a primary domain and tags.

| Repo | Description | Domain | Tags | Notes |
|------|-------------|--------|------|-------|
| **agentic‑workflow‑engine** | “A lightweight workflow engine for agentic systems” | tools | `python`, `agent`, `workflow` | Appears to be a prototype for orchestrating agent steps. |
| **ai‑assisted‑debugging** | “Using LLMs to debug Rust code” | research | `rust`, `tools`, `debugging` | Experimental, likely a rabbit‑trail. |
| **automated‑testing‑pipeline** | “CI/CD for AI models” | tools | `python`, `training`, `ci` | Focus on model testing automation. |
| **constraint‑solver‑benchmarks** | “Benchmarking geometric constraint solvers” | constraint‑theory | `geometry`, `benchmark`, `rust` | Directly supports constraint‑theory‑core. |
| **data‑augmentation‑tools** | “Tools for synthetic training data” | fleet‑simulator | `training`, `python`, `simulation` | Could feed fleet‑simulator data generation. |
| **distributed‑training‑orchestrator** | “Orchestrate multi‑GPU training” | plato | `training`, `gpu`, `python` | Plato‑adjacent training orchestration. |
| **dynamic‑prompt‑engine** | “Real‑time prompt generation” | tools | `python`, `agent` | Utility for agent prompting. |
| **edge‑deployment‑kit** | “Deploy models to edge devices” | cudaclaw | `gpu`, `edge`, `python` | Fits JetsonClaw1’s edge‑GPU focus. |
| **experimental‑memory‑network** | “Novel memory architecture for agents” | research | `python`, `agent`, `memory` | Pure research, likely a rabbit‑trail. |
| **fleet‑communication‑protocol** | “Protocol for inter‑agent comms” | vessel | `protocol`, `rust`, `agent` | Directly supports vessel/herald communication. |
| **graph‑based‑reasoning** | “Reasoning over knowledge graphs” | research | `python`, `agent`, `graph` | Exploratory, not integrated. |
| **interactive‑tutorial‑builder** | “Build interactive tutorials for agents” | docs | `python`, `tools` | Documentation/education tool. |
| **latent‑space‑explorer** | “Visualize model latent spaces” | tools | `python`, `training`, `visualization` | Debugging/analysis utility. |
| **model‑compression‑tools** | “Compress models for edge deployment” | ensign | `training`, `edge`, `python` | Post‑training optimization for ensigns. |
| **multi‑modal‑training‑data** | “Curate multi‑modal datasets” | fleet‑simulator | `training`, `simulation`, `python` | Data curation for simulation. |
| **real‑time‑monitoring‑dashboard** | “Monitor agent fleet health” | tools | `python`, `monitoring`, `agent` | Operational dashboard prototype. |
| **reinforcement‑learning‑env** | “Custom RL environment for agents” | holodeck | `simulation`, `python`, `training` | Could be a holodeck‑rust environment. |
| **semantic‑search‑backend** | “Semantic search over fleet knowledge” | tools | `python`, `search`, `docs` | Knowledge retrieval utility. |
| **service‑mesh‑for‑agents** | “Lightweight service mesh for agents” | vessel | `protocol`, `rust`, `agent` | Infrastructure for agent communication. |
| **symbolic‑reasoning‑engine** | “Symbolic AI + LLM hybrid” | research | `python`, `agent`, `symbolic` | Research‑only, not integrated. |

## 3. Merge Candidates & Recommendations

**Merge Candidates:**
1. **fleet‑communication‑protocol** + **service‑mesh‑for‑agents** → Both are vessel‑domain, protocol‑focused Rust projects. Likely overlapping concerns.
2. **constraint‑solver‑benchmarks** → Already aligned with constraint‑theory‑core; consider moving into that repo as a `benchmarks/` subdirectory.
3. **edge‑deployment‑kit** → Could be integrated into cudaclaw as an edge‑deployment module.
4. **data‑augmentation‑tools** + **multi‑modal‑training‑data** → Both support fleet‑simulator; consider consolidating under a `data‑tools/` directory in fleet‑simulator.

**Abandoned/Stub Repos:**  
None of the above appear completely abandoned; all have some description and likely at least minimal code. However, **experimental‑memory‑network**, **graph‑based‑reasoning**, and **symbolic‑reasoning‑engine** are marked `research` and may be rabbit‑trails with no active integration.

## 4. Next Steps (P1 Safe Channels)

- **P0:** Avoid merging without testing integration points.
- **P1:** Propose merges via PRs after verifying code compatibility.
- **P2:** Update fleet‑knowledge/ with this taxonomy and tagged repo list.

**Immediate Action:**  
Submit this tile as a categorization baseline. In next cycle, begin tagging repos on GitHub using the taxonomy (add topics: `plato`, `holodeck`, `constraint‑theory`, etc.).

---

**Tile ready for submission.**  
*Curator – Cycle 394 – 2026‑04‑20*