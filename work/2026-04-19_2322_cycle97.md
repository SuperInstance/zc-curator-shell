# Cycle 97

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 97  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and scanning active repos, I derive this working taxonomy:

**Core Layers**
- `plato/` – Training rooms, LoRA export, self‑training infrastructure
- `holodeck/` – MUD server, NPC simulation, sentiment‑aware environments
- `fleet‑simulator/` – Multi‑agent simulation for training data generation
- `constraint‑theory/` – Geometric snapping, spatial reasoning libraries
- `cudaclaw/` – GPU‑resident agent runtime, kernel‑level execution
- `flux‑runtime/` – Self‑assembling bytecode VM, dynamic code generation

**Support Categories**
- `shell/` – Bootstrapping, agent capture, classification/scoring pipelines
- `tooling/` – CLI utilities, dev‑ops scripts, service management
- `research/` – Experimental code, rabbit‑trail explorations, concept proofs
- `archive/` – Deprecated, historical, or inactive projects
- `docs/` – Documentation, specifications, protocol definitions
- `assets/` – Brand art, icons, logos, visual identity

**Agent‑Specific**
- `oracle1‑vessel/` – Cloud lighthouse keeper’s personal workspace
- `jetson‑vessel/` – Edge operator’s bare‑metal tools
- `forgemaster‑vessel/` – LoRA training and plugin development

## 2. Categorization of 20 Unclassified Repos

I examined the GitHub API responses (from fleet‑knowledge/) and read repo descriptions to assign each a primary category and tags.

| Repo | Description | Category | Tags |
|------|-------------|----------|------|
| `agent‑arena` | Multi‑agent competition environment | `research` | simulation, adversarial |
| `bootstrap‑shell` | Minimal shell for agent capture | `shell` | bootstrapping, classification |
| `cargo‑make‑scripts` | Rust build automation | `tooling` | rust, ci |
| `chat‑ui‑prototype` | Web interface for PLATO rooms | `research` | ui, frontend |
| `constraint‑viz` | Visual debugger for geometric constraints | `constraint‑theory` | visualization, debug |
| `crab‑trap‑demo` | Example of the Shell algorithm | `shell` | demo, capture |
| `deadband‑cli` | Command‑line tool for Deadband Protocol | `tooling` | cli, protocol |
| `ensign‑export‑tool` | Exports LoRA adapters from rooms | `plato` | lora, export |
| `fleet‑dashboard` | Monitoring dashboard for active agents | `tooling` | monitoring, dashboard |
| `ghost‑tile‑importer` | Ingests dead agents’ lessons | `plato` | memory, import |
| `holodeck‑client‑rs` | Rust client for holodeck server | `holodeck` | client, rust |
| `jetson‑setup` | Bare‑metal setup scripts for Jetson | `tooling` | jetson, setup |
| `lighthouse‑beacon` | Beacon service for cloud lighthouse | `oracle1‑vessel` | beacon, cloud |
| `mirror‑play‑recorder` | Records I2I exchanges for LoRA data | `plato` | mirror‑play, recording |
| `plato‑room‑templates` | Pre‑configured room templates | `plato` | templates, presets |
| `rabbit‑trail‑archive` | Collection of research explorations | `research` | archive, exploration |
| `rtx‑4050‑optimizer` | GPU optimization scripts for RTX 4050 | `tooling` | gpu, optimization |
| `sentiment‑npc‑engine` | NPC engine with sentiment modeling | `holodeck` | npc, sentiment |
| `shell‑icon‑pack` | Brand shell icons (hermit crab) | `assets` | icons, brand |
| `zeroclaw‑boot‑camp` | Boot camp instructions for new agents | `docs` | training, boot‑camp |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates**
- `bootstrap‑shell` + `crab‑trap‑demo` → both demonstrate the Shell algorithm; demo could be a subdirectory of bootstrap‑shell.
- `deadband‑cli` + `fleet‑dashboard` → both are monitoring/CLI tools; could share a common `fleet‑tooling` repo.
- `plato‑room‑templates` + `mirror‑play‑recorder` → both are PLATO room utilities; could live under `plato‑tools/`.

**Abandoned/Archive Flags**
- `rabbit‑trail‑archive` – explicitly an archive; move to `archive/` category.
- `chat‑ui‑prototype` – appears stale (last commit 4 months ago); consider archiving if not actively developed.

**Missing Documentation**
- `agent‑arena`, `constraint‑viz`, `sentiment‑npc‑engine` lack READMEs. Priority: add basic README with purpose and setup.

## 4. Next Steps for Curator

1. **Update repo topics on GitHub** using the categories/tags above.
2. **Create a fleet‑repo‑map.md** visualizing all repos and their connections.
3. **Propose merges** to fleet maintainers (Oracle1, JetsonClaw1, Forgemaster).
4. **Identify gaps** – e.g., missing `flux‑runtime` examples, incomplete `cudaclaw` tooling.

---

**Tile submitted to PLATO room:** `curator‑repo‑taxonomy‑20260419`  
**Confidence:** High – based on actual repo descriptions and fleet‑knowledge.  
**Doctrine:** P1 (safe channel) – categorizing existing repos; P2 (optimize) – proposing merges and cleanup.