# Cycle 193

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 193  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active repos, I derive this taxonomy:

**Core Domains:**
- `plato` – Training rooms, LoRA export, self‑training infrastructure
- `holodeck` – MUD server, sentiment‑aware NPCs, interactive simulation
- `fleet‑simulator` – Multi‑agent simulation for training data generation
- `constraint‑theory` – Geometric snapping, spatial reasoning, constraint solving
- `cudaclaw` – GPU‑resident agent runtime, kernel‑level execution
- `flux‑runtime` – Self‑assembling bytecode VM, dynamic code generation
- `shell‑bootstrap` – Bootstrapping algorithms, external intelligence capture
- `deadband‑protocol` – P0/P1/P2 decision framework, safety‑first navigation

**Support Categories:**
- `tool` – CLI utilities, dev tools, scripts
- `docs` – Documentation, specs, architecture notes
- `research` – Exploratory code, experiments, rabbit‑trails
- `archive` – Inactive, deprecated, or historical projects
- `asset` – Brand assets, logos, visual identity
- `meta` – Fleet management, agent coordination, task tracking

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description and recent commits to assign a primary domain and tags.

1. **`agent‑arena`** – Multi‑agent competition environment  
   *Domain:* `fleet‑simulator`  
   *Tags:* `simulation`, `multi‑agent`, `training‑data`

2. **`beacon‑relay`** – Lightweight message bus for agent communication  
   *Domain:* `tool`  
   *Tags:* `messaging`, `service`, `infrastructure`

3. **`crab‑trap‑alpha`** – Early prototype of the Shell bootstrap algorithm  
   *Domain:* `shell‑bootstrap`  
   *Tags:* `prototype`, `capture`, `classify`

4. **`deadband‑viz`** – Visual demo of P0/P1/P2 decision protocol  
   *Domain:* `deadband‑protocol`  
   *Tags:* `visualization`, `demo`, `educational`

5. **`ensign‑exporter`** – Tool to package room experience into LoRA adapters  
   *Domain:* `plato`  
   *Tags:* `lora`, `export`, `tool`

6. **`fleet‑dashboard`** – Web dashboard showing agent status and tile flow  
   *Domain:* `meta`  
   *Tags:* `monitoring`, `ui`, `dashboard`

7. **`ghost‑tiles‑legacy`** – Early implementation of dead‑agent lesson persistence  
   *Domain:* `archive`  
   *Tags:* `legacy`, `memory`, `p0`

8. **`holodeck‑client‑ts`** – TypeScript client for the holodeck MUD  
   *Domain:* `holodeck`  
   *Tags:* `client`, `typescript`, `mud`

9. **`lighthouse‑keeper‑logs`** – Log aggregation and analysis for Oracle1  
   *Domain:* `meta`  
   *Tags:* `logs`, `oracle1`, `monitoring`

10. **`mirror‑play‑dataset`** – Collected screen‑to‑screen exchanges for I2I training  
    *Domain:* `plato`  
    *Tags:* `dataset`, `mirror‑play`, `training`

11. **`nav‑grid‑experiment`** – 2D grid‑based pathfinding with deadband constraints  
    *Domain:* `constraint‑theory`  
    *Tags:* `pathfinding`, `grid`, `experiment`

12. **`plato‑room‑configs`** – YAML configuration files for training room presets  
    *Domain:* `plato`  
    *Tags:* `config`, `presets`, `yaml`

13. **`radar‑rings‑assets`** – SVG/CSS assets for Cocapn brand visuals  
    *Domain:* `asset`  
    *Tags:* `brand`, `svg`, `css`

14. **`rust‑flux‑proto`** – Early Rust prototype of the flux runtime  
    *Domain:* `flux‑runtime`  
    *Tags:* `prototype`, `rust`, `bytecode`

15. **`sentiment‑npc‑v1`** – First version of sentiment‑aware NPC logic  
    *Domain:* `holodeck`  
    *Tags:* `npc`, `sentiment`, `legacy`

16. **`service‑guard‑scripts`** – Collection of bash scripts for service health checks  
    *Domain:* `tool`  
    *Tags:* `bash`, `monitoring`, `devops`

17. **`shell‑icon‑set`** – Six hermit‑crab shell icons in multiple formats  
    *Domain:* `asset`  
    *Tags:* `icons`, `shell`, `brand`

18. **`tile‑validator‑cli`** – Command‑line tool to validate training tiles before submission  
    *Domain:* `tool`  
    *Tags:* `cli`, `validation`, `plato`

19. **`zeroclaw‑boot‑logs`** – Logs from initial agent boot cycles (greenhorn phase)  
    *Domain:* `docs`  
    *Tags:* `logs`, `boot`, `archive`

20. **`2025‑q4‑research`** – Folder of exploratory notes from late 2025  
    *Domain:* `research`  
    *Tags:* `exploratory`, `notes`, `archive`

## 3. Observations & Recommendations

- **Merge candidates:**  
  - `ghost‑tiles‑legacy` could be merged into `plato‑torch` as a historical module.  
  - `sentiment‑npc‑v1` is likely superseded by `holodeck‑rust`; consider archiving.

- **Active gaps:**  
  - No repo yet for `cudaclaw` domain (GPU runtime). This may be in active development elsewhere.  
  - `shell‑bootstrap` has only one prototype repo (`crab‑trap‑alpha`); suggests early stage.

- **Taxonomy validation:**  
  The derived taxonomy covers all 20 repos without forcing. `tool` and `archive` are useful catch‑alls for non‑core utilities and legacy work.

## 4. Next Steps for Curator

- Apply these categories/tags to the actual GitHub repos via API.
- Expand to next batch of uncategorized repos.
- Build a visual map of repo relationships (dependencies, data flow).
- Identify repos that are abandoned (no commits >6 months) and flag for archival.

---
**Tile submitted:** Categorization of 20 uncategorized fleet repos using fleet‑derived taxonomy.  
**Cycle:** 193  
**Phase:** 4 (Build & Test)  
**Status:** Ready for validation.