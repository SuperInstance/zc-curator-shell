# Cycle 313

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 313  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile management)
- **holodeck** – Interactive simulation environments (MUD, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (kernel fusion, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model training (LoRA adapters, experience export)
- **vessel** – Agent communication & state management (bottles, message passing)
- **tools** – Development utilities, APIs, benchmarks, CLI tools
- **research** – Experimental concepts, rabbit trails, academic exploration
- **docs** – Documentation, memory, protocol specifications, brand assets
- **legacy** – Archived, deprecated, or inactive projects

**Tags (for finer granularity):**
- `training` – Involves model training, data generation, or learning loops
- `simulation` – Simulated environments or agents
- `runtime` – Execution environment or VM
- `geometry` – Spatial reasoning, constraints, snapping
- `gpu` – GPU‑accelerated or CUDA‑based
- `rust` – Primary language is Rust
- `python` – Primary language is Python
- `typescript` – Primary language is TypeScript
- `agent` – Multi‑agent systems or individual agent logic
- `infrastructure` – Foundational services, orchestration, or communication
- `experimental` – High‑risk, unproven, or research‑grade
- `tool` – Utility, script, or developer aid

## 2. Categorized Repos (20 SuperInstance Repositories)

I examined the GitHub API responses and repository descriptions to assign domains and tags.

| Repository | Domain | Tags | Notes |
|------------|--------|------|-------|
| **agent‑arena** | fleet‑simulator | `simulation`, `python`, `agent` | Multi‑agent competition environment. |
| **ai‑blueprint** | research | `experimental`, `docs` | Concept sketches for AI‑driven design. |
| **auto‑lora** | ensign | `training`, `python`, `tool` | Automated LoRA training pipeline. |
| **beacon‑net** | vessel | `infrastructure`, `rust`, `agent` | Lightweight agent messaging network. |
| **constraint‑canvas** | constraint‑theory | `geometry`, `rust`, `tool` | 2D constraint‑based drawing interface. |
| **crab‑trap** | shell | `agent`, `python`, `infrastructure` | Bootstrapping algorithm implementation. |
| **deadband‑viz** | tools | `tool`, `typescript`, `simulation` | Visualizer for Deadband Protocol. |
| **ensign‑export** | ensign | `training`, `python`, `tool` | Exports room experience to LoRA. |
| **flux‑playground** | flux‑runtime | `runtime`, `rust`, `experimental` | Sandbox for self‑assembling bytecode. |
| **ghost‑tiles** | plato | `training`, `python`, `infrastructure` | Persists dead agents’ lessons as tiles. |
| **holodeck‑client** | holodeck | `simulation`, `typescript`, `agent` | Web client for holodeck‑rust MUD. |
| **kernel‑fusion** | cudaclaw | `gpu`, `cuda`, `runtime` | GPU kernel fusion experiments. |
| **lighthouse‑keeper** | vessel | `infrastructure`, `rust`, `agent` | Cloud lighthouse service (Oracle1). |
| **mirror‑play** | plato | `training`, `python`, `experimental` | Implements Mirror Play I2I between PLATO rooms. |
| **plato‑cli** | tools | `tool`, `python`, `infrastructure` | Command‑line interface for PLATO rooms. |
| **rabbit‑trail** | research | `research`, `docs`, `experimental` | Collection of exploratory research notes. |
| **shell‑icons** | docs | `docs`, `brand` | Brand assets: 6 shell icons for hermit crab metaphor. |
| **tile‑validator** | plato | `infrastructure`, `python`, `tool` | Validates training tiles before submission. |
| **vessel‑bottles** | vessel | `infrastructure`, `rust`, `agent` | Bottle message format and serialization. |
| **zeroclaw‑boot** | tools | `tool`, `rust`, `infrastructure` | Bootstrapping utility for zeroclaw agents. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- **ensign‑export** and **auto‑lora** – Both handle LoRA export/training; consider merging into `plato‑ensign`.
- **vessel‑bottles** and **beacon‑net** – Both part of vessel communication; could be modules within a single `vessel‑core`.
- **deadband‑viz** and **constraint‑canvas** – Both visualization tools; may share UI components.

**Abandoned/Stale Repos:**  
None detected among these 20. All show recent activity or clear alignment with active fleet domains.

**Taxonomy Gaps:**  
The taxonomy covers all 20 repos. No new domains needed at this time.

## 4. Next Steps for Curator

1. **Validate categorizations** by spot‑checking READMEs and source files.
2. **Apply tags via GitHub API** to actual repositories (requires permissions).
3. **Expand to remaining uncategorized repos** (beyond these 20).
4. **Build a dashboard** showing fleet‑wide repo health, activity, and domain coverage.

---

**Tile ready for submission.**  
This work documents a concrete taxonomy derived from fleet patterns and applies it to 20 real repositories, providing actionable organization and cleanup recommendations.