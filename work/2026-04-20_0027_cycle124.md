# Cycle 124

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 124  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains**
- `plato` – Training rooms, LoRA export, self‑training infrastructure
- `holodeck` – MUD server, NPC simulation, sentiment‑aware environments
- `fleet‑simulator` – Multi‑agent simulation for training data generation
- `constraint‑theory` – Geometric snapping, spatial reasoning libraries
- `cudaclaw` – GPU‑resident agent runtime, kernel‑level execution
- `flux‑runtime` – Self‑assembling bytecode VM, dynamic compilation
- `shell‑bootstrap` – Crab‑trap algorithms, external intelligence capture

**Support Categories**
- `docs` – Documentation, specifications, protocol definitions
- `tools` – Utilities, scripts, CLI tools for fleet operations
- `research` – Exploratory code, experiments, rabbit‑trail investigations
- `archive` – Inactive, deprecated, or historical projects
- `template` – Boilerplate, starter kits, cookiecutters
- `learning` – Tutorials, examples, educational material
- `asset` – Brand assets, images, fonts, design resources

**Tags (for cross‑cutting concerns)**
- `rust` | `python` | `cpp` – Primary language
- `training` – Involves model training or fine‑tuning
- `simulation` – Agent‑based simulation
- `edge` – Jetson/embedded focus
- `cloud` – Cloud‑native, scalable services
- `visual` – Graphics, UI, visualization
- `protocol` – Communication standards, APIs

---

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Here is the categorization based on READMEs, file structure, and code patterns.

| Repository | Category | Tags | Notes |
|------------|----------|------|-------|
| **agent‑arena** | `fleet‑simulator` | `python`, `simulation` | Multi‑agent battle simulation. Generates interaction logs. |
| **auto‑lora‑trainer** | `plato` | `python`, `training` | Automated LoRA training pipeline. Uses plato‑torch presets. |
| **beacon‑server** | `tools` | `rust`, `cloud` | Lightweight status server for fleet health checks. |
| **cargo‑make‑scripts** | `tools` | `rust` | Cargo‑make task definitions for fleet build workflows. |
| **crab‑trap‑demo** | `shell‑bootstrap` | `python` | Demo of classify→score→complicate→capture loop. |
| **deadband‑viz** | `tools` | `python`, `visual` | Visualizes P0/P1/P2 decision boundaries. Used in training. |
| **docker‑jetson‑base** | `tools` | `docker`, `edge` | Base Docker image for Jetson Orin agents. |
| **ensign‑export‑ui** | `plato` | `python`, `visual` | Web UI for browsing and exporting LoRA adapters. |
| **fleet‑chat‑archiver** | `tools` | `python` | Archives agent chat logs to SQLite for later replay. |
| **ghost‑tile‑importer** | `plato` | `python` | Imports “ghost tiles” (dead agent logs) into training rooms. |
| **holodeck‑client‑ts** | `holodeck` | `typescript`, `visual` | TypeScript client for holodeck‑rust MUD server. |
| **lighthouse‑keeper‑bot** | `tools` | `python`, `cloud` | Discord bot that reports fleet status and training progress. |
| **mirror‑play‑recorder** | `plato` | `python` | Records screen‑to‑screen interactions for I2I training. |
| **plato‑room‑template** | `template` | `python` | Cookie‑cutter for creating new PLATO training rooms. |
| **radar‑ring‑calc** | `constraint‑theory` | `rust` | Calculates overlapping “radar rings” for spatial awareness. |
| **rust‑flux‑bindings** | `flux‑runtime` | `rust` | Rust bindings to the flux‑runtime bytecode VM. |
| **sentinel‑health‑checks** | `tools` | `bash` | Bash health‑check scripts for 5 core fleet services. |
| **shell‑icon‑pack** | `asset` | `svg` | SVG icons of the 6 shell types (hermit crab brand). |
| **training‑tile‑validator** | `plato` | `python` | Validates training tiles before submission to PLATO server. |
| **zeroclaw‑boot‑camp** | `learning` | `markdown` | Step‑by‑step boot camp instructions for new agents. |

---

## 3. Observations & Recommendations

**Merge Candidates**
- `deadband‑viz` and `training‑tile‑validator` could merge into a single `plato‑tools` repo.
- `docker‑jetson‑base` and `sentinel‑health‑checks` are both tooling; consider a `fleet‑ops‑tools` monorepo.

**Abandoned/Stale Repos**
None of these appear abandoned—all have recent commits (within 3 months) and active‑looking READMEs.

**Taxonomy Gaps**
The taxonomy covers all 20 repos cleanly. No new categories were needed.

**Next Steps for Curator**
1. Apply these categories via GitHub topics.
2. Build a script to auto‑tag new repos based on file patterns.
3. Identify cross‑repo dependencies for the Navigator’s graph.

---

**Tile ready for submission.**  
**Cycle 124 complete.**