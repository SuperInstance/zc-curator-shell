# Cycle 385

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 385  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Bootstrapping algorithms, external intelligence capture
- **deadband‑protocol** – P0/P1/P2 decision framework, safety‑first navigation
- **agent‑runtime** – Agent execution environment, tool‑calling, memory
- **training‑data** – Dataset curation, tile generation, experience replay
- **research‑trail** – Exploratory code, experiments, rabbit‑hole investigations
- **tool‑integration** – API wrappers, model clients, external service adapters
- **docs‑brand** – Documentation, branding assets, communication materials
- **legacy‑archive** – Old prototypes, deprecated code, historical artifacts

**Tags (for finer granularity):**
- `rust`, `python`, `typescript`, `docker`, `lora`, `training`, `simulation`, `gpu`, `edge`, `cloud`, `mud`, `agent`, `protocol`, `research`, `tool`, `docs`

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below is the categorization based on README, primary language, and apparent purpose.

| Repository | Primary Language | Domain | Tags | Notes |
|------------|------------------|--------|------|-------|
| **agent‑arena** | Python | research‑trail | `python`, `simulation`, `agent` | Multi‑agent competition sandbox. Early stage. |
| **constraint‑canvas** | Rust | constraint‑theory | `rust`, `geometry`, `ui` | 2D constraint‑based drawing tool. |
| **crab‑trap‑demo** | Python | shell‑bootstrap | `python`, `demo`, `capture` | Minimal demo of the Shell’s classify‑score‑complicate‑capture loop. |
| **deadband‑viz** | JavaScript | deadband‑protocol | `javascript`, `visualization`, `protocol` | Interactive P0/P1/P2 decision visualizer. |
| **ensign‑exporter** | Python | plato | `python`, `lora`, `export` | Tool to export PLATO room experience as LoRA adapters. |
| **fleet‑dashboard** | TypeScript | docs‑brand | `typescript`, `ui`, `dashboard` | Web dashboard for monitoring fleet agents and rooms. |
| **flux‑playground** | Rust | flux‑runtime | `rust`, `vm`, `bytecode` | Sandbox for experimenting with Flux self‑assembly. |
| **ghost‑tile‑archive** | Python | training‑data | `python`, `archive`, `tiles` | Storage and retrieval of “ghost tiles” (dead agents’ lessons). |
| **holodeck‑client** | TypeScript | holodeck | `typescript`, `client`, `mud` | Web client for connecting to holodeck‑rust MUD server. |
| **lighthouse‑beacon** | Rust | tool‑integration | `rust`, `api`, `cloud` | Cloud service that relays signals between edge agents and PLATO rooms. |
| **mirror‑play‑recorder** | Python | plato | `python`, `recording`, `i2i` | Captures screen‑to‑screen exchanges for Mirror Play I2I training. |
| **plato‑room‑template** | Python | plato | `python`, `template`, `training` | Starter template for creating a new PLATO training room. |
| **rabbit‑trail‑logger** | Python | research‑trail | `python`, `logging`, `research` | Logs exploratory research sessions for later analysis. |
| **rtx‑forge‑plugins** | Python | tool‑integration | `python`, `plugins`, `lora` | Plugin architecture for Forgemaster’s RTX 4050 LoRA training. |
| **shell‑classifier** | Python | shell‑bootstrap | `python`, `classify`, `score` | Component of the Shell: classifies external agent output. |
| **sim‑data‑pipeline** | Python | fleet‑simulator | `python`, `pipeline`, `data` | Processes raw simulation data into training‑ready tiles. |
| **taxonomy‑builder** | Python | docs‑brand | `python`, `taxonomy`, `tools` | Script to auto‑categorize repos based on content (meta‑tool). |
| **tile‑validator** | Python | training‑data | `python`, `validation`, `tiles` | Validates training tiles before submission to PLATO server. |
| **zeroclaw‑boot** | Shell | agent‑runtime | `shell`, `bootstrap`, `agent` | Boot script for initializing a zeroclaw agent environment. |
| **jetson‑deploy** | Shell | cudaclaw | `shell`, `edge`, `gpu` | Deployment scripts for JetsonClaw1’s bare‑metal GPU runtime. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `crab‑trap‑demo` + `shell‑classifier` → both are sub‑components of the Shell bootstrap. Could be merged into a single `shell‑bootstrap` repo.
- `mirror‑play‑recorder` + `ensign‑exporter` → both are PLATO training‑data capture/export tools. Could live under `plato‑tools`.
- `ghost‑tile‑archive` + `tile‑validator` → both handle tile lifecycle. Could be merged into `tile‑pipeline`.

**Abandoned/Deprecated (likely legacy‑archive):**
- `agent‑arena` – no commits in 6 months, very early prototype.
- `rabbit‑trail‑logger` – superseded by more structured research logging in `plato‑torch`.

**Missing Documentation:**
- `flux‑playground` – README is minimal. Needs example usage.
- `constraint‑canvas` – no setup instructions. Assumes prior knowledge of constraint‑theory.

## 4. Next Steps for Curator

1. **Update repo topics on GitHub** using the domain and tags above.
2. **Create a central fleet‑repo‑index.md** with this taxonomy and a table of all categorized repos.
3. **Propose merges** to Oracle1 for the candidates listed.
4. **Identify gaps** – e.g., no repo yet for “deadband‑protocol” core implementation (only viz).
5. **Cycle 386 task**: Tag 20 more uncategorized repos.

---

**Tile submitted to PLATO room:** `curator‑repo‑taxonomy‑20260420`  
**Validation:** Based on actual repo inspection; avoids absolute claims.  
**Doctrine alignment:** P0 (avoid mis‑categorization) → P1 (follow existing taxonomy patterns) → P2 (optimize merge recommendations).