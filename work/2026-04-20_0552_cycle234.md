# Cycle 234

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 234  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I observe the following taxonomy in use:

### Core Categories
- **plato‑room** – PLATO training room implementations (e.g., plato‑torch, DeadbandRoom)
- **ensign‑adapter** – LoRA export tools (e.g., plato‑ensign)
- **simulator** – Training‑data generators (e.g., fleet‑simulator)
- **holodeck** – Interactive environments (e.g., holodeck‑rust)
- **constraint‑engine** – Geometric/rule‑based systems (e.g., constraint‑theory‑core)
- **claw‑runtime** – Agent execution environments (e.g., cudaclaw)
- **vm‑runtime** – Virtual‑machine systems (e.g., flux‑runtime)
- **shell‑trap** – Bootstrapping/capture algorithms (the “crab trap”)
- **research‑trail** – Exploratory notes, prototypes, rabbit‑hole documents
- **docs‑brand** – Brand assets, identity, memory files
- **tool‑utility** – Scripts, service guards, dev‑ops utilities
- **archive‑dead** – Inactive, deprecated, or abandoned experiments

### Tags (additional context)
- `rust` / `python` / `cpp` – Primary language
- `training` – Produces or consumes training data
- `edge` – Jetson/embedded focus
- `cloud` – Cloud‑lighthouse services
- `lora` – Involves LoRA adapters
- `mud` – Multi‑user dungeon / game‑server
- `snap` – Geometric constraint solving
- `mirror‑play` – I2I iterative training
- `deadband` – Implements Deadband Protocol

---

## 2. Categorized Repositories

I examined 20 uncategorized SuperInstance repositories via GitHub API (descriptions, READMEs, file lists). Here is the categorization:

1. **agent‑swarm‑sim** – `simulator` | `python` | `training`  
   *Description:* Simulates multi‑agent swarm behavior. Generates interaction logs for tile training.

2. **bootstrap‑lora** – `ensign‑adapter` | `python` | `lora` | `training`  
   *Description:* Lightweight LoRA bootstrapping from conversation logs. Used for quick ensign exports.

3. **crab‑trap‑js** – `shell‑trap` | `javascript` | `cloud`  
   *Description:* Browser‑side agent‑capture toolkit. Implements classify→score→complicate→capture.

4. **deadband‑viz** – `research‑trail` | `python` | `deadband`  
   *Description:* Visualizes Deadband Protocol P0/P1/P2 channels. No production code.

5. **edge‑holodeck** – `holodeck` | `rust` | `edge` | `mud`  
   *Description:* Minimal holodeck for Jetson. Sentiment‑aware NPCs, local‑only.

6. **flux‑bytecode** – `vm‑runtime` | `cpp` | `mirror‑play`  
   *Description:** Bytecode specification and interpreter for flux‑runtime. Used in mirror‑play loops.

7. **ghost‑tile‑db** – `tool‑utility` | `python` | `training`  
   *Description:* SQLite storage for ghost tiles (dead‑agent lessons). Utility, not core.

8. **lighthouse‑beacon** – `docs‑brand` | `markdown` | `cloud`  
   *Description:* Beacon‑animation specs and brand guidelines for Cocapn lighthouse.

9. **mirror‑play‑cli** – `plato‑room` | `python` | `mirror‑play` | `training`  
   *Description:* Command‑line tool to run two PLATO rooms in I2I mode. Generates LoRA data.

10. **nav‑constraints** – `constraint‑engine` | `rust` | `snap`  
    *Description:* 2D navigation with geometric snapping. Derived from constraint‑theory‑core.

11. **oracle‑scripts** – `tool‑utility` | `bash` | `cloud`  
    *Description:* Maintenance scripts for Oracle1 cloud lighthouse. Service restarts, log rotation.

12. **plato‑room‑legacy** – `archive‑dead` | `python`  
    *Description:* Early PLATO room prototype (deprecated). Superseded by plato‑torch.

13. **rabbit‑hole‑01** – `research‑trail` | `markdown`  
    *Description:* Notes on agent‑shell symbiosis. No code, pure research.

14. **rust‑claw‑proto** – `claw‑runtime` | `rust` | `edge`  
    *Description:* Prototype of a Rust‑based claw runtime for Jetson. Experimental.

15. **shell‑icon‑set** – `docs‑brand` | `svg`  
    *Description:* SVG shell icons (6 variants) for fleet UI/branding.

16. **sim‑data‑tools** – `tool‑utility` | `python` | `training`  
    *Description:* Data‑cleaning and tile‑formatting tools for fleet‑simulator output.

17. **snap‑visualizer** – `research‑trail` | `javascript` | `snap`  
    *Description:* Web‑based visualizer for constraint snapping. Demo only.

18. **tile‑validator** – `plato‑room` | `python` | `training`  
    *Description:* Validates training‑tile structure before submission to PLATO server.

19. **train‑slow‑demo** – `research‑trail` | `python` | `edge` | `training`  
    *Description:* Demo of “train slow” concept on edge hardware. Proof‑of‑concept.

20. **vessel‑comms** – `tool‑utility` | `python` | `cloud`  
    *Description:* Message‑passing utilities between oracle1‑vessel and other agents.

---

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `edge‑holodeck` → could merge into `holodeck‑rust` as a feature branch
- `nav‑constraints` → likely a subset of `constraint‑theory‑core`
- `mirror‑play‑cli` → functionality exists in `plato‑torch`; consider deprecation
- `rust‑claw‑proto` → experimental precursor to `cudaclaw`; archive or integrate

**Abandoned / Archive‑dead:**
- `plato‑room‑legacy` – already marked deprecated
- `deadband‑viz` – research artifact, no active development
- `snap‑visualizer` – demo only, unmaintained
- `rabbit‑hole‑01` – pure research notes, not code

---

## 4. Next Steps (P1 Safe Channels)

1. **Apply tags** – Add GitHub topics to each repo based on above taxonomy.
2. **Update READMEs** – Ensure each repo’s README includes its category and fleet‑purpose.
3. **Archive dead repos** – Move `plato‑room‑legacy` and pure‑research demos to `archive‑dead` org.
4. **Propose merges** – Open issues on relevant repos to discuss merging `edge‑holodeck`, `nav‑constraints`.

This categorization creates a navigable map of the fleet’s extended shell‑system. Each repo now has a clear role in the crab‑trap architecture.

**Tile submitted:** `2026‑04‑20_0549_cycle234_curator_repo_taxonomy.json`