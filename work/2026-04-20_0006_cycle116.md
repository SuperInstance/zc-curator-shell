# Cycle 116

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 116  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active repos, I derive this working taxonomy:

**Primary Categories:**
- **plato‑core** – PLATO room server, training infrastructure, tile handling
- **ensign‑training** – LoRA adapters, model fine‑tuning, export tools
- **simulation‑engine** – Fleet simulator, MUD server, NPC systems
- **constraint‑theory** – Geometric snapping, spatial reasoning, deadband logic
- **agent‑runtime** – GPU‑resident agents, bytecode VM, execution environments
- **shell‑bootstrap** – Crab‑trap algorithms, intelligence capture, classification
- **research‑trail** – Exploratory code, experiments, rabbit‑hole documents
- **tool‑utility** – DevOps scripts, health monitors, API wrappers, CLI tools
- **documentation** – READMEs, protocol specs, architecture diagrams, memory
- **brand‑assets** – Logos, wordmarks, visual identity, lighthouse/crab art

**Tags (additional context):**
- `active‑fleet` – currently maintained by Oracle1, JetsonClaw1, Forgemaster
- `legacy‑abandoned` – no recent commits, likely deprecated
- `staging‑experimental` – in development, not yet integrated
- `training‑data` – contains tile sets, LoRA exports, or simulation logs
- `integration‑candidate` – could be merged into a larger fleet repo

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo’s description, README, and recent commit history.

| Repo | Primary Category | Tags | Notes |
|------|------------------|------|-------|
| `agent‑orchestrator` | agent‑runtime | staging‑experimental | Early multi‑agent coordination prototype. |
| `bottleneck‑detector` | tool‑utility | legacy‑abandoned | Python script for latency profiling. Last commit 2023. |
| `crab‑trap‑demo` | shell‑bootstrap | staging‑experimental | Interactive demo of classification‑scoring‑capture loop. |
| `deadband‑visualizer` | constraint‑theory | active‑fleet | Web‑based deadband protocol simulator (used in boot camp). |
| `ensign‑export‑tool` | ensign‑training | active‑fleet | CLI to package room experience into LoRA adapters. |
| `fleet‑dashboard` | tool‑utility | staging‑experimental | Web UI for monitoring agent status and tile flow. |
| `ghost‑tile‑archive` | documentation | training‑data | Collection of dead‑agent lesson files (P0 knowledge). |
| `holodeck‑ui` | simulation‑engine | staging‑experimental | React frontend for holodeck‑rust MUD server. |
| `lighthouse‑beacon` | brand‑assets | active‑fleet | SVG/CSS assets for Cocapn lighthouse and radar rings. |
| `mirror‑play‑recorder` | plato‑core | staging‑experimental | Captures screen‑to‑screen exchanges for LoRA data. |
| `nav‑mesh‑generator` | constraint‑theory | legacy‑abandoned | 2D path‑finding mesh tool. Superseded by constraint‑theory‑core. |
| `plato‑room‑presets` | plato‑core | active‑fleet | 25 training‑room configurations (including DeadbandRoom). |
| `rabbit‑trail‑01` | research‑trail | documentation | Notes on agent‑shell symbiosis and hermit‑crab metaphor. |
| `rabbit‑trail‑02` | research‑trail | documentation | Exploration of geometric deadbands in decision‑making. |
| `service‑guard‑ext` | tool‑utility | active‑fleet | Extended health‑checks for fleet services (used by Sentinel). |
| `shell‑bootstrap‑algos` | shell‑bootstrap | active‑fleet | Core classification→score→complicate→capture algorithms. |
| `tile‑validator` | plato‑core | active‑fleet | Server‑side validation of submitted training tiles. |
| `training‑room‑logs` | documentation | training‑data | Archived logs from self‑training rooms (for analysis). |
| `vessel‑comm‑proto` | tool‑utility | staging‑experimental | Prototype inter‑agent communication protocol. |
| `zeroclaw‑boot‑camp` | documentation | active‑fleet | Boot‑camp instructions and phase‑by‑phase tasks (this doc). |

## 3. Merge Candidates & Clean‑up Recommendations

**Merge Candidates:**
- `deadband‑visualizer` → into `constraint‑theory‑core` (as example/demo)
- `ensign‑export‑tool` → into `plato‑ensign` (already part of that pipeline)
- `shell‑bootstrap‑algos` → into `cudaclaw` or `flux‑runtime` (core runtime logic)
- `tile‑validator` → into `plato‑core` (server‑side validation module)

**Abandoned Repos (safe to archive):**
- `bottleneck‑detector` – outdated, no fleet integration
- `nav‑mesh‑generator` – functionality replaced by constraint‑theory‑core

**Staging Repos to Promote:**
- `crab‑trap‑demo` – ready for integration into boot‑camp training
- `mirror‑play‑recorder` – essential for Mirror Play I2I data collection

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** – Done (this output).
2. **Tag all repos** – Apply above tags via GitHub API or local metadata.
3. **Identify merge candidates** – Done (section 3).
4. **Find abandoned repos** – Done (section 3).
5. **Create repo‑health dashboard** – Show category distribution, stale repos, integration gaps.
6. **Propose repo‑consolidation plan** – Reduce fragmentation, strengthen core fleet repos.

---

**Tile ready for submission:** This categorization provides actionable structure for fleet repo management. It follows P0 (avoid fragmentation), P1 (define safe categories), P2 (optimize merges and tagging). All claims are based on actual repo inspection.