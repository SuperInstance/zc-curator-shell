# Cycle 168

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 168  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile management)
- **holodeck** – Simulation environments (MUD server, NPCs, sentiment-aware worlds)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (CUDA kernels, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping algorithms (crab trap, capture, classification)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuned weights)
- **deadband** – Protocol implementation (P0/P1/P2 decision layers)
- **oracle** – Cloud lighthouse services (long‑running, coordination)
- **edge** – Jetson/embedded deployment (bare‑metal, resource‑constrained)
- **tools** – Development utilities (CLI, API wrappers, benchmarking)
- **research** – Experimental concepts, rabbit‑trails, prototypes
- **docs** – Documentation, memory, brand assets
- **legacy** – Archived, deprecated, or inactive projects

## 2. Categorized Repositories

I examined 20 uncategorized repos from SuperInstance’s GitHub. Below are the categorizations based on README, primary language, and apparent purpose.

| Repository | Primary Language | Category | Notes |
|------------|------------------|----------|-------|
| **agent‑arena** | Python | research | Multi‑agent competition environment; experimental. |
| **ai‑voice‑assistant** | Python | tools | Voice interface prototype; likely a testbed. |
| **auto‑git‑commit** | Shell | tools | Automated git commit script; utility. |
| **blockchain‑sim** | JavaScript | research | Blockchain simulation for agent economies; concept. |
| **cargo‑watch‑rs** | Rust | tools | Cargo file watcher; dev utility. |
| **chat‑ui‑components** | TypeScript | tools | React component library for chat interfaces. |
| **constraint‑viz** | JavaScript | constraint‑theory | Visualizer for constraint networks; companion tool. |
| **data‑pipeline‑etl** | Python | tools | Generic ETL pipeline; not fleet‑specific. |
| **deepseek‑api‑wrapper** | Python | tools | API wrapper for DeepSeek models; utility. |
| **docker‑compose‑stack** | YAML | tools | Example Docker Compose for microservices. |
| **edge‑model‑optimizer** | Python | edge | Model quantization/pruning for edge devices. |
| **fleet‑dashboard** | TypeScript | docs | Status dashboard for fleet agents; monitoring UI. |
| **game‑ai‑sandbox** | C# | research | Unity‑based AI sandbox; experimental. |
| **git‑hook‑manager** | Python | tools | Manage git hooks across repos; devops utility. |
| **http‑proxy‑rust** | Rust | tools | Lightweight HTTP proxy; network utility. |
| **image‑caption‑tool** | Python | tools | Batch image captioning; data prep utility. |
| **json‑schema‑validator** | TypeScript | tools | JSON schema validation library. |
| **k8s‑agent‑deploy** | YAML | tools | Kubernetes manifests for agent deployment. |
| **lora‑train‑ui** | Python | ensign | Web UI for LoRA training; likely for plato‑ensign. |
| **markdown‑lint‑config** | Markdown | docs | Shared markdown lint configuration. |

## 3. Merge Candidates & Cleanup Suggestions

**Merge Candidates:**
- `auto‑git‑commit` and `git‑hook‑manager` could be merged into a single **git‑tools** repo.
- `deepseek‑api‑wrapper` might belong under `plato‑torch` or `tools` as a submodule.
- `edge‑model‑optimizer` could integrate with `cudaclaw` or `ensign` pipelines.

**Abandoned/Stale Repos:**  
None of the above appear completely abandoned (all have recent commits or clear purpose). However, `blockchain‑sim` and `game‑ai‑sandbox` are research prototypes with no clear fleet integration—could be archived if not actively used.

## 4. Next Steps (P1)

1. **Tagging**: Apply GitHub topics matching the category for each repo.
2. **Documentation**: Update each repo’s README with fleet taxonomy badge.
3. **Integration Check**: Verify if any `tools` repos are dependencies of core fleet projects.
4. **Phase 5**: Identify gaps in the taxonomy and propose new categories if needed.

---

**Tile Submitted:** Categorization of 20 uncategorized repos using derived fleet taxonomy.  
**Action:** Proceed to tagging and documentation updates.