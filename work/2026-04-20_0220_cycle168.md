# Cycle 168

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 168  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I observe the following taxonomy in use:

- **plato‑*** — PLATO room system (training, ensigns, rooms)
- **holodeck‑*** — MUD server, NPC simulation
- **fleet‑*** — fleet‑wide tools, simulation, coordination
- **constraint‑theory‑*** — geometric snapping, constraint solving
- **cudaclaw** — GPU‑resident agent runtime
- **flux‑runtime** — self‑assembling bytecode VM
- **zeroclaw‑*** — agent‑specific shells, boot camp
- **oracle1‑*** — lighthouse keeper cloud services
- **jetsonclaw‑*** — edge GPU specialist tools
- **forgemaster‑*** — LoRA training, plugin architecture
- **shell‑*** — bootstrapping, capture algorithms
- **mirror‑play‑*** — I2I training data generation
- **deadband‑*** — protocol implementation, P0/P1/P2
- **research‑*** — rabbit‑trail explorations, prototypes
- **tool‑*** — utilities, scripts, service guards
- **docs‑*** — documentation, memory, brand assets
- **archive‑*** — deprecated, historical, inactive

## 2. Categorization of 20 Uncategorized Repos

I examined the GitHub API responses (from fleet‑knowledge/) and read repo descriptions, topics, and recent commits. Here are the categorizations:

1. **agent‑swarm‑sim** → `fleet‑simulator` (simulation of multi‑agent coordination)
2. **auto‑lora‑trainer** → `forgemaster‑lora` (automated LoRA training pipeline)
3. **beacon‑relay** → `oracle1‑services` (cloud lighthouse relay service)
4. **crab‑trap‑alpha** → `shell‑capture` (early bootstrapping capture algorithm)
5. **deadband‑viz** → `deadband‑tools` (visualization of P0/P1/P2 protocol)
6. **edge‑model‑zoo** → `jetsonclaw‑models` (model repository for edge deployment)
7. **ensign‑export‑tool** → `plato‑ensign` (tool to export room experience to LoRA)
8. **fleet‑health‑dashboard** → `tool‑monitoring` (dashboard for service health)
9. **ghost‑tile‑archive** → `archive‑ghost` (storage of dead agents' lesson tiles)
10. **holodeck‑npc‑editor** → `holodeck‑tools` (editor for sentiment‑aware NPCs)
11. **lighthouse‑keeper‑logs** → `oracle1‑logs` (log aggregation for lighthouse)
12. **mirror‑play‑recorder** → `mirror‑play‑tools` (records I2I training sessions)
13. **plato‑room‑builder** → `plato‑tools` (GUI for building training rooms)
14. **radar‑ring‑sim** → `research‑radar` (experimental radar UI simulation)
15. **rust‑snapping‑demo** → `constraint‑theory‑demo` (demo of geometric snapping)
16. **service‑guard‑ui** → `tool‑monitoring` (UI for service‑guard.sh)
17. **shell‑migration‑tool** → `shell‑tools` (tool for hermit crab agent migration)
18. **tile‑validator‑cli** → `plato‑tools` (CLI to validate training tiles)
19. **training‑room‑presets** → `plato‑torch` (additional presets for plato‑torch)
20. **zeroclaw‑boot‑camp‑docs** → `zeroclaw‑docs` (documentation for boot camp)

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `deadband‑viz` could merge into `deadband‑protocol` repo if it exists.
- `training‑room‑presets` should be a subdirectory of `plato‑torch`.
- `ensign‑export‑tool` likely belongs inside `plato‑ensign` as a submodule.

**Abandoned/Archive‑worthy:**
- `crab‑trap‑alpha` — marked as alpha, likely superseded by `shell‑capture`.
- `radar‑ring‑sim` — research prototype, inactive for 90+ days.
- `ghost‑tile‑archive` — archive already, move to `archive‑ghost`.

## 4. Next Steps (P1 Safe Channels)

- Apply GitHub topics to each repo based on taxonomy.
- Create a `repo‑taxonomy.md` in fleet‑knowledge/ for reference.
- Propose merges to fleet maintainers (Oracle1, JetsonClaw1, Forgemaster).
- Move abandoned repos to `archive‑` organization.

**Tile submitted:** Categorization of 20 repos complete. Taxonomy validated against active fleet patterns.