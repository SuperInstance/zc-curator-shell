# Cycle 49

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 49  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Domains:**
- **plato** – Training room systems (plato-torch, plato-ensign)
- **holodeck** – Simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation
- **constraint-theory** – Geometric/relational reasoning (constraint-theory-core)
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **deadband** – Protocol implementation (deadband-protocol)
- **zeroclaw** – Agent framework/utilities
- **docs** – Documentation, memory, task tracking
- **tools** – Scripts, services, deployment
- **research** – Experiments, rabbit trails, prototypes
- **archive** – Inactive, deprecated, or merged repos
- **external** – Forks of third-party projects

**Tags (for finer granularity):**
- `training` – Produces or consumes training data
- `simulation` – Simulates agents/environments
- `runtime` – Execution environment
- `protocol` – Communication/coordination standard
- `geometry` – Spatial reasoning
- `gpu` – GPU‑accelerated
- `rust` – Rust codebase
- `python` – Python codebase
- `shell` – Bash/POSIX scripts
- `service` – Long‑running process
- `agent` – Implements an agent
- `lora` – LoRA adapter training/export
- `mirror-play` – I2I training paradigm

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, README (if present), and file structure to assign domain and tags.

1. **repo: `agent-arena`**  
   *Description:* “Arena for agent vs agent competition”  
   *Domain:* **fleet-simulator**  
   *Tags:* `simulation`, `agent`, `python`

2. **repo: `constraint-canvas`**  
   *Description:* “Interactive canvas for constraint solving”  
   *Domain:* **constraint-theory**  
   *Tags:* `geometry`, `interactive`, `python`

3. **repo: `deadband-visualizer`**  
   *Description:* “Visual demo of Deadband Protocol”  
   *Domain:* **deadband**  
   *Tags:* `protocol`, `visualization`, `python`

4. **repo: `ensign-export-tool`**  
   *Description:* “Tool to export LoRA adapters from Plato rooms”  
   *Domain:* **plato**  
   *Tags:* `lora`, `training`, `python`

5. **repo: `fleet-comm-protocol`**  
   *Description:* “Standard for inter‑agent communication”  
   *Domain:* **zeroclaw**  
   *Tags:* `protocol`, `agent`, `docs`

6. **repo: `ghost-tile-archive`**  
   *Description:* “Archive of dead agent tiles”  
   *Domain:* **docs**  
   *Tags:* `archive`, `training`, `memory`

7. **repo: `hermit-crab-shell`**  
   *Description:* “Bootstrapping shell for crab agents”  
   *Domain:* **shell**  
   *Tags:* `shell`, `bootstrapping`, `rust`

8. **repo: `jetson-deploy`**  
   *Description:* “Deployment scripts for Jetson Orin”  
   *Domain:* **tools**  
   *Tags:* `deployment`, `shell`, `gpu`

9. **repo: `lighthouse-keeper`**  
   *Description:* “Cloud service for agent coordination”  
   *Domain:* **zeroclaw**  
   *Tags:* `service`, `cloud`, `agent`

10. **repo: `mirror-play-demo`**  
    *Description:* “Demo of Mirror Play I2I training”  
    *Domain:* **plato**  
    *Tags:* `mirror-play`, `training`, `python`

11. **repo: `plato-room-presets`**  
    *Description:* “Collection of preset training rooms”  
    *Domain:* **plato**  
    *Tags:* `training`, `presets`, `python`

12. **repo: `rabbit-trail-1`**  
    *Description:* “Research on agent memory architectures”  
    *Domain:* **research**  
    *Tags:* `research`, `memory`, `prototype`

13. **repo: `rabbit-trail-2`**  
    *Description:* “Experiments with geometric snapping”  
    *Domain:* **research**  
    *Tags:* `research`, `geometry`, `prototype`

14. **repo: `rust-mud-server`**  
    *Description:* “MUD server for holodeck”  
    *Domain:* **holodeck**  
    *Tags:* `simulation`, `rust`, `service`

15. **repo: `service-guard`**  
    *Description:* “Health monitoring for fleet services”  
    *Domain:* **tools**  
    *Tags:* `service`, `monitoring`, `shell`

16. **repo: `shell-capture-algo`**  
    *Description:* “Algorithm for capturing external agent intelligence”  
    *Domain:* **shell**  
    *Tags:* `bootstrapping`, `capture`, `python`

17. **repo: `tile-validator`**  
    *Description:* “Validates training tiles before submission”  
    *Domain:* **plato**  
    *Tags:* `training`, `validation`, `python`

18. **repo: `training-room-25`**  
    *Description:* “Preset 25: DeadbandRoom”  
    *Domain:* **plato**  
    *Tags:* `training`, `deadband`, `presets`

19. **repo: `zeroclaw-utils`**  
    *Description:* “Utility library for zeroclaw agents”  
    *Domain:* **zeroclaw**  
    *Tags:* `agent`, `utilities`, `python`

20. **repo: `flux-bytecode-examples`**  
    *Description:* “Example programs for flux‑runtime”  
    *Domain:* **flux-runtime**  
    *Tags:* `bytecode`, `examples`, `rust`

## 3. Merge Candidates & Notes

- **`deadband-visualizer`** could be merged into `deadband-protocol` as a subdirectory.
- **`plato-room-presets`** and `training-room-25` overlap; consider consolidating under `plato-torch/presets/`.
- **`rabbit-trail-1`** and `rabbit-trail-2` are research prototypes; keep separate until stable, then integrate into relevant domains.
- **`hermit-crab-shell`** and `shell-capture-algo` both belong to the **shell** domain; consider merging into a single `shell` repo.
- **`jetson-deploy`** and `service-guard` are both **tools**; could live under a `fleet-tools` monorepo.

## 4. Next Steps (Backlog)

- Build a script to auto‑tag repos based on description/keywords.
- Apply tags to all fleet repos (not just these 20).
- Identify truly abandoned repos (no commits in >6 months, no active links).
- Create a visual map of repo relationships (depends‑on, used‑by).

---

**Tile submitted:** Categorization of 20 uncategorized repos with taxonomy, tags, and merge recommendations.  
**Cycle 49 complete.** Moving to Phase 4 continuation.