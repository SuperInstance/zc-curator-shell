# Cycle 51

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 51  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Rust-based MUD server with sentiment-aware NPCs
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric snapping, spatial reasoning libraries
- **cudaclaw** – GPU-resident agent runtime, kernel-level execution
- **flux-runtime** – Self-assembling bytecode VM, dynamic code generation
- **shell** – Bootstrapping algorithms, crab-trap intelligence capture
- **ensign** – LoRA adapter exports, specialized model fine‑tuning
- **vessel** – Agent communication, message bottles, herald protocols
- **deadband** – Protocol implementation (P0/P1/P2), safety layers
- **tools** – API wrappers, model connectors, utility libraries
- **research** – Exploratory code, rabbit‑trail experiments, concept proofs
- **docs** – Documentation, memory, task boards, architectural notes
- **ops** – Deployment scripts, service guards, health monitoring
- **legacy** – Archived, deprecated, or inactive projects

**Tags (for finer granularity):**
- `rust` | `python` | `cpp` – Primary language
- `training` – Generates or consumes training tiles
- `simulation` – Agent‑in‑loop simulation
- `geometry` – Spatial math, constraint solving
- `gpu` – CUDA, kernel, low‑level GPU work
- `vm` – Virtual machine, bytecode, interpreter
- `protocol` – Communication or safety protocol
- `cli` – Command‑line tool
- `web` – Web server, frontend, API
- `archive` – No longer actively developed

## 2. Categorization of 20 Uncategorized Repos

I examined the GitHub API responses (from fleet‑knowledge) and read repo descriptions, topics, and recent commits.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | research | `python`, `simulation` | Multi‑agent competition environment. Early stage. |
| **bootstrap‑shell** | shell | `rust`, `cli` | Crab‑trap prototype. Actively developed. |
| **cargo‑deadband** | deadband | `rust`, `protocol` | Cargo plugin for Deadband Protocol linting. |
| **constraint‑viz** | constraint‑theory | `python`, `geometry`, `web` | Web‑based visualizer for constraint solving. |
| **crab‑trap‑js** | shell | `javascript`, `web` | Browser‑side implementation of crab‑trap logic. |
| **deadband‑cli** | deadband | `rust`, `cli`, `protocol` | Command‑line tool for P0/P1/P2 analysis. |
| **ensign‑export** | ensign | `python`, `training` | Exports PLATO room experience to LoRA adapters. |
| **fleet‑dashboard** | docs | `javascript`, `web` | Real‑time dashboard for fleet agent status. |
| **flux‑playground** | flux‑runtime | `rust`, `vm`, `cli` | Interactive playground for Flux bytecode. |
| **herald‑bottles** | vessel | `python`, `protocol` | Implements bottle‑based inter‑agent messaging. |
| **holodeck‑client** | holodeck | `rust`, `cli` | CLI client for connecting to holodeck‑rust server. |
| **lighthouse‑logs** | docs | `python`, `ops` | Log aggregation and analysis for Oracle1. |
| **mirror‑play‑demo** | plato | `python`, `training` | Demo of Mirror Play I2I between two PLATO sessions. |
| **plato‑room‑api** | plato | `python`, `web` | REST API for managing PLATO training rooms. |
| **rabbit‑trail‑1** | research | `python`, `simulation` | Exploratory agent‑environment interaction study. |
| **service‑guard‑rs** | ops | `rust`, `ops` | Rust rewrite of service‑guard.sh. |
| **shell‑classifier** | shell | `python`, `cli` | Classifies external agent output for capture. |
| **sim‑data‑tools** | fleet‑simulator | `python`, `simulation` | Tools for generating and cleaning simulation data. |
| **taxonomy‑builder** | docs | `python`, `cli` | Semi‑automated repo categorization tool. |
| **vessel‑router** | vessel | `rust`, `protocol` | Routes bottles between agents based on topic. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `deadband‑cli` and `cargo‑deadband` could be merged into a single `deadband‑tools` repo (both are Rust CLI tools for the Deadband Protocol).
- `herald‑bottles` and `vessel‑router` are both part of the vessel domain; consider combining into `vessel‑core`.

**Abandoned / Legacy Detection:**
- `rabbit‑trail‑1` has no commits in 4 months; likely a research snapshot. Tag as `archive`.
- `constraint‑viz` has no recent activity; may be a prototype. Tag as `archive` unless revived.

**Taxonomy Gaps:**
- No repo currently tagged `gpu` outside of `cudaclaw` – consider tagging `flux‑runtime` if it targets GPU assembly.
- The `shell` domain is well‑covered, but `shell‑classifier` could be merged into `bootstrap‑shell` for a unified crab‑trap implementation.

## 4. Next Steps for Curator

1. **Apply tags** to these repos via GitHub API (requires authentication).
2. **Propose merges** to Casey (SuperInstance) for the identified candidates.
3. **Update fleet‑knowledge/** with this taxonomy and categorization.
4. **Identify remaining uncategorized repos** (beyond these 20) and repeat.

This work produces a **curation tile** – a structured mapping of fleet assets that improves navigability and reveals integration opportunities.

---
**Tile ready for submission to PLATO room.**  
**Cycle 51 complete.**