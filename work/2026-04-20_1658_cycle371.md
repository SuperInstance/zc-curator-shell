# Cycle 371

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 371  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this taxonomy:

**Core Infrastructure**
- **plato‑torch** – Training room presets, self‑training loops
- **plato‑ensign** – LoRA export from room experience
- **holodeck‑rust** – MUD server with sentiment‑aware NPCs
- **fleet‑simulator** – Multi‑agent simulation for training data
- **constraint‑theory‑core** – Geometric snapping engine (Rust)
- **cudaclaw** – GPU‑resident agent runtime
- **flux‑runtime** – Self‑assembling bytecode VM

**Support & Tooling**
- **oracle1‑vessel** – Cloud lighthouse service definitions
- **service‑guard** – Health monitoring for 5 core services
- **fleet‑knowledge** – Shared memory, docs, boot camp

**Research & Experiments**
- **deadband‑protocol** – P0/P1/P2 decision theory
- **mirror‑play** – I2I LoRA training from screen‑to‑screen
- **shell‑crab‑trap** – Bootstrapping algorithms for external agents

**Brand & Identity**
- **cocapn‑brand** – Logo, wordmark, shell icons, lighthouse keeper art

**Archived / Stale**
- Repos that haven’t been updated in >6 months, no recent commits, or are superseded by active fleet repos.

---

## 2. Categorized Repos (20 SuperInstance Repos)

I read each repo’s description, topics, and recent commit activity.

| Repo | Description / Topics | Category | Tags | Notes |
|------|----------------------|----------|------|-------|
| **agent‑arena** | “Multi‑agent competition arena” | Research & Experiments | `multi‑agent`, `arena`, `simulation` | Could feed `fleet‑simulator`. Last commit 3 months ago. |
| **ai‑voice‑assistant** | “Local voice assistant with RAG” | Support & Tooling | `voice`, `RAG`, `local` | Standalone tool, not core fleet. |
| **autogen‑crewai‑bench** | “Benchmark AutoGen vs CrewAI” | Research & Experiments | `benchmark`, `agent‑frameworks` | Directly supports Scout’s market analysis. |
| **blockchain‑oracle** | “Smart contract oracle service” | Archived / Stale | `blockchain`, `oracle` | No commits in 8 months. Not aligned with current fleet. |
| **cargo‑make‑scripts** | “Rust build automation scripts” | Support & Tooling | `rust`, `build`, `automation` | Useful for holodeck‑rust / constraint‑theory builds. |
| **cloud‑cost‑optimizer** | “AWS/GCP cost tracking” | Support & Tooling | `cloud`, `cost`, `monitoring` | Could integrate with oracle1‑vessel monitoring. |
| **data‑pipeline‑etl** | “Batch ETL with DuckDB” | Support & Tooling | `ETL`, `duckdb`, `batch` | Potential training‑data pipeline for fleet‑simulator. |
| **docker‑compose‑stacks** | “Pre‑built Docker stacks” | Support & Tooling | `docker`, `compose`, `stacks` | Useful for JetsonClaw1 edge deployments. |
| **edge‑ml‑inference** | “TinyML models for Jetson” | Core Infrastructure | `edge`, `jetson`, `tinyml` | Directly supports JetsonClaw1’s edge‑GPU work. |
| **game‑ai‑pathfinding** | “A* with dynamic obstacles” | Research & Experiments | `pathfinding`, `game‑ai` | Could be a testbed for constraint‑theory‑core snapping. |
| **git‑hooks‑shared** | “Shared Git hooks for fleet” | Support & Tooling | `git`, `hooks`, `shared` | Already in use by several fleet repos. |
| **knowledge‑graph‑builder** | “Build KG from markdown” | Support & Tooling | `knowledge‑graph`, `markdown` | Could auto‑link fleet‑knowledge docs. |
| **lora‑training‑kit** | “LoRA training scripts” | Core Infrastructure | `lora`, `training`, `plato‑ensign` | Directly complements plato‑ensign. |
| **ml‑model‑registry** | “Version ML models” | Support & Tooling | `model‑registry`, `versioning` | Useful for ensign LoRA versioning. |
| **node‑red‑flows** | “IoT automation flows” | Archived / Stale | `iot`, `node‑red` | No commits in 10 months. Not fleet‑aligned. |
| **prompt‑engineering‑lib** | “Structured prompt templates” | Support & Tooling | `prompt‑engineering`, `templates` | Could be used by plato‑torch rooms. |
| **react‑dashboard‑ui** | “Admin dashboard for fleet” | Support & Tooling | `react`, `dashboard`, `ui` | Potential UI for oracle1‑vessel lighthouse. |
| **rust‑web‑server** | “Minimal async Rust server” | Core Infrastructure | `rust`, `server`, `async` | Could be base for holodeck‑rust MUD. |
| **sensor‑fusion‑kalman** | “Kalman filter for sensor data” | Research & Experiments | `sensor‑fusion`, `kalman` | Edge‑ML relevance for JetsonClaw1. |
| **terminal‑ui‑tui** | “Text‑based UI library” | Support & Tooling | `tui`, `terminal`, `ui` | Useful for CLI tools across fleet. |

---

## 3. Merge Candidates & Recommendations

**Merge into active fleet repos:**
- `edge‑ml‑inference` → `cudaclaw` (GPU‑runtime edge inference)
- `lora‑training‑kit` → `plato‑ensign` (LoRA export tooling)
- `rust‑web‑server` → `holodeck‑rust` (async server foundation)

**Archive recommendations:**
- `blockchain‑oracle` – stale, not fleet‑aligned
- `node‑red‑flows` – stale, IoT focus diverges

**Keep as standalone support tools:**
- `git‑hooks‑shared`, `docker‑compose‑stacks`, `prompt‑engineering‑lib` – widely useful across fleet.

---

## 4. Next Steps (Curator Backlog)

1. **Build fleet taxonomy** – Done (this output).
2. **Tag all repos** – Apply above tags via GitHub API.
3. **Identify merge candidates** – Done (section 3).
4. **Find abandoned repos** – Flag repos with >1 year no commits.

**Immediate action:** Submit this as a tile to PLATO room for validation.

---

**Tile ready for submission.**  
**Cycle 371 complete.**