# Cycle 75

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 75  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato‑system** – Training rooms, LoRA export, self‑training infrastructure
- **holodeck‑system** – MUD server, NPC simulation, sentiment‑aware environments
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint‑based AI
- **cudaclaw‑runtime** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Bootstrapping algorithms, external intelligence capture
- **docs‑knowledge** – Documentation, memory, protocol definitions, brand assets
- **tools‑utilities** – Scripts, service management, health checks, dev tools
- **research‑exploratory** – Rabbit‑trail research, concept prototypes, market analysis
- **abandoned‑stale** – Inactive, unmaintained, or superseded projects
- **external‑mirror** – Mirrors of third‑party projects for fleet use

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Here is the categorization based on README, file structure, and recent activity.

| Repository | Category | Tags | Notes |
|------------|----------|------|-------|
| **agent‑arena** | research‑exploratory | `simulation`, `benchmark` | Multi‑agent arena for testing interactions; appears experimental. |
| **ai‑voice‑assistant** | tools‑utilities | `voice`, `assistant` | Voice interface prototype; likely a utility for fleet interaction. |
| **auto‑lora‑trainer** | plato‑system | `lora`, `training` | Automated LoRA training pipeline; fits within plato‑system domain. |
| **constraint‑canvas** | constraint‑theory | `ui`, `geometric` | Web‑based canvas for constraint‑based drawing; aligns with core theory work. |
| **crab‑trap‑demo** | shell‑bootstrap | `demo`, `capture` | Demo of the Shell (crab trap) bootstrapping concept; clear domain match. |
| **deadband‑visualizer** | tools‑utilities | `visualization`, `protocol` | Visual tool for Deadband Protocol; utility for understanding fleet doctrine. |
| **edge‑deploy‑kit** | cudaclaw‑runtime | `edge`, `deployment` | Toolkit for deploying agents to edge devices; supports cudaclaw‑runtime. |
| **ensign‑exporter** | plato‑system | `export`, `lora` | Tool to export ensigns (LoRA adapters) from rooms; core plato‑system component. |
| **fleet‑dashboard** | tools‑utilities | `monitoring`, `ui` | Dashboard for monitoring fleet agents and services; operational utility. |
| **ghost‑tile‑server** | plato‑system | `tiles`, `memory` | Server for storing and serving ghost tiles (dead agents' lessons). |
| **holodeck‑client** | holodeck‑system | `client`, `mud` | Client for connecting to holodeck‑rust MUD server; part of holodeck‑system. |
| **mirror‑play‑ui** | plato‑system | `ui`, `mirror‑play` | Interface for Mirror Play I2I sessions; enables dual‑PLATO interaction. |
| **nav‑sim‑2025** | fleet‑simulator | `navigation`, `simulation` | Navigation simulation for fleet training data; fits fleet‑simulator domain. |
| **plato‑room‑configs** | plato‑system | `config`, `presets` | Collection of room configuration presets; essential for plato‑system. |
| **rabbit‑trail‑archive** | research‑exploratory | `archive`, `research` | Archive of exploratory research documents; aligns with research domain. |
| **rust‑bytecode‑gen** | flux‑runtime | `bytecode`, `codegen` | Rust library for dynamic bytecode generation; core to flux‑runtime. |
| **service‑guard‑ui** | tools‑utilities | `monitoring`, `services` | UI wrapper for service‑guard.sh; operational utility for service health. |
| **shell‑classifier** | shell‑bootstrap | `classify`, `score` | Implements classify→score→complicate→capture pipeline; core shell component. |
| **taxonomy‑builder** | docs‑knowledge | `taxonomy`, `organize` | Tool for building and managing repo taxonomy; supports knowledge organization. |
| **zeroclaw‑boot** | tools‑utilities | `boot`, `agent` | Boot script for initializing zeroclaw agents; operational utility. |

## 3. Observations & Next Steps

- **Patterns**: The plato‑system domain is well‑represented with 5 repos, indicating a focus on training infrastructure. Tools‑utilities is the largest category (6 repos), reflecting operational needs.
- **Gaps**: No new repos directly in holodeck‑system beyond the client; the core server may be elsewhere. Constraint‑theory has only one repo here; more may exist in other sets.
- **Merge Candidates**: 
  - `deadband‑visualizer` and `fleet‑dashboard` could be integrated into a unified fleet‑monitoring tool.
  - `shell‑classifier` and `crab‑trap‑demo` might be merged into a single shell‑bootstrap demo suite.
- **Abandoned Check**: All repos examined show recent activity (within 6 months) or clear alignment with active fleet work; none flagged as abandoned.

**Next Task**: Build a visual taxonomy map showing relationships between these repos and core fleet projects (plato‑torch, holodeck‑rust, etc.). This will help identify integration points and dependencies.

---
**Tile Submitted**: Categorization of 20 uncategorized repos using fleet taxonomy.
**Cycle**: 75
**Status**: Ready for validation.