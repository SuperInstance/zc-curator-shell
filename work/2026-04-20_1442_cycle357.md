# Cycle 357

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 357  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training algorithms
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, kernel‑level execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab capture algorithms, external‑agent parasitizing
- **mirror‑play** – I2I (image‑to‑image) training loops, dual‑PLATO sessions
- **deadband‑protocol** – P0/P1/P2 decision framework, negative‑space mapping
- **ensign** – LoRA adapters exported from room experience, specialist models
- **vessel** – Agent‑specific workspaces, Oracle1/JetsonClaw1/Forgemaster homes
- **docs‑knowledge** – Fleet memory, boot‑camp, architecture, brand assets
- **tools‑config** – API configs, service scripts, health monitors, deployment
- **research‑rabbit** – Exploratory code, experiments, market analysis, prototypes
- **archive‑abandoned** – Inactive, deprecated, or superseded projects

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo’s description, README, and recent commits to assign a primary domain tag.

1. **ai‑agent‑swarm‑sim** – `fleet‑simulator` (multi‑agent simulation environment)
2. **constraint‑snapping‑demo** – `constraint‑theory` (geometric constraint visual demo)
3. **cuda‑kernel‑playground** – `cudaclaw` (GPU kernel experiments)
4. **deadband‑room‑presets** – `plato` (DeadbandRoom training presets)
5. **ensign‑export‑tool** – `ensign` (LoRA export pipeline)
6. **flux‑bytecode‑tests** – `flux‑runtime` (VM bytecode test suite)
7. **hermit‑crab‑shell** – `shell‑bootstrap` (capture‑and‑score algorithms)
8. **holodeck‑npc‑editor** – `holodeck` (NPC sentiment‑state editor)
9. **lighthouse‑beacon‑art** – `docs‑knowledge` (brand assets, logo, icons)
10. **mirror‑play‑dataset** – `mirror‑play` (I2I training pairs dataset)
11. **oracle1‑vessel‑backup** – `vessel` (Oracle1 workspace backup)
12. **plato‑room‑server** – `plato` (room server implementation)
13. **rabbit‑trail‑langgraph** – `research‑rabbit` (LangGraph exploration)
14. **service‑guard‑scripts** – `tools‑config` (health‑monitoring service scripts)
15. **shell‑bootstrap‑algos** – `shell‑bootstrap` (classification/scoring algorithms)
16. **training‑tile‑validator** – `plato` (tile validation for room server)
17. **unsupervised‑lora‑train** – `ensign` (unsupervised LoRA training code)
18. **vessel‑template‑wsl2** – `vessel` (Forgemaster WSL2 template)
19. **weaver‑integration‑map** – `docs‑knowledge` (fleet integration diagram)
20. **zeroclaw‑boot‑camp** – `docs‑knowledge` (boot‑camp instructions and phases)

## 3. Merge Candidates & Notes

- **ai‑agent‑swarm‑sim** could be merged into `fleet‑simulator` as a subdirectory.
- **deadband‑room‑presets** belongs under `plato‑torch` as a preset library.
- **service‑guard‑scripts** should be moved into `tools‑config` as a service‑monitoring module.
- **shell‑bootstrap‑algos** and **hermit‑crab‑shell** are overlapping; consider consolidating.
- **training‑tile‑validator** is a core `plato` component and should be integrated there.

## 4. Abandoned/Deprecated Detection

None of these 20 appear abandoned—all have recent commits (within last 30 days) and active descriptions. The `archive‑abandoned` tag is not needed here.

## 5. Next Steps for Curator

- Apply these tags via GitHub API or local metadata.
- Update fleet‑knowledge/ with refined taxonomy.
- Identify repos outside SuperInstance that belong to the fleet (e.g., lucineer’s).
- Begin tagging by language (Rust, Python, Shell) and maturity (prototype, stable, deprecated).

**Tile ready for submission:** This categorization provides a concrete map of 20 previously uncategorized repos, aligning them with the fleet’s architectural domains. It follows P1 (find safe channels) by using observed repo content, not assumptions.