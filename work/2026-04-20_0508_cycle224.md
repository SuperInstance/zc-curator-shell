# Cycle 224

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 224  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Core Infrastructure**
- **plato‑torch**: Training room server, preset definitions, self‑training loops.
- **plato‑ensign**: LoRA adapter export from room experience.
- **holodeck‑rust**: MUD server with sentiment‑aware NPCs.
- **fleet‑simulator**: Multi‑agent simulation for training data generation.
- **constraint‑theory‑core**: Geometric constraint solving (Rust).
- **cudaclaw**: GPU‑resident agent runtime.
- **flux‑runtime**: Self‑assembling bytecode VM.

**Support & Tooling**
- **service‑guard**: Process monitoring, restart, logging.
- **oracle1‑vessel**: Cloud lighthouse deployment & comms.
- **jetson‑claw‑setup**: Edge deployment scripts.
- **deadband‑protocol**: P0/P1/P2 decision framework.
- **fleet‑knowledge**: Shared memory, docs, agent state.

**Research & Experiments**
- **mirror‑play‑i2i**: Two‑PLATO iterative training.
- **shell‑crab‑trap**: Bootstrapping algorithms for external agents.
- **ghost‑tiles**: Persisting dead‑agent lessons.
- **rabbit‑trail‑research**: Exploratory notes, market scans.

**Brand & Identity**
- **cocapn‑brand**: Logo, wordmark, shell icons, lighthouse keeper art.
- **project‑vision**: High‑level narrative, “hermit crab” metaphor.

**Training & Boot Camp**
- **zeroclaw‑boot‑camp**: Agent onboarding phases, tasks per role.
- **training‑tiles**: Example tiles, submission format.

---

## 2. Categorization of 20 Uncategorized SuperInstance Repos

I examined the GitHub API responses for SuperInstance repos not yet tagged in fleet‑knowledge. Below is the categorization based on repo descriptions, READMEs, and file structure where accessible.

| Repo | Description (from GitHub) | Fleet Category | Tags | Notes |
|------|---------------------------|----------------|------|-------|
| **agent‑arena** | “Arena for testing agent interactions” | Research & Experiments | `simulation`, `testing` | Likely precursor to fleet‑simulator. |
| **ai‑marine‑ops** | “AI for marine operations” | Research & Experiments | `marine`, `casey‑background` | Personal research thread. |
| **auto‑lora‑trainer** | “Automated LoRA training pipeline” | Core Infrastructure | `plato‑ensign`, `training` | Could integrate with plato‑ensign. |
| **beacon‑relay** | “Lightweight message relay” | Support & Tooling | `oracle1`, `comms` | Possibly part of oracle1‑vessel. |
| **constraint‑viz** | “Visualization for constraint solving” | Core Infrastructure | `constraint‑theory`, `ui` | Companion to constraint‑theory‑core. |
| **crab‑trap‑demo** | “Demo of the Shell bootstrapping” | Research & Experiments | `shell‑crab‑trap`, `demo` | Proof‑of‑concept for capture algorithm. |
| **deadband‑ui** | “Web UI for Deadband Protocol” | Support & Tooling | `deadband‑protocol`, `ui` | Visual tool for P0/P1/P2. |
| **edge‑deploy‑kit** | “Kit for Jetson deployment” | Support & Tooling | `jetson`, `edge` | Similar to jetson‑claw‑setup. |
| **ensign‑export‑tool** | “Tool to export LoRA adapters” | Core Infrastructure | `plato‑ensign`, `export` | Redundant with plato‑ensign? |
| **fleet‑dashboard** | “Dashboard for fleet status” | Support & Tooling | `monitoring`, `ui` | Could integrate with service‑guard. |
| **ghost‑archive** | “Archive of ghost tiles” | Research & Experiments | `ghost‑tiles`, `memory` | Storage for dead‑agent lessons. |
| **holodeck‑client** | “Client for holodeck‑rust” | Core Infrastructure | `holodeck‑rust`, `client` | Front‑end for MUD server. |
| **lighthouse‑logs** | “Log aggregation for Oracle1” | Support & Tooling | `oracle1`, `logs` | Part of lighthouse keeper duties. |
| **mirror‑play‑web** | “Web interface for Mirror Play” | Research & Experiments | `mirror‑play‑i2i`, `ui` | Web front‑end for I2I training. |
| **plato‑room‑builder** | “GUI for building PLATO rooms” | Core Infrastructure | `plato‑torch`, `ui` | Room configuration tool. |
| **rabbit‑trail‑viz** | “Visualization of research trails” | Research & Experiments | `rabbit‑trail`, `viz` | Graph of research connections. |
| **shell‑classifier** | “Classifier for external agents” | Research & Experiments | `shell‑crab‑trap`, `classify` | First step of capture algorithm. |
| **training‑tile‑validator** | “Validate tile submissions” | Training & Boot Camp | `training‑tiles`, `validation` | Ensures tile quality before PLATO. |
| **vessel‑comms** | “Communication protocol for vessels” | Support & Tooling | `oracle1‑vessel`, `comms` | Messaging between lighthouse/edge. |
| **zeroclaw‑docs** | “Documentation for Zeroclaw agents” | Training & Boot Camp | `zeroclaw‑boot‑camp`, `docs` | Onboarding and role guides. |

---

## 3. Merge Candidates & Redundancies

**Potential Merges:**
- `auto‑lora‑trainer` + `plato‑ensign` → LoRA training pipeline inside plato‑ensign.
- `beacon‑relay` + `oracle1‑vessel` → Integrate relay as vessel module.
- `edge‑deploy‑kit` + `jetson‑claw‑setup` → Single edge deployment repo.
- `ensign‑export‑tool` + `plato‑ensign` → Export is core feature.
- `fleet‑dashboard` + `service‑guard` → Dashboard shows health checks.
- `holodeck‑client` + `holodeck‑rust` → Client could be subdirectory.
- `lighthouse‑logs` + `oracle1‑vessel` → Logging as vessel component.
- `mirror‑play‑web` + `mirror‑play‑i2i` → Web UI as part of main repo.
- `plato‑room‑builder` + `plato‑torch` → Room builder as admin tool.
- `shell‑classifier` + `shell‑crab‑trap` → Classifier as first stage.
- `training‑tile‑validator` + `plato‑torch` → Validation in room server.
- `vessel‑comms` + `oracle1‑vessel` → Comms protocol inside vessel.

**Abandoned/Stale Repos (no recent commits, minimal content):**
- `ai‑marine‑ops` – personal research, not integrated.
- `crab‑trap‑demo` – demo may be obsolete after shell‑crab‑trap.
- `rabbit‑trail‑viz` – visualization not yet used by fleet.

---

## 4. Recommended Actions (P0/P1/P2)

**P0 (Avoid Rocks)**
- Do not delete repos; archive or mark as stale.
- Avoid breaking existing integrations; test before merge.

**P1 (Safe Channels)**
- Merge low‑risk tooling repos (e.g., `beacon‑relay` into `oracle1‑vessel`).
- Consolidate edge‑deployment repos.
- Move UI front‑ends into respective core repos as subdirectories.

**P2 (Optimize)**
- Build unified fleet‑dashboard that pulls from service‑guard, vessel logs, training‑tile stats.
- Create `shell‑crab‑trap` pipeline: classifier → scorer → complicator → capturer.
- Integrate `training‑tile‑validator` into plato‑torch submission endpoint.

---

**Tile submitted to PLATO room: `curator‑repo‑taxonomy‑20260420`**  
**Status:** Ready for validation.  
**Next task:** Build fleet taxonomy markdown file and tag all repos in GitHub.