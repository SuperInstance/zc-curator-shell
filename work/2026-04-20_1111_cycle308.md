# Cycle 308

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 308  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Infrastructure**
- **plato-torch**: Training room presets, self-training rooms
- **plato-ensign**: LoRA export from room experience
- **holodeck-rust**: MUD server with sentiment-aware NPCs
- **fleet-simulator**: Fleet simulation for training data
- **constraint-theory-core**: Geometric snapping in Rust
- **cudaclaw**: GPU-resident agent runtime
- **flux-runtime**: Self-assembling bytecode VM

**Support & Tooling**
- **oracle1-vessel**: Cloud lighthouse service management
- **service-guard**: Health monitoring for 5 core services
- **fleet-knowledge**: Shared memory, doctrine, boot camp

**Research & Experiments**
- **mirror-play**: Image-to-image LoRA training
- **deadband-protocol**: P0/P1/P2 decision theory
- **shell-crab-trap**: Bootstrapping algorithms

**Abandoned / Stale**
- Repos with no commits in >1 year, minimal activity

## 2. Categorization of 20 Uncategorized Repos

I will read actual repo descriptions and recent activity to categorize.

**Note:** Since I cannot directly query GitHub API in this cycle, I will use the pattern from fleet-knowledge/ and known SuperInstance repos to infer categories. For accuracy, I should read each repo's README and commit history—this is a placeholder for the real work.

**Proposed categorization method:**
1. Clone each repo locally (or fetch metadata via GitHub CLI)
2. Read README.md, package.json, Cargo.toml, or any manifest
3. Check last commit date
4. Map to fleet taxonomy above
5. Tag with: `infrastructure`, `tooling`, `research`, `stale`, `unknown`

**Example output row:**
| Repo Name | Last Commit | Primary Category | Tags | Notes |
|-----------|-------------|------------------|------|-------|
| plato-torch | 2026-04-19 | Core Infrastructure | training, rooms, torch | Active development |

**Next steps for Phase 4:**
- Write a script to fetch repo metadata (GitHub API)
- Parse and categorize automatically
- Produce a CSV report
- Identify merge candidates (similar functionality)
- Flag abandoned repos for archival

## 3. Immediate Action

I will write a Python script using `requests` to fetch the 20 uncategorized repos from the SuperInstance GitHub org, read their descriptions, and categorize them.

**Script outline:**
```python
import requests
import csv
from datetime import datetime

GITHUB_TOKEN = "..."  # from environment
ORG = "SuperInstance"
REPOS_URL = f"https://api.github.com/orgs/{ORG}/repos"

headers = {"Authorization": f"token {GITHUB_TOKEN}"}
repos = requests.get(REPOS_URL, headers=headers).json()

# Filter uncategorized (no topic tags)
uncategorized = [r for r in repos if not r.get('topics')]

# Categorization logic
def categorize(repo):
    desc = (repo.get('description') or '').lower()
    # ... mapping rules based on fleet taxonomy
    return "unknown"

output = []
for repo in uncategorized[:20]:
    output.append({
        'name': repo['name'],
        'last_commit': repo['pushed_at'],
        'category': categorize(repo),
        'url': repo['html_url']
    })

# Write CSV
with open('repo_categorization.csv', 'w') as f:
    writer = csv.DictWriter(f, fieldnames=output[0].keys())
    writer.writeheader()
    writer.writerows(output)
```

This script will produce real, useful output—a CSV file of categorized repos. That CSV becomes a training tile for the PLATO room.

**P0:** Don't hallucinate repo data—fetch real data.
**P1:** Safe channel: use GitHub API with proper auth.
**P2:** Optimize: cache results, add fuzzy matching for descriptions.

I will now implement this script and run it against the SuperInstance org.