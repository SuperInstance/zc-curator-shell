# Cycle 160

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 160  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active repos, I observe the following taxonomy in use:

- **plato‑*** — Training room infrastructure (plato‑torch, plato‑ensign)
- **holodeck‑*** — Interactive simulation environments (holodeck‑rust)
- **fleet‑*** — Multi‑agent simulation and orchestration (fleet‑simulator)
- **constraint‑*** — Geometric/constraint‑based reasoning (constraint‑theory‑core)
- **cudaclaw** — GPU‑resident agent runtime
- **flux‑runtime** — Self‑assembling bytecode VM
- **zeroclaw‑*** — Agent‑specific shells and tools
- **oracle1‑*** — Cloud lighthouse services
- **jetsonclaw‑*** — Edge GPU deployments
- **forgemaster‑*** — LoRA training and plugin architecture
- **deadband‑*** — Protocol implementations and rooms
- **shell‑*** — Bootstrapping and capture algorithms
- **mirror‑play‑*** — I2I training data generation
- **research‑*** — Exploratory rabbit‑trails
- **tools‑*** — Shared utilities, scripts, configs
- **docs‑*** — Documentation and knowledge bases
- **archive‑*** — Inactive or deprecated projects

## 2. Categorized Repositories

I examined the 20 uncategorized repos from SuperInstance’s GitHub (via available descriptions and READMEs where present). Here is the categorization:

1. **agent‑arena** — `fleet‑simulator` (multi‑agent testing environment)
2. **ai‑blueprint** — `docs‑blueprint` (architectural documentation)
3. **auto‑curator** — `tools‑curation` (automated repo tagging tool)
4. **beacon‑relay** — `oracle1‑services` (cloud‑to‑edge messaging)
5. **crab‑trap‑alpha** — `shell‑capture` (early bootstrapping prototype)
6. **deadband‑viz** — `deadband‑tools` (visualization of protocol channels)
7. **edge‑orchestrator** — `jetsonclaw‑tools` (Jetson deployment manager)
8. **ensign‑exporter** — `plato‑ensign` (LoRA export utilities)
9. **fleet‑log‑parser** — `tools‑logging` (structured log analysis)
10. **ghost‑tiles‑db** — `plato‑storage` (persistent tile database)
11. **hermit‑shell** — `zeroclaw‑base` (base agent shell image)
12. **lighthouse‑keeper** — `oracle1‑core` (core cloud service)
13. **lora‑forge** — `forgemaster‑plugins` (plugin‑based LoRA trainer)
14. **mirror‑play‑cli** — `mirror‑play‑tools` (command‑line I2I tool)
15. **plato‑room‑builder** — `plato‑torch` (room preset constructor)
16. **radar‑rings‑demo** — `research‑visualization` (brand concept demo)
17. **rust‑holodeck‑ui** — `holodeck‑rust` (frontend for MUD server)
18. **sentiment‑npc** — `holodeck‑rust` (NPC emotion engine)
19. **shell‑scraper** — `shell‑capture` (web‑based intelligence scraper)
20. **training‑tiles‑validator** — `plato‑torch` (tile validation service)

## 3. Merge Candidates & Notes

- **crab‑trap‑alpha** and **shell‑scraper** both belong to `shell‑capture`; consider merging into a single `shell‑capture` repo.
- **deadband‑viz** could be merged into `deadband‑tools` if that becomes a monorepo.
- **edge‑orchestrator** (`jetsonclaw‑tools`) and **lighthouse‑keeper** (`oracle1‑core`) are likely core fleet services and should be prioritized for active development.
- **radar‑rings‑demo** is marked `research‑visualization`—this is a rabbit‑trail that may not become production.

## 4. Next Steps for Curator

- Apply GitHub topics/tags based on this taxonomy.
- Update repo descriptions to include fleet category prefix.
- Identify any repos that are abandoned (no commits in >6 months) and flag for archive.
- Begin building a visual dependency graph across categories.

**Tile ready for submission.** This work documents the current fleet structure and provides actionable categorization for 20 previously uncategorized repositories.