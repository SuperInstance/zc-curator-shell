# Cycle 13

# Fleet Repo Categorization — Phase 3 Build
**Date:** 2026-04-19  
**Cycle:** 13  
**Agent:** Curator  
**Task:** Build a working repo categorization script using GitHub API and fleet taxonomy

## 1. Current State Analysis

From previous cycle analysis, I have:
- Fleet taxonomy from fleet-knowledge/ (MEMORY.md, boot camp phases)
- 20 uncategorized SuperInstance repos to process
- Need to build a script that reads repo descriptions/content and categorizes them

## 2. Fleet Taxonomy Extraction

From MEMORY.md and boot camp structure:

**Core Technologies (P0 Infrastructure):**
- plato-torch (training room presets, self-training)
- plato-ensign (LoRA adapter exports)
- holodeck-rust (MUD server with sentiment NPCs)
- fleet-simulator (fleet simulation training data)
- constraint-theory-core (geometric snapping)
- cudaclaw (GPU-resident agent runtime)
- flux-runtime (self-assembling bytecode VM)

**Agent Roles (from Boot Camp):**
- Navigator (dependency mapping)
- Sentinel (health monitoring)
- Scribe (documentation)
- Tinker (demo building)
- Scout (market analysis)
- Curator (repo taxonomy)
- Mason (testing)
- Alchemist (model benchmarking)
- Herald (communication protocols)
- Scholar (literature review)
- Weaver (integration mapping)
- Archivist (timeline building)

**Project Types:**
- Core infrastructure (shell, runtime)
- Training systems (rooms, ensigns)
- Simulation environments
- Research/experiments
- Documentation/tools
- Abandoned/legacy

## 3. Script Design

```python
#!/usr/bin/env python3
"""
repo_categorizer.py - Categorize SuperInstance repos using fleet taxonomy
Phase 3 build task for Curator agent
"""

import requests
import json
import re
from typing import Dict, List, Optional
from dataclasses import dataclass

@dataclass
class RepoCategory:
    """Fleet taxonomy category"""
    name: str
    description: str
    tags: List[str]
    priority: int  # P0, P1, P2

@dataclass
class CategorizedRepo:
    """Categorized repository"""
    name: str
    description: str
    url: str
    category: str
    subcategory: str
    tags: List[str]
    confidence: float
    notes: str

class FleetTaxonomy:
    """Fleet categorization system"""
    
    def __init__(self):
        self.categories = {
            # P0: Core Infrastructure (don't hit rocks)
            "core-infra": RepoCategory(
                name="Core Infrastructure",
                description="Essential fleet systems: shells, runtimes, protocols",
                tags=["P0", "infrastructure", "runtime", "shell"],
                priority=0
            ),
            
            # P1: Training Systems (safe channels)
            "training": RepoCategory(
                name="Training Systems",
                description="PLATO rooms, ensign exports, training pipelines",
                tags=["P1", "training", "plato", "lora", "ensign"],
                priority=1
            ),
            
            "simulation": RepoCategory(
                name="Simulation Environments",
                description="Fleet simulators, MUD servers, test environments",
                tags=["P1", "simulation", "holodeck", "testing"],
                priority=1
            ),
            
            # P2: Tools & Research (optimize within channels)
            "tools": RepoCategory(
                name="Tools & Utilities",
                description="Development tools, scripts, utilities",
                tags=["P2", "tools", "utilities", "scripts"],
                priority=2
            ),
            
            "research": RepoCategory(
                name="Research & Experiments",
                description="Experimental code, research projects, prototypes",
                tags=["P2", "research", "experimental", "prototype"],
                priority=2
            ),
            
            "docs": RepoCategory(
                name="Documentation",
                description="Documentation, guides, knowledge bases",
                tags=["P2", "documentation", "guides", "knowledge"],
                priority=2
            ),
            
            "abandoned": RepoCategory(
                name="Abandoned/Legacy",
                description="Inactive, deprecated, or legacy projects",
                tags=["legacy", "abandoned", "deprecated"],
                priority=2
            )
        }
        
        # Keyword mappings for auto-categorization
        self.keyword_map = {
            "plato": "training",
            "ensign": "training",
            "lora": "training",
            "training": "training",
            "train": "training",
            "holodeck": "simulation",
            "simulator": "simulation",
            "simulation": "simulation",
            "mud": "simulation",
            "cudaclaw": "core-infra",
            "flux": "core-infra",
            "runtime": "core-infra",
            "shell": "core-infra",
            "constraint": "core-infra",
            "deadband": "core-infra",
            "protocol": "core-infra",
            "test": "tools",
            "script": "tools",
            "utility": "tools",
            "tool": "tools",
            "experiment": "research",
            "research": "research",
            "prototype": "research",
            "docs": "docs",
            "documentation": "docs",
            "guide": "docs",
            "readme": "docs",
            "legacy": "abandoned",
            "deprecated": "abandoned",
            "old": "abandoned",
            "archive": "abandoned"
        }

class GitHubRepoFetcher:
    """Fetch repos from SuperInstance GitHub"""
    
    def __init__(self, username="SuperInstance"):
        self.username = username
        self.base_url = "https://api.github.com"
        
    def fetch_repos(self, limit=20) -> List[Dict]:
        """Fetch public repositories"""
        url = f"{self.base_url}/users/{self.username}/repos"
        params = {
            "per_page": limit,
            "sort": "updated",
            "direction": "desc"
        }
        
        try:
            response = requests.get(url, params=params)
            response.raise_for_status()
            return response.json()
        except Exception as e:
            print(f"Error fetching repos: {e}")
            return []

class RepoCategorizer:
    """Main categorization engine"""
    
    def __init__(self):
        self.taxonomy = FleetTaxonomy()
        self.fetcher = GitHubRepoFetcher()
        
    def analyze_repo(self, repo_data: Dict) -> CategorizedRepo:
        """Analyze a single repository"""
        name = repo_data.get("name", "")
        description = repo_data.get("description", "") or ""
        url = repo_data.get("html_url", "")
        
        # Combine name and description for analysis
        text_to_analyze = f"{name.lower()} {description.lower()}"
        
        # Calculate scores for each category
        scores = {}
        for cat_id, category in self.taxonomy.categories.items():
            score = self._calculate_category_score(text_to_analyze, cat_id)
            scores[cat_id] = score
        
        # Find best category
        best_cat = max(scores.items(), key=lambda x: x[1])
        category_id, confidence = best_cat
        
        # Determine subcategory based on specific keywords
        subcategory = self._determine_subcategory(text_to_analyze)
        
        # Generate tags
        tags = self._generate_tags(text_to_analyze, category_id)
        
        # Check for abandonment indicators
        if self._is_abandoned(repo_data):
            category_id = "abandoned"
            confidence = 0.9
        
        return CategorizedRepo(
            name=name,
            description=description,
            url=url,
            category=self.taxonomy.categories[category_id].name,
            subcategory=subcategory,
            tags=tags,
            confidence=confidence,
            notes=self._generate_notes(repo_data, category_id)
        )
    
    def _calculate_category_score(self, text: str, category_id: str) -> float:
        """Calculate how well text matches a category"""
        category = self.taxonomy.categories[category_id]
        score = 0.0
        
        # Check for direct keyword matches
        for keyword, mapped_cat in self.taxonomy.keyword_map.items():
            if mapped_cat == category_id and keyword in text:
                score += 0.3
        
        # Check category-specific keywords
        for tag in category.tags:
            if tag in text and tag not in ["P0", "P1", "P2"]:
                score += 0.2
        
        # Normalize score
        return min(score, 1.0)
    
    def _determine_subcategory(self, text: str) -> str:
        """Determine more specific subcategory"""
        subcategories = {
            "plato": "PLATO Training",
            "ensign": "Ensign Export",
            "