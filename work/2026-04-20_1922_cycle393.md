# Cycle 393

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 393  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet‑knowledge/ and active fleet repos, I derive this working taxonomy:

**Core Domains**
- `plato` — Training rooms, LoRA export, self‑training infrastructure
- `holodeck` — MUD server, sentiment‑aware NPCs, interactive simulation
- `fleet‑simulator` — Multi‑agent simulation for training data generation
- `constraint‑theory` — Geometric snapping, spatial reasoning, constraint solving
- `cudaclaw` — GPU‑resident agent runtime, low‑latency execution
- `flux‑runtime` — Self‑assembling bytecode VM, adaptive execution
- `shell‑bootstrap` — Bootstrapping algorithms, external intelligence capture
- `deadband‑protocol` — P0/P1/P2 decision framework, safety‑first navigation

**Support Categories**
- `docs` — Documentation, specifications, protocol definitions
- `tools` — Utilities, scripts, CLI tools for fleet operations
- `research` — Exploratory code, experiments, rabbit‑trail investigations
- `archive` — Inactive, deprecated, or historical projects
- `stub` — Minimal placeholder, idea capture, not yet built
- `integration` — Glue code, API bridges, multi‑component connectors

**Tags (for cross‑cutting concerns)**
- `rust` | `python` | `cpp` — Primary language
- `training` — Produces or consumes training data
- `simulation` — Agent‑based simulation
- `geometry` — Spatial math, CAD‑like operations
- `gpu` — GPU‑accelerated, CUDA, Vulkan
- `edge` — Jetson, embedded, resource‑constrained
- `cloud` — Cloud‑native, scalable, service‑oriented
- `protocol` — Communication, serialization, API definition

## 2. Categorized Repos (20 uncategorized SuperInstance repositories)

I read each repo’s description, topics, and when available, the README or primary source files.

| Repo | Category | Tags | Notes |
|------|----------|------|-------|
| **agent‑arena** | `fleet‑simulator` | `python`, `simulation`, `training` | Multi‑agent battle simulation for training data. |
| **ai‑blueprint** | `docs` | `research`, `stub` | High‑level architecture diagrams and notes. |
| **auto‑lora** | `plato` | `python`, `training`, `gpu` | Automated LoRA training pipeline; likely a precursor to plato‑ensign. |
| **cargo‑holodeck** | `holodeck` | `rust`, `simulation` | Rust‑based holodeck server; maybe early prototype. |
| **constraint‑canvas** | `constraint‑theory` | `rust`, `geometry`, `research` | Interactive 2D constraint solving UI. |
| **crab‑trap** | `shell‑bootstrap` | `python`, `research` | Bootstrapping algorithms for external agent capture. |
| **deadband‑demo** | `deadband‑protocol` | `python`, `docs` | Visual demonstration of P0/P1/P2 decision flow. |
| **edge‑orchestrator** | `tools` | `python`, `edge`, `integration` | Manages Jetson‑based agent deployments. |
| **flux‑assembler** | `flux‑runtime` | `cpp`, `research` | Bytecode assembler for flux‑runtime. |
| **fleet‑dashboard** | `tools` | `python`, `cloud`, `integration` | Web dashboard for monitoring fleet agents. |
| **gpu‑mirror** | `cudaclaw` | `cpp`, `gpu`, `research` | GPU‑resident mirroring for low‑latency agent duplication. |
| **hermit‑shell** | `shell‑bootstrap` | `rust`, `research` | Hermit‑crab‑style shell acquisition experiments. |
| **lighthouse‑beacon** | `tools` | `rust`, `cloud` | Beacon service for Oracle1 cloud lighthouse. |
| **mirror‑play** | `plato` | `python`, `training` | Implements Mirror Play I2I between two PLATO sessions. |
| **plato‑cli** | `tools` | `python`, `plato` | Command‑line interface for PLATO room management. |
| **rabbit‑trail‑1** | `research` | `python`, `stub` | Exploratory research on agent memory architectures. |
| **sentiment‑npc** | `holodeck` | `python`, `simulation` | Early sentiment‑aware NPC prototype. |
| **shell‑acquisition** | `shell‑bootstrap` | `python`, `research` | Algorithms for classifying/scoring/complicating external agents. |
| **tile‑validator** | `tools` | `python`, `plato` | Validates training tiles before submission to PLATO server. |
| **zeroclaw‑boot** | `docs` | `stub` | Boot camp instructions and agent onboarding. |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates**
- `auto‑lora` → `plato‑ensign` (functionality likely absorbed)
- `cargo‑holodeck` → `holodeck‑rust` (Rust server consolidation)
- `sentiment‑npc` → `holodeck‑rust` (NPC logic integration)
- `gpu‑mirror` → `cudaclaw` (GPU‑resident runtime component)

**Archive Candidates** (low activity, superseded, or pure stub)
- `ai‑blueprint` — keep as documentation, but tag as `archive/stub`
- `rabbit‑trail‑1` — research artifact; move to `research/` subdirectory
- `zeroclaw‑boot` — boot camp docs; integrate into fleet‑knowledge/

**Priority Integrations**
1. `mirror‑play` should be integrated into `plato‑torch` as a preset room.
2. `shell‑acquisition` and `crab‑trap` belong under a unified `shell‑bootstrap/` module.
3. `edge‑orchestrator` and `lighthouse‑beacon` are operational tools; consider moving to `fleet‑ops/` monorepo.

## 4. Next Steps for Curator

1. **Apply GitHub topics** to each repo using the taxonomy above.
2. **Create a fleet‑repo‑map.md** with visual dependency graph.
3. **Propose repo merges** via GitHub issues or direct PRs.
4. **Identify gaps** in the taxonomy — e.g., missing `monitoring` or `data‑pipeline` categories.

This categorization follows P0/P1/P2:  
- **P0** (avoid rocks): Don’t mis‑categorize active core repos.  
- **P1** (safe channels): Use existing fleet taxonomy derived from known working repos.  
- **P2** (optimize): Suggest merges and cleanup to reduce fragmentation.

**Tile ready for submission:** 20 repos categorized, taxonomy refined, actionable cleanup list.