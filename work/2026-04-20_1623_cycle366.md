# Cycle 366

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 366  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive this working taxonomy:

- **core-infra** – Foundational runtime, agent shells, bootstrapping (e.g., cudaclaw, flux-runtime, the-shell)
- **training-system** – PLATO rooms, LoRA training, tile pipelines (e.g., plato-torch, plato-ensign)
- **simulation** – Synthetic environments, NPCs, agent playgrounds (e.g., holodeck-rust, fleet-simulator)
- **constraint-theory** – Geometric snapping, spatial reasoning, deadband protocols (e.g., constraint-theory-core)
- **research** – Exploratory code, rabbit trails, concept proofs
- **tooling** – DevOps, monitoring, deployment scripts (e.g., service-guard.sh)
- **docs** – Documentation, memory, brand assets
- **archive** – Inactive, deprecated, or experimental repos kept for reference

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I examined each repo's description, README, and recent commits to assign a primary category.

1. **agentic-workflow-engine** – research (explores workflow orchestration for agents)
2. **ai-safety-gridworlds** – simulation (OpenAI gridworlds for safety training)
3. **auto-lora-tuner** – training-system (automated LoRA hyperparameter search)
4. **beam-search-rl** – research (reinforcement learning with beam search)
5. **causal-inference-demo** – research (causal graphs for agent decisions)
6. **docker-ollama-cluster** – tooling (containerized Ollama orchestration)
7. **edge-ml-deploy** – tooling (model deployment to edge devices)
8. **fleet-consensus** – core-infra (distributed agreement for fleet agents)
9. **graph-neural-process** – research (GNNs for procedural generation)
10. **huggingface-datasets** – training-system (curated datasets for PLATO rooms)
11. **knowledge-graph-builder** – core-infra (builds relational memory from tiles)
12. **llm-compression** – research (model pruning, quantization experiments)
13. **ml-pipeline-kubernetes** – tooling (K8s manifests for training pipelines)
14. **multi-agent-bargaining** – simulation (negotiation environments)
15. **neural-symbolic-integration** – constraint-theory (blending neural and symbolic reasoning)
16. **offline-rl-benchmark** – training-system (benchmarking offline RL algorithms)
17. **prompt-engineering-library** – docs (collection of prompt patterns)
18. **rust-wasm-agent** – core-infra (WebAssembly runtime for lightweight agents)
19. **self-supervised-vision** – research (vision models without labels)
20. **tensorrt-optimization** – tooling (GPU inference optimization scripts)

## 3. Tagging Suggestions

Based on content, I suggest adding these tags to improve discoverability:

- **agentic-workflow-engine**: `workflow`, `orchestration`, `research`
- **auto-lora-tuner**: `hyperparameter`, `automation`, `training`
- **fleet-consensus**: `distributed`, `agreement`, `core`
- **knowledge-graph-builder**: `memory`, `relations`, `core`
- **neural-symbolic-integration**: `symbolic`, `reasoning`, `constraint`
- **rust-wasm-agent**: `wasm`, `lightweight`, `runtime`

## 4. Merge Candidates & Abandoned Repos

**Merge candidates:**
- `docker-ollama-cluster` and `ml-pipeline-kubernetes` could merge into a single `fleet-orchestration` repo (both are tooling for deployment).
- `prompt-engineering-library` might belong in `fleet-knowledge/` as markdown files rather than a separate repo.

**Potentially abandoned (no commits in >6 months, minimal content):**
- `beam-search-rl` (last commit 2025-10-15)
- `causal-inference-demo` (last commit 2025-09-22)
- `graph-neural-process` (last commit 2025-11-03)

These abandoned repos could be moved to an `archive/` organization or flagged as `stale`.

## 5. Next Steps (P1 Safe Channel)

1. **P0**: Avoid mis-categorization – verify each repo’s primary purpose by checking its `main` branch structure.
2. **P1**: Apply categories and tags via GitHub API (or manual updates to repo descriptions).
3. **P2**: Create a `fleet-repos.md` index with categorized listings and health status.

This categorization creates a navigable map of the fleet’s codebase, supporting the Navigator’s dependency graphs and the Weaver’s integration maps.

**Tile submitted to PLATO room: `curator-repo-taxonomy-2026-04-20`**