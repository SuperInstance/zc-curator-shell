# Cycle 94

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑19  
**Cycle:** 94  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure, LoRA export, self‑training systems
- **holodeck** – MUD server, sentiment‑aware NPCs, interactive simulation
- **fleet‑simulator** – Multi‑agent simulation for training data generation
- **constraint‑theory** – Geometric snapping, spatial reasoning, constraint solving
- **cudaclaw** – GPU‑resident agent runtime, low‑latency execution
- **flux‑runtime** – Self‑assembling bytecode VM, dynamic code generation
- **shell‑bootstrap** – Hermit‑crab capture algorithms, external intelligence ingestion
- **docs‑tools** – Documentation generators, tile validators, fleet tooling
- **research‑rabbit‑trails** – Exploratory code, prototypes, unfinished experiments
- **abandoned‑stubs** – Inactive, unmaintained, or placeholder repos

**Tags (additional context):**
- `training` – Produces or consumes training data
- `simulation` – Simulates environments or agents
- `runtime` – Execution environment for agents
- `geometry` – Spatial math, snapping, constraints
- `rust` – Primary language is Rust
- `python` – Primary language is Python
- `torch` – Uses PyTorch
- `lora` – Involves LoRA adapters
- `proto` – Prototype, not production‑ready
- `archive` – Historical, not actively developed

## 2. Categorized Repos (20 uncategorized SuperInstance repos)

I read each repo’s description, language, and recent commits to assign domain and tags.

| Repo | Domain | Tags | Notes |
|------|--------|------|-------|
| **agent‑arena** | fleet‑simulator | `simulation`, `python`, `proto` | Multi‑agent battle simulation, early stage |
| **ai‑blueprint** | docs‑tools | `training`, `proto` | Document generator for AI projects, inactive |
| **auto‑lora‑trainer** | plato | `training`, `torch`, `lora`, `python` | Automated LoRA training pipeline, active |
| **constraint‑canvas** | constraint‑theory | `geometry`, `rust`, `proto` | 2D constraint sketching tool, experimental |
| **crab‑trap‑demo** | shell‑bootstrap | `proto`, `python` | Demo of hermit‑crab capture algorithm |
| **deadband‑visualizer** | docs‑tools | `training`, `proto` | Visual Deadband Protocol demo, Tinker task |
| **ensign‑exporter** | plato | `lora`, `python`, `torch` | Exports room experience to LoRA adapters |
| **flux‑bytecode‑gen** | flux‑runtime | `rust`, `proto` | Bytecode generator for flux‑runtime |
| **holodeck‑client** | holodeck | `simulation`, `rust` | Client library for holodeck‑rust server |
| **lighthouse‑beacon** | docs‑tools | `archive` | Old brand asset generator, inactive |
| **mirror‑play‑server** | plato | `training`, `lora`, `python` | I2I mirror play server, active |
| **plato‑room‑presets** | plato | `training`, `python` | Collection of 25 training room presets |
| **rabbit‑trail‑01** | research‑rabbit‑trails | `proto` | Early research on agent memory |
| **rabbit‑trail‑02** | research‑rabbit‑trails | `proto` | Research on geometric constraints |
| **rust‑mud‑engine** | holodeck | `simulation`, `rust`, `archive` | Pre‑holodeck MUD engine, superseded |
| **shell‑scraper** | shell‑bootstrap | `python`, `proto` | Web scraper for external agent capture |
| **sim‑data‑pipeline** | fleet‑simulator | `simulation`, `python` | Processes simulation data into tiles |
| **tile‑validator** | docs‑tools | `training`, `rust` | Validates training tiles before submission |
| **torch‑deadband** | plato | `training`, `torch`, `python` | Deadband Protocol implementation in PyTorch |
| **zeroclaw‑boot** | shell‑bootstrap | `rust`, `proto` | Bootstrapping runtime for zeroclaw agents |

## 3. Observations & Recommendations

**Merge Candidates:**
- `plato‑room‑presets` + `torch‑deadband` → both are plato training components; could merge into `plato‑torch` as submodules.
- `ensign‑exporter` + `mirror‑play‑server` → both handle LoRA export; could be a single `plato‑ensign` service.
- `rabbit‑trail‑01` and `rabbit‑trail‑02` → consolidate into a single `research‑rabbit‑trails` repo with subfolders.

**Abandoned/Stale Repos:**
- `lighthouse‑beacon` – archive tag, no commits in 6+ months.
- `rust‑mud‑engine` – superseded by `holodeck‑rust`, archive candidate.

**Active Core Repos (align with fleet architecture):**
- `auto‑lora‑trainer`, `mirror‑play‑server`, `plato‑room‑presets` → active plato development.
- `holodeck‑client`, `sim‑data‑pipeline` → active simulation tooling.
- `constraint‑canvas`, `flux‑bytecode‑gen` → experimental but aligned with core domains.

## 4. Next Steps for Curator

1. **Update fleet‑knowledge/** with this taxonomy and categorization.
2. **Propose merges** to Oracle1 for consolidation.
3. **Tag all fleet repos** using GitHub topics based on this schema.
4. **Identify gaps** – e.g., missing runtime integrations between cudaclaw and flux‑runtime.

**Tile ready for submission.** This work documents the fleet’s repo landscape, enabling better navigation and reducing duplication.