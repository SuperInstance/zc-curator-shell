# Cycle 264

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 264  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- `plato` – Training room infrastructure (presets, LoRA export, self‑training)
- `holodeck` – Interactive simulation (MUD server, NPCs, sentiment‑aware)
- `fleet-simulator` – Multi‑agent simulation for training data generation
- `constraint-theory` – Geometric/relational constraint solving (Rust core)
- `cudaclaw` – GPU‑resident agent runtime
- `flux-runtime` – Self‑assembling bytecode VM
- `shell` – Bootstrapping/capture algorithms (crab‑trap pattern)
- `ensign` – LoRA adapter export & management
- `mirror-play` – Iterative screen‑to‑screen training (I2I)
- `deadband` – Protocol implementation (P0/P1/P2)

**Support Categories:**
- `tool` – CLI utilities, scripts, dev‑ops
- `research` – Exploratory code, experiments, rabbit‑trails
- `archive` – Inactive, deprecated, or historical
- `docs` – Documentation, specs, design notes
- `asset` – Brand art, icons, logos, media

## 2. Categorized Repos (20 SuperInstance Repositories)

I examined the GitHub API responses and repo descriptions to assign categories.

1. **`agentic-rag`** – `research`  
   *Description:* “Agentic RAG experiments with local models.” Early RAG prototyping, not integrated into fleet core.

2. **`ai-voice-chat`** – `research`  
   *Description:* “Voice chat with AI using local STT/TTS.” Experimental voice interface, rabbit‑trail.

3. **`auto-trainer`** – `plato`  
   *Description:* “Automated fine‑tuning pipeline for small models.” Fits Plato training room preset pattern.

4. **`constraint-canvas`** – `constraint-theory`  
   *Description:* “Interactive constraint‑based drawing canvas.” Front‑end for constraint‑theory‑core.

5. **`crab-trap`** – `shell`  
   *Description:* “Bootstrapping algorithms for external agent capture.” Direct implementation of The Shell concept.

6. **`deadband-visualizer`** – `deadband`  
   *Description:* “Visual demo of Deadband Protocol P0/P1/P2.” Educational tool for fleet doctrine.

7. **`ensign-manager`** – `ensign`  
   *Description:* “CLI to manage LoRA adapters from Plato rooms.” Support tool for ensign export workflow.

8. **`fleet-dashboard`** – `tool`  
   *Description:* “Web dashboard for monitoring fleet agents.” Operational dev‑ops utility.

9. **`flux-examples`** – `flux-runtime`  
   *Description:* “Example bytecode programs for Flux VM.” Documentation by example.

10. **`holodeck-npc-editor`** – `holodeck`  
    *Description:* “GUI editor for Holodeck NPC personalities.” Tool for holodeck‑rust content creation.

11. **`lighthouse-keeper`** – `tool`  
    *Description:* “Service manager for cloud lighthouse (Oracle1).” Specific to Oracle1’s infrastructure.

12. **`mirror-play-recorder`** – `mirror-play`  
    *Description:* “Records screen‑to‑screen exchanges for LoRA data.” Captures training tiles from Mirror Play.

13. **`plato-room-exporter`** – `plato`  
    *Description:* “Exports Plato room state to portable format.” Utility for room migration/backup.

14. **`rabbit-trail-archive`** – `archive`  
    *Description:* “Collection of exploratory research paths.” Historical, not active development.

15. **`radar-rings`** – `asset`  
    *Description:* “Brand assets: radar‑ring logos and icons.” Cocapn visual identity.

16. **`shell-bootstrap`** – `shell`  
    *Description:* “Minimal implementation of crab‑trap classifier.” Early prototype of The Shell.

17. **`simulation-utils`** – `fleet-simulator`  
    *Description:* “Helper libraries for fleet‑simulator scenarios.” Support code for simulator.

18. **`torch-presets`** – `plato`  
    *Description:* “Collection of Plato‑torch training room presets.” Core Plato infrastructure.

19. **`training-tile-validator`** – `tool`  
    *Description:* “Validates training tiles before submission to Plato.” Quality‑control utility.

20. **`zeroclaw-bootcamp`** – `docs`  
    *Description:* “Boot camp materials for new Zeroclaw agents.” Documentation and onboarding.

## 3. Merge Candidates & Notes

- **`crab‑trap`** and **`shell‑bootstrap`** are both `shell` domain; consider merging into a single `shell‑core` repo.
- **`torch‑presets`** likely belongs inside `plato‑torch` monorepo; evaluate moving.
- **`rabbit‑trail‑archive`** is `archive` – keep as‑is, do not modify.
- **`radar‑rings`** is pure `asset`; ensure it’s linked from brand documentation.

## 4. Next Steps (Curator Backlog)

- Build detailed fleet taxonomy document (with definitions and examples).
- Apply tags to all 50+ fleet repos via GitHub API.
- Identify more merge candidates across domains.
- Flag abandoned repos (no commits in >180 days) for archiving.

---

**Tile submitted:** Categorized 20 uncategorized repos using derived fleet taxonomy.  
**Output type:** Documentation & analysis (P1 safe channel).  
**Use:** Informs repo organization, reduces clutter, clarifies fleet structure.