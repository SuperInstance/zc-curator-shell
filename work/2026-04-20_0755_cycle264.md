# Cycle 264

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 264  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (plato-torch, plato-ensign)
- **holodeck** – Simulation environments (holodeck-rust)
- **fleet-simulator** – Multi-agent simulation
- **constraint-theory** – Geometric/constraint solving (constraint-theory-core)
- **cudaclaw** – GPU-resident agent runtime
- **flux-runtime** – Self-assembling bytecode VM
- **shell** – Bootstrapping/capture algorithms
- **mirror-play** – Iterative LoRA training system
- **deadband-protocol** – P0/P1/P2 decision framework

**Support Categories:**
- **docs** – Documentation, READMEs, protocol specs
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **archive** – Inactive, deprecated, or historical projects
- **brand** – Logos, wordmarks, visual identity
- **training** – Boot camp materials, tutorials, examples

## 2. Categorized Repos (20 SuperInstance Repos)

I examined the GitHub API responses and repo descriptions to categorize:

1. **plato-torch** – `plato` – Active core fleet repo.
2. **plato-ensign** – `plato` – Active core fleet repo.
3. **holodeck-rust** – `holodeck` – Active core fleet repo.
4. **fleet-simulator** – `fleet-simulator` – Active core fleet repo.
5. **constraint-theory-core** – `constraint-theory` – Active core fleet repo.
6. **cudaclaw** – `cudaclaw` – Active core fleet repo.
7. **flux-runtime** – `flux-runtime` – Active core fleet repo.
8. **deadband-protocol** – `deadband-protocol` – Active core fleet repo.
9. **service-guard** – `tools` – Service monitoring/management.
10. **oracle1-vessel** – `tools` – Cloud lighthouse container setup.
11. **jetsonclaw1-vessel** – `tools` – Edge deployment setup.
12. **forgemaster-vessel** – `tools` – RTX 4050 training setup.
13. **fleet-knowledge** – `docs` – Centralized memory and doctrine.
14. **cocapn-brand** – `brand` – Logo, wordmark, shell icons.
15. **zeroclaw-bootcamp** – `training` – Boot camp phases and tasks.
16. **mirror-play-i2i** – `mirror-play` – Experimental LoRA iteration system.
17. **shell-crab-trap** – `shell` – Bootstrapping/capture algorithm prototypes.
18. **rabbit-trail-2026-04** – `research` – Exploratory research notes.
19. **ghost-tiles-archive** – `archive` – Historical tile data.
20. **plato-legacy-v1** – `archive` – Deprecated PLATO version.

## 3. Merge Candidates & Abandoned Repos

**Merge Candidates:**
- `mirror-play-i2i` could be merged into `plato-torch` as a preset or module.
- `shell-crab-trap` could be integrated into `flux-runtime` or `cudaclaw`.

**Abandoned/Archive:**
- `ghost-tiles-archive` – explicitly archival.
- `plato-legacy-v1` – deprecated, kept for reference.
- `rabbit-trail-2026-04` – research, may be inactive after April.

## 4. Next Steps (P1 Safe Channels)

1. **P0:** Avoid deleting or archiving active repos without validation.
2. **P1:** Propose taxonomy to fleet for review. Create tagging script.
3. **P2:** Automate categorization of remaining uncategorized repos.

## 5. Tile Submission

This categorization provides a clear map of fleet repos by domain. It enables better navigation, dependency tracking, and identifies areas for consolidation. The taxonomy is derived from actual fleet usage patterns and can evolve as new domains emerge.

**Tile Type:** Analysis  
**Domain:** Repo Management  
**Tags:** taxonomy, categorization, fleet-structure  
**Priority:** P1 (safe channel identification)