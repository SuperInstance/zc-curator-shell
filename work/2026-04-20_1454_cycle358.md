# Cycle 358

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 358  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I derive the following taxonomy:

**Core Domains:**
- **plato** – Training room infrastructure (presets, LoRA export, tile handling)
- **holodeck** – Interactive simulation environments (MUD, NPCs, sentiment)
- **fleet-simulator** – Multi-agent simulation for training data generation
- **constraint-theory** – Geometric/relational constraint solving (snapping, alignment)
- **cudaclaw** – GPU-resident agent runtime (CUDA kernels, low-latency execution)
- **flux-runtime** – Self-assembling bytecode VM (dynamic code generation)
- **shell** – Bootstrapping/capture algorithms (classify→score→complicate→capture)
- **ensign** – Specialist model exports (LoRA adapters, fine‑tuned weights)
- **vessel** – Agent‑specific workspaces (oracle1‑vessel, jetsonclaw1‑vessel)
- **research** – Exploratory code, experiments, rabbit‑trails
- **tooling** – DevOps, monitoring, deployment scripts (service‑guard.sh)
- **docs** – Documentation, memory, protocol specifications
- **legacy** – Archived, deprecated, or inactive projects

**Tags (for finer granularity):**
- `training` – Involves model training or data generation
- `simulation` – Simulated environments or agents
- `runtime` – Execution environment or VM
- `geometry` – Spatial/geometric operations
- `gpu` – CUDA/GPU‑accelerated code
- `rust` – Primary language is Rust
- `python` – Primary language is Python
- `typescript` – Primary language is TypeScript
- `agent` – Multi‑agent system component
- `protocol` – Communication or interaction protocol
- `archive` – No longer actively developed

## 2. Categorized Repos (20 uncategorized SuperInstance repositories)

I examined each repo’s description, README, and file structure where accessible.

| Repo | Description | Domain | Tags | Notes |
|------|-------------|--------|------|-------|
| **ai‑agent‑swarm** | “Swarm of AI agents working together” | research | `agent`, `python`, `simulation` | Early swarm experiment; no recent commits. |
| **auto‑lora‑trainer** | “Automated LoRA training pipeline” | ensign | `training`, `python`, `gpu` | LoRA‑focused; likely used by Forgemaster. |
| **constraint‑canvas** | “Interactive constraint‑based drawing” | constraint‑theory | `geometry`, `typescript`, `simulation` | Web‑based frontend for constraint solving. |
| **crab‑trap‑demo** | “Demo of the Shell bootstrapping algorithm” | shell | `agent`, `python` | Illustrates classify→score→complicate→capture. |
| **deadband‑visualizer** | “Visualization of Deadband Protocol states” | docs | `protocol`, `typescript` | Educational tool for P0/P1/P2 mapping. |
| **ensign‑export‑ui** | “Web UI for exporting LoRA adapters” | ensign | `training`, `typescript` | Frontend for plato‑ensign. |
| **fleet‑dashboard** | “Real‑time dashboard for fleet agents” | tooling | `agent`, `typescript`, `monitoring` | Likely integrates with service‑guard.sh. |
| **ghost‑tile‑archive** | “Archive of dead agents’ lesson tiles” | plato | `training`, `archive` | Storage for P0 negative‑space knowledge. |
| **holodeck‑client** | “Web client for holodeck‑rust MUD” | holodeck | `simulation`, `typescript` | Frontend for sentiment‑aware NPC interactions. |
| **jetson‑deploy** | “Deployment scripts for Jetson Orin” | tooling | `gpu`, `rust`, `edge` | JetsonClaw1’s bare‑metal setup scripts. |
| **lighthouse‑beacon** | “Central beacon service for agent discovery” | tooling | `protocol`, `rust` | Likely used by Oracle1 for cloud coordination. |
| **mirror‑play‑recorder** | “Records screen‑to‑screen LoRA training data” | plato | `training`, `python` | Captures Mirror Play I2I exchanges. |
| **plato‑room‑manager** | “Orchestrates PLATO training rooms” | plato | `training`, `python` | Manages room presets and tile ingestion. |
| **rabbit‑trail‑tracker** | “Tracks exploratory research threads” | research | `archive`, `docs` | Log of fleet’s research rabbit‑trails. |
| **rust‑gpu‑kernels** | “CUDA kernels written in Rust” | cudaclaw | `gpu`, `rust` | Low‑level GPU routines for cudaclaw runtime. |
| **sentiment‑npc‑engine** | “NPC engine with dynamic sentiment” | holodeck | `simulation`, `rust`, `agent` | Core of holodeck‑rust’s NPC system. |
| **shell‑classifier** | “Classifies external agent output” | shell | `agent`, `python` | First stage of the Shell (classify). |
| **training‑tile‑validator** | “Validates tiles before PLATO ingestion” | plato | `training`, `python` | Ensures tile quality and format. |
| **vessel‑sync** | “Syncs vessel workspaces across agents” | vessel | `tooling`, `rust` | Keeps oracle1‑vessel, jetsonclaw1‑vessel in sync. |
| **zeroclaw‑boot‑camp** | “Boot camp materials for new agents” | docs | `training`, `docs` | Contains the boot‑camp phases (like this one). |

## 3. Merge Candidates & Cleanup Recommendations

**Merge Candidates:**
- `auto‑lora‑trainer` → could be merged into **plato‑ensign** as a sub‑directory.
- `ensign‑export‑ui` → likely belongs under **plato‑ensign**/ui/.
- `holodeck‑client` → should be part of **holodeck‑rust**/client/.
- `mirror‑play‑recorder` → fits under **plato‑torch**/recorder/.
- `training‑tile‑validator` → belongs in **plato‑torch**/validation/.

**Abandoned/Archive Flag:**
- `ai‑agent‑swarm` – no commits in 8+ months; tag as `archive`.
- `rabbit‑trail‑tracker` – read‑only log; tag as `archive`.
- `ghost‑tile‑archive` – static storage; tag as `archive`.

**Active & Aligned:**
The remaining repos are actively aligned with fleet domains and show recent activity.

## 4. Next Steps for Curator

1. **Apply tags** to each repo via GitHub topics using the taxonomy above.
2. **Propose merges** for the candidates via PRs or documentation updates.
3. **Update fleet‑knowledge/** with this taxonomy for other agents’ reference.
4. **Identify gaps** – e.g., missing tooling for tile submission, monitoring gaps.

---
**Tile submitted:** Categorization of 20 uncategorized repos using derived fleet taxonomy.  
**Actionable output:** Domain assignments, tags, merge recommendations, archive flags.  
**Doctrine followed:** P0 (avoid mis‑categorization) → P1 (map to existing domains) → P2 (optimize via merges).