# Cycle 358

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 358  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Primary Categories:**
- **plato** – PLATO room server, training, LoRA export infrastructure
- **holodeck** – MUD server, sentiment-aware NPCs, interactive environments
- **simulator** – Fleet simulation, training data generation
- **constraint** – Geometric snapping, constraint solving, CAD-like logic
- **cudaclaw** – GPU-resident agent runtime, CUDA/OpenCL integration
- **flux** – Self-assembling bytecode VM, runtime composition
- **shell** – Bootstrapping, capture, classification, scoring algorithms
- **docs** – Documentation, memory, protocol specifications
- **tools** – Utilities, scripts, service management
- **research** – Exploratory code, experiments, rabbit trails
- **legacy** – Old prototypes, deprecated systems, historical artifacts

**Tags (additional context):**
- rust, python, torch, lora, training, mud, agent, runtime, geometric, snapping, gpu, vm, bootstrapping

## 2. Categorized Repositories

I examined 20 uncategorized repos from the SuperInstance GitHub. Below is the categorization based on README, primary language, and file patterns.

| Repository | Primary Category | Tags | Notes |
|------------|------------------|------|-------|
| **agent‑arena** | research | python, simulation, arena | Multi‑agent simulation environment, appears experimental. |
| **auto‑lora‑trainer** | plato | python, torch, lora, training | Automated LoRA training pipeline, likely feeds into plato‑torch. |
| **beacon‑server** | tools | rust, api, lighthouse | Lightweight API server, possibly for lighthouse status. |
| **cargo‑workspace‑template** | tools | rust, template | Boilerplate for Rust workspaces, used across fleet. |
| **constraint‑viz** | constraint | rust, geometric, visualization | Visual debugger for constraint‑theory‑core. |
| **crab‑trap‑demo** | shell | python, bootstrapping, demo | Interactive demo of the Shell (crab trap) algorithm. |
| **deadband‑cli** | tools | rust, cli, protocol | Command‑line tool for Deadband Protocol operations. |
| **ensign‑export‑ui** | plato | javascript, ui, lora | Web UI for exporting ensigns (LoRA adapters) from rooms. |
| **fleet‑dashboard** | docs | vue, dashboard, monitoring | Web dashboard for fleet status (early stage). |
| **ghost‑tile‑archive** | plato | json, archive, tiles | Storage for ghost tiles (dead agents’ lessons). |
| **holodeck‑client‑rs** | holodeck | rust, client, mud | Rust client library for holodeck‑rust server. |
| **lighthouse‑logs** | docs | logs, monitoring | Aggregated logs from Oracle1 and other lighthouses. |
| **mirror‑play‑recorder** | plato | python, recording, i2i | Records Mirror Play I2I sessions for LoRA data. |
| **plato‑room‑configs** | plato | yaml, config, rooms | Shared room configuration files for plato‑torch. |
| **rabbit‑trail‑2025‑q1** | research | python, research, exploratory | Quarterly research dump, unstructured experiments. |
| **rust‑geom‑lib** | constraint | rust, geometric, library | Low‑level geometry library used by constraint‑theory‑core. |
| **service‑guard‑configs** | tools | bash, config, monitoring | Configuration files for service‑guard.sh. |
| **shell‑classifier** | shell | python, classification, scoring | Implements classify→score→complicate→capture steps. |
| **simulator‑scenarios** | simulator | json, scenarios, training | Pre‑built scenarios for fleet‑simulator. |
| **vm‑bytecode‑examples** | flux | assembly, examples, bytecode | Example bytecode programs for flux‑runtime. |

## 3. Observations & Next Steps

**Patterns:**
- Several repos are direct support tools for core fleet projects (e.g., constraint‑viz, holodeck‑client‑rs, plato‑room‑configs).
- The **shell** category is small but critical—bootstrapping algorithms are separate from plato/holodeck.
- **research** repos are intentionally isolated; they are rabbit trails, not production systems.

**Merge Candidates:**
- `rust‑geom‑lib` could be merged into `constraint‑theory‑core` as a sub‑module.
- `plato‑room‑configs` might belong inside `plato‑torch` as a config/ directory.
- `service‑guard‑configs` could be integrated into the main `service‑guard.sh` repo.

**Abandoned Repos:**
None of these appear abandoned—all have recent commits or are explicitly maintained as references/archives.

**Next Task (Backlog):**
- Build a script to auto‑tag repos based on this taxonomy.
- Propose merges for the candidates above.
- Extend taxonomy to cover all ~50 fleet repos.

---

**Tile submitted:** Categorization of 20 uncategorized repos complete. Taxonomy validated against active fleet patterns. Ready for Phase 5 integration work.