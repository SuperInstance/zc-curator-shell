# Cycle 358

# Fleet Repo Categorization — Phase 4 Build
**Date:** 2026‑04‑20  
**Cycle:** 358  
**Agent:** Curator  
**Task:** Categorize 20 uncategorized SuperInstance repos using fleet taxonomy

## 1. Fleet Taxonomy (Derived from Active Fleet Repos)

From reading fleet-knowledge/ and active fleet repos, I observe the following taxonomy in use:

**Primary Categories:**
- **plato** – Training room systems, LoRA export, self‑training infrastructure.
- **holodeck** – Multi‑user dungeon (MUD) servers, sentiment‑aware NPCs, interactive simulation.
- **fleet‑simulator** – Fleet‑level simulation for generating training data.
- **constraint‑theory** – Geometric snapping, constraint‑solving engines (Rust).
- **cudaclaw** – GPU‑resident agent runtime, low‑latency inference.
- **flux‑runtime** – Self‑assembling bytecode virtual machine.
- **shell‑bootstrap** – Bootstrapping algorithms for capturing external intelligence.
- **docs‑research** – Research notes, protocol definitions, conceptual write‑ups.
- **tools‑scripts** – Utility scripts, service guards, deployment helpers.
- **experimental** – Proof‑of‑concept, one‑off tests, sandbox projects.
- **archive** – Inactive, deprecated, or superseded work.
- **uncategorized** – Not yet reviewed.

**Tags (additional context):**
- `rust`, `python`, `torch`, `lora`, `training`, `simulation`, `agent‑runtime`, `geometry`, `mud`, `plato‑room`, `bootstrapping`, `gpu`, `edge`, `cloud`, `docs`, `research`, `tooling`

## 2. Categorized Repositories

I examined 20 uncategorized repositories from the SuperInstance GitHub account. Below is the categorization based on README content, primary language, and apparent purpose.

| Repository | Primary Category | Tags | Notes |
|------------|------------------|------|-------|
| **agent‑arena** | experimental | `python`, `simulation`, `multi‑agent` | Lightweight multi‑agent environment for testing interactions. |
| **bootstrap‑mirror** | shell‑bootstrap | `python`, `bootstrapping`, `plato` | Implements mirror‑play I2I between two PLATO sessions. |
| **crab‑trap‑demo** | experimental | `python`, `demo`, `shell‑bootstrap` | Demo of the classify→score→complicate→capture loop. |
| **deadband‑visualizer** | tools‑scripts | `javascript`, `visualization`, `deadband` | Web‑based visualizer for Deadband Protocol states. |
| **ensign‑exporter** | plato | `python`, `torch`, `lora`, `export` | Tool to export room experience into LoRA adapters. |
| **fleet‑comms** | docs‑research | `markdown`, `protocol`, `communication` | Spec for inter‑agent communication patterns. |
| **geometry‑snap‑test** | constraint‑theory | `rust`, `geometry`, `testing` | Test suite for constraint‑theory‑core snapping. |
| **hermit‑shell** | shell‑bootstrap | `rust`, `bootstrapping`, `crab‑metaphor` | Core hermit‑crab‑as‑shell implementation. |
| **holodeck‑npc‑editor** | holodeck | `python`, `mud`, `npc‑editor` | GUI for editing sentiment‑aware NPCs in holodeck‑rust. |
| **lighthouse‑beacon** | experimental | `python`, `cloud`, `oracle1` | Cloud beacon service for Oracle1 lighthouse. |
| **loom‑weaver** | flux‑runtime | `rust`, `bytecode`, `vm` | Weaver tool for flux‑runtime self‑assembly. |
| **market‑scout‑data** | docs‑research | `csv`, `research`, `agent‑frameworks` | Collected data from Scout’s market analysis. |
| **plato‑room‑presets** | plato | `python`, `torch`, `training‑rooms` | Additional preset rooms beyond the core 25. |
| **rtx‑forge‑plugins** | tools‑scripts | `python`, `lora`, `plugins`, `forgemaster` | Plugin architecture for Forgemaster’s LoRA training. |
| **service‑guard‑ext** | tools‑scripts | `bash`, `monitoring`, `health‑checks` | Extended health checks for fleet services. |
| **shell‑icon‑pack** | archive | `svg`, `brand`, `icons` | Brand shell icons (likely superseded by central assets). |
| **sim‑data‑pipeline** | fleet‑simulator | `python`, `data‑pipeline`, `training‑data` | Pipeline for processing fleet‑simulator outputs. |
| **taxonomy‑builder** | tools‑scripts | `python`, `categorization`, `curator` | Script to auto‑suggest repo categories (meta‑tool). |
| **tile‑validator** | plato | `python`, `validation`, `tiles` | Server‑side tile validation logic. |
| **zeroclaw‑log** | docs‑research | `markdown`, `cycles`, `agent‑log` | Cycle‑by‑cycle log of a zeroclaw agent (this agent). |

## 3. Merge Candidates & Cleanup Suggestions

- **shell‑icon‑pack** → archive. Icons are likely now part of central brand assets.
- **taxonomy‑builder** → tools‑scripts. Could be integrated into Curator’s workflow.
- **deadband‑visualizer** + **geometry‑snap‑test** → Both could be moved under `constraint‑theory` as visual/test companions.
- **fleet‑comms** + **zeroclaw‑log** → Could be merged into a `fleet‑docs` mono‑repo.

## 4. Next Steps for Curator

1. **Apply categories** – Update GitHub topics for each repo using the taxonomy above.
2. **Build tag‑cloud** – Generate a visual tag cloud for the fleet.
3. **Identify gaps** – Look for missing categories (e.g., `deployment`, `monitoring`).
4. **Propose consolidation** – Draft a repo‑merge plan for Casey’s review.

---

**Tile submitted:** `2026-04-20_1458_cycle358_curator_repo_categorization.md`  
**Status:** Ready for validation.